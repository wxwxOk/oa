---
phase: 21
plan: 01
wave: 0
title: "Visit frontend contract tests"
status: ready
depends_on: []
files_modified:
  - frontend/src/types/__tests__/visit.test.ts
  - frontend/src/stores/__tests__/visit.test.ts
  - frontend/src/pages/__tests__/VisitPage.test.ts
autonomous: true
requirements:
  - VISIT-02
  - VISIT-03
  - VISIT-04
  - QUERY-01
  - QUERY-02
  - QUERY-03
  - QUERY-04
---

# Phase 21 Plan 01: Visit Frontend Contract Tests

<objective>
Create focused Vitest contracts for the Phase 21 frontend surface before implementation. The tests should pin types/helpers, store API behavior, route/menu permissions, page layout contracts and negative scope boundaries.
</objective>

<context>
Phase 21 is frontend-only and consumes the Phase 20 `/visits` backend. Tests should match the existing lightweight source-contract style used by approval pages and store tests.
</context>

<source_audit>
Read first:

- `.planning/phases/21-crud/21-CONTEXT.md`
- `.planning/phases/21-crud/21-RESEARCH.md`
- `.planning/phases/21-crud/21-VALIDATION.md`
- `frontend/src/pages/__tests__/ApprovalArchivePage.test.ts`
- `frontend/src/stores/__tests__/approvalArchive.test.ts`
- `frontend/src/stores/__tests__/approvalTask.test.ts`
- `frontend/src/types/__tests__/approvalArchive.test.ts`
- `frontend/src/router/routes.ts`
- `frontend/src/layouts/MainLayout.vue`
</source_audit>

<tasks>

<task id="01" title="Add visit type/helper contract">
<read_first>
- `frontend/src/types/__tests__/visit.test.ts` (create if missing)
- `.planning/phases/21-crud/21-CONTEXT.md`
- `.planning/phases/21-crud/21-RESEARCH.md`
- `.planning/phases/21-crud/21-VALIDATION.md`
- `frontend/src/types/__tests__/approvalArchive.test.ts`
- `backend/src/modules/visit/visit.route.ts`
</read_first>
<action>
Create `frontend/src/types/__tests__/visit.test.ts`.

- Import planned helper exports from `../visit`: `VISIT_WRITE_PAYLOAD_KEYS`, `VISIT_LIST_FILTER_KEYS`, `VISIT_FILTER_OPTION_KEYS`, `createEmptyVisitFilters`, `formatVisitDate`, and `normalizeVisitPayload`.
- Assert `VISIT_WRITE_PAYLOAD_KEYS` is exactly the 15 business write fields from the Phase 20 backend schema.
- Assert payload keys exclude trusted/server fields: `id`, `creatorId`, `creator`, `createdAt`, `updatedAt`.
- Assert `VISIT_LIST_FILTER_KEYS` includes `keyword`, channel/person/status filters, `dateFrom` and `dateTo`.
- Assert `VISIT_FILTER_OPTION_KEYS` is exactly `channelPartners`, `consultants`, `receptionists`, `receptionStatuses`, `consultationStatuses`, `statusCategories`.
- Assert `formatVisitDate('2026-05-02T08:30:00.000Z')` returns `2026-05-02`, `formatVisitDate('2026-05-02')` returns `2026-05-02`, `formatVisitDate('')` returns `-`, and `formatVisitDate(null)` returns `-`.
- Assert `normalizeVisitPayload` trims `name`, preserves integer `age`, and converts blank optional string/date fields to `null`.
- Import the module namespace and assert it does not contain `VISIT_IMPORT_PAYLOAD_KEYS`, `VISIT_STATS_KEYS`, or `VISIT_EXPORT_KEYS`.
</action>
<acceptance_criteria>
- `frontend/src/types/__tests__/visit.test.ts` contains `VISIT_WRITE_PAYLOAD_KEYS`, `VISIT_LIST_FILTER_KEYS`, `VISIT_FILTER_OPTION_KEYS`, `formatVisitDate`, and `normalizeVisitPayload`.
- `frontend/src/types/__tests__/visit.test.ts` contains the exact strings `VISIT_IMPORT_PAYLOAD_KEYS`, `VISIT_STATS_KEYS`, and `VISIT_EXPORT_KEYS` only as negative assertions.
- `cd frontend && bun test src/types/__tests__/visit.test.ts` fails before `frontend/src/types/visit.ts` exists and exits 0 after Plan 02.
</acceptance_criteria>
</task>

<task id="02" title="Add visit store contract">
<read_first>
- `frontend/src/stores/__tests__/visit.test.ts` (create if missing)
- `.planning/phases/21-crud/21-CONTEXT.md`
- `.planning/phases/21-crud/21-VALIDATION.md`
- `frontend/src/stores/__tests__/approvalArchive.test.ts`
- `frontend/src/stores/__tests__/approvalTask.test.ts`
- `frontend/src/stores/approvalApplication.ts`
- `backend/src/modules/visit/visit.route.ts`
</read_first>
<action>
Create `frontend/src/stores/__tests__/visit.test.ts`.

- Mock `src/boot/axios` with `get`, `post`, `put`, and `delete` vi functions.
- Use `createPinia` and `setActivePinia` before each test.
- Assert `fetchList` calls `api.get('/visits', { params })` with `page`, `size`, `keyword`, all filter dimensions and date range.
- Assert empty filters are omitted from request params.
- Assert `fetchFilterOptions` calls `/visits/filter-options` and stores returned distinct option arrays.
- Assert `fetchDetail(id)` calls `/visits/${id}`.
- Assert `createVisit`, `updateVisit`, and `deleteVisit` use `POST /visits`, `PUT /visits/:id`, and `DELETE /visits/:id`.
- Assert `loading`, `detailLoading`, and `actionLoading` reset to `false` when API calls reject.
</action>
<acceptance_criteria>
- `frontend/src/stores/__tests__/visit.test.ts` contains `api.get('/visits'`, `api.get('/visits/filter-options'`, `api.post('/visits'`, `api.put`, and `api.delete`.
- `frontend/src/stores/__tests__/visit.test.ts` contains all filter param names: `keyword`, `channelPartner`, `consultant`, `receptionist`, `receptionStatus`, `consultationStatus`, `statusCategory`, `dateFrom`, `dateTo`.
- `frontend/src/stores/__tests__/visit.test.ts` contains rejection assertions for `loading`, `detailLoading`, and `actionLoading` being `false`.
- `cd frontend && bun test src/stores/__tests__/visit.test.ts` exits 0 after Plan 02.
</acceptance_criteria>
</task>

<task id="03" title="Add visit page, route, and menu source contract">
<read_first>
- `frontend/src/pages/__tests__/VisitPage.test.ts` (create if missing)
- `.planning/phases/21-crud/21-CONTEXT.md`
- `.planning/phases/21-crud/21-UI-SPEC.md`
- `frontend/src/pages/__tests__/ApprovalArchivePage.test.ts`
- `frontend/src/router/routes.ts`
- `frontend/src/layouts/MainLayout.vue`
</read_first>
<action>
Create `frontend/src/pages/__tests__/VisitPage.test.ts`.

- Read `VisitPage.vue`, `VisitFormDialog.vue`, `routes.ts`, and `MainLayout.vue` with `readFileSync`.
- Assert page copy: `到访管理`, `暂无到访记录`, `新建到访记录`.
- Assert filter labels: keyword, channelPartner, consultant, receptionist, receptionStatus, consultationStatus, statusCategory, start/end reception dates.
- Assert desktop/mobile contracts: `q-table`, `:rows-per-page-options="[10, 20, 50]"`, `visit-card`, filter sheet/dialog copy.
- Assert dialog/action contracts: `VisitFormDialog`, detail/edit/create/delete strings, delete confirmation copy.
- Assert permission strings: `visit:list`, `visit:create`, `visit:update`, `visit:delete`, and `v-perm`.
- Assert route and menu contain `/visits` and `到访管理`.
- Assert source does not contain `导入 Excel`, `导出 Excel`, `统计`, `/visits/import`, or `/visits/stats`.
</action>
<acceptance_criteria>
- `frontend/src/pages/__tests__/VisitPage.test.ts` contains `readFileSync` and does not import `@vue/test-utils`.
- `frontend/src/pages/__tests__/VisitPage.test.ts` contains `visit:list`, `visit:create`, `visit:update`, `visit:delete`, `/visits`, `q-table`, and `visit-card`.
- `frontend/src/pages/__tests__/VisitPage.test.ts` contains negative assertions for `导入 Excel`, `导出 Excel`, `统计`, `/visits/import`, and `/visits/stats`.
- `cd frontend && bun test src/pages/__tests__/VisitPage.test.ts` exits 0 after Plan 03.
</acceptance_criteria>
</task>

</tasks>

<threat_model>
- Contract tests can become brittle if they assert incidental CSS. Keep assertions on business copy, permissions, API paths and major component names only.
- Do not assert implementation details that would force a poor UI structure.
- Negative scope assertions must target specific Phase 22/23 strings and endpoints, not generic words that may appear in comments.
</threat_model>

<verification>
Run after adding tests:

```bash
cd frontend && bun test src/types/__tests__/visit.test.ts src/stores/__tests__/visit.test.ts src/pages/__tests__/VisitPage.test.ts
```

Expected immediately after Plan 01: failures are acceptable only because Phase 21 implementation files/exports do not exist yet.
</verification>

<success_criteria>
- Three focused test files exist.
- Tests encode VISIT-02/03/04 and QUERY-01/02/03/04.
- Tests are ready to pass after Plans 02 and 03.
</success_criteria>

<must_haves>
- VISIT-02/03/04 and QUERY-01/02/03/04 appear in focused contracts before implementation.
- Store API tests pin `/visits`, `/visits/filter-options`, and `/visits/:id`.
- Source contracts pin `/visits`, `到访管理`, responsive table/card behavior, CRUD permissions, and negative scope exclusions.
</must_haves>

<output>
- `frontend/src/types/__tests__/visit.test.ts`
- `frontend/src/stores/__tests__/visit.test.ts`
- `frontend/src/pages/__tests__/VisitPage.test.ts`
</output>
