---
phase: 18-approval-task-inbox-mobile-approval
plan: "05"
type: execute
wave: 3
depends_on: ["18-02", "18-03", "18-04"]
files_modified:
  - frontend/src/components/approval/ApplicationTimeline.vue
  - frontend/src/pages/ApprovalTaskDetailPage.vue
autonomous: true
requirements: [APR-02, APR-03, APR-05, APR-06]
requirements_addressed: [APR-02, APR-03, APR-05, APR-06]
user_setup: []
must_haves:
  truths:
    - "Approvers can open task detail by task id, review the historical snapshot, and act safely from desktop or mobile."
    - "Mobile detail keeps a sticky action bar without covering long form content, dynamic tables, signatures, or timeline entries."
    - "Internal remarks are visible on approver detail timelines but remain distinct from applicant-submitted form data."
  artifacts:
    - path: "frontend/src/pages/ApprovalTaskDetailPage.vue"
      provides: "Task-detail snapshot page with approve/reject/comment dialogs and mobile sticky actions"
    - path: "frontend/src/components/approval/ApplicationTimeline.vue"
      provides: "Timeline copy support for internal remarks"
  key_links:
    - from: "frontend/src/pages/ApprovalTaskDetailPage.vue"
      to: "frontend/src/stores/approvalTask.ts"
      via: "fetchDetail, approve, reject, comment, and post-action refresh"
      pattern: "fetchDetail|approve|reject|comment"
    - from: "frontend/src/pages/ApprovalTaskDetailPage.vue"
      to: "frontend/src/components/renderer/GridFormRenderer.vue"
      via: "historical read-only snapshot rendering"
      pattern: "mode=\"print\""
---

<objective>
Build the approval task detail page, action flows, and mobile-safe sticky action region.

Purpose: Complete the approver-side workflow by letting the assignee review the historical submission, submit approve/reject opinions, and add internal remarks on desktop and mobile.
Output: Task detail page with snapshot rendering, timeline, comment dialog, approve/reject confirmation flows, and mobile sticky action bar.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-approval-task-inbox-mobile-approval/18-CONTEXT.md
@.planning/phases/18-approval-task-inbox-mobile-approval/18-UI-SPEC.md
@.planning/phases/18-approval-task-inbox-mobile-approval/18-04-PLAN.md
@.planning/phases/17-my-applications-dynamic-submission/17-05-SUMMARY.md
@frontend/src/pages/ApprovalApplicationDetailPage.vue
@frontend/src/components/approval/ApplicationTimeline.vue
@frontend/src/components/renderer/GridFormRenderer.vue

<interfaces>
From frontend/src/stores/approvalTask.ts:
```ts
export const useApprovalTaskStore = defineStore('approvalTask', {
  state: () => ({ current: null, detailLoading: false, actionLoading: false }),
  actions: { async fetchDetail(id: number) {}, async approve(id: number, comment?: string) {}, async reject(id: number, comment: string) {}, async comment(id: number, comment: string) {} }
});
```

From frontend/src/types/approvalTask.ts:
```ts
export interface ApprovalTaskDetail { id: number; applicationNo: string; taskStatus: ApprovalTaskStatus; applicationStatus: ApprovalApplicationStatus; schemaSnapshot: SchemaV2; formData: Record<string, unknown>; timeline: ApprovalTimelineEvent[]; canHandle: boolean; canComment: boolean; }
export function canHandleTask(detail: Pick<ApprovalTaskDetail, 'taskStatus' | 'canHandle'>): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend timeline copy for internal remarks and build the task detail skeleton</name>
  <read_first>.planning/phases/18-approval-task-inbox-mobile-approval/18-UI-SPEC.md, frontend/src/pages/ApprovalApplicationDetailPage.vue, frontend/src/components/approval/ApplicationTimeline.vue, frontend/src/types/approvalTask.ts, frontend/src/stores/approvalTask.ts, frontend/src/pages/__tests__/ApprovalTaskDetailPage.test.ts</read_first>
  <files>frontend/src/components/approval/ApplicationTimeline.vue, frontend/src/pages/ApprovalTaskDetailPage.vue</files>
  <acceptance_criteria>`ApplicationTimeline.vue` maps `COMMENT` to `内部备注` while keeping oldest-to-newest order and `white-space: pre-wrap`; `ApprovalTaskDetailPage.vue` renders `审批详情`, `申请信息`, `当前任务`, `表单内容`, and `审批动态`; the page uses `GridFormRenderer` with `mode=\"print\"`; mobile CSS reserves at least `96px` bottom padding for scroll content and preserves the existing print-table block fallback at `< 1024px`.</acceptance_criteria>
  <action>Extend `ApplicationTimeline.vue` so `COMMENT` events display `内部备注` and continue wrapping multiline comments correctly. Create `ApprovalTaskDetailPage.vue` from the Phase 17 full-page detail pattern per D-11 through D-15 and D-23 through D-25: desktop two-column layout, mobile single-column flow, summary cards for application/task info, snapshot rendering from `schemaSnapshot + formData`, and timeline placement after the snapshot on mobile. Keep the detail read-only for applicant form data and show `—` for missing summary values.</action>
  <verify>
    <automated>cd frontend && bun run test --run src/pages/__tests__/ApprovalTaskDetailPage.test.ts && bun run build</automated>
  </verify>
  <done>The task detail page exists with correct snapshot rendering, timeline copy, and mobile-safe layout scaffolding before action dialogs are added.</done>
</task>

<task type="auto">
  <name>Task 2: Implement approve/reject/comment flows with sticky mobile actions and post-action refresh</name>
  <read_first>.planning/phases/18-approval-task-inbox-mobile-approval/18-CONTEXT.md, .planning/phases/18-approval-task-inbox-mobile-approval/18-UI-SPEC.md, frontend/src/pages/ApprovalTaskDetailPage.vue, frontend/src/stores/approvalTask.ts, frontend/src/components/approval/ApprovalTaskStatusChip.vue</read_first>
  <files>frontend/src/pages/ApprovalTaskDetailPage.vue</files>
  <acceptance_criteria>`ApprovalTaskDetailPage.vue` shows approve/reject controls only when `canHandle` and pending status are true; desktop actions live in the current-task card, mobile actions render a sticky bottom bar with buttons `驳回审批` and `通过审批`; approve dialog title is `确认通过审批` with optional opinion textarea max 200; reject dialog title is `确认驳回审批` with required opinion textarea max 200; internal remark dialog title is `添加内部备注`; successful actions call store `approve`, `reject`, or `comment`, then refresh detail plus the inbox list data.</acceptance_criteria>
  <action>Finish the detail page action logic per D-16 through D-21 and D-27 through D-30. Add approve and reject confirmation dialogs that show application number, template name, and node name. Reject must require a non-empty comment before enabling submit. Add `添加内部备注` as a separate secondary action, available when `canComment` is true, storing text through the `comment` store action only. Use `Notify` for success/error, disable duplicate submissions while `actionLoading` is true, and after every successful action reload the current detail plus the pending/handled lists so status chips and tabs stay consistent.</action>
  <verify>
    <automated>cd frontend && bun run test --run src/pages/__tests__/ApprovalTaskDetailPage.test.ts src/stores/__tests__/approvalTask.test.ts src/types/__tests__/approvalTask.test.ts && bun run build</automated>
  </verify>
  <done>Approvers can safely approve, reject, and add internal remarks from task detail on desktop and mobile, with refresh behavior and sticky mobile controls matching the UI contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approver detail page → task action store/API | User-triggered actions must remain confirmation-gated and permission-aware before they reach the backend |
| sticky mobile action region → long snapshot content | Mobile layout must preserve access to all snapshot and timeline content without obscuring data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | T | approve/reject UI flow | mitigate | Show actions only on pending tasks, require confirm dialogs, require reject comment, and disable duplicate submissions while `actionLoading` is true |
| T-18-15 | I | detail timeline rendering | mitigate | Render internal remarks as explicit `内部备注` events on approver detail only; never edit or merge them into `formData` |
| T-18-16 | D | mobile detail layout | mitigate | Reserve bottom padding and safe-area space so the sticky action bar cannot hide long tables, signatures, or final timeline items |
</threat_model>

<verification>
Run the new detail page test plus the task store/type tests and a full frontend build. The detail page must satisfy the mobile sticky-action assertions introduced in Wave 0.
</verification>

<success_criteria>
Task detail at `/approval/tasks/:id` supports historical snapshot review, safe approve/reject/comment actions, and a mobile sticky action bar that does not obscure content.
</success_criteria>

<output>
After completion, create `.planning/phases/18-approval-task-inbox-mobile-approval/18-05-SUMMARY.md`
</output>
