---
phase: 18-approval-task-inbox-mobile-approval
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/modules/approval/__tests__/task.service.test.ts
  - backend/src/modules/approval/__tests__/task.route.test.ts
  - backend/src/modules/approval/__tests__/application-submission.service.test.ts
  - frontend/src/types/__tests__/approvalTask.test.ts
  - frontend/src/stores/__tests__/approvalTask.test.ts
  - frontend/src/pages/__tests__/ApprovalTaskDetailPage.test.ts
autonomous: true
requirements: [APR-01, APR-02, APR-03, APR-04, APR-05, APR-06]
requirements_addressed: [APR-01, APR-02, APR-03, APR-04, APR-05, APR-06]
user_setup: []
must_haves:
  truths:
    - "Approvers only see pending and handled tasks assigned to themselves, and cannot view or act on other users' tasks."
    - "Approval task detail stays usable on mobile, with a readable single-column layout and sticky approve/reject controls that do not cover long form content."
    - "Internal remarks remain visible in approver task history while staying hidden from the applicant's own application detail."
  artifacts:
    - path: "backend/src/modules/approval/__tests__/task.service.test.ts"
      provides: "Assignee-scoped service and action regression coverage"
    - path: "backend/src/modules/approval/__tests__/task.route.test.ts"
      provides: "Route schema and serializer contract coverage for /approval/tasks"
    - path: "frontend/src/stores/__tests__/approvalTask.test.ts"
      provides: "Pinia endpoint and loading-state contract coverage"
    - path: "frontend/src/pages/__tests__/ApprovalTaskDetailPage.test.ts"
      provides: "Mobile sticky-action and detail rendering coverage"
  key_links:
    - from: "backend/src/modules/approval/__tests__/task.service.test.ts"
      to: "backend/src/modules/approval/task.service.ts"
      via: "expected exported helpers and assignee-only behavior"
      pattern: "listApprovalTasks|getApprovalTaskDetail|approveApprovalTask|rejectApprovalTask|commentApprovalTask"
    - from: "frontend/src/stores/__tests__/approvalTask.test.ts"
      to: "frontend/src/stores/approvalTask.ts"
      via: "exact /approval/tasks API paths"
      pattern: "/approval/tasks"
---

<objective>
Lock Phase 18 behavior into backend and frontend tests before implementation changes.

Purpose: Create Wave 0 regression scaffolds for assignee-only queue access, task actions, internal remark visibility, and mobile approval detail behavior.
Output: New backend and frontend test files that define the implementation contract for Plans 18-02 through 18-05.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-approval-task-inbox-mobile-approval/18-CONTEXT.md
@.planning/phases/18-approval-task-inbox-mobile-approval/18-RESEARCH.md
@.planning/phases/18-approval-task-inbox-mobile-approval/18-PATTERNS.md
@.planning/phases/18-approval-task-inbox-mobile-approval/18-VALIDATION.md
@.planning/phases/17-my-applications-dynamic-submission/17-02-SUMMARY.md
@.planning/phases/17-my-applications-dynamic-submission/17-03-SUMMARY.md

<interfaces>
From backend/src/modules/approval/application.service.ts:
```ts
export type ApprovalActor = { id: number; name: string };
export async function approveTask(taskId: number, actor: ApprovalActor, comment?: string);
export async function rejectTask(taskId: number, actor: ApprovalActor, comment?: string);
export async function appendApplicationEvent(input: AppendApplicationEventInput): Promise<void>;
```

From backend/src/modules/approval/application.route.ts:
```ts
export const cancelBodySchema = t.Object({ reason: t.Optional(t.String({ maxLength: 200 })) }, { additionalProperties: false });
export function serializeApplicationDetail(detail: ApplicationRouteDetail);
```

From frontend/src/stores/approvalApplication.ts:
```ts
export const useApprovalApplicationStore = defineStore('approvalApplication', { actions: { async fetchList() {}, async fetchDetail(id: number) {}, async cancel(id: number, reason?: string) {} } });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend task queue and visibility test contracts</name>
  <read_first>.planning/phases/18-approval-task-inbox-mobile-approval/18-VALIDATION.md, .planning/phases/18-approval-task-inbox-mobile-approval/18-PATTERNS.md, backend/src/modules/approval/__tests__/application.route.test.ts, backend/src/modules/approval/__tests__/application-submission.service.test.ts, backend/src/modules/approval/__tests__/application.service.test.ts, backend/src/modules/approval/application.service.ts, backend/src/modules/approval/application-submission.service.ts</read_first>
  <files>backend/src/modules/approval/__tests__/task.service.test.ts, backend/src/modules/approval/__tests__/task.route.test.ts, backend/src/modules/approval/__tests__/application-submission.service.test.ts</files>
  <acceptance_criteria>`task.service.test.ts` contains cases named for `pending assignee list`, `handled history`, `approveApprovalTask`, `rejectApprovalTask`, `commentApprovalTask`, and `internal remark hidden from own detail`; `task.route.test.ts` asserts `/approval/tasks` prefix, `approve/reject/comment` body schemas, and forbidden trusted fields; `application-submission.service.test.ts` gains an assertion that applicant detail excludes timeline events with `payload.visibility === 'INTERNAL'`.</acceptance_criteria>
  <action>Create Bun tests that define the exact backend contract for Phase 18 per D-02, D-03, D-17, D-18, D-21, D-28, and D-29: assignee-scoped `pending` and `handled` task list behavior; `templateId`, `applicantName`, `departmentId`, `status`, `dateFrom`, and `dateTo` filters; task detail by task id using `schemaSnapshot`; `POST /approval/tasks/:id/approve` with optional `comment` max 200; `POST /approval/tasks/:id/reject` with required `comment` max 200; `POST /approval/tasks/:id/comment` with required `comment` max 200; comment events stored with `title: '内部备注'` and `payload.visibility: 'INTERNAL'`; and applicant own-detail hiding those internal comment events.</action>
  <verify>
    <automated>rg -n "pending assignee list|handled history|approveApprovalTask|rejectApprovalTask|commentApprovalTask|payload.visibility === 'INTERNAL'|/approval/tasks" backend/src/modules/approval/__tests__/task.service.test.ts backend/src/modules/approval/__tests__/task.route.test.ts backend/src/modules/approval/__tests__/application-submission.service.test.ts</automated>
  </verify>
  <done>Backend Wave 0 tests exist and pin every Phase 18 backend requirement plus the applicant-side remark leak guard before implementation starts.</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend approval-task store, type, and detail-layout test contracts</name>
  <read_first>.planning/phases/18-approval-task-inbox-mobile-approval/18-UI-SPEC.md, .planning/phases/18-approval-task-inbox-mobile-approval/18-VALIDATION.md, .planning/phases/17-my-applications-dynamic-submission/17-03-SUMMARY.md, frontend/src/types/__tests__/approvalApplication.test.ts, frontend/src/stores/__tests__/approvalApplication.test.ts, frontend/src/pages/ApprovalApplicationDetailPage.vue</read_first>
  <files>frontend/src/types/__tests__/approvalTask.test.ts, frontend/src/stores/__tests__/approvalTask.test.ts, frontend/src/pages/__tests__/ApprovalTaskDetailPage.test.ts</files>
  <acceptance_criteria>`approvalTask.test.ts` covers task status labels/colors, handled-state helpers, and payload key constants for `approve`, `reject`, and `comment`; store tests assert `GET /approval/tasks`, `GET /approval/tasks/meta`, `GET /approval/tasks/:id`, `POST /approval/tasks/:id/approve`, `POST /approval/tasks/:id/reject`, and `POST /approval/tasks/:id/comment`; page tests assert strings `审批详情`, `内部备注`, and mobile sticky buttons `驳回审批` / `通过审批`, plus reserved bottom padding for long snapshot content.</acceptance_criteria>
  <action>Create Vitest contracts for the frontend task slice per D-07, D-08, D-19, D-23, D-24, D-25, and D-26. The store tests must pin the exact endpoint paths and loading-flag cleanup. The type tests must pin Chinese status labels and helper behavior that distinguishes task status from application status. The detail page test must mount a future `ApprovalTaskDetailPage.vue` with mocked detail data and assert a single-column mobile render, sticky action region, and `COMMENT -> 内部备注` timeline copy.</action>
  <verify>
    <automated>rg -n "approval/tasks/meta|approval/tasks/.*/approve|approval/tasks/.*/reject|approval/tasks/.*/comment|审批详情|内部备注|驳回审批|通过审批" frontend/src/types/__tests__/approvalTask.test.ts frontend/src/stores/__tests__/approvalTask.test.ts frontend/src/pages/__tests__/ApprovalTaskDetailPage.test.ts</automated>
  </verify>
  <done>Frontend Wave 0 tests exist for DTO helpers, store API wiring, and mobile detail rendering risks before the implementation plans begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test contracts → later implementation | Wave 0 must pin assignee scope, trusted-field exclusions, and mobile safety so later code cannot drift silently |
| applicant detail serializer → frontend own-detail page | Internal-only comment visibility must be defined and tested before new remark events ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | E | `task.service.ts` / task action routes | mitigate | Add explicit tests for foreign-task access and assignee-only approve/reject/comment paths before implementation |
| T-18-02 | T | route body schemas | mitigate | Add forbidden trusted-field schema tests so approve/reject/comment bodies accept only `comment` and no snapshot or applicant fields |
| T-18-03 | I | applicant own-detail serializer | mitigate | Add regression coverage proving internal `COMMENT` events marked with `payload.visibility === 'INTERNAL'` are removed from own-detail responses |
</threat_model>

<verification>
Wave 0 completion requires the new test files to exist with the exact task names and endpoint strings referenced in `18-VALIDATION.md`, even though the implementation plans will make them pass later.
</verification>

<success_criteria>
All missing backend and frontend tests named in `18-VALIDATION.md` exist and define Phase 18 behavior without touching production code.
</success_criteria>

<output>
After completion, create `.planning/phases/18-approval-task-inbox-mobile-approval/18-01-SUMMARY.md`
</output>
