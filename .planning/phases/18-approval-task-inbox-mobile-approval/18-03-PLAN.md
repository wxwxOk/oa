---
phase: 18-approval-task-inbox-mobile-approval
plan: "03"
type: execute
wave: 1
depends_on: ["18-01"]
files_modified:
  - frontend/src/types/approvalTask.ts
  - frontend/src/types/__tests__/approvalTask.test.ts
  - frontend/src/stores/approvalTask.ts
  - frontend/src/stores/__tests__/approvalTask.test.ts
autonomous: true
requirements: [APR-01, APR-02, APR-03, APR-04, APR-06]
requirements_addressed: [APR-01, APR-02, APR-03, APR-04, APR-06]
user_setup: []
must_haves:
  truths:
    - "Approvers receive dedicated pending and handled task inbox data instead of applicant-owned application list data."
    - "Task list and detail views show task status separately from application status, so approvers can tell what they handled from the application's overall workflow state."
    - "Task detail actions use task-scoped approve, reject, and internal remark requests that match the approval task inbox behavior."
  artifacts:
    - path: "frontend/src/types/approvalTask.ts"
      provides: "Approval task DTOs and UI helper functions"
    - path: "frontend/src/stores/approvalTask.ts"
      provides: "Pinia task queue/detail/action API wrapper"
  key_links:
    - from: "frontend/src/stores/approvalTask.ts"
      to: "/approval/tasks"
      via: "authenticated axios calls"
      pattern: "/approval/tasks"
    - from: "frontend/src/types/__tests__/approvalTask.test.ts"
      to: "frontend/src/types/approvalTask.ts"
      via: "status labels, colors, helper, and payload-key assertions"
      pattern: "TASK_.*_PAYLOAD_KEYS|taskStatusLabel|canHandleTask"
---

<objective>
Create the frontend approval-task contracts and Pinia store that Phase 18 pages will consume.

Purpose: Keep the approver queue UI task-centered and isolated from the applicant-owned application store.
Output: `approvalTask` DTO helpers, task payload key constants, and a tested Pinia store for list/detail/meta/action calls.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-approval-task-inbox-mobile-approval/18-CONTEXT.md
@.planning/phases/18-approval-task-inbox-mobile-approval/18-UI-SPEC.md
@.planning/phases/18-approval-task-inbox-mobile-approval/18-01-PLAN.md
@.planning/phases/17-my-applications-dynamic-submission/17-03-SUMMARY.md
@frontend/src/types/approvalApplication.ts
@frontend/src/stores/approvalApplication.ts

<interfaces>
From frontend/src/types/approvalApplication.ts:
```ts
export interface ApprovalTimelineEvent { id: number; taskId: number | null; type: 'SUBMIT' | 'ASSIGN' | 'APPROVE' | 'REJECT' | 'CANCEL' | 'EDIT' | 'MARK' | 'COMMENT'; title: string; comment: string | null; createdAt: string; }
export interface ApprovalTaskSummary { id: number; nodeOrder: number; nodeName: string; status: 'PENDING' | 'APPROVED' | 'REJECTED' | 'CANCELED' | 'SKIPPED'; }
export function statusLabel(status: ApprovalApplicationStatus): string;
export function statusColor(status: ApprovalApplicationStatus): string;
```

From frontend/src/stores/approvalApplication.ts:
```ts
export const useApprovalApplicationStore = defineStore('approvalApplication', {
  state: () => ({ rows: [], total: 0, loading: false, detailLoading: false, actionLoading: false }),
  actions: { async fetchList(filters?: ApplicationListFilters) {}, async fetchDetail(id: number) {}, async cancel(id: number, reason?: string) {} }
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create task DTOs, helper functions, and type tests</name>
  <read_first>.planning/phases/18-approval-task-inbox-mobile-approval/18-UI-SPEC.md, frontend/src/types/approvalApplication.ts, frontend/src/types/__tests__/approvalApplication.test.ts, frontend/src/pages/ApprovalApplicationPage.vue</read_first>
  <files>frontend/src/types/approvalTask.ts, frontend/src/types/__tests__/approvalTask.test.ts</files>
  <acceptance_criteria>`approvalTask.ts` defines concrete types for task list rows, task detail, task filter options, pending/handled view filters, approve/reject/comment payloads, and handled summary helpers; helper exports include Chinese labels/colors for task statuses and booleans for pending-action visibility; tests assert `PENDING -> 待处理`, `APPROVED -> 已通过`, `REJECTED -> 已驳回`, `CANCELED -> 已关闭`, and payload key constants for `comment` only.</acceptance_criteria>
  <action>Create `frontend/src/types/approvalTask.ts` as the Phase 18 source of truth for approver DTOs per D-07, D-08, D-10, D-11, D-19, and D-29. Include `ApprovalTaskStatus`, `ApprovalTaskListView`, `ApprovalTaskRow`, `ApprovalTaskDetail`, `ApprovalTaskFilterOptions`, `ApprovalTaskListFilters`, and payload interfaces for `approve`, `reject`, and `comment`. Reuse `ApprovalTimelineEvent`, `ApprovalTaskSummary`, and application status types from `approvalApplication.ts` where compatible. Export helper functions such as `taskStatusLabel`, `taskStatusColor`, `isHandledTask`, and `canHandleTask` so list/detail pages do not inline task-state rules.</action>
  <verify>
    <automated>cd frontend && bun run test --run src/types/__tests__/approvalTask.test.ts</automated>
  </verify>
  <done>The frontend has a dedicated, tested task contract layer that distinguishes task-state semantics from applicant application-state semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the approval-task Pinia store and endpoint wiring tests</name>
  <read_first>.planning/phases/18-approval-task-inbox-mobile-approval/18-RESEARCH.md, frontend/src/stores/approvalApplication.ts, frontend/src/stores/__tests__/approvalApplication.test.ts, frontend/src/types/approvalTask.ts, frontend/src/stores/auth.ts</read_first>
  <files>frontend/src/stores/approvalTask.ts, frontend/src/stores/__tests__/approvalTask.test.ts</files>
  <acceptance_criteria>`approvalTask.ts` store uses the authenticated shared `api` instance only; actions include `fetchMeta`, `fetchList`, `fetchDetail`, `approve`, `reject`, and `comment`; tests assert the exact endpoints `GET /approval/tasks`, `GET /approval/tasks/meta`, `GET /approval/tasks/:id`, `POST /approval/tasks/:id/approve`, `POST /approval/tasks/:id/reject`, and `POST /approval/tasks/:id/comment`, and verify `loading`, `detailLoading`, and `actionLoading` always reset in `finally` blocks.</acceptance_criteria>
  <action>Create `useApprovalTaskStore` modeled on the Phase 17 store pattern. State must include `rows`, `total`, `page`, `size`, `view`, `filters`, `filterOptions`, `current`, `loading`, `detailLoading`, and `actionLoading`. `fetchList` must send `view`, pagination, and active filters. `approve`, `reject`, and `comment` must post `{ comment }` only and update `current` when the open task detail matches the acted-on id. Keep the store task-centered; do not call `/approval/applications` anywhere in this file.</action>
  <verify>
    <automated>cd frontend && bun run test --run src/stores/__tests__/approvalTask.test.ts</automated>
  </verify>
  <done>The approver queue has a dedicated tested store API surface that later pages can consume without re-deriving endpoint paths or loading-state rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend task pages → Pinia store | UI code must not invent alternate payloads or route paths for approval actions |
| shared task DTOs → approval pages | Centralized helper functions must keep task/application status semantics distinct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | T | frontend payload helpers | mitigate | Restrict approve/reject/comment payload key constants to `comment` only and test them explicitly |
| T-18-09 | I | frontend store routing | mitigate | Keep the task store on `/approval/tasks*` exclusively so applicant-owned `/approval/applications` data paths are not reused accidentally |
| T-18-10 | R | action loading state | mitigate | Use deterministic `finally` cleanup on `actionLoading`, `loading`, and `detailLoading` so duplicate action attempts do not linger in the UI after failures |
</threat_model>

<verification>
Run the new type and store tests green. No page or route files should change in this plan.
</verification>

<success_criteria>
Phase 18 has a standalone frontend task DTO/store layer with tested endpoint wiring and no dependency on applicant-owned `/approval/applications` APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/18-approval-task-inbox-mobile-approval/18-03-SUMMARY.md`
</output>
