---
phase: 20-api
plan: 3
type: execute
wave: 2
depends_on: [20-01, 20-02]
files_modified:
  - backend/src/modules/visit/visit.route.ts
  - backend/src/index.ts
  - backend/src/modules/visit/__tests__/visit.route.test.ts
  - backend/src/modules/visit/__tests__/visit-import.test.ts
  - backend/src/modules/visit/__tests__/visit-stats.test.ts
autonomous: true
requirements: [VISIT-01, PERM-02]
user_setup: []
must_haves:
  truths:
    - "`visitModule` is registered under `/api/v1/visits` and exposes all Phase 20 endpoints."
    - "Every visit endpoint uses the exact `visit:*` permission required by the context."
    - "Create/update/import explicitly pick writable fields and derive `creatorId` from `currentUser.id`."
    - "List, filter-options, stats and import contracts are stable for Phases 21-23."
  artifacts:
    - path: "backend/src/modules/visit/visit.route.ts"
      provides: "Visit schemas, serializers, list/detail/CRUD/filter/import/stats handlers and helper exports"
      contains: "export const visitModule"
    - path: "backend/src/index.ts"
      provides: "Registration of visitModule inside `/api/v1`"
      contains: "visitModule"
    - path: "backend/src/modules/visit/__tests__/visit.route.test.ts"
      provides: "Passing route/schema/list/filter contract checks"
    - path: "backend/src/modules/visit/__tests__/visit-import.test.ts"
      provides: "Passing import validation and attribution checks"
    - path: "backend/src/modules/visit/__tests__/visit-stats.test.ts"
      provides: "Passing stats grouping and date-filter checks"
  key_links:
    - from: "backend/src/modules/visit/visit.route.ts"
      to: "backend/prisma/schema.prisma"
      via: "generated Prisma client"
      pattern: "prisma.visitRecord"
    - from: "backend/src/modules/visit/visit.route.ts"
      to: "backend/src/middlewares/auth.ts"
      via: "route guards and currentUser"
      pattern: "authGuard('visit:list')|currentUser.id"
---

<objective>
Implement the Phase 20 visit backend API.

Purpose: complete the fixed visit ledger backend so later frontend, import preview and stats phases can consume stable endpoints.
Output: `visitModule`, `/api/v1` registration, CRUD/list/filter-options/import/stats behavior, and passing Phase 20 backend tests.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-api/20-CONTEXT.md
@.planning/phases/20-api/20-RESEARCH.md
@.planning/phases/20-api/20-VALIDATION.md
@.planning/phases/20-api/20-01-PLAN.md
@.planning/phases/20-api/20-02-PLAN.md
@backend/prisma/schema.prisma
@backend/src/index.ts
@backend/src/middlewares/auth.ts
@backend/src/modules/user/user.route.ts
@backend/src/modules/submission/submission.route.ts
@backend/src/modules/form-stats/form-stats.route.ts
@backend/src/modules/visit/__tests__/visit.route.test.ts
@backend/src/modules/visit/__tests__/visit-import.test.ts
@backend/src/modules/visit/__tests__/visit-stats.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add visit schemas, serializers and helper functions</name>
  <files>backend/src/modules/visit/visit.route.ts</files>
  <read_first>
    - @.planning/phases/20-api/20-CONTEXT.md decisions D-03, D-10, D-12 through D-18
    - @.planning/phases/20-api/20-RESEARCH.md TypeBox, import and stats findings
    - @backend/src/modules/visit/__tests__/visit.route.test.ts
    - @backend/src/modules/visit/__tests__/visit-import.test.ts
    - @backend/src/modules/visit/__tests__/visit-stats.test.ts
  </read_first>
  <behavior>
    - Client input can only contain business fields.
    - Import validation happens before any row is inserted.
    - Stats grouping rules are small exported helpers so Phase 23 can adjust them safely.
  </behavior>
  <action>
    Create `backend/src/modules/visit/visit.route.ts` with exported TypeBox schemas and helpers expected by Wave 0 tests: `visitListQuery`, `visitWriteBody`, `visitImportBody`, `visitModule`, `validateVisitImportRows`, `buildVisitStats`, `isIntentVisit`, and `isSignedVisit` as needed. `visitWriteBody` must include only `name`, `age`, `education`, `gender`, `channelPartner`, `consultant`, `receptionStatus`, `receptionist`, `receptionDate`, `consultationStatus`, `statusCategory`, `statusDescription`, `trialStatus`, `solution`, and `trialDate`, with `{ additionalProperties: false }`. Add a small field-picking helper for Prisma writes; do not pass `body` directly into Prisma. Normalize blank optional strings to `null` if useful, trim `name`, parse date strings into `Date` only after validity checks, and keep duplicate rows untouched. Implement `isIntentVisit` and `isSignedVisit` with conservative string keyword checks over `consultationStatus`, `statusCategory`, and `trialStatus`; do not add dictionaries or enums.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/visit/__tests__/visit-import.test.ts src/modules/visit/__tests__/visit-stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export const visitWriteBody|additionalProperties: false|validateVisitImportRows|buildVisitStats|isIntentVisit|isSignedVisit" backend/src/modules/visit/visit.route.ts` finds exported contracts.
    - `rg "creatorId.*body|data: body" backend/src/modules/visit/visit.route.ts` finds no unsafe body passthrough.
    - `cd backend && bun test src/modules/visit/__tests__/visit-import.test.ts src/modules/visit/__tests__/visit-stats.test.ts` exits 0 once route behavior is complete.
  </acceptance_criteria>
  <done>Shared visit schemas and helpers satisfy import/stats contract tests without over-modeling statuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement list, detail, CRUD and filter-options routes</name>
  <files>backend/src/modules/visit/visit.route.ts, backend/src/index.ts, backend/src/modules/visit/__tests__/visit.route.test.ts</files>
  <read_first>
    - @.planning/phases/20-api/20-CONTEXT.md decisions D-07 through D-15
    - @backend/src/middlewares/auth.ts for `authGuard(requiredPerm)` and `currentUser`
    - @backend/src/modules/submission/submission.route.ts for `{ rows, total, page, size }` and date filtering
    - @backend/src/modules/user/user.route.ts for guarded CRUD style
  </read_first>
  <action>
    Implement `visitModule = new Elysia({ prefix: '/visits' })`. Declare static routes before `/:id`: `GET /filter-options`, `GET /stats`, and `POST /import`, then detail/update/delete as appropriate. Implement list `GET /` with pagination, keyword/name/person/status filters, `receptionDate` date range and response `{ rows, total, page, size }`. Use `dateTo + 'T23:59:59.999Z'` for inclusive end dates. Implement `GET /:id`, `POST /`, `PUT /:id`, and `DELETE /:id` with `notFound`/business errors consistent with existing modules. Guard list/detail/filter-options with `visit:list`; create with `visit:create`; update with `visit:update`; delete with `visit:delete`. Implement `GET /filter-options` by returning non-empty distinct values for `channelPartners`, `consultants`, `receptionists`, `receptionStatuses`, `consultationStatuses`, and `statusCategories`. Register `visitModule` inside the `/api/v1` group in `backend/src/index.ts`.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/visit/__tests__/visit.route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "new Elysia\(\{ prefix: '/visits' \}\)|authGuard\('visit:list'\)|authGuard\('visit:create'\)|authGuard\('visit:update'\)|authGuard\('visit:delete'\)" backend/src/modules/visit/visit.route.ts` finds route module and guards.
    - `rg "filter-options|rows|total|page|size|dateTo.*T23:59:59.999Z|distinct" backend/src/modules/visit/visit.route.ts` finds list/filter behavior.
    - `rg "visitModule" backend/src/index.ts` finds module registration.
    - `cd backend && bun test src/modules/visit/__tests__/visit.route.test.ts` exits 0.
  </acceptance_criteria>
  <done>Visit CRUD/list/filter endpoints satisfy Phase 20 route and permission contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement import, stats and final backend validation</name>
  <files>backend/src/modules/visit/visit.route.ts, backend/src/modules/visit/__tests__/visit-import.test.ts, backend/src/modules/visit/__tests__/visit-stats.test.ts</files>
  <read_first>
    - @.planning/phases/20-api/20-CONTEXT.md decisions D-15 through D-18
    - @.planning/phases/20-api/20-VALIDATION.md tasks 20-00-06 and 20-00-07
    - @backend/src/modules/form-stats/form-stats.route.ts for aggregation precedent
  </read_first>
  <behavior>
    - Import accepts normalized JSON rows only, not files.
    - Invalid import rows reject the entire batch before inserts.
    - Stats are protected by `visit:stats`, not `visit:list`.
  </behavior>
  <action>
    Implement `POST /import` with `authGuard('visit:import')`, `visitImportBody`, `validateVisitImportRows`, all-or-nothing validation, `creatorId: currentUser.id`, and response `{ createdCount, total }`. Do not dedupe, merge, skip duplicates, parse Excel files, or add file upload handling. Implement `GET /stats` with `authGuard('visit:stats')`, `dateFrom/dateTo` filters on `receptionDate`, grouped counts by channel partner, consultant, receptionist, reception status, consultation status, status category and trial status, plus `total`, `intentCount`, and `signedCount`. Keep stats helpers deterministic and easy to adjust later. Run focused visit tests, Prisma validate/generate if needed, then backend build.
  </action>
  <verify>
    <automated>cd backend && bun test && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "authGuard\('visit:import'\)|createdCount|validateVisitImportRows|createMany|creatorId: currentUser\.id" backend/src/modules/visit/visit.route.ts` finds import behavior.
    - `rg "authGuard\('visit:stats'\)|byChannelPartner|byConsultant|byReceptionist|byTrialStatus|intentCount|signedCount" backend/src/modules/visit/visit.route.ts` finds stats behavior.
    - `rg "xlsx|excel|upload|multer|unique|upsert" backend/src/modules/visit/visit.route.ts` finds no file parsing, dedupe or merge implementation.
    - `cd backend && bun test src/modules/visit/__tests__/visit.route.test.ts src/modules/visit/__tests__/visit-import.test.ts src/modules/visit/__tests__/visit-stats.test.ts` exits 0.
    - `cd backend && bun test && bun run build` exits 0.
  </acceptance_criteria>
  <done>Import and stats endpoints are complete, tested and ready for later v1.3 frontend phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->visit API | Authenticated clients send filters, writes, imports and stats queries. |
| route body->Prisma | Only explicitly picked fields may cross into Prisma writes. |
| stats/list permissions | Users with list access do not automatically gain import or stats access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-PERM | E/I | route guards | mitigate | Exact `authGuard('visit:*')` per endpoint. |
| T-20-TAMPER | T/E | create/update/import | mitigate | Strict schemas and explicit field picking; creator from JWT only. |
| T-20-IDOR | I/E | detail/list | accept with control | Module-level permission scope is intentional in Phase 20 and route tests pin it. |
| T-20-IMPORT | R/T | import endpoint | mitigate | Validate all rows first; no partial insert, auto skip, merge or dedupe. |
| T-20-STATS | I | stats endpoint | mitigate | Separate stats permission and receptionDate-only filters. |
</threat_model>

<verification>
Final Phase 20 backend gate: `cd backend && bun test && bun run build` must pass before phase verification.
</verification>

<success_criteria>
Phase 20 success criteria 3 and 4 are satisfied, and the full visit backend foundation is ready for Phases 21-23.
</success_criteria>

<output>
After completion, create `.planning/phases/20-api/20-03-SUMMARY.md`
</output>
