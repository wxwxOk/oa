---
phase: 20-api
plan: 2
type: execute
wave: 1
depends_on: [20-01]
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/20260502090000_add_visit_records/migration.sql
  - backend/prisma/seed.ts
  - backend/src/modules/role/__tests__/visit-permissions.seed.test.ts
autonomous: true
requirements: [VISIT-01, PERM-01, PERM-02]
user_setup: []
must_haves:
  truths:
    - "Prisma stores visits in a fixed `VisitRecord` table with all 15 sample-sheet fields."
    - "Visit records derive creator linkage from `User` and include created/updated timestamps."
    - "ADMIN receives all visit permissions while EMPLOYEE receives none by default."
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "VisitRecord model, User.visitRecords relation, fields and indexes"
      contains: "model VisitRecord"
    - path: "backend/prisma/migrations/20260502090000_add_visit_records/migration.sql"
      provides: "Database migration for the visit ledger table and indexes"
    - path: "backend/prisma/seed.ts"
      provides: "VISIT_PERMISSION_CODES and visit permission seed data"
      contains: "visit:import"
    - path: "backend/src/modules/role/__tests__/visit-permissions.seed.test.ts"
      provides: "Passing seed contract for visit permissions"
  key_links:
    - from: "backend/prisma/schema.prisma"
      to: "backend/src/modules/visit/visit.route.ts"
      via: "future Prisma client model"
      pattern: "prisma.visitRecord"
    - from: "backend/prisma/seed.ts"
      to: "backend/src/modules/visit/visit.route.ts"
      via: "route guard permission literals"
      pattern: "visit:list|visit:create|visit:update|visit:delete|visit:import|visit:stats"
---

<objective>
Add the Phase 20 data model, migration and permission seed.

Purpose: provide the database and RBAC foundation needed before implementing `/api/v1/visits` handlers.
Output: updated Prisma schema/migration, generated client gate, visit permission seed codes and passing seed tests.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-api/20-CONTEXT.md
@.planning/phases/20-api/20-RESEARCH.md
@.planning/phases/20-api/20-VALIDATION.md
@.planning/phases/20-api/20-01-PLAN.md
@backend/prisma/schema.prisma
@backend/prisma/seed.ts
@backend/src/modules/role/__tests__/visit-permissions.seed.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add VisitRecord Prisma model</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    - @.planning/phases/20-api/20-CONTEXT.md decisions D-01 through D-06
    - @.planning/phases/20-api/20-RESEARCH.md Data model section
    - @backend/prisma/schema.prisma current `User` model and relation/index conventions
  </read_first>
  <behavior>
    - Visit data is stored as fixed business ledger rows, not `Submission` or JSONB dynamic form data.
    - Status-like values remain nullable strings.
    - Duplicate candidates are allowed at database level.
  </behavior>
  <action>
    Extend `schema.prisma` by adding `visitRecords VisitRecord[]` to `model User` and adding `model VisitRecord`. The model must include `id Int @id @default(autoincrement())`, required `name String`, nullable `age Int?`, nullable string fields `education`, `gender`, `channelPartner`, `consultant`, `receptionStatus`, `receptionist`, `consultationStatus`, `statusCategory`, `statusDescription`, `trialStatus`, and `solution`, nullable date fields `receptionDate DateTime?` and `trialDate DateTime?`, `creatorId Int`, `creator User @relation(fields: [creatorId], references: [id])`, `createdAt DateTime @default(now())`, and `updatedAt DateTime @updatedAt`. Add indexes for `name`, `channelPartner`, `consultant`, `receptionist`, `receptionStatus`, `receptionDate`, `consultationStatus`, `statusCategory`, and `creatorId`. Do not add enum types, dictionary tables, JSONB schema storage, or a unique constraint across `name`, `receptionDate`, and `consultant`.
  </action>
  <verify>
    <automated>cd backend && bun --env-file=../.env prisma format && bun --env-file=../.env prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `rg "model VisitRecord|visitRecords VisitRecord\[\]" backend/prisma/schema.prisma` finds the model and User relation.
    - `rg "name +String|age +Int\?|receptionDate +DateTime\?|trialDate +DateTime\?|creatorId +Int" backend/prisma/schema.prisma` finds the required field types.
    - `rg "@@index\(\[name\]\)|@@index\(\[channelPartner\]\)|@@index\(\[consultant\]\)|@@index\(\[creatorId\]\)" backend/prisma/schema.prisma` finds key indexes.
    - `rg "enum Visit|@@unique\(\[name, receptionDate, consultant\]\)" backend/prisma/schema.prisma` finds no matches.
  </acceptance_criteria>
  <done>`VisitRecord` schema exists, validates, and preserves the fixed-ledger model boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create VisitRecord migration and generated client gate</name>
  <files>backend/prisma/migrations/20260502090000_add_visit_records/migration.sql, backend/prisma/schema.prisma</files>
  <read_first>
    - @backend/prisma/schema.prisma with Task 1 changes
    - @.planning/phases/20-api/20-VALIDATION.md Prisma validation commands
  </read_first>
  <action>
    Create the Prisma migration for `VisitRecord` at `backend/prisma/migrations/20260502090000_add_visit_records/migration.sql`, using Prisma's normal generated SQL when possible. The migration must create the visit table, foreign key to `User(id)`, timestamp defaults, nullable business fields, and the indexes listed in Task 1. Then run Prisma formatting, validation and client generation. If the local `.env` points at Docker service host `postgres`, use the existing project convention of overriding `DATABASE_URL` to localhost only for host-run migration commands; do not edit `.env` files.
  </action>
  <verify>
    <automated>cd backend && bun --env-file=../.env prisma validate && bun --env-file=../.env prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - `rg "VisitRecord|visit_records|VisitRecord" backend/prisma/migrations/20260502090000_add_visit_records/migration.sql backend/prisma/schema.prisma` finds migration/model DDL references.
    - `rg "CREATE INDEX|VisitRecord.*channelPartner|VisitRecord.*creatorId" backend/prisma/migrations/20260502090000_add_visit_records/migration.sql` finds index creation statements, allowing Prisma's exact quoted names.
    - `cd backend && bun --env-file=../.env prisma validate` exits 0.
    - `cd backend && bun --env-file=../.env prisma generate` exits 0.
  </acceptance_criteria>
  <done>Migration and generated client support `prisma.visitRecord` before route implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seed visit permissions</name>
  <files>backend/prisma/seed.ts, backend/src/modules/role/__tests__/visit-permissions.seed.test.ts</files>
  <read_first>
    - @.planning/phases/20-api/20-CONTEXT.md decisions D-19 and D-20
    - @.planning/phases/20-api/20-01-PLAN.md Task 1 seed contract
    - @backend/prisma/seed.ts current permission constants, `PERMISSIONS`, `EMPLOYEE_PERMISSION_CODES`, and ADMIN assignment
    - @backend/src/modules/role/__tests__/visit-permissions.seed.test.ts failing Wave 0 contract
  </read_first>
  <behavior>
    - ADMIN inherits all visit permissions from the existing all-permissions seed flow.
    - EMPLOYEE does not receive visit permissions by default.
  </behavior>
  <action>
    Add and export `VISIT_PERMISSION_CODES` containing exactly `visit:list`, `visit:create`, `visit:update`, `visit:delete`, `visit:import`, and `visit:stats`. Add these six permissions to `PERMISSIONS` with `module: 'visit'` and concise Chinese names matching the operation. Keep `EMPLOYEE_PERMISSION_CODES` unchanged unless tests prove it needs a mechanical import fix; do not grant any visit code to EMPLOYEE. Ensure the seed test from 20-01 passes without changing its contract intent.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/role/__tests__/visit-permissions.seed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "VISIT_PERMISSION_CODES|visit:list|visit:create|visit:update|visit:delete|visit:import|visit:stats" backend/prisma/seed.ts backend/src/modules/role/__tests__/visit-permissions.seed.test.ts` finds all six codes.
    - `rg "module: 'visit'|module: \"visit\"" backend/prisma/seed.ts` finds visit permission seed entries.
    - `rg "EMPLOYEE_PERMISSION_CODES[\s\S]*visit:" backend/prisma/seed.ts` finds no employee default visit grants, or the test explicitly proves exclusion.
    - `cd backend && bun test src/modules/role/__tests__/visit-permissions.seed.test.ts` exits 0.
  </acceptance_criteria>
  <done>Visit permissions are seeded and covered by focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma schema->visit route | Route handlers will rely on the generated `visitRecord` model and must not write trusted fields from the client. |
| seed permissions->authGuard | Permission literals in seed data must match future route guard literals exactly. |
| database uniqueness->import behavior | Database must allow duplicates because duplicate warning is a UI/import-preview concern, not an automatic backend merge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-PERM | E/I | permission seed | mitigate | Export exact visit codes, seed them once, keep EMPLOYEE excluded by default. |
| T-20-TAMPER | T | Prisma audit fields | mitigate | `creatorId` exists for server-derived attribution; later routes must pick fields explicitly. |
| T-20-DATA-MODEL | T/D | schema design | mitigate | Fixed table with indexed business fields avoids dynamic JSON queries and premature enums. |
| T-20-IMPORT | R/T | uniqueness design | mitigate | No unique constraint that could silently reject or merge confirmed duplicate rows. |
</threat_model>

<verification>
Blocking gate: `cd backend && bun --env-file=../.env prisma validate && bun --env-file=../.env prisma generate && bun test src/modules/role/__tests__/visit-permissions.seed.test.ts` must pass before Plan 20-03 route implementation proceeds.
</verification>

<success_criteria>
The backend data model, migration and permission seed satisfy Phase 20 success criteria 1 and 2 and unblock `prisma.visitRecord` route implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/20-api/20-02-SUMMARY.md`
</output>
