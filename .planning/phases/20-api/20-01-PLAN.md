---
phase: 20-api
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/modules/visit/__tests__/visit.route.test.ts
  - backend/src/modules/visit/__tests__/visit-import.test.ts
  - backend/src/modules/visit/__tests__/visit-stats.test.ts
  - backend/src/modules/role/__tests__/visit-permissions.seed.test.ts
autonomous: true
requirements: [VISIT-01, PERM-01, PERM-02]
user_setup: []
must_haves:
  truths:
    - "Phase 20 backend behavior is pinned by failing contract tests before implementation."
    - "Every Phase 20 success criterion has at least one backend automated test target."
    - "Visit write/import/stat routes reject trusted-field tampering and use exact permission gates."
  artifacts:
    - path: "backend/src/modules/visit/__tests__/visit.route.test.ts"
      provides: "Visit route prefix, route signatures, query/body schema, list/detail/CRUD, and filter-options contracts"
    - path: "backend/src/modules/visit/__tests__/visit-import.test.ts"
      provides: "Import validation, all-or-nothing behavior, creator attribution, and no-dedupe contracts"
    - path: "backend/src/modules/visit/__tests__/visit-stats.test.ts"
      provides: "Stats permission, date filtering, grouping, and intent/signed string-rule contracts"
    - path: "backend/src/modules/role/__tests__/visit-permissions.seed.test.ts"
      provides: "Visit permission seed and default role-grant contracts"
  key_links:
    - from: "backend/src/modules/visit/__tests__/visit.route.test.ts"
      to: "backend/src/modules/visit/visit.route.ts"
      via: "expected exported route module and schemas"
      pattern: "visitModule|visitWriteBody|visitListQuery"
    - from: "backend/src/modules/visit/__tests__/visit-import.test.ts"
      to: "backend/src/modules/visit/visit.route.ts"
      via: "expected import validation helper"
      pattern: "validateVisitImportRows"
    - from: "backend/src/modules/role/__tests__/visit-permissions.seed.test.ts"
      to: "backend/prisma/seed.ts"
      via: "expected exported permission constants"
      pattern: "VISIT_PERMISSION_CODES"
---

<objective>
Create Wave 0 backend validation contracts for Phase 20.

Purpose: lock the fixed visit ledger model/API/permission/import/stats contracts before Prisma schema and route implementation.
Output: backend test files that initially fail until later plans satisfy the contracts.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-api/20-CONTEXT.md
@.planning/phases/20-api/20-RESEARCH.md
@.planning/phases/20-api/20-VALIDATION.md
@backend/prisma/seed.ts
@backend/src/modules/user/user.route.ts
@backend/src/modules/submission/submission.route.ts
@backend/src/modules/form-stats/form-stats.route.ts
@backend/src/modules/approval/__tests__/archive.route.test.ts
@backend/src/modules/role/__tests__/approval-permissions.seed.test.ts
</context>

<source_audit>
## Phase 20 Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| ROADMAP | `VisitRecord` maps 15 fields plus `creatorId`, timestamps and indexes | 20-01 route/model contracts; 20-02 Prisma model; 20-03 route serialization |
| ROADMAP | Seed contains `visit:list/create/update/delete/import/stats`, ADMIN gets all | 20-01 seed contracts; 20-02 seed implementation |
| ROADMAP | `visitModule` registered at `/api/v1/visits` with list/detail/create/update/delete/filter-options/stats/import | 20-01 route contracts; 20-03 module implementation |
| ROADMAP | Backend endpoints use exact permissions and Elysia `t.Object` body validation | 20-01 schema/guard contracts; 20-03 route implementation |
| REQUIREMENTS | VISIT-01 create visit records with base fields | 20-01 write schema tests; 20-02 model; 20-03 create/update/detail routes |
| REQUIREMENTS | PERM-01 independent visit permissions | 20-01 seed tests; 20-02 permission seed |
| REQUIREMENTS | PERM-02 backend endpoint permissions | 20-01 guard contracts; 20-03 route guards |
| CONTEXT | D-01 through D-06 fixed model, nullable string statuses, no unique dedupe | 20-01 model contract tests; 20-02 schema |
| CONTEXT | D-07 through D-15 API, list, filter-options and stats contracts | 20-01 route/stats tests; 20-03 route implementation |
| CONTEXT | D-16 through D-18 import boundary | 20-01 import tests; 20-03 import implementation |
| RESEARCH | Static routes before `/:id`, `{ rows, total, page, size }`, end-of-day `dateTo` | 20-01 route tests; 20-03 handlers |
| DEFERRED | UI, Excel parsing preview, charts, export, dictionaries, auto dedupe | Not planned |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add visit permission seed contracts</name>
  <files>backend/src/modules/role/__tests__/visit-permissions.seed.test.ts</files>
  <read_first>
    - @.planning/phases/20-api/20-CONTEXT.md decisions D-19 and D-20
    - @.planning/phases/20-api/20-VALIDATION.md task 20-00-01
    - @backend/prisma/seed.ts for current `PERMISSIONS`, role seed and exported permission-code style
    - @backend/src/modules/role/__tests__/approval-permissions.seed.test.ts for existing seed test patterns
  </read_first>
  <action>
    Create `visit-permissions.seed.test.ts` using `bun:test`. Strictly reuse the import path style from `approval-permissions.seed.test.ts` for future seed exports. Assert `VISIT_PERMISSION_CODES` is exactly `['visit:list', 'visit:create', 'visit:update', 'visit:delete', 'visit:import', 'visit:stats']`; every code appears once in `PERMISSIONS` with `module: 'visit'`; ADMIN receives all visit permissions through the all-permissions seed flow; and `EMPLOYEE_PERMISSION_CODES` does not include any `visit:*` permission by default. Keep the test focused on seed data and role defaults, not route behavior.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path 'backend/src/modules/role/__tests__/visit-permissions.seed.test.ts'"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "VISIT_PERMISSION_CODES|visit:list|visit:create|visit:update|visit:delete|visit:import|visit:stats" backend/src/modules/role/__tests__/visit-permissions.seed.test.ts` finds all six codes.
    - `rg "module.*visit|EMPLOYEE_PERMISSION_CODES|not\.toContain|not\.toInclude" backend/src/modules/role/__tests__/visit-permissions.seed.test.ts` finds module and employee-exclusion assertions.
    - Running `cd backend && bun test src/modules/role/__tests__/visit-permissions.seed.test.ts` fails only because visit seed exports/codes are not implemented yet.
  </acceptance_criteria>
  <done>Visit permission contracts cover PERM-01 and the backend seed portion of PERM-02.</done>
</task>

<task type="auto">
  <name>Task 2: Add visit route and schema contract tests</name>
  <files>backend/src/modules/visit/__tests__/visit.route.test.ts</files>
  <read_first>
    - @.planning/phases/20-api/20-CONTEXT.md decisions D-01 through D-15
    - @.planning/phases/20-api/20-RESEARCH.md Routes and validation section
    - @backend/src/modules/approval/__tests__/archive.route.test.ts for route contract and schema strictness style
    - @backend/src/modules/user/user.route.ts and @backend/src/modules/submission/submission.route.ts for query/body schema precedents
  </read_first>
  <action>
    Create `visit.route.test.ts` for future `../visit.route`. Assert `visitModule.config.prefix === '/visits'`; route signatures include `GET /`, `GET /filter-options`, `GET /stats`, `GET /:id`, `POST /`, `PUT /:id`, `DELETE /:id`, and `POST /import`; static route paths are declared before `/:id`; list query accepts `page`, `size`, `keyword`, `name`, `channelPartner`, `consultant`, `receptionist`, `receptionStatus`, `consultationStatus`, `statusCategory`, `dateFrom`, and `dateTo`; write schema accepts only business fields and has `additionalProperties: false`; forbidden trusted fields include `id`, `creatorId`, `creator`, `createdAt`, and `updatedAt`; list response contract uses `{ rows, total, page, size }`; filter-options response contract contains `channelPartners`, `consultants`, `receptionists`, `receptionStatuses`, `consultationStatuses`, and `statusCategories`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path 'backend/src/modules/visit/__tests__/visit.route.test.ts'"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "prefix.*visits|filter-options|visit:list|visit:create|visit:update|visit:delete" backend/src/modules/visit/__tests__/visit.route.test.ts` finds route and permission contracts.
    - `rg "additionalProperties.*false|creatorId|createdAt|updatedAt|trusted" backend/src/modules/visit/__tests__/visit.route.test.ts` finds schema hardening assertions.
    - `rg "rows|total|page|size|channelPartners|consultants|receptionists" backend/src/modules/visit/__tests__/visit.route.test.ts` finds list/filter response contracts.
  </acceptance_criteria>
  <done>Visit route contracts cover VISIT-01 and PERM-02 route boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add import and stats contract tests</name>
  <files>backend/src/modules/visit/__tests__/visit-import.test.ts, backend/src/modules/visit/__tests__/visit-stats.test.ts</files>
  <read_first>
    - @.planning/phases/20-api/20-CONTEXT.md decisions D-12 through D-18
    - @.planning/phases/20-api/20-VALIDATION.md tasks 20-00-06 and 20-00-07
    - @backend/src/modules/form-stats/form-stats.route.ts for lightweight grouping precedent
    - @backend/src/modules/submission/submission.route.ts for date filter precedent
  </read_first>
  <action>
    Create import tests for future exported helper `validateVisitImportRows` and route behavior. Assert `{ rows: VisitWriteInput[] }` is required; `name` is required after trim; `age` must be an integer when present; `receptionDate` and `trialDate` must be parseable dates when present; invalid rows reject the whole batch with row-level errors; valid rows preserve duplicates and derive `creatorId` from `currentUser.id`, never from body. Create stats tests for future exported helpers `buildVisitStats`, `isIntentVisit`, and `isSignedVisit`. Assert stats use `visit:stats`, date filtering uses `receptionDate` with end-of-day `dateTo`, grouped counts cover channel partner, consultant, receptionist, reception status, consultation status, status category and trial status, and intent/signed counts are based on conservative string keywords without dictionaries.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path 'backend/src/modules/visit/__tests__/visit-import.test.ts'; Test-Path 'backend/src/modules/visit/__tests__/visit-stats.test.ts'"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "validateVisitImportRows|all-or-nothing|creatorId|duplicate|dedupe|name|age|receptionDate|trialDate" backend/src/modules/visit/__tests__/visit-import.test.ts` finds import contracts.
    - `rg "buildVisitStats|isIntentVisit|isSignedVisit|visit:stats|receptionDate|dateTo" backend/src/modules/visit/__tests__/visit-stats.test.ts` finds stats/date contracts.
    - `rg "channelPartner|consultant|receptionist|receptionStatus|consultationStatus|statusCategory|trialStatus" backend/src/modules/visit/__tests__/visit-stats.test.ts` finds all grouping dimensions.
  </acceptance_criteria>
  <done>Import and stats contracts cover the backend APIs needed by Phases 22 and 23.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->visit API | Authenticated clients submit visit filters, write bodies, import rows and stats queries. |
| seed permissions->route guards | Visit capability is granted only by role permissions and checked by backend guards. |
| import preview->backend insert | Phase 22 will preview normalized rows; Phase 20 must not silently skip or change them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-PERM | E/I | seed and route contracts | mitigate | Tests pin six exact visit codes and employee exclusion. |
| T-20-TAMPER | T/E | write/import schemas | mitigate | Tests require strict TypeBox schemas and forbidden trusted fields. |
| T-20-IDOR | I/E | list/detail/import scope | accept with control | Phase 20 deliberately uses module-level permissions; tests pin exact guards and creator derivation. |
| T-20-IMPORT | T/R | import endpoint | mitigate | Tests require all-or-nothing validation and no auto dedupe/merge. |
| T-20-STATS | I | stats endpoint | mitigate | Tests require separate `visit:stats` permission and receptionDate filters. |
| T-20-DATA-MODEL | T | data model contract | mitigate | Tests pin fixed ledger fields and forbid dynamic form overuse by expected field list. |
</threat_model>

<verification>
Wave 0 verification is contract scaffolding. These tests are expected to fail until plans 20-02 and 20-03 implement the referenced seed, Prisma model, route module and helpers.
</verification>

<success_criteria>
All Phase 20 Wave 0 backend test files exist and pin route strings, DTO keys, permission codes, import validation and stats contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/20-api/20-01-SUMMARY.md`
</output>
