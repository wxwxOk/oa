---
phase: 36-notify-visibility
plan: 2
type: execute
wave: 1
depends_on: [36-01]
files_modified:
  - frontend/src/types/notification.ts
  - frontend/src/stores/notification.ts
  - frontend/src/stores/__tests__/notification.test.ts
  - frontend/src/layouts/MainLayout.vue
  - frontend/src/layouts/__tests__/MainLayoutNotification.test.ts
  - frontend/src/router/routes.ts
  - frontend/src/pages/ChannelPushReviewPage.vue
  - frontend/src/pages/ChannelPushReviewDetailPage.vue
  - frontend/src/pages/__tests__/ChannelPushReviewPage.test.ts
  - frontend/src/pages/__tests__/ChannelPushReviewDetailPage.test.ts
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-04, REVIEW-02]
requirements_addressed: [NOTIF-01, NOTIF-02, NOTIF-04, REVIEW-02]
user_setup: []
must_haves:
  truths:
    - "The header notification control shows unread count, loads the notification list on open, and polls unread count on focus and a fixed interval."
    - "Clicking a notification navigates to its target route and then marks it read."
    - "Review pages remain mutable only for `channelPush:review`; `channelPush:viewScope` users can open the pages but only as read-only viewers."
  artifacts:
    - path: "frontend/src/types/notification.ts"
      provides: "Notification DTOs and helper labels for header badge/list rendering."
    - path: "frontend/src/stores/notification.ts"
      provides: "Current-user notification unread/list/read/mark-all-read API store."
    - path: "frontend/src/layouts/MainLayout.vue"
      provides: "Header badge, menu/dialog list, polling, navigation, and mark-read integration."
    - path: "frontend/src/router/routes.ts"
      provides: "Review route visibility for `channelPush:review` and `channelPush:viewScope`."
    - path: "frontend/src/pages/ChannelPushReviewPage.vue"
      provides: "Read-only banner and page-level viewScope handling."
    - path: "frontend/src/pages/ChannelPushReviewDetailPage.vue"
      provides: "Read-only banner and action gating for scope-only users."
  key_links:
    - from: "frontend/src/layouts/MainLayout.vue"
      to: "frontend/src/stores/notification.ts"
      via: "unread polling and notification menu refresh"
      pattern: "useNotificationStore|fetchUnreadCount|fetchList|markRead|markAllRead"
    - from: "frontend/src/layouts/MainLayout.vue"
      to: "frontend/src/router/routes.ts"
      via: "notification click routing"
      pattern: "targetRoute"
    - from: "frontend/src/router/routes.ts"
      to: "frontend/src/pages/ChannelPushReviewPage.vue"
      via: "menu and route visibility"
      pattern: "permAny"
---

<objective>
Build the frontend notification center and expose the read-only review experience to `channelPush:viewScope` users.

Scope: header badge/menu/dialog, unread polling, navigation + mark-read behavior, and the route/menu/page contracts for read-only review visibility.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-notify-visibility/36-CONTEXT.md
@frontend/src/layouts/MainLayout.vue
@frontend/src/router/routes.ts
@frontend/src/stores/auth.ts
@frontend/src/stores/channelPush.ts
@frontend/src/pages/ChannelPushReviewPage.vue
@frontend/src/pages/ChannelPushReviewDetailPage.vue
@frontend/src/components/channel-push/ChannelPushStatusChip.vue
@frontend/src/pages/__tests__/ChannelPushReviewPage.test.ts
@frontend/src/pages/__tests__/ChannelPushReviewDetailPage.test.ts
</context>

<source_audit>
| Source | Item | Covered By |
|--------|------|------------|
| ROADMAP | NOTIF-01 recipient notification badge count + jump to review detail | Task 1 |
| ROADMAP | NOTIF-02 click notification -> target route | Task 1 |
| ROADMAP | NOTIF-04 partner notification after review decision | Task 1 |
| ROADMAP | REVIEW-02 read-only visibility for department heads / upper heads / ADMIN | Task 2 |
| REQUIREMENTS | NOTIF-01 / NOTIF-02 / NOTIF-04 | Task 1 |
| REQUIREMENTS | REVIEW-02 | Task 2 |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add notification DTOs/store/tests and the header badge/menu</name>
  <files>frontend/src/types/notification.ts, frontend/src/stores/notification.ts, frontend/src/stores/__tests__/notification.test.ts, frontend/src/layouts/MainLayout.vue, frontend/src/layouts/__tests__/MainLayoutNotification.test.ts</files>
  <read_first>
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-04-SUMMARY.md and 19-10-SUMMARY.md (notification store/layout patterns)
    - @frontend/src/layouts/MainLayout.vue (toolbar structure and mobile drawer/footer layout)
    - @frontend/src/stores/auth.ts (login state and permission helpers)
    - @frontend/src/boot/axios.ts (global Notify / axios error handling)
    - @frontend/src/types/reimbursement.ts (DTO organization pattern)
  </read_first>
  <action>
    Create `frontend/src/types/notification.ts` with:
    - `NotificationItem` / `NotificationListResponse` / `NotificationUnreadCount`
    - helper labels for `CHANNEL_PUSH_PENDING_REVIEW` and `CHANNEL_PUSH_REVIEWED`
    - target-route and date formatting helpers that expose `targetRoute`, `createdAt`, and `readAt`

    Create `frontend/src/stores/notification.ts` with actions:
    - `fetchUnreadCount()`
    - `fetchList()`
    - `markRead(id)`
    - `markAllRead()`

    Store rules:
    - never accept or send `userId`
    - list rows come newest-first
    - unread count is user-scoped and refreshed separately from the list
    - `targetRoute` is treated as the navigation target for clicks

    Integrate `MainLayout.vue` with:
    - a `notifications` icon button near the dark-mode and user controls
    - a floating unread badge capped at `99+`
    - desktop `q-menu` behavior with a 360px panel
    - mobile full-screen `q-dialog` behavior
    - empty copy `暂无站内通知` and `新的待办和审批结果会显示在这里。`
    - `全部标为已读` when unread count is greater than 0
    - focus refresh plus a 60-second interval while logged in
    - list refresh on menu/dialog open
    - click navigation to `targetRoute` followed by `markRead(id)`

    Add focused tests that pin:
    - the store request shapes
    - the unread badge copy and `99+` cap
    - focus / interval polling
    - navigation + mark-read behavior
    - touch-safe mobile rendering
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/stores/__tests__/notification.test.ts src/layouts/__tests__/MainLayoutNotification.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "useNotificationStore|fetchUnreadCount|fetchList|markRead|markAllRead" frontend/src/stores/notification.ts` finds all store actions.
    - `rg "notifications|aria-label=\"站内通知\"|q-badge|99\\+|全部标为已读|暂无站内通知|新的待办和审批结果会显示在这里" frontend/src/layouts/MainLayout.vue` finds the header UI.
    - `rg "setInterval|addEventListener\\('focus'|removeEventListener\\('focus'|targetRoute|markRead" frontend/src/layouts/MainLayout.vue` finds the polling and navigation wiring.
    - `cd frontend && npm test -- src/stores/__tests__/notification.test.ts src/layouts/__tests__/MainLayoutNotification.test.ts` exits 0.
  </acceptance_criteria>
  <done>The main layout exposes a usable notification center with polling, unread count, and read-state updates.</done>
</task>

<task type="auto">
  <name>Task 2: Make the review pages and router/menu scope-aware for read-only viewers</name>
  <files>frontend/src/router/routes.ts, frontend/src/pages/ChannelPushReviewPage.vue, frontend/src/pages/ChannelPushReviewDetailPage.vue, frontend/src/pages/__tests__/ChannelPushReviewPage.test.ts, frontend/src/pages/__tests__/ChannelPushReviewDetailPage.test.ts</files>
  <read_first>
    - @frontend/src/router/routes.ts (existing `perm` / `permAny` route contract)
    - @frontend/src/pages/ChannelPushReviewPage.vue (review list UI and tabs)
    - @frontend/src/pages/ChannelPushReviewDetailPage.vue (review detail and action gating)
    - @frontend/src/pages/__tests__/ChannelPushReviewPage.test.ts (source-contract style)
    - @frontend/src/pages/__tests__/ChannelPushReviewDetailPage.test.ts (source-contract style)
    - @frontend/src/layouts/MainLayout.vue (menu registration and permission filtering)
  </read_first>
  <action>
    Update review navigation to accept both reviewer and read-only scope users:
    - `frontend/src/router/routes.ts` should use `permAny: ['channelPush:review', 'channelPush:viewScope']` for `/review/channel-push` and `/review/channel-push/:id`
    - `MainLayout.vue` should keep the review menu visible for the same `permAny` pair

    Make the review pages explicitly read-only for `channelPush:viewScope` users:
    - add a small `q-banner` or equivalent top-level notice that shows only when the user has `channelPush:viewScope` but not `channelPush:review`
    - keep `saveReviewInternalFields`, `approveReview`, and `rejectReview` gated by `auth.hasPerm('channelPush:review')`
    - keep the attachment panel read-only
    - preserve the current reviewer copy, filters, and handled-history behavior

    Update the page source-contract tests to pin:
    - `permAny` with both `channelPush:review` and `channelPush:viewScope`
    - the read-only banner copy
    - the fact that viewScope users can open the pages without gaining mutation controls
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/pages/__tests__/ChannelPushReviewPage.test.ts src/pages/__tests__/ChannelPushReviewDetailPage.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "permAny.*channelPush:viewScope|channelPush:review" frontend/src/router/routes.ts frontend/src/layouts/MainLayout.vue` finds the widened menu/route gates.
    - `rg "只读查看|read-only|viewScope" frontend/src/pages/ChannelPushReviewPage.vue frontend/src/pages/ChannelPushReviewDetailPage.vue` finds the read-only banner or scope copy.
    - `rg "auth.hasPerm\\('channelPush:review'\\)" frontend/src/pages/ChannelPushReviewPage.vue frontend/src/pages/ChannelPushReviewDetailPage.vue` still finds the mutation gate.
    - `cd frontend && npm test -- src/pages/__tests__/ChannelPushReviewPage.test.ts src/pages/__tests__/ChannelPushReviewDetailPage.test.ts && npm run build` exits 0.
  </acceptance_criteria>
  <done>Channel-push review pages are reachable for `channelPush:viewScope` users but remain read-only unless the user also has reviewer permission.</done>
</task>

</tasks>

<verification>
  <step>cd frontend && npm test -- src/stores/__tests__/notification.test.ts src/layouts/__tests__/MainLayoutNotification.test.ts src/pages/__tests__/ChannelPushReviewPage.test.ts src/pages/__tests__/ChannelPushReviewDetailPage.test.ts</step>
  <step>cd frontend && npm run build</step>
</verification>

<success_criteria>
- The header notification center is visible, polls unread count, and navigates to the correct target routes.
- Partner review-result notifications open the partner detail page, and recipient review notifications open the review detail page.
- Read-only viewers can open the review pages while mutation controls remain restricted to reviewers.
</success_criteria>
