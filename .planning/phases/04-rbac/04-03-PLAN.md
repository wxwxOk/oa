---
phase: 4
plan: "04-03"
title: "前端路由守卫 + 权限刷新 — maybeRefreshProfile 60s 防抖"
wave: 2
depends_on: []
requirements_addressed: [FR-5.1, FR-5.2]
files_modified:
  - frontend/src/stores/auth.ts
  - frontend/src/router/index.ts
autonomous: true
must_haves:
  truths:
    - "路由切换时对已登录用户调用 maybeRefreshProfile() 刷新权限"
    - "60 秒内重复路由切换不会重复发起 /auth/profile 请求"
    - "权限刷新后若 meta.perm 不满足，Notify 提示后重定向到 /403"
    - "并发路由切换共享同一个 profile 请求（Promise 去重）"
  artifacts:
    - path: "frontend/src/stores/auth.ts"
      provides: "maybeRefreshProfile action + lastProfileFetch state + 模块级 pendingRefresh"
      contains: "maybeRefreshProfile"
    - path: "frontend/src/router/index.ts"
      provides: "async beforeEach + maybeRefreshProfile 调用 + Notify 权限变更提示"
      contains: "maybeRefreshProfile"
  key_links:
    - from: "router/index.ts beforeEach"
      to: "auth.maybeRefreshProfile()"
      via: "await auth.maybeRefreshProfile()"
    - from: "auth.ts maybeRefreshProfile"
      to: "auth.ts fetchProfile"
      via: "this.fetchProfile()"
    - from: "router/index.ts"
      to: "Notify"
      via: "import { Notify } from 'quasar'"
---

<objective>
实现权限刷新机制：(1) authStore 新增 maybeRefreshProfile() action，带 60s 防抖和 Promise 并发去重 (D-10/D-11)；(2) Router.beforeEach 改造为 async，在 perm 检查前调用 maybeRefreshProfile()，失权限时 Notify 提示并重定向 /403 (D-12)。

Purpose: 确保管理员修改角色权限后，目标用户在下次路由切换时（最迟 60s）感知到权限变更。
Output: 修改后的 auth.ts 和 router/index.ts。
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | /auth/profile 请求携带 access token，由 authGuard 校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | 过期权限缓存 | mitigate | 60s 防抖刷新确保权限最迟 60s 内更新 (D-11) |
| T-04-07 | Denial of Service | 频繁 /auth/profile 请求 | mitigate | 60s 防抖 + Promise 去重，避免每次路由切换都发请求 |
| T-04-08 | Spoofing | access token 过期 | accept | axios 拦截器已处理 401 -> refresh -> 重试，maybeRefreshProfile 的 catch 静默处理最终失败 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: auth.ts — 新增 lastProfileFetch state + 模块级 pendingRefresh + maybeRefreshProfile/_doRefreshProfile actions</name>

  <read_first>
    - frontend/src/stores/auth.ts (当前完整实现，修改目标)
    - frontend/src/boot/axios.ts (Promise 去重 analog: isRefreshing + waiters 模式)
    - .planning/phases/04-rbac/04-PATTERNS.md (推荐实现模式)
    - .planning/phases/04-rbac/04-RESEARCH.md §Open Questions #1 (pendingRefresh 放模块级变量)
  </read_first>

  <files>frontend/src/stores/auth.ts</files>

  <action>
修改 `frontend/src/stores/auth.ts`，共 3 处变更：

**变更 1 — 在 defineStore 之前添加模块级变量 (在 UserInfo 接口之后、export const useAuthStore 之前)**
```typescript
// 模块级变量，避免 Pinia 序列化 Promise 的问题
let pendingRefresh: Promise<void> | null = null;
```

**变更 2 — state 新增 lastProfileFetch (在 user 字段之后)**
在 state 函数的 return 对象中，`user: ...` 行之后新增:
```typescript
lastProfileFetch: 0,
```
完整 state 变为:
```typescript
state: () => ({
  accessToken: localStorage.getItem('oa_access') || '',
  refreshToken: localStorage.getItem('oa_refresh') || '',
  user: JSON.parse(localStorage.getItem('oa_user') || 'null') as UserInfo | null,
  lastProfileFetch: 0,
}),
```

**变更 3 — actions 新增两个方法 (在 hasPerm 方法之后、actions 对象闭合花括号之前)**
```typescript
async maybeRefreshProfile() {
  if (!this.accessToken) return;
  if (Date.now() - this.lastProfileFetch < 60_000) return;
  if (pendingRefresh) return pendingRefresh;
  pendingRefresh = this._doRefreshProfile();
  try {
    await pendingRefresh;
  } finally {
    pendingRefresh = null;
  }
},
async _doRefreshProfile() {
  try {
    await this.fetchProfile();
    this.lastProfileFetch = Date.now();
  } catch {
    // 401 由 axios 拦截器处理，此处静默
  }
},
```
`maybeRefreshProfile` 三层防护：无 token 跳过、60s 防抖跳过、并发去重。`_doRefreshProfile` 复用已有 `fetchProfile()` 方法。
  </action>

  <verify>
    <automated>
cd /e/webspace/oa && grep "let pendingRefresh" frontend/src/stores/auth.ts && grep "lastProfileFetch" frontend/src/stores/auth.ts && grep "maybeRefreshProfile" frontend/src/stores/auth.ts && grep "_doRefreshProfile" frontend/src/stores/auth.ts && grep "60_000" frontend/src/stores/auth.ts
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "let pendingRefresh" frontend/src/stores/auth.ts` 匹配 1 处（模块级变量）
    - `grep "lastProfileFetch" frontend/src/stores/auth.ts` 至少匹配 3 处（state 定义 + 防抖比较 + 赋值）
    - `grep "maybeRefreshProfile" frontend/src/stores/auth.ts` 至少匹配 1 处（方法定义）
    - `grep "_doRefreshProfile" frontend/src/stores/auth.ts` 至少匹配 2 处（方法定义 + 调用）
    - `grep "60_000" frontend/src/stores/auth.ts` 匹配 1 处
    - `grep "this.fetchProfile" frontend/src/stores/auth.ts` 至少匹配 1 处（_doRefreshProfile 内复用）
  </acceptance_criteria>

  <done>
    auth.ts 包含 lastProfileFetch state、模块级 pendingRefresh 变量、maybeRefreshProfile 和 _doRefreshProfile 两个 action。60s 防抖和 Promise 去重均已实现。
  </done>
</task>

<task type="auto">
  <name>Task 2: router/index.ts — beforeEach 改造为 async + 调用 maybeRefreshProfile + Notify 权限变更提示</name>

  <read_first>
    - frontend/src/router/index.ts (当前完整实现，修改目标)
    - .planning/phases/04-rbac/04-PATTERNS.md (router 改造的精确代码)
    - .planning/phases/04-rbac/04-RESEARCH.md §Router.beforeEach 改造
  </read_first>

  <files>frontend/src/router/index.ts</files>

  <action>
修改 `frontend/src/router/index.ts`，共 2 处变更：

**变更 1 — 新增 Notify 导入 (在文件顶部 import 区域)**
在 `import { useAuthStore } from 'src/stores/auth';` 之后新增:
```typescript
import { Notify } from 'quasar';
```

**变更 2 — 替换 beforeEach (lines 19-26)**
将:
```typescript
Router.beforeEach((to, _from, next) => {
  const auth = useAuthStore();
  if (to.meta.public) return next();
  if (!auth.isLogin) return next({ path: '/login', query: { redirect: to.fullPath } });
  const perm = to.meta.perm as string | undefined;
  if (perm && !auth.hasPerm(perm)) return next({ path: '/403' });
  next();
});
```
替换为:
```typescript
Router.beforeEach(async (to) => {
  const auth = useAuthStore();
  if (to.meta.public) return true;
  if (!auth.isLogin) return { path: '/login', query: { redirect: to.fullPath } };

  await auth.maybeRefreshProfile();

  const perm = to.meta.perm as string | undefined;
  if (perm && !auth.hasPerm(perm)) {
    Notify.create({ type: 'warning', message: '您的权限已更新' });
    return { path: '/403' };
  }
  return true;
});
```
关键变化：(1) 回调签名从 `(to, _from, next)` 改为 `async (to)` + 返回值式（Vue Router 4 推荐）；(2) `return next()` 改为 `return true`；(3) `return next({ path })` 改为 `return { path }`；(4) 新增 `await auth.maybeRefreshProfile()` 在 perm 检查之前；(5) 失权限时先 Notify 再返回 /403。
  </action>

  <verify>
    <automated>
cd /e/webspace/oa && grep "async (to)" frontend/src/router/index.ts && grep "Notify" frontend/src/router/index.ts && grep "maybeRefreshProfile" frontend/src/router/index.ts && grep "return true" frontend/src/router/index.ts && grep "您的权限已更新" frontend/src/router/index.ts
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "async (to)" frontend/src/router/index.ts` 匹配 1 处
    - `grep "import { Notify } from 'quasar'" frontend/src/router/index.ts` 匹配 1 处
    - `grep "maybeRefreshProfile" frontend/src/router/index.ts` 匹配 1 处
    - `grep "return true" frontend/src/router/index.ts` 至少匹配 2 处（public 放行 + 最终放行）
    - `grep "您的权限已更新" frontend/src/router/index.ts` 匹配 1 处
    - `grep "next" frontend/src/router/index.ts` 匹配 0 处（next 回调已完全移除）
    - `grep "return { path: '/403' }" frontend/src/router/index.ts` 匹配 1 处
  </acceptance_criteria>

  <done>
    router/index.ts 的 beforeEach 已改造为 async 返回值式，在 perm 检查前调用 maybeRefreshProfile()，失权限时 Notify 提示并重定向 /403。next 回调已完全移除。
  </done>
</task>

</tasks>

<verification>
浏览器验证：
1. 登录后在 DevTools Network 面板观察：首次路由切换触发 /auth/profile 请求
2. 60s 内连续切换路由，不再重复发起 /auth/profile 请求
3. 用 admin 撤销某用户的 role:list 权限 → 该用户等 60s 后切换到 /roles → 跳转 /403 + 看到 "您的权限已更新" 提示
</verification>

<success_criteria>
auth.ts 和 router/index.ts 的变更全部到位，grep 验证通过。权限刷新 60s 防抖、Promise 去重、失权限 Notify + 重定向三项功能均可在浏览器中验证。
</success_criteria>

<output>
After completion, create `.planning/phases/04-rbac/04-03-SUMMARY.md`
</output>
