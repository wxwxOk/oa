---
phase: 06-docker-docs
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - README.md
autonomous: true
requirements: [NFR-2, NFR-4]

must_haves:
  truths:
    - "README 包含 Mermaid 架构图展示三服务关系"
    - "README 包含默认端口表"
    - "README 包含一键部署和脚本使用说明"
    - "README 包含反向代理/HTTPS 配置示例"
    - "README 包含备份恢复操作说明"
    - "README 包含故障排查 FAQ"
    - "README 包含升级流程"
    - "README 全中文（技术词保留英文）"
  artifacts:
    - path: "README.md"
      provides: "完整项目文档"
      contains: "```mermaid"
      min_lines: 150
  key_links:
    - from: "README.md 部署章节"
      to: "scripts/init.sh"
      via: "脚本使用说明"
      pattern: "scripts/init"
    - from: "README.md 备份章节"
      to: "scripts/backup.sh"
      via: "备份命令说明"
      pattern: "scripts/backup"
    - from: "README.md 架构图"
      to: "docker-compose.yml"
      via: "Mermaid 展示三服务"
      pattern: "mermaid"
---

<objective>
将 README.md 扩充为完整项目文档，包含架构图、部署说明、脚本使用、反向代理示例、备份恢复、故障排查、升级流程、贡献指南。

Purpose: 让用户（开发者/运维）能通过 README 完成部署、运维、排障的全流程，满足 NFR-4 部署文档要求。
Output: 完整 README.md
</objective>

<execution_context>
@C:/Users/11828/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-docker-docs/06-CONTEXT.md
@.planning/phases/06-docker-docs/06-RESEARCH.md
@.planning/phases/06-docker-docs/06-01-SUMMARY.md
@.planning/phases/06-docker-docs/06-02-SUMMARY.md

<interfaces>
From docker-compose.yml (after Plan 01 updates):
- postgres: port 5432, container oa-postgres
- backend: port 3000, container oa-backend, /health endpoint
- frontend: port 9000 -> 80, container oa-frontend

From .env.example:
- POSTGRES_USER=oa, POSTGRES_PASSWORD, POSTGRES_DB=oa_db, POSTGRES_PORT=5432
- BACKEND_PORT=3000, JWT_SECRET, JWT_EXPIRES_IN=2h, JWT_REFRESH_EXPIRES_IN=7d
- FRONTEND_PORT=9000, VITE_API_BASE, CORS_ORIGIN

From scripts/ (after Plan 02):
- init.sh / init.ps1 — 首次部署
- backup.sh / backup.ps1 — 数据库备份
- restore.sh / restore.ps1 — 数据库恢复
- upgrade.sh / upgrade.ps1 — 服务升级
- health.sh / health.ps1 — 健康检查
- check-env.sh / check-env.ps1 — 环境变量校验

From frontend/nginx.conf:
- /api/* proxied to backend:3000
- SPA fallback to index.html
- gzip enabled
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md 完整文档</name>
  <read_first>
    - README.md
    - docker-compose.yml
    - .env.example
    - frontend/nginx.conf
    - .planning/phases/06-docker-docs/06-CONTEXT.md
    - .planning/phases/06-docker-docs/06-RESEARCH.md
  </read_first>
  <files>README.md</files>
  <action>
    Per D-08, D-09, D-10: 重写 README.md 为完整中文文档。保留现有内容的核心信息，大幅扩充。

    文档结构（按此顺序）：

    **1. 标题 + 简介**
    - 项目名称、一句话描述
    - 功能模块列表（已有内容保留）

    **2. 架构图（Mermaid，per D-10）**
    ```mermaid
    graph LR
      User([用户/浏览器]) --> Frontend[Frontend<br/>Nginx :9000]
      Frontend -->|/api/*| Backend[Backend<br/>Bun+Elysia :3000]
      Backend --> DB[(PostgreSQL :5432)]
      subgraph Docker Compose
        Frontend
        Backend
        DB
      end
    ```

    **3. 技术栈**（保留现有内容）

    **4. 默认端口表**
    | 服务 | 容器名 | 端口 | 说明 |
    |------|--------|------|------|
    | PostgreSQL | oa-postgres | 5432 | 数据库 |
    | Backend | oa-backend | 3000 | API 服务 |
    | Frontend | oa-frontend | 9000 | Web 界面 |

    **5. 快速开始**
    - 一键部署（使用 init 脚本）：
      - Linux/macOS: `bash scripts/init.sh`
      - Windows: `powershell scripts/init.ps1`
    - 手动部署：cp .env.example .env → 修改密码 → docker compose up -d --build
    - 本地开发（保留现有内容）
    - 默认账号表（保留现有内容）

    **6. 环境变量说明**
    列出 .env.example 中所有变量，说明用途和默认值。标注哪些必须修改（JWT_SECRET, POSTGRES_PASSWORD）。

    **7. 部署脚本**
    每个脚本的用途和用法：
    - `scripts/init.sh` / `init.ps1` — 首次部署
    - `scripts/backup.sh` / `backup.ps1` — 备份（输出到 backups/）
    - `scripts/restore.sh <file>` / `restore.ps1 -BackupFile <file>` — 恢复
    - `scripts/upgrade.sh` / `upgrade.ps1` — 升级
    - `scripts/health.sh` / `health.ps1` — 健康检查
    - `scripts/check-env.sh` / `check-env.ps1` — 环境变量校验

    **8. 反向代理 / HTTPS**
    提供 Caddy 和 Nginx 两种示例（per Claude's Discretion — 两者都给）：
    - Caddy: 3 行 Caddyfile，零配置 HTTPS
    - Nginx: server block with ssl_certificate 配置

    **9. 备份与恢复**
    - 备份命令 + 输出路径说明
    - 恢复命令 + 注意事项
    - 建议定期备份（cron 示例，但不内置 per deferred）

    **10. 故障排查 FAQ**
    至少覆盖：
    - 容器启动失败 → docker compose logs
    - 数据库连接失败 → 检查 DATABASE_URL 和 postgres 容器状态
    - 前端白屏 → 检查 VITE_API_BASE 和 nginx 代理
    - healthcheck 持续 unhealthy → 检查端口和依赖服务
    - JWT 相关错误 → 检查 JWT_SECRET 长度

    **11. 升级流程**
    - 备份 → 拉取 → 重建 → 验证
    - 或直接使用 upgrade 脚本

    **12. 目录结构**（扩充现有内容，加入 scripts/）

    **13. 接口文档**（保留 Swagger 链接）

    **14. 贡献指南**
    - Fork → Branch → PR 流程
    - 代码规范简述

    **15. License**（如有）

    全文中文，技术词/命令保留英文原文（per D-09）。
  </action>
  <verify>
    <automated>
      grep -q "mermaid" README.md && grep -q "scripts/init" README.md && grep -q "scripts/backup" README.md && grep -q "Caddy" README.md && grep -q "FAQ" README.md && grep -q "oa-postgres" README.md && grep -q "oa-backend" README.md && wc -l < README.md | awk '{if ($1 >= 150) print "PASS"; else print "FAIL: only " $1 " lines"}'
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains ````mermaid` code block with architecture diagram
    - README.md contains port table with oa-postgres (5432), oa-backend (3000), oa-frontend (9000)
    - README.md contains `scripts/init.sh` and `scripts/init.ps1` usage
    - README.md contains `scripts/backup.sh` usage
    - README.md contains `scripts/restore.sh` usage with file argument
    - README.md contains `scripts/upgrade.sh` usage
    - README.md contains `scripts/health.sh` usage
    - README.md contains Caddy reverse proxy example with `reverse_proxy`
    - README.md contains Nginx reverse proxy example with `ssl_certificate`
    - README.md contains FAQ section with at least 5 troubleshooting items
    - README.md contains upgrade flow description
    - README.md contains contribution guide section
    - README.md is >= 150 lines
    - README.md is written in Chinese (技术词保留英文)
    - README.md does NOT mention Docker Hub/GHCR publishing (deferred)
    - README.md does NOT mention cron container for scheduled backups (deferred)
  </acceptance_criteria>
  <done>README.md 扩充为完整文档：Mermaid 架构图 + 端口表 + 一键部署 + 脚本说明 + Caddy/Nginx 反向代理 + 备份恢复 + FAQ + 升级流程 + 贡献指南，全中文</done>
</task>

</tasks>

<verification>
- `wc -l README.md` — >= 150 行
- `grep -c "mermaid" README.md` — >= 1
- `grep -c "scripts/" README.md` — >= 6（每个脚本至少提及一次）
- `grep "Caddy\|Nginx" README.md` — 两者都出现
</verification>

<success_criteria>
- README.md 完整覆盖 D-08 要求的所有章节
- 架构图使用 Mermaid 代码块（D-10）
- 全中文，技术词保留英文（D-09）
- 包含所有 12 个脚本的使用说明
- 包含 Caddy + Nginx 两种反向代理示例
- 包含至少 5 条故障排查 FAQ
- 不包含 deferred 内容（Docker Hub 发布、cron 容器等）
</success_criteria>

<output>
After completion, create `.planning/phases/06-docker-docs/06-03-SUMMARY.md`
</output>
