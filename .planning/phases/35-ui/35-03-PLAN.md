---
phase: 35-ui
plan: 3
type: execute
wave: 3
depends_on: [35-02]
files_modified:
  - frontend/src/pages/ChannelPushReviewPage.vue
  - frontend/src/pages/__tests__/ChannelPushReviewPage.test.ts
  - frontend/src/router/routes.ts
  - frontend/src/layouts/MainLayout.vue
autonomous: true
requirements: [REVIEW-01, REVIEW-07, PERM-04]
requirements_addressed: [REVIEW-01, REVIEW-07, PERM-04]
user_setup: []
must_haves:
  truths:
    - "The reviewer inbox page is the first visible entry point for Phase 35 and is titled `待我审核`."
    - "Desktop uses q-table; mobile uses q-card rows plus a bottom filter sheet. Both views support the same filter set."
    - "The list page is view-only: row actions navigate to detail, and there are no inline approve/reject shortcuts in the list."
    - "Route and menu wiring are permission gated with `channelPush:review`; `channelPush:viewScope` remains reserved for Phase 36 cross-role read-only visibility."
  artifacts:
    - path: "frontend/src/pages/ChannelPushReviewPage.vue"
      provides: "Responsive reviewer inbox with pending/handled tabs, filters, list cards/tables, and detail navigation."
    - path: "frontend/src/pages/__tests__/ChannelPushReviewPage.test.ts"
      provides: "Responsive list-page contract tests for tabs, filters, status chips, and view-only row actions."
    - path: "frontend/src/router/routes.ts"
      provides: "`/review/channel-push` route registration."
    - path: "frontend/src/layouts/MainLayout.vue"
      provides: "Top-level `待我审核` menu item with fact_check icon and permission gating."
  key_links:
    - from: "frontend/src/pages/ChannelPushReviewPage.vue"
      to: "frontend/src/stores/channelPush.ts"
      via: "fetchReviewPending / fetchReviewHandled / review filters / pagination"
      pattern: "fetchReviewPending|fetchReviewHandled|reviewPendingRows|reviewHandledRows"
    - from: "frontend/src/pages/ChannelPushReviewPage.vue"
      to: "frontend/src/router/routes.ts"
      via: "detail navigation from list rows"
      pattern: "/review/channel-push/:id"
---

<objective>
Build the reviewer inbox page and wire it into navigation.

Scope: the list / inbox entry point only. The detail page and review actions land in the next plan.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-ui/35-CONTEXT.md
@frontend/src/pages/ReimbursementPage.vue
@frontend/src/pages/ChannelPushPage.vue
@frontend/src/components/EmptyState.vue
@frontend/src/components/FilterSheet.vue
@frontend/src/components/channel-push/ChannelPushStatusChip.vue
@frontend/src/composables/useResponsive.ts
@frontend/src/stores/channelPush.ts
@frontend/src/types/channelPush.ts
@frontend/src/router/routes.ts
@frontend/src/layouts/MainLayout.vue
</context>

<source_audit>
| Source | Item | Covered By |
|--------|------|------------|
| REVIEW-01 | 待我审核列表 + 渠道商 / 状态 / 时间筛选 | task 1 |
| REVIEW-07 | 已审核历史列表 + 状态 / 时间筛选 | task 1 |
| PERM-04 | 前端按钮显隐和路由守卫覆盖接收人侧操作 | task 2 |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Create the reviewer inbox page</name>
  <files>frontend/src/pages/ChannelPushReviewPage.vue, frontend/src/pages/__tests__/ChannelPushReviewPage.test.ts</files>
  <read_first>
    - @frontend/src/pages/ReimbursementPage.vue (responsive list + filter patterns)
    - @frontend/src/pages/ChannelPushPage.vue (mobile card and action spacing patterns)
    - @frontend/src/components/EmptyState.vue
    - @frontend/src/components/channel-push/ChannelPushStatusChip.vue
    - @frontend/src/composables/useResponsive.ts
    - @frontend/src/stores/channelPush.ts
    - @frontend/src/types/channelPush.ts
  </read_first>
  <action>
    Create `frontend/src/pages/ChannelPushReviewPage.vue` as the `/review/channel-push` inbox.

    Required structure:
    - Page title: `待我审核`
    - Tabs / mode switch: `待我审核` and `已审核`
    - Desktop: q-table with columns for `渠道商`, `学员`, `状态`, `提交时间`, `审核时间`, `操作`
    - Mobile: q-card rows with the same summary fields plus a bottom sheet filter dialog
    - Shared filters: `渠道商`, `状态`, `开始日期`, `结束日期`
    - View-only row action: `查看详情`
    - No inline approve / reject shortcuts in the list

    Behavior:
    - Pending tab calls `store.fetchReviewPending`
    - Handled tab calls `store.fetchReviewHandled`
    - Both tabs reuse the same filter state shape from `ChannelPushReviewListFilters`
    - The page routes row clicks and the `查看详情` button to `/review/channel-push/:id`
    - Mobile filter trigger, reset, apply, and row action buttons should be touch-safe (`min-height: 44px`) and use grep-friendly class names such as `review-mobile-filter-trigger`, `review-filter-reset`, `review-filter-apply`, and `review-row-action`
    - Use `ChannelPushStatusChip` for every status render and `EmptyState` for the empty inbox state

    The page should keep separate pending/handled pagination state so switching tabs does not discard the other tab's filters.
    Write a source-level page test that pins the title, tab labels, filter labels, q-table / q-card split, view-only actions, and responsive filter sheet.
  </action>
  <verify>
    <automated>cd frontend && bun run test --run src/pages/__tests__/ChannelPushReviewPage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "待我审核|已审核|渠道商|开始日期|结束日期|查看详情" frontend/src/pages/ChannelPushReviewPage.vue` matches the required copy.
    - `rg "review-mobile-filter-trigger|review-filter-reset|review-filter-apply|review-row-action|min-height: 44px" frontend/src/pages/ChannelPushReviewPage.vue` matches the touch-safe selectors.
    - `rg "fetchReviewPending|fetchReviewHandled|/review/channel-push/|review-row-action" frontend/src/pages/ChannelPushReviewPage.vue` matches the store calls and detail navigation.
    - `cd frontend && bun run test --run src/pages/__tests__/ChannelPushReviewPage.test.ts` exits 0.
  </acceptance_criteria>
  <done>The reviewer inbox is a responsive, filterable, view-only entry point to the Phase 35 review workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the list route and navigation entry</name>
  <files>frontend/src/router/routes.ts, frontend/src/layouts/MainLayout.vue</files>
  <read_first>
    - @frontend/src/router/routes.ts
    - @frontend/src/layouts/MainLayout.vue
    - @frontend/src/pages/ChannelPushReviewPage.vue
    - @frontend/src/stores/auth.ts
  </read_first>
  <action>
    Register the new reviewer inbox route and navigation entry after the page exists.

    Required changes:
    - Add `/review/channel-push` to `frontend/src/router/routes.ts` with `component: () => import('pages/ChannelPushReviewPage.vue')`
    - Gate it with `perm: 'channelPush:review'`
    - Add a top-level `待我审核` menu item in `frontend/src/layouts/MainLayout.vue` with `icon: 'fact_check'` and the same permission gate
    - Leave the partner `我的推送` menu entry and `/channel-push` routes unchanged

    The new entry must appear in both desktop drawer rendering and the mobile footer's flattened menu list through the existing menu filter logic.
  </action>
  <verify>
    <automated>cd frontend && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "review/channel-push|待我审核|fact_check|channelPush:review" frontend/src/router/routes.ts frontend/src/layouts/MainLayout.vue` matches the route/menu wiring.
    - `rg "channelPush:viewScope" frontend/src/router/routes.ts frontend/src/layouts/MainLayout.vue` returns 0 results.
    - `rg "我的推送|channel-push" frontend/src/layouts/MainLayout.vue` still finds the existing partner entry unchanged.
    - `cd frontend && bun run build` exits 0 after the route exists.
  </acceptance_criteria>
  <done>The reviewer inbox is reachable from navigation and the partner entry remains untouched.</done>
</task>

</tasks>

<verification>
  <step>cd frontend && bun run test --run src/pages/__tests__/ChannelPushReviewPage.test.ts</step>
  <step>cd frontend && bun run build</step>
</verification>

<success_criteria>
- The reviewer inbox page is the visible Phase 35 entry point.
- The new route and menu item are permission-gated.
- The list page remains view-only and prepares the detail flow for the next plan.
</success_criteria>
