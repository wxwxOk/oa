---
phase: 35-ui
plan: 2
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - frontend/src/types/channelPush.ts
  - frontend/src/stores/channelPush.ts
  - frontend/src/types/__tests__/channelPush.test.ts
  - frontend/src/stores/__tests__/channelPush.test.ts
autonomous: true
requirements: [REVIEW-01, REVIEW-03, REVIEW-04, REVIEW-05, REVIEW-06, REVIEW-07]
requirements_addressed: [REVIEW-01, REVIEW-03, REVIEW-04, REVIEW-05, REVIEW-06, REVIEW-07]
user_setup: []
must_haves:
  truths:
    - "Review state is separated from partner state so the partner `rows/current/filters` contract from Phase 33 remains intact."
    - "Review list/detail/action payloads are typed independently from partner payloads and match the new `/review/channel-push` backend contract."
    - "Review attachment preview/download actions use review-specific blob endpoints, not the partner ownership endpoints."
    - "Existing partner actions (`fetchMine`, `fetchDetail`, `create`, `update`, `cancel`, attachment CRUD) remain unchanged."
  artifacts:
    - path: "frontend/src/types/channelPush.ts"
      provides: "Review DTOs, review list filters, internal-field payloads, decision payloads, and review helpers."
    - path: "frontend/src/stores/channelPush.ts"
      provides: "Separate review list/detail state plus review list/detail/internal-field/approve/reject/attachment blob actions."
    - path: "frontend/src/types/__tests__/channelPush.test.ts"
      provides: "Type-level contract tests for review constants and helpers."
    - path: "frontend/src/stores/__tests__/channelPush.test.ts"
      provides: "Store contract tests for review endpoints, loading flags, and blob actions."
  key_links:
    - from: "frontend/src/stores/channelPush.ts"
      to: "backend/src/modules/channel-push/channel-push-review.route.ts"
      via: "review list/detail/action API calls"
      pattern: "/review/channel-push/pending|/review/channel-push/handled|/review/channel-push/.*/internal-fields|/review/channel-push/.*/approve|/review/channel-push/.*/reject"
    - from: "frontend/src/stores/channelPush.ts"
      to: "backend/src/modules/channel-push/channel-push-file.service.ts"
      via: "review attachment blob access"
      pattern: "previewReviewAttachmentBlob|downloadReviewAttachmentBlob"
---

<objective>
Build the frontend review data layer for Phase 35: review-specific types, separate review state in the existing Pinia store, and test coverage for the new API boundary.

Scope: data contract only. No page, route, or menu work in this plan.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-ui/35-CONTEXT.md
@frontend/src/stores/channelPush.ts
@frontend/src/types/channelPush.ts
@frontend/src/stores/__tests__/channelPush.test.ts
@frontend/src/types/__tests__/reimbursement.test.ts
@frontend/src/pages/ChannelPushPage.vue
@frontend/src/pages/ReimbursementPage.vue
</context>

<source_audit>
| Source | Item | Covered By |
|--------|------|------------|
| REVIEW-01 | 待我审核列表 + 筛选 | task 1 |
| REVIEW-03 | 详情页所需 reviewer DTO 字段 | task 1 |
| REVIEW-04 | 内部字段 payload / 可见性 | task 1 |
| REVIEW-05 | approve / reject payload 与动作 | task 1 |
| REVIEW-06 | 审核状态派生字段不联动 VisitRecord | task 1 |
| REVIEW-07 | 已审核历史 list response + filters | task 1 |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Extend channelPush types and store with review-specific contracts</name>
  <files>frontend/src/types/channelPush.ts, frontend/src/stores/channelPush.ts, frontend/src/types/__tests__/channelPush.test.ts, frontend/src/stores/__tests__/channelPush.test.ts</files>
  <read_first>
    - @frontend/src/types/channelPush.ts (partner DTO and helper patterns)
    - @frontend/src/stores/channelPush.ts (existing partner store state and axios call style)
    - @frontend/src/types/__tests__/reimbursement.test.ts (type-helper test style)
    - @frontend/src/stores/__tests__/channelPush.test.ts (existing Vitest mock style)
    - @backend/src/modules/channel-push/channel-push-review.route.ts (to keep endpoints aligned)
  </read_first>
  <action>
    Extend `frontend/src/types/channelPush.ts` with review-specific constants, DTOs, and helpers without breaking the partner exports.

    Add:
    - `CHANNEL_PUSH_REVIEW_VIEW_MODES = ['pending', 'handled'] as const`
    - `type ChannelPushReviewViewMode`
    - `CHANNEL_PUSH_REVIEW_LIST_FILTER_KEYS = ['channelPartnerKeyword', 'status', 'dateFrom', 'dateTo'] as const`
    - `interface ChannelPushReviewListFilters`
    - `interface ChannelPushReviewRow`
    - `interface ChannelPushReviewDetail`
    - `interface ChannelPushReviewInternalFieldsPayload`
    - `interface ChannelPushReviewDecisionPayload`
    - `interface ChannelPushReviewListResponse`
    - `interface ChannelPushReviewDetailResponse`
    - `createEmptyChannelPushReviewFilters()`
    - `channelPushReviewViewLabel(view)` returning `待我审核` / `已审核`

    Review row fields must cover the backend contract from Plan 35-01:
    `id`, `channelPartnerId`, `channelPartnerName`, `recipientUserId`, `studentName`, `studentPhone`, `studentAge`, `studentEducation`, `studentGender`, `intentStatus`, `intentNote`, `remark`, `status`, `submittedAt`, `reviewedAt`, `reviewComment`, `internalScheduledReceiverId`, `internalScheduledReceiverName`, `internalScheduledDate`, `internalNote`, `attachmentCount`, `createdAt`, `updatedAt`.

    Review detail must extend the row with:
    `attachments`, `reviewActions`, `duplicateHints`.

    Then extend `frontend/src/stores/channelPush.ts` with a separate review slice:
    - state for pending and handled rows / totals / pagination / filters / current detail
    - `fetchReviewPending(filters?)`
    - `fetchReviewHandled(filters?)`
    - `fetchReviewDetail(id)`
    - `saveReviewInternalFields(id, payload)`
    - `approveReview(id, payload)`
    - `rejectReview(id, payload)`
    - `previewReviewAttachmentBlob(id, attachmentId)`
    - `downloadReviewAttachmentBlob(id, attachmentId)`

    Endpoint mapping:
    - `GET /review/channel-push/pending`
    - `GET /review/channel-push/handled`
    - `GET /review/channel-push/:id`
    - `PATCH /review/channel-push/:id/internal-fields`
    - `POST /review/channel-push/:id/approve`
    - `POST /review/channel-push/:id/reject`
    - `GET /review/channel-push/:id/attachments/:attachmentId/preview`
    - `GET /review/channel-push/:id/attachments/:attachmentId/download`

    Keep partner actions untouched, and keep loading flags separate so pending/handled review activity cannot corrupt the partner list state.
  </action>
  <verify>
    <automated>cd frontend && bun run test --run src/types/__tests__/channelPush.test.ts src/stores/__tests__/channelPush.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "CHANNEL_PUSH_REVIEW_VIEW_MODES|ChannelPushReviewRow|ChannelPushReviewDetail|ChannelPushReviewInternalFieldsPayload|ChannelPushReviewDecisionPayload|createEmptyChannelPushReviewFilters|channelPushReviewViewLabel" frontend/src/types/channelPush.ts` matches the review exports.
    - `rg "fetchReviewPending|fetchReviewHandled|fetchReviewDetail|saveReviewInternalFields|approveReview|rejectReview|previewReviewAttachmentBlob|downloadReviewAttachmentBlob" frontend/src/stores/channelPush.ts` matches the review store actions.
    - `rg "reviewPendingRows|reviewHandledRows|reviewCurrent|reviewDetailLoading|reviewActionLoading|reviewAttachmentLoading" frontend/src/stores/channelPush.ts` matches the isolated review state slice.
    - `rg "/review/channel-push/pending|/review/channel-push/handled|/review/channel-push/:id/internal-fields|/review/channel-push/:id/approve|/review/channel-push/:id/reject" frontend/src/stores/channelPush.ts` matches the endpoint paths.
    - `cd frontend && bun run test --run src/types/__tests__/channelPush.test.ts src/stores/__tests__/channelPush.test.ts` exits 0.
  </acceptance_criteria>
  <done>The frontend can speak to the new review API without disturbing the partner list/form/detail workflow.</done>
</task>

</tasks>

<verification>
  <step>cd frontend && bun run test --run src/types/__tests__/channelPush.test.ts src/stores/__tests__/channelPush.test.ts</step>
  <step>cd frontend && bun run build</step>
</verification>

<success_criteria>
- The review-specific frontend data contract is complete and isolated from partner state.
- Page and route plans can consume the new review store actions without adding ad hoc API calls.
- Existing partner tests and partner store behavior remain unchanged.
</success_criteria>
