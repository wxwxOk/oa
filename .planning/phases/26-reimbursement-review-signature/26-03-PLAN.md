---
phase: 26
plan: 03
type: execute
wave: 2
depends_on: [26-01, 26-02]
files_modified:
  - frontend/src/types/reimbursement.ts
  - frontend/src/stores/reimbursement.ts
  - frontend/src/components/reimbursement/ReimbursementSignaturePad.vue
  - frontend/src/components/reimbursement/ReimbursementActionTimeline.vue
autonomous: true
requirements: [APPROVAL-01, APPROVAL-02, APPROVAL-03, APPROVAL-04, APPROVAL-05, PERM-02, UX-01, UX-02]
user_setup:
  - "None. This wave uses existing frontend dependencies; `signature_pad` is already installed."
must_haves:
  truths:
    - "Frontend review API calls stay centralized in `useReimbursementStore`; pages and components do not import `api` directly."
    - "Approve actions send multipart `FormData` with exact key `signature`; reject actions send JSON `{ comment }`."
    - "Timeline signature rendering uses authenticated blob requests and object URLs, not direct protected endpoint URLs."
  artifacts:
    - path: "frontend/src/types/reimbursement.ts"
      provides: "Review scope/action constants, review permission helpers, reject normalization and signature data URL conversion"
    - path: "frontend/src/stores/reimbursement.ts"
      provides: "Review queue, approve/reject and signature blob methods"
    - path: "frontend/src/components/reimbursement/ReimbursementSignaturePad.vue"
      provides: "Reusable reimbursement-specific Canvas signature component"
    - path: "frontend/src/components/reimbursement/ReimbursementActionTimeline.vue"
      provides: "Authenticated action-bound signature image preview in the audit timeline"
  key_links:
    - from: "frontend/src/stores/__tests__/reimbursement.test.ts"
      to: "frontend/src/stores/reimbursement.ts"
      via: "Wave 0 store contract"
      pattern: "fetchDepartmentReviewList|departmentApprove|financeReject|previewSignatureBlob|FormData|signature"
    - from: "frontend/src/pages/__tests__/ReimbursementPage.test.ts"
      to: "frontend/src/components/reimbursement/ReimbursementActionTimeline.vue"
      via: "signature blob preview contract"
      pattern: "applicationId|previewSignatureBlob|URL.createObjectURL|URL.revokeObjectURL"
---

# Phase 26 Plan 03: Frontend Review Types, Store, Signature Pad, and Timeline Preview

<objective>
Implement the frontend data layer and reusable components for Phase 26 review actions: review DTO/helpers, Pinia review endpoints, Canvas signature capture, and authenticated timeline signature images.
</objective>

<context>
This plan makes the Phase 26 frontend type/store/component contracts pass before list/detail UX wiring. It extends existing Phase 25 reimbursement files, keeps API access inside `useReimbursementStore`, and adapts the existing `SignatureField.vue` behavior into a fixed reimbursement review component.
</context>

<source_audit>
Read first:

- `.planning/phases/26-reimbursement-review-signature/26-CONTEXT.md`
- `.planning/phases/26-reimbursement-review-signature/26-RESEARCH.md`
- `.planning/phases/26-reimbursement-review-signature/26-UI-SPEC.md`
- `.planning/phases/26-reimbursement-review-signature/26-VALIDATION.md`
- `.planning/phases/26-reimbursement-review-signature/26-01-PLAN.md`
- `.planning/phases/26-reimbursement-review-signature/26-02-PLAN.md`
- `frontend/src/types/reimbursement.ts`
- `frontend/src/types/__tests__/reimbursement.test.ts`
- `frontend/src/stores/reimbursement.ts`
- `frontend/src/stores/__tests__/reimbursement.test.ts`
- `frontend/src/components/reimbursement/ReimbursementActionTimeline.vue`
- `frontend/src/components/reimbursement/ReimbursementAttachmentPanel.vue`
- `frontend/src/components/designer/fields/SignatureField.vue`
- `frontend/src/composables/useResponsive.ts`
</source_audit>

<tasks>

<task id="26-03-01" type="auto">
  <name>Add review DTOs, helpers and signature data URL conversion</name>
  <files>frontend/src/types/reimbursement.ts</files>
  <read_first>
    - `frontend/src/types/reimbursement.ts` for current status, DTO, formatting and payload helper organization.
    - `frontend/src/types/__tests__/reimbursement.test.ts` for exact Phase 26 expected exports.
    - `.planning/phases/26-reimbursement-review-signature/26-UI-SPEC.md` Entry and Dialog sections.
  </read_first>
  <action>
    Extend `frontend/src/types/reimbursement.ts` without changing existing Phase 25 exports. Add `export const REIMBURSEMENT_REVIEW_SCOPES = ['all', 'department', 'finance'] as const` and `export type ReimbursementReviewScope = (typeof REIMBURSEMENT_REVIEW_SCOPES)[number]`. Add `export const REIMBURSEMENT_REVIEW_ACTIONS = ['departmentApprove', 'departmentReject', 'financeApprove', 'financeReject'] as const` and `export type ReimbursementReviewAction = (typeof REIMBURSEMENT_REVIEW_ACTIONS)[number]`.

    Add `export interface ReimbursementReviewPayload { comment?: string | null; signature: File }` and `export interface ReimbursementRejectPayload { comment: string }`. Add `export function normalizeReimbursementRejectPayload(payload: ReimbursementRejectPayload): ReimbursementRejectPayload` that returns `{ comment: payload.comment.trim() }`.

    Add `export function canReviewDepartmentReimbursement(row: Pick<ReimbursementRow, 'status'>, permissions: string[] = [], isAdmin = false): boolean` returning true only when `row.status === 'DEPARTMENT_REVIEW'` and either `isAdmin` is true or `permissions.includes('reimbursement:department-review')`. Add `export function canReviewFinanceReimbursement(row: Pick<ReimbursementRow, 'status'>, permissions: string[] = [], isAdmin = false): boolean` returning true only when `row.status === 'FINANCE_REVIEW'` and either `isAdmin` is true or `permissions.includes('reimbursement:finance-review')`.

    Add `export function reimbursementSignatureDataUrlToFile(dataUrl: string, fileName = 'signature.png'): File`. It must require a `data:image/png;base64,` prefix, decode the base64 payload using `atob`, create a `Uint8Array`, and return `new File([bytes], fileName, { type: 'image/png' })`. Throw `new Error('Invalid reimbursement signature data URL')` for blank or non-PNG data URLs.
  </action>
  <verify>
    <automated>cd frontend && bun test src/types/__tests__/reimbursement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "REIMBURSEMENT_REVIEW_SCOPES = \['all', 'department', 'finance'\]|type ReimbursementReviewScope" frontend/src/types/reimbursement.ts` finds review scopes.
    - `rg "REIMBURSEMENT_REVIEW_ACTIONS = \['departmentApprove', 'departmentReject', 'financeApprove', 'financeReject'\]|type ReimbursementReviewAction" frontend/src/types/reimbursement.ts` finds review actions.
    - `rg "interface ReimbursementReviewPayload|interface ReimbursementRejectPayload|normalizeReimbursementRejectPayload" frontend/src/types/reimbursement.ts` finds review payload types.
    - `rg "canReviewDepartmentReimbursement|DEPARTMENT_REVIEW|reimbursement:department-review|canReviewFinanceReimbursement|FINANCE_REVIEW|reimbursement:finance-review" frontend/src/types/reimbursement.ts` finds permission helpers.
    - `rg "reimbursementSignatureDataUrlToFile|data:image/png;base64|atob|new File|Invalid reimbursement signature data URL" frontend/src/types/reimbursement.ts` finds PNG conversion logic.
    - `cd frontend && bun test src/types/__tests__/reimbursement.test.ts` exits 0.
  </acceptance_criteria>
  <done>Frontend types expose fixed review scopes/actions, permission helpers, reject normalization and PNG signature conversion.</done>
</task>

<task id="26-03-02" type="auto">
  <name>Add review queue, action and signature blob store methods</name>
  <files>frontend/src/stores/reimbursement.ts</files>
  <read_first>
    - `frontend/src/stores/reimbursement.ts` for current list/detail/draft/attachment methods and loading flags.
    - `frontend/src/stores/__tests__/reimbursement.test.ts` for exact endpoint and payload expectations.
    - `frontend/src/types/reimbursement.ts` for new `ReimbursementReviewPayload` and `ReimbursementRejectPayload` types.
    - `.planning/phases/26-reimbursement-review-signature/26-RESEARCH.md` Store-Owned API Boundary section.
  </read_first>
  <action>
    Extend `frontend/src/stores/reimbursement.ts` imports to include `normalizeReimbursementRejectPayload`, `type ReimbursementReviewPayload`, and `type ReimbursementRejectPayload`. Keep existing state shape and loading flags; reuse `loading` for review queue fetches, `actionLoading` for approve/reject, and `downloadLoading` for signature preview.

    Add `async fetchDepartmentReviewList(filters?: ReimbursementListRequest)` that sets `loading = true`, builds page/size/filter params using the same `mergeFilters()` and `buildListParams()` helpers as `fetchList()`, calls `api.get('/reimbursements/review/department', { params })`, writes `rows`, `total`, `page`, `size`, and `filters`, and returns `ReimbursementListResponse`. Add `async fetchFinanceReviewList(filters?: ReimbursementListRequest)` with the same behavior using `/reimbursements/review/finance`.

    Add private-style helper logic inside the store actions or local functions so approve methods build `FormData` with `formData.append('signature', payload.signature)` and append `comment` only when `payload.comment?.trim()` is non-empty. Add `async departmentApprove(id: number, payload: ReimbursementReviewPayload)` posting to `/reimbursements/${id}/department-review/approve`; `async financeApprove(id: number, payload: ReimbursementReviewPayload)` posting to `/reimbursements/${id}/finance-review/approve`; both must update `this.current` row fields when `this.current?.id === id` and return `ReimbursementRow`.

    Add `async departmentReject(id: number, payload: ReimbursementRejectPayload)` posting `normalizeReimbursementRejectPayload(payload)` to `/reimbursements/${id}/department-review/reject`. Add `async financeReject(id: number, payload: ReimbursementRejectPayload)` posting to `/reimbursements/${id}/finance-review/reject`. Both must use `actionLoading`, update `this.current` row fields when matching, and return `ReimbursementRow`.

    Add `async previewSignatureBlob(applicationId: number, actionId: number)` that sets `downloadLoading = true`, calls `api.get(`/reimbursements/${applicationId}/actions/${actionId}/signature`, { responseType: 'blob' })`, returns `data as Blob`, and always resets `downloadLoading` in `finally`.
  </action>
  <verify>
    <automated>cd frontend && bun test src/stores/__tests__/reimbursement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "fetchDepartmentReviewList|/reimbursements/review/department|fetchFinanceReviewList|/reimbursements/review/finance" frontend/src/stores/reimbursement.ts` finds review queue methods.
    - `rg "departmentApprove|department-review/approve|financeApprove|finance-review/approve|FormData|append\('signature'" frontend/src/stores/reimbursement.ts` finds multipart approve methods.
    - `rg "departmentReject|department-review/reject|financeReject|finance-review/reject|normalizeReimbursementRejectPayload" frontend/src/stores/reimbursement.ts` finds reject methods.
    - `rg "previewSignatureBlob|actions/\$\{actionId\}/signature|responseType: 'blob'|downloadLoading" frontend/src/stores/reimbursement.ts` finds signature blob preview.
    - `rg "api\.get\('/reimbursements/review/department'|api\.post\(`/reimbursements/\$\{id\}/department-review/approve`|api\.post\(`/reimbursements/\$\{id\}/finance-review/reject`" frontend/src/stores/reimbursement.ts` finds exact fixed endpoints.
    - `cd frontend && bun test src/stores/__tests__/reimbursement.test.ts` exits 0.
  </acceptance_criteria>
  <done>Pinia store owns all review queue, approve/reject and signature blob API calls.</done>
</task>

<task id="26-03-03" type="auto">
  <name>Create reimbursement signature pad and render timeline signature images</name>
  <files>frontend/src/components/reimbursement/ReimbursementSignaturePad.vue, frontend/src/components/reimbursement/ReimbursementActionTimeline.vue</files>
  <read_first>
    - `frontend/src/components/designer/fields/SignatureField.vue` for existing `signature_pad`, dialog, resize and exposed method patterns.
    - `frontend/src/components/reimbursement/ReimbursementActionTimeline.vue` for current action labels, metadata display and ordering.
    - `frontend/src/components/reimbursement/ReimbursementAttachmentPanel.vue` for authenticated blob object URL lifecycle.
    - `frontend/src/stores/reimbursement.ts` for `previewSignatureBlob()`.
    - `frontend/src/pages/__tests__/ReimbursementPage.test.ts` for source contract strings.
  </read_first>
  <action>
    Create `frontend/src/components/reimbursement/ReimbursementSignaturePad.vue` by adapting the existing `SignatureField.vue` implementation. Props must be `modelValue?: string` and optional `disabled?: boolean`; emit `update:modelValue`. The visible trigger must include text `手写签名` and empty hint `点击签名`. Use `SignaturePad` from `signature_pad`, `useResponsive()`, a `q-dialog`, mobile `:maximized="isMobile"`, mobile transitions `slide-up` and `slide-down`, `toDataURL('image/png')`, and exposed methods `save(): string`, `clear(): void`, and `isEmpty(): boolean`. Do not import `api`, do not upload files, and do not reference reimbursement endpoints in this component.

    Update `ReimbursementActionTimeline.vue` props to `{ actions: ReimbursementAction[]; applicationId: number }`. Import `onBeforeUnmount`, `ref`, `watch`, and `useReimbursementStore`. Keep existing action labels for `SUBMIT`, `DEPARTMENT_APPROVE`, `DEPARTMENT_REJECT`, `FINANCE_APPROVE`, and `FINANCE_REJECT`. Replace metadata-only signature display with object URL image rendering: for each action with `signatureRelativePath`, call `store.previewSignatureBlob(props.applicationId, action.id)`, store the resulting `URL.createObjectURL(blob)` in a `signatureUrls` map keyed by action id, render `<img :src="signatureUrls[action.id]" class="signature-image" alt="手写签名" />` when available, and keep a metadata/fallback line containing `签名附件`, `signatureMimeType`, and formatted size. Revoke all created URLs with `URL.revokeObjectURL()` before reload and in `onBeforeUnmount()`.

    Ensure the timeline never uses a protected backend route string directly in an image `src`; the only signature network call must be through `store.previewSignatureBlob()`.
  </action>
  <verify>
    <automated>cd frontend && bun test src/pages/__tests__/ReimbursementPage.test.ts src/stores/__tests__/reimbursement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "SignaturePad|手写签名|点击签名|toDataURL\('image/png'\)|defineExpose\(\{ save, clear, isEmpty \}\)" frontend/src/components/reimbursement/ReimbursementSignaturePad.vue` finds signature component behavior.
    - `rg "q-dialog|:maximized=\"isMobile\"|slide-up|slide-down|useResponsive" frontend/src/components/reimbursement/ReimbursementSignaturePad.vue` finds desktop/mobile dialog behavior.
    - `rg "applicationId: number|previewSignatureBlob|URL.createObjectURL|URL.revokeObjectURL|signatureUrls|signature-image" frontend/src/components/reimbursement/ReimbursementActionTimeline.vue` finds authenticated timeline preview logic.
    - `rg "SUBMIT|DEPARTMENT_APPROVE|DEPARTMENT_REJECT|FINANCE_APPROVE|FINANCE_REJECT|签名附件" frontend/src/components/reimbursement/ReimbursementActionTimeline.vue` finds retained labels and fallback metadata.
    - `rg "api\.get|/actions/.*/signature|window.open" frontend/src/components/reimbursement/ReimbursementSignaturePad.vue frontend/src/components/reimbursement/ReimbursementActionTimeline.vue` returns no matches.
    - `cd frontend && bun test src/pages/__tests__/ReimbursementPage.test.ts src/stores/__tests__/reimbursement.test.ts` exits 0 after Plan 26-04 wires `applicationId` from detail page.
  </acceptance_criteria>
  <done>Frontend has a reusable reimbursement signature pad and timeline signature image preview through authenticated blobs.</done>
</task>

</tasks>

<verification>
- `cd frontend && bun test src/types/__tests__/reimbursement.test.ts src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts` must exit 0 after Plan 26-04 completes UI wiring.
- `cd frontend && bun run build` should be run in Plan 26-04 after detail/list integration.
</verification>

<success_criteria>
Frontend review DTOs, store endpoints, signature capture and authenticated timeline signature image preview are implemented without page-local API calls or direct protected image URLs.
</success_criteria>

<output>
After completion, create `.planning/phases/26-reimbursement-review-signature/26-03-SUMMARY.md`.
</output>
