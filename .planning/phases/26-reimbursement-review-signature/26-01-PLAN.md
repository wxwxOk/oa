---
phase: 26
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts
  - backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts
  - backend/src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts
  - frontend/src/types/__tests__/reimbursement.test.ts
  - frontend/src/stores/__tests__/reimbursement.test.ts
  - frontend/src/pages/__tests__/ReimbursementPage.test.ts
autonomous: true
requirements: [APPROVAL-01, APPROVAL-02, APPROVAL-03, APPROVAL-04, APPROVAL-05, PERM-01, PERM-02, UX-01, UX-02]
user_setup:
  - "None. This wave adds focused source/store/service contracts only."
must_haves:
  truths:
    - "Wave 0 contracts pin actionable review queues separately from ordinary reimbursement visibility."
    - "Wave 0 contracts pin PNG-only action-bound signature storage and authenticated signature preview."
    - "Wave 0 contracts pin frontend review queue labels, detail review actions, signature dialog behavior and negative scope boundaries."
  artifacts:
    - path: "backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts"
      provides: "Backend review queue, transition, input normalization and signature/reject reason contracts"
    - path: "backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts"
      provides: "Backend review route signature, guard and body schema contracts"
    - path: "frontend/src/pages/__tests__/ReimbursementPage.test.ts"
      provides: "Frontend fixed reimbursement review UI source contracts"
  key_links:
    - from: ".planning/phases/26-reimbursement-review-signature/26-CONTEXT.md"
      to: "backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts"
      via: "D-02 through D-10 actionable queue and review action decisions"
      pattern: "DEPARTMENT_REVIEW|FINANCE_REVIEW|DEPARTMENT_APPROVE|FINANCE_APPROVE|REJECTED|completedAt"
    - from: ".planning/phases/26-reimbursement-review-signature/26-UI-SPEC.md"
      to: "frontend/src/pages/__tests__/ReimbursementPage.test.ts"
      via: "review labels, dialogs, mobile sticky actions and negative contract"
      pattern: "全部可见|待部门初审|待财务复核|部门初审通过|财务复核通过|mobile-review-actions"
---

# Phase 26 Plan 01: Wave 0 Review and Signature Contracts

<objective>
Add failing-but-specific contracts for Phase 26 two-level reimbursement review, handwritten PNG signatures, authenticated signature preview, and fixed reimbursement reviewer UI boundaries.
</objective>

<context>
This is the Wave 0 contract plan. It should not implement review behavior yet. It extends the existing reimbursement Bun/Vitest tests so later plans can work against exact routes, helper names, UI labels and security boundaries.
</context>

<source_audit>
Read first:

- `.planning/phases/26-reimbursement-review-signature/26-CONTEXT.md`
- `.planning/phases/26-reimbursement-review-signature/26-RESEARCH.md`
- `.planning/phases/26-reimbursement-review-signature/26-UI-SPEC.md`
- `.planning/phases/26-reimbursement-review-signature/26-VALIDATION.md`
- `backend/src/modules/reimbursement/reimbursement.state.ts`
- `backend/src/modules/reimbursement/reimbursement.service.ts`
- `backend/src/modules/reimbursement/reimbursement.route.ts`
- `backend/src/modules/reimbursement/reimbursement-file.service.ts`
- `backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts`
- `backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts`
- `backend/src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts`
- `frontend/src/types/reimbursement.ts`
- `frontend/src/stores/reimbursement.ts`
- `frontend/src/pages/ReimbursementPage.vue`
- `frontend/src/pages/ReimbursementDetailPage.vue`
- `frontend/src/components/reimbursement/ReimbursementActionTimeline.vue`
- `frontend/src/components/designer/fields/SignatureField.vue`
- `frontend/src/types/__tests__/reimbursement.test.ts`
- `frontend/src/stores/__tests__/reimbursement.test.ts`
- `frontend/src/pages/__tests__/ReimbursementPage.test.ts`
</source_audit>

<tasks>

<task id="26-01-01" type="auto">
  <name>Extend backend service review contracts</name>
  <files>backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts</files>
  <read_first>
    - `backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts` for current helper-test style and local `actor()` factory.
    - `backend/src/modules/reimbursement/reimbursement.service.ts` for exported helper names and existing visibility semantics.
    - `backend/src/modules/reimbursement/reimbursement.state.ts` for transition constants and current `REIMBURSEMENT_DEPARTMENT_REVIEW_NODE` export.
    - `.planning/phases/26-reimbursement-review-signature/26-CONTEXT.md` decisions D-02 through D-10.
    - `.planning/phases/26-reimbursement-review-signature/26-VALIDATION.md` Wave 0 requirements.
  </read_first>
  <action>
    Extend `backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts` with contract tests for Phase 26 helper exports. Import these expected exports from `../reimbursement.state`: `REIMBURSEMENT_FINANCE_REVIEW_NODE`. Import these expected exports from `../reimbursement.service`: `canDepartmentReviewReimbursement`, `canFinanceReviewReimbursement`, `normalizeReimbursementReviewInput`, and `type ReimbursementReviewInput`.

    Add a test named `pins review node constants and legal review transitions` asserting: `REIMBURSEMENT_DEPARTMENT_REVIEW_NODE` is `部门初审`, `REIMBURSEMENT_FINANCE_REVIEW_NODE` is `财务复核`, `assertReimbursementTransition('DEPARTMENT_REVIEW', 'FINANCE_REVIEW')` does not throw, `assertReimbursementTransition('DEPARTMENT_REVIEW', 'REJECTED')` does not throw, `assertReimbursementTransition('FINANCE_REVIEW', 'APPROVED')` does not throw, `assertReimbursementTransition('FINANCE_REVIEW', 'REJECTED')` does not throw, and `assertReimbursementTransition('APPROVED', 'REJECTED')` throws `BizError`.

    Add a test named `pins department and finance actionable predicates` with an application object `{ applicantId: 10, applicantDepartmentId: 3, status: 'DEPARTMENT_REVIEW' }`. Assert `canDepartmentReviewReimbursement(actor({ id: 2, permissions: ['reimbursement:department-review'] }), application, 3)` is `true`; same actor with department `4` is `false`; actor with only `reimbursement:list` is `false`; status `FINANCE_REVIEW` is `false`. Assert `canFinanceReviewReimbursement(actor({ id: 4, permissions: ['reimbursement:finance-review'] }), { ...application, status: 'FINANCE_REVIEW' })` is `true`; status `APPROVED` is `false`; actor with only `reimbursement:list` is `false`.

    Add a test named `pins review input normalization` that creates a PNG `File` with `new File(['png'], 'signature.png', { type: 'image/png' })`. Assert `normalizeReimbursementReviewInput({ comment: ' 同意 ', signature }, 'approve')` returns `{ comment: '同意', signature }`; optional blank approve comment returns `comment: null`; approve with `signature: null` throws `BizError`; approve with `new File(['x'], 'signature.jpg', { type: 'image/jpeg' })` throws `BizError`; reject with `{ comment: ' 资料不全 ' }` returns `{ comment: '资料不全', signature: null }`; reject with blank comment throws `BizError`.

    Keep existing Phase 24/25 service helper tests intact. Do not mock Prisma or implement service behavior in this task.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "REIMBURSEMENT_FINANCE_REVIEW_NODE|canDepartmentReviewReimbursement|canFinanceReviewReimbursement|normalizeReimbursementReviewInput" backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts` finds the new expected exports.
    - `rg "DEPARTMENT_REVIEW.*FINANCE_REVIEW|FINANCE_REVIEW.*APPROVED|APPROVED.*REJECTED" backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts` finds legal and illegal transition assertions.
    - `rg "reimbursement:department-review|reimbursement:finance-review|reimbursement:list" backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts` finds permission boundary assertions.
    - `rg "signature.png|image/png|image/jpeg|资料不全" backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts` finds signature and rejection normalization assertions.
    - `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts` exits non-zero until Plan 26-02 implements the missing exports, then exits 0.
  </acceptance_criteria>
  <done>Backend service contracts pin review predicates, state transitions and input normalization.</done>
</task>

<task id="26-01-02" type="auto">
  <name>Extend backend route and signature file contracts</name>
  <files>backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts, backend/src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts</files>
  <read_first>
    - `backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts` for existing route signature and source contract style.
    - `backend/src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts` for existing file-helper test style.
    - `backend/src/modules/reimbursement/reimbursement.route.ts` for route ordering and current guard markers.
    - `backend/src/modules/reimbursement/reimbursement-file.service.ts` for safe path/header helper conventions.
    - `.planning/phases/26-reimbursement-review-signature/26-RESEARCH.md` Backend Architecture and Routes sections.
  </read_first>
  <action>
    Update `backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts` so `routeSignatures()` is expected to include these exact signatures in addition to existing routes: `GET /review/department`, `GET /review/finance`, `POST /:id/department-review/approve`, `POST /:id/department-review/reject`, `POST /:id/finance-review/approve`, `POST /:id/finance-review/reject`, and `GET /:id/actions/:actionId/signature`.

    Add imports for expected route schemas `reimbursementReviewApproveBody` and `reimbursementReviewRejectBody`. Add a schema test asserting `schemaPropertyNames(reimbursementReviewApproveBody)` contains `signature` and `comment`, and that `schemaPropertyNames(reimbursementReviewRejectBody)` equals `['comment']`. Add a source contract assertion that `reimbursement.route.ts` contains `authGuard('reimbursement:department-review')`, `authGuard('reimbursement:finance-review')`, `/review/department`, `/review/finance`, `department-review/approve`, `department-review/reject`, `finance-review/approve`, `finance-review/reject`, `actions/:actionId/signature`, `assertCanViewReimbursement`, and `responseType` must not appear in backend route source.

    Update or create `backend/src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts` with imports for expected signature exports: `ALLOWED_REIMBURSEMENT_SIGNATURE_MIME_TYPES`, `MAX_REIMBURSEMENT_SIGNATURE_SIZE`, `assertAllowedReimbursementSignature`, `getSafeReimbursementSignatureStoredName`, `buildReimbursementSignatureRelativePath`, and `buildReimbursementSignaturePreviewHeaders`. Add assertions that allowed signature MIME types equal `['image/png']`, max size is `2 * 1024 * 1024`, PNG files pass, JPEG/PDF files throw, zero-byte files throw, stored names end with `.png`, relative path is exactly `signatures/25/DEPARTMENT_APPROVE/<storedName>` for application ID `25` and action type `DEPARTMENT_APPROVE`, and preview headers include `Content-Type: image/png` plus `Content-Disposition` containing `inline` and `signature.png`.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.route.test.ts src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "GET /review/department|GET /review/finance|department-review/approve|finance-review/reject|actions/:actionId/signature" backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts` finds exact route signatures.
    - `rg "reimbursementReviewApproveBody|reimbursementReviewRejectBody|signature|comment" backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts` finds body schema contracts.
    - `rg "ALLOWED_REIMBURSEMENT_SIGNATURE_MIME_TYPES|MAX_REIMBURSEMENT_SIGNATURE_SIZE|assertAllowedReimbursementSignature|buildReimbursementSignatureRelativePath|buildReimbursementSignaturePreviewHeaders" backend/src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts` finds signature helper contracts.
    - `rg "image/png|2 \* 1024 \* 1024|signatures/25/DEPARTMENT_APPROVE|signature.png|inline" backend/src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts` finds PNG-only storage assertions.
    - `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.route.test.ts src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts` exits non-zero until Plan 26-02 implements missing exports/routes, then exits 0.
  </acceptance_criteria>
  <done>Backend route and signature file contracts pin exact endpoints, guards, body schemas and PNG-only helper behavior.</done>
</task>

<task id="26-01-03" type="auto">
  <name>Extend frontend review and signature contracts</name>
  <files>frontend/src/types/__tests__/reimbursement.test.ts, frontend/src/stores/__tests__/reimbursement.test.ts, frontend/src/pages/__tests__/ReimbursementPage.test.ts</files>
  <read_first>
    - `frontend/src/types/__tests__/reimbursement.test.ts` for helper contract style and negative-scope assertions.
    - `frontend/src/stores/__tests__/reimbursement.test.ts` for mocked axios and existing blob tests.
    - `frontend/src/pages/__tests__/ReimbursementPage.test.ts` for source contract aggregation.
    - `frontend/src/types/reimbursement.ts` for DTO/helper export organization.
    - `frontend/src/stores/reimbursement.ts` for existing store API boundary and loading flags.
    - `frontend/src/pages/ReimbursementPage.vue` and `frontend/src/pages/ReimbursementDetailPage.vue` for list/detail source contracts.
    - `frontend/src/components/reimbursement/ReimbursementActionTimeline.vue` for timeline source contracts.
    - `.planning/phases/26-reimbursement-review-signature/26-UI-SPEC.md` for exact labels, dialog copy and negative contract.
  </read_first>
  <action>
    Extend `frontend/src/types/__tests__/reimbursement.test.ts` to expect Phase 26 review helpers. Import `REIMBURSEMENT_REVIEW_SCOPES`, `REIMBURSEMENT_REVIEW_ACTIONS`, `canReviewDepartmentReimbursement`, `canReviewFinanceReimbursement`, `normalizeReimbursementRejectPayload`, and `reimbursementSignatureDataUrlToFile`. Assert scopes equal `['all', 'department', 'finance']`; actions include `departmentApprove`, `departmentReject`, `financeApprove`, `financeReject`; department review returns true only for `DEPARTMENT_REVIEW` with permission `reimbursement:department-review`; finance review returns true only for `FINANCE_REVIEW` with permission `reimbursement:finance-review`; reject payload trims `comment`; `reimbursementSignatureDataUrlToFile('data:image/png;base64,c2ln', 'signature.png')` returns a `File` named `signature.png` with type `image/png`. Update the existing negative test so Phase 26 no longer expects `REIMBURSEMENT_REVIEW_ACTIONS` to be absent, but still expects export/OCR/category dictionary helpers to be absent.

    Extend `frontend/src/stores/__tests__/reimbursement.test.ts` with a test named `handles review queues, actions and signature blobs through fixed endpoints`. Mock GET responses for department and finance queues, POST responses for approve/reject actions, and a Blob response for signature preview. Assert `fetchDepartmentReviewList({ page: 1, size: 10 })` calls `/reimbursements/review/department`, `fetchFinanceReviewList({ page: 1, size: 10 })` calls `/reimbursements/review/finance`, `departmentApprove(25, { comment: '同意', signature })` posts `FormData` to `/reimbursements/25/department-review/approve` with `signature` key and `comment` key, `departmentReject(25, { comment: '资料不全' })` posts `{ comment: '资料不全' }` to `/reimbursements/25/department-review/reject`, `financeApprove` posts to `/reimbursements/25/finance-review/approve`, `financeReject` posts to `/reimbursements/25/finance-review/reject`, and `previewSignatureBlob(25, 88)` calls `/reimbursements/25/actions/88/signature` with `{ responseType: 'blob' }`. Extend the loading-reset test to include `departmentApprove` or `previewSignatureBlob` rejection and assert `actionLoading` and `downloadLoading` reset to false.

    Extend `frontend/src/pages/__tests__/ReimbursementPage.test.ts` to read `frontend/src/components/reimbursement/ReimbursementSignaturePad.vue` if present, append it to `uiSource`, and assert Phase 26 source contains `全部可见`, `待部门初审`, `待财务复核`, `部门初审通过`, `财务复核通过`, `确认部门初审通过`, `确认财务复核通过`, `确认驳回申请`, `审核意见（选填）`, `驳回原因`, `手写签名`, `点击签名`, `mobile-review-actions`, `URL.createObjectURL`, `URL.revokeObjectURL`, `previewSignatureBlob`, and `applicationId`. Update the forbidden list so `部门初审通过`, `财务复核通过`, `驳回申请`, and `手写签名` are allowed, while `/approval/applications`, `/approval/tasks`, `/reimbursements/export`, `导出 Excel`, `OCR`, `发票验真`, and `window.open` remain forbidden.
  </action>
  <verify>
    <automated>cd frontend && bun test src/types/__tests__/reimbursement.test.ts src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "REIMBURSEMENT_REVIEW_SCOPES|REIMBURSEMENT_REVIEW_ACTIONS|canReviewDepartmentReimbursement|canReviewFinanceReimbursement|reimbursementSignatureDataUrlToFile" frontend/src/types/__tests__/reimbursement.test.ts` finds Phase 26 type/helper contracts.
    - `rg "/reimbursements/review/department|/reimbursements/review/finance|department-review/approve|finance-review/reject|actions/88/signature|responseType: 'blob'" frontend/src/stores/__tests__/reimbursement.test.ts` finds store API boundary contracts.
    - `rg "FormData|signature|资料不全|actionLoading|downloadLoading" frontend/src/stores/__tests__/reimbursement.test.ts` finds multipart/reject/loading assertions.
    - `rg "全部可见|待部门初审|待财务复核|部门初审通过|财务复核通过|确认驳回申请|mobile-review-actions|previewSignatureBlob" frontend/src/pages/__tests__/ReimbursementPage.test.ts` finds UI source contracts.
    - `rg "/approval/applications|/approval/tasks|/reimbursements/export|导出 Excel|OCR|发票验真|window.open" frontend/src/pages/__tests__/ReimbursementPage.test.ts` finds the remaining negative contract.
    - `cd frontend && bun test src/types/__tests__/reimbursement.test.ts src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts` exits non-zero until Plans 26-03 and 26-04 implement missing frontend behavior, then exits 0.
  </acceptance_criteria>
  <done>Frontend contracts pin review scopes/actions, store endpoints, signature conversion, timeline preview and reviewer UI boundaries.</done>
</task>

</tasks>

<verification>
- `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts src/modules/reimbursement/__tests__/reimbursement.route.test.ts src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts` — expected red after Wave 0 until backend implementation lands.
- `cd frontend && bun test src/types/__tests__/reimbursement.test.ts src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts` — expected red after Wave 0 until frontend implementation lands.
</verification>

<success_criteria>
Wave 0 adds executable contracts for every Phase 26 backend and frontend boundary, and the failures point to the exact missing symbols, routes, labels, methods and source markers later waves must implement.
</success_criteria>
