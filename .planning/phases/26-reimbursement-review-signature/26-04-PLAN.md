---
phase: 26
plan: 04
type: execute
wave: 3
depends_on: [26-01, 26-02, 26-03]
files_modified:
  - frontend/src/pages/ReimbursementPage.vue
  - frontend/src/pages/ReimbursementDetailPage.vue
  - frontend/src/components/reimbursement/ReimbursementActionTimeline.vue
autonomous: true
requirements: [APPROVAL-01, APPROVAL-02, APPROVAL-03, APPROVAL-04, APPROVAL-05, PERM-01, PERM-02, UX-01, UX-02]
user_setup:
  - "For manual smoke checks, use accounts with `reimbursement:department-review` and `reimbursement:finance-review` permissions and at least one submitted reimbursement."
must_haves:
  truths:
    - "The reimbursement list exposes reviewer queue controls under `/reimbursements` and keeps the ordinary all-visible list separate from actionable queues."
    - "The reimbursement detail page shows approve/reject controls only for the current actionable review node and permission, with desktop side-card actions and mobile sticky actions."
    - "Approve dialogs require a non-empty Canvas PNG signature, reject dialogs require a non-empty reason, and all actions refresh detail/list state with explicit success/failure feedback."
  artifacts:
    - path: "frontend/src/pages/ReimbursementPage.vue"
      provides: "All-visible, department-review and finance-review queue UI under fixed reimbursement list"
    - path: "frontend/src/pages/ReimbursementDetailPage.vue"
      provides: "Reviewer workbench with approve/reject dialogs, handwritten signature capture and mobile sticky actions"
    - path: "frontend/src/components/reimbursement/ReimbursementActionTimeline.vue"
      provides: "Action timeline rendered with applicationId from detail page for signature preview"
  key_links:
    - from: ".planning/phases/26-reimbursement-review-signature/26-UI-SPEC.md"
      to: "frontend/src/pages/ReimbursementDetailPage.vue"
      via: "exact labels, dialogs, mobile sticky controls and feedback copy"
      pattern: "部门初审通过|财务复核通过|确认驳回申请|手写签名|mobile-review-actions"
    - from: "frontend/src/stores/reimbursement.ts"
      to: "frontend/src/pages/ReimbursementDetailPage.vue"
      via: "review action methods"
      pattern: "departmentApprove|departmentReject|financeApprove|financeReject|reimbursementSignatureDataUrlToFile"
---

# Phase 26 Plan 04: Reviewer Queue UI, Detail Actions, and Final Validation

<objective>
Complete the Phase 26 reviewer-facing reimbursement UI by wiring department/finance queues, detail approve/reject/signature dialogs, mobile sticky review actions, timeline signature previews and the final focused validation gates.
</objective>

<context>
This is the final Phase 26 wave. It consumes backend review endpoints from Plan 02 and frontend types/store/signature/timeline infrastructure from Plan 03. It must keep review inside the fixed `/reimbursements` route family and must not introduce dynamic approval pages, export, OCR, invoice verification or payment/statistics surfaces.
</context>

<source_audit>
Read first:

- `.planning/phases/26-reimbursement-review-signature/26-CONTEXT.md`
- `.planning/phases/26-reimbursement-review-signature/26-RESEARCH.md`
- `.planning/phases/26-reimbursement-review-signature/26-UI-SPEC.md`
- `.planning/phases/26-reimbursement-review-signature/26-VALIDATION.md`
- `.planning/phases/26-reimbursement-review-signature/26-01-PLAN.md`
- `.planning/phases/26-reimbursement-review-signature/26-02-PLAN.md`
- `.planning/phases/26-reimbursement-review-signature/26-03-PLAN.md`
- `frontend/src/types/reimbursement.ts`
- `frontend/src/stores/reimbursement.ts`
- `frontend/src/pages/ReimbursementPage.vue`
- `frontend/src/pages/ReimbursementDetailPage.vue`
- `frontend/src/components/reimbursement/ReimbursementSignaturePad.vue`
- `frontend/src/components/reimbursement/ReimbursementActionTimeline.vue`
- `frontend/src/components/reimbursement/ReimbursementAttachmentPanel.vue`
- `frontend/src/pages/ApprovalTaskDetailPage.vue`
- `frontend/src/pages/__tests__/ReimbursementPage.test.ts`
- `frontend/src/stores/__tests__/reimbursement.test.ts`
- `frontend/src/types/__tests__/reimbursement.test.ts`
- `frontend/src/stores/auth.ts`
- `frontend/src/composables/useResponsive.ts`
</source_audit>

<tasks>

<task id="26-04-01" type="auto">
  <name>Wire department and finance reviewer queues in the reimbursement list</name>
  <files>frontend/src/pages/ReimbursementPage.vue</files>
  <read_first>
    - `frontend/src/pages/ReimbursementPage.vue` for current filter/table/card behavior and `store.fetchList()` usage.
    - `frontend/src/stores/reimbursement.ts` for `fetchDepartmentReviewList()` and `fetchFinanceReviewList()` methods.
    - `frontend/src/types/reimbursement.ts` for `ReimbursementReviewScope` and `REIMBURSEMENT_REVIEW_SCOPES`.
    - `frontend/src/stores/auth.ts` for `auth.hasPerm()` and admin handling conventions.
    - `.planning/phases/26-reimbursement-review-signature/26-UI-SPEC.md` Entry and List Contract section.
  </read_first>
  <action>
    Extend `ReimbursementPage.vue` with a review-scope control under the existing list header using exact labels `全部可见`, `待部门初审`, and `待财务复核`. Use a local state value typed as `ReimbursementReviewScope` with default `all`. Show the `待部门初审` option only when `auth.hasPerm('reimbursement:department-review')` is true. Show the `待财务复核` option only when `auth.hasPerm('reimbursement:finance-review')` is true.

    Update the existing list-loading function so scope `all` calls `store.fetchList(filters)`, scope `department` calls `store.fetchDepartmentReviewList(filters)`, and scope `finance` calls `store.fetchFinanceReviewList(filters)`. Preserve the current filter keys `keyword`, `status`, `category`, `dateFrom`, `dateTo`, desktop `q-table`, mobile `reimbursement-card`, pagination options `[10, 20, 50]`, `查询`, `重置筛选`, and mobile `应用筛选` behavior. Switching review scope must reset to page `1` and reload using the current filters.

    Keep create/edit/submit behavior unchanged: `新建报销申请`, `继续编辑`, and `提交申请` remain gated by `reimbursement:create` and draft status. Do not add approve/reject buttons to list rows; row/card review work must still route through `/reimbursements/${row.id}` detail.
  </action>
  <verify>
    <automated>cd frontend && bun test src/pages/__tests__/ReimbursementPage.test.ts src/stores/__tests__/reimbursement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "全部可见|待部门初审|待财务复核|ReimbursementReviewScope|REIMBURSEMENT_REVIEW_SCOPES" frontend/src/pages/ReimbursementPage.vue` finds review-scope UI.
    - `rg "reimbursement:department-review|reimbursement:finance-review|auth\.hasPerm" frontend/src/pages/ReimbursementPage.vue` finds permission-gated queue options.
    - `rg "fetchDepartmentReviewList|fetchFinanceReviewList|fetchList" frontend/src/pages/ReimbursementPage.vue` finds separate queue/list loading calls.
    - `rg "q-table|reimbursement-card|keyword|status|category|dateFrom|dateTo|\[10, 20, 50\]|应用筛选" frontend/src/pages/ReimbursementPage.vue` confirms existing list/filter behavior remains present.
    - `rg "departmentApprove|departmentReject|financeApprove|financeReject|确认部门初审通过|确认财务复核通过" frontend/src/pages/ReimbursementPage.vue` returns no matches.
    - `cd frontend && bun test src/pages/__tests__/ReimbursementPage.test.ts src/stores/__tests__/reimbursement.test.ts` exits 0 after Task 26-04-02 completes.
  </acceptance_criteria>
  <done>Reimbursement list can switch between all-visible rows and dedicated department/finance actionable queues without list-row action side effects.</done>
</task>

<task id="26-04-02" type="auto">
  <name>Build detail review workbench dialogs and mobile sticky actions</name>
  <files>frontend/src/pages/ReimbursementDetailPage.vue, frontend/src/components/reimbursement/ReimbursementActionTimeline.vue</files>
  <read_first>
    - `frontend/src/pages/ReimbursementDetailPage.vue` for current read-only sections, draft actions, detail-side layout and mobile actions.
    - `frontend/src/components/reimbursement/ReimbursementSignaturePad.vue` for `v-model`, `save()`, `clear()` and `isEmpty()` behavior.
    - `frontend/src/components/reimbursement/ReimbursementActionTimeline.vue` for required `applicationId` prop.
    - `frontend/src/stores/reimbursement.ts` for `departmentApprove()`, `departmentReject()`, `financeApprove()`, `financeReject()` and `fetchDetail()`.
    - `frontend/src/types/reimbursement.ts` for `canReviewDepartmentReimbursement()`, `canReviewFinanceReimbursement()`, `reimbursementSignatureDataUrlToFile()`, `formatReimbursementAmount()` and `formatReimbursementDate()`.
    - `frontend/src/pages/ApprovalTaskDetailPage.vue` for desktop side-card and mobile sticky action patterns.
    - `.planning/phases/26-reimbursement-review-signature/26-UI-SPEC.md` Detail Review Actions, Dialogs and Feedback Copy sections.
  </read_first>
  <action>
    Extend `ReimbursementDetailPage.vue` into the reimbursement review workbench. Keep current sections `申请信息`, `申请人信息`, `报销明细`, `附件`, and `审核轨迹`. Pass the required timeline prop as `<ReimbursementActionTimeline :application-id="detail.id" :actions="detail.actions" />`.

    Add computed values from `auth.user?.permissions ?? []` and `auth.isAdmin`: `canDepartmentReview = detail.status === 'DEPARTMENT_REVIEW' && canReviewDepartmentReimbursement(detail, permissions, auth.isAdmin)`, `canFinanceReview = detail.status === 'FINANCE_REVIEW' && canReviewFinanceReimbursement(detail, permissions, auth.isAdmin)`, and `canShowReviewActions = canDepartmentReview || canFinanceReview`. Review controls must not render for `DRAFT`, `APPROVED`, or `REJECTED`.

    Add a desktop review action card inside the existing `detail-side` column. When `canDepartmentReview` is true, render buttons `部门初审通过` and `驳回申请`. When `canFinanceReview` is true, render buttons `财务复核通过` and `驳回申请`. Keep draft `继续编辑`/`提交申请` behavior separate and only for `DRAFT` with `reimbursement:create`.

    Add a mobile sticky action bar outside the main content with class `mobile-review-actions`, visible only when `isMobile && canShowReviewActions`. It must render the same approve/reject button labels as desktop, use `padding-bottom: calc(12px + env(safe-area-inset-bottom))`, and enforce `min-height: 44px` on buttons. Add a wrapper class such as `has-mobile-review-actions` to reserve bottom space so the sticky bar does not cover detail content.

    Add approval dialog state for department/finance. Dialog title and confirm label must be `确认部门初审通过` for department and `确认财务复核通过` for finance. Include a summary showing `applicationNo`, `title`, formatted `amount`, `applicantName`, and `applicantDepartmentName`. Include `q-input` label `审核意见（选填）`. Include `ReimbursementSignaturePad` with visible `手写签名` and `点击签名`. Confirm must be disabled until a non-empty PNG signature data URL exists. On confirm, convert the data URL with `reimbursementSignatureDataUrlToFile(signatureDataUrl, 'signature.png')`, call `store.departmentApprove()` or `store.financeApprove()`, show success `部门初审已通过，申请进入财务复核` or `财务复核已通过，报销申请完成`, close/clear dialog state, and reload detail.

    Add reject dialog state. Dialog title must be `确认驳回申请`; textarea label must be `驳回原因`; confirm label must be `确认驳回`; confirm must be disabled while `rejectComment.trim().length === 0`. On confirm, call `store.departmentReject()` or `store.financeReject()` based on the active review stage, show success `报销申请已驳回`, close/clear dialog state, and reload detail. All review failures must show `审核操作失败，请检查后重试。`. Disable duplicate submissions with `store.actionLoading`.

    Keep all API calls inside `useReimbursementStore`. Do not import `api`. Do not use `/approval/applications`, `/approval/tasks`, `/reimbursements/export`, `导出 Excel`, `OCR`, `发票验真`, direct protected signature URLs, or `window.open`.
  </action>
  <verify>
    <automated>cd frontend && bun test src/pages/__tests__/ReimbursementPage.test.ts src/stores/__tests__/reimbursement.test.ts && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "ReimbursementSignaturePad|reimbursementSignatureDataUrlToFile|departmentApprove|departmentReject|financeApprove|financeReject" frontend/src/pages/ReimbursementDetailPage.vue` finds review dialog action wiring.
    - `rg "部门初审通过|财务复核通过|驳回申请|确认部门初审通过|确认财务复核通过|确认驳回申请|审核意见（选填）|驳回原因|确认驳回" frontend/src/pages/ReimbursementDetailPage.vue` finds exact UI copy.
    - `rg "部门初审已通过，申请进入财务复核|财务复核已通过，报销申请完成|报销申请已驳回|审核操作失败，请检查后重试。" frontend/src/pages/ReimbursementDetailPage.vue` finds feedback copy.
    - `rg "mobile-review-actions|env\(safe-area-inset-bottom\)|min-height: 44px|has-mobile-review-actions" frontend/src/pages/ReimbursementDetailPage.vue` finds mobile sticky safety.
    - `rg ":application-id=\"detail.id\"|ReimbursementActionTimeline" frontend/src/pages/ReimbursementDetailPage.vue` finds timeline applicationId wiring.
    - `rg "api\.get|api\.post|api\.put|api\.delete|/approval/applications|/approval/tasks|/reimbursements/export|导出 Excel|OCR|发票验真|window.open" frontend/src/pages/ReimbursementDetailPage.vue` returns no matches.
    - `cd frontend && bun test src/pages/__tests__/ReimbursementPage.test.ts src/stores/__tests__/reimbursement.test.ts && bun run build` exits 0.
  </acceptance_criteria>
  <done>Reimbursement detail exposes safe desktop/mobile review actions with required signature/reject validation and post-action refresh.</done>
</task>

<task id="26-04-03" type="auto">
  <name>Run final focused backend and frontend gates</name>
  <files>backend/src/modules/reimbursement/reimbursement.service.ts, backend/src/modules/reimbursement/reimbursement.route.ts, frontend/src/pages/ReimbursementPage.vue, frontend/src/pages/ReimbursementDetailPage.vue, frontend/src/components/reimbursement/ReimbursementActionTimeline.vue</files>
  <read_first>
    - `.planning/phases/26-reimbursement-review-signature/26-VALIDATION.md` for final focused commands and known suite caveats.
    - `backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts`
    - `backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts`
    - `backend/src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts`
    - `frontend/src/types/__tests__/reimbursement.test.ts`
    - `frontend/src/stores/__tests__/reimbursement.test.ts`
    - `frontend/src/pages/__tests__/ReimbursementPage.test.ts`
  </read_first>
  <action>
    Run the final Phase 26 focused backend and frontend gates exactly:

    `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts src/modules/reimbursement/__tests__/reimbursement.route.test.ts src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts && bun run build`

    Then run:

    `cd frontend && bun test src/types/__tests__/reimbursement.test.ts src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts && bun run build`

    Fix only Phase 26 regressions introduced by Plans 26-01 through 26-04. Do not repair unrelated global backend approval-suite failures or unrelated frontend browser-global test environment failures in this plan. If a focused gate fails, inspect the failing assertion and adjust only the relevant reimbursement source or focused test contract.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts src/modules/reimbursement/__tests__/reimbursement.route.test.ts src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts && bun run build; cd ../frontend && bun test src/types/__tests__/reimbursement.test.ts src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts src/modules/reimbursement/__tests__/reimbursement.route.test.ts src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts && bun run build` exits 0.
    - `cd frontend && bun test src/types/__tests__/reimbursement.test.ts src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts && bun run build` exits 0.
    - `rg "responseType: 'blob'|URL.createObjectURL|URL.revokeObjectURL|previewSignatureBlob" frontend/src/stores/reimbursement.ts frontend/src/components/reimbursement/ReimbursementActionTimeline.vue` finds authenticated signature preview evidence.
    - `rg "signatureRelativePath|signatureMimeType|signatureSize|completedAt|DEPARTMENT_APPROVE|FINANCE_APPROVE|DEPARTMENT_REJECT|FINANCE_REJECT" backend/src/modules/reimbursement/reimbursement.service.ts` finds action trail and terminal-time evidence.
    - `rg "/approval/applications|/approval/tasks|/reimbursements/export|导出 Excel|OCR|发票验真|window.open" frontend/src/pages/ReimbursementPage.vue frontend/src/pages/ReimbursementDetailPage.vue frontend/src/components/reimbursement/ReimbursementActionTimeline.vue frontend/src/components/reimbursement/ReimbursementSignaturePad.vue` returns no matches.
  </acceptance_criteria>
  <done>Phase 26 focused backend/frontend gates and builds are green with no out-of-scope UI/API surface.</done>
</task>

</tasks>

<verification>
- Backend focused gate: `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts src/modules/reimbursement/__tests__/reimbursement.route.test.ts src/modules/reimbursement/__tests__/reimbursement-file.service.test.ts && bun run build`.
- Frontend focused gate: `cd frontend && bun test src/types/__tests__/reimbursement.test.ts src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts && bun run build`.
- Manual smoke after implementation: department approve with signature, department reject with reason, finance approve with signature, finance reject with reason, timeline signature preview, mobile sticky actions at 375px width.
</verification>

<success_criteria>
Phase 26 delivers reviewer queue entry points, department and finance approve/reject detail actions, required Canvas PNG signatures, required reject reasons, complete timeline signature preview and focused backend/frontend validation.
</success_criteria>

<output>
After completion, create `.planning/phases/26-reimbursement-review-signature/26-04-SUMMARY.md` and update Phase 26 verification artifacts.
</output>
