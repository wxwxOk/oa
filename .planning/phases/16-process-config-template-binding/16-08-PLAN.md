---
phase: 16-process-config-template-binding
plan: 08
type: execute
wave: 4
depends_on: [16-04, 16-05]
files_modified:
  - frontend/src/pages/DepartmentPage.vue
  - frontend/src/components/renderer/FieldRenderer.vue
autonomous: true
requirements: [CFG-04, DYN-01]

must_haves:
  truths:
    - "Department page displays each department's default approver or explicit missing state"
    - "Department create/edit dialog can save nullable defaultApproverId"
    - "Department default approver options come from the department approver-options API, not a user:list-only workflow"
    - "Required validation remains configured by existing SchemaField.required"
    - "FieldRenderer imperative validation covers text, textarea, date, phone, radio, checkbox, and signature"
    - "PC and mobile public fill paths share GridFormRenderer and FieldRenderer validation behavior"
  artifacts:
    - path: "frontend/src/pages/DepartmentPage.vue"
      provides: "Department default approver UI"
      contains: "负责人/默认审批人"
    - path: "frontend/src/components/renderer/FieldRenderer.vue"
      provides: "Shared required-field validation for PC/mobile fill"
      contains: "请输入有效手机号"
  key_links:
    - from: "DepartmentPage.vue"
      to: "/departments/approver-options"
      via: "active default approver selector"
      pattern: "approver-options"
    - from: "GridFormRenderer.validateFields"
      to: "FieldRenderer.validate"
      via: "imperative required validation"
      pattern: "validate\\("
---

<objective>
Add department default-approver UI and complete shared frontend required-field validation.

Purpose: Department-manager workflows require maintainable approvers, and Phase 16 must close the PC/mobile required-validation gap without adding new schema concepts.
Output: Department page supports default approver display/editing, and FieldRenderer validates all Phase 16 required field types.
</objective>

<execution_context>
@.planning/phases/16-process-config-template-binding/16-CONTEXT.md
@.planning/phases/16-process-config-template-binding/16-RESEARCH.md
@.planning/phases/16-process-config-template-binding/16-PATTERNS.md
@.planning/phases/16-process-config-template-binding/16-UI-SPEC.md
@.planning/phases/16-process-config-template-binding/16-VALIDATION.md
@.claude/skills/ui-ux-pro-max/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/16-process-config-template-binding/16-04-SUMMARY.md
@.planning/phases/16-process-config-template-binding/16-05-SUMMARY.md

<interfaces>
Department tree nodes from backend:

```ts
interface DeptNode {
  id: number;
  name: string;
  parentId: number | null;
  sort: number;
  defaultApproverId: number | null;
  defaultApprover: { id: number; username: string; realName: string } | null;
  children: DeptNode[];
}
```

Required frontend messages:
- text/textarea: `此项为必填`
- date: `请选择日期`
- phone: `请输入有效手机号`
- radio: `请选择一项`
- checkbox: `请至少选择一项`
- signature: `请签名`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add department default approver selector and display</name>
  <files>frontend/src/pages/DepartmentPage.vue</files>
  <read_first>
    - frontend/src/pages/DepartmentPage.vue
    - frontend/src/pages/UserPage.vue
    - frontend/src/boot/axios.ts
    - frontend/src/stores/auth.ts
    - .planning/phases/16-process-config-template-binding/16-UI-SPEC.md
    - .claude/skills/ui-ux-pro-max/SKILL.md
  </read_first>
  <action>
Modify `frontend/src/pages/DepartmentPage.vue`.

Decision trace: implements D-19 and D-20 by extending the existing department management page/dialog with负责人/默认审批人 display and edit controls under `department:update`.

Implementation target:
1. Extend local `DeptNode` interface with:
   - `defaultApproverId: number | null`
   - `defaultApprover: { id: number; username: string; realName: string } | null`
2. In tree header, show text beside/under department name:
   - with approver: `负责人：{realName}`
   - without approver: `未设置负责人`
   Use visible text, not color alone.
3. Add approver options state:
```ts
const approverOptions = ref<Array<{ label: string; value: number }>>([]);
```
4. Load approver options from `GET /departments/approver-options` when opening create/edit dialog and user has `department:update`.
5. Option label format is `{realName}（{username}）`.
6. Dialog adds `q-select outlined clearable emit-value map-options` labeled exactly `负责人/默认审批人`.
7. `openEdit` initializes `form.defaultApproverId` from the selected node or `null`.
8. Create and update payloads include `defaultApproverId: form.defaultApproverId ?? null`.
9. Keep existing parent department cycle protection UI, mobile maximized dialog, and save button copy `保存部门`.
10. Icon-only tree action buttons keep `aria-label` or `q-tooltip`; mobile controls remain at least 44px high.

Do not create a separate department approver page or a new permission code.
  </action>
  <verify>
    <automated>cd frontend && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/DepartmentPage.vue` contains `defaultApproverId`
    - The page contains `defaultApprover`
    - The page contains `负责人/默认审批人`
    - The page contains `负责人：`
    - The page contains `未设置负责人`
    - The page contains `/departments/approver-options`
    - The page contains `department:update`
    - `cd frontend && bun run build` exits 0
  </acceptance_criteria>
  <done>Department UI can display and persist default approvers using existing department permissions.</done>
</task>

<task type="auto">
  <name>Task 2: Complete shared FieldRenderer required validation</name>
  <files>frontend/src/components/renderer/FieldRenderer.vue</files>
  <read_first>
    - frontend/src/components/renderer/FieldRenderer.vue
    - frontend/src/components/renderer/GridFormRenderer.vue
    - frontend/src/components/renderer/GroupRenderer.vue
    - frontend/src/pages/PublicFillPage.vue
    - frontend/src/types/schema.ts
    - backend/src/modules/template/schema.validation.ts
    - .planning/phases/16-process-config-template-binding/16-UI-SPEC.md
  </read_first>
  <action>
Modify `frontend/src/components/renderer/FieldRenderer.vue`.

Decision trace: implements D-25, D-26, and D-27 by keeping `SchemaField.required` as the UI source of truth, using the shared `GridFormRenderer`/`FieldRenderer` PC/mobile validation path, covering the required field types, and not adding dynamic-table column-level required settings.

Implementation target:
1. Keep existing `SchemaField.required` as the only requiredness source; do not add dynamic-table column required settings.
2. Preserve existing `q-input` rules for browser-visible validation messages.
3. Add imperative validation inside `function validate(value: any, field: SchemaField): boolean` for all required field types:
   - `text`: trimmed non-empty string, message `此项为必填`
   - `textarea`: trimmed non-empty string, message `此项为必填`
   - `date`: non-empty value, message `请选择日期`
   - `phone`: `^1\\d{10}$`, message `请输入有效手机号`
   - `radio`: non-null and non-empty, message `请选择一项`
   - `checkbox`: non-empty array, message `请至少选择一项`
   - `signature`: non-empty string/data URL after calling existing signature save flow, message `请签名`
4. Add reactive error state for imperative validation so `GridFormRenderer.validateFields()` shows a nearby message even when QForm rules are not triggered by a future caller:
```ts
const imperativeError = ref('');
```
Bind `:error="!!imperativeError"` and `:error-message="imperativeError"` to text, textarea, date, and phone `q-input`s, and keep existing radio/checkbox/signature inline errors.
5. Clear `imperativeError`, `radioError`, `checkboxError`, and `sigError` when the field becomes valid.
6. Do not modify `GridFormRenderer.validateFields()` unless TypeScript requires a typing adjustment; the existing call path must remain the shared PC/mobile validation path.

Validation messages must match the UI-SPEC copy exactly.
  </action>
  <verify>
    <automated>cd frontend && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/renderer/FieldRenderer.vue` contains `imperativeError`
    - The component contains `此项为必填`
    - The component contains `请选择日期`
    - The component contains `请输入有效手机号`
    - The component contains `请选择一项`
    - The component contains `请至少选择一项`
    - The component contains `请签名`
    - The component contains `^1\\d{10}$`
    - The component still exposes `validate`
    - The component still exposes `saveSignature`
    - `cd frontend && bun run build` exits 0
  </acceptance_criteria>
  <done>Shared frontend required validation covers all Phase 16 required field types for PC and mobile fill paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Department admin UI -> department API | UI-selected approver becomes future department-manager approver source |
| User-entered dynamic form data -> frontend validator | Missing required fields must be caught consistently before submission |
| Frontend validator -> backend validator | Frontend helps UX, but backend remains enforcement authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T16-04 | Elevation of Privilege | `DepartmentPage.vue` | mitigate | UI uses backend `/departments/approver-options` active-user list and persists nullable `defaultApproverId`; backend still validates active users |
| T16-05 | Tampering | `FieldRenderer.vue` | mitigate | Imperative validation covers all Phase 16 required field types through shared GridFormRenderer path; backend validator in Plan 05 remains authoritative |
| T16-06 | Elevation of Privilege | department approver UI | mitigate | Approver editing remains in DepartmentPage and uses existing `department:update` permission boundary |
</threat_model>

<verification>
```powershell
cd frontend
bun run build
```

Full phase verification after all plans:
```powershell
cd backend
bun test src/modules/approval/__tests__/process-config.service.test.ts src/modules/template/__tests__/template.approval-mode.test.ts src/modules/template/__tests__/schema.validation.test.ts
bun test src/modules/role/__tests__/approval-permissions.seed.test.ts
bun run build
cd ../frontend
bun run build
```
</verification>

<success_criteria>
- Department tree shows `负责人：{realName}` or `未设置负责人`
- Department dialog saves nullable `defaultApproverId`
- Required frontend validation covers text, textarea, date, phone, radio, checkbox, and signature
- Dynamic-table column-level required remains out of scope
- Frontend build passes
- Full backend/template/RBAC tests and frontend build pass after all plans
</success_criteria>

<output>
After completion, create `.planning/phases/16-process-config-template-binding/16-08-SUMMARY.md`.
</output>
