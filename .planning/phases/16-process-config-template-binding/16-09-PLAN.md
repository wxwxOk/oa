---
phase: 16-process-config-template-binding
plan: 09
type: execute
wave: 6
gap_closure: true
depends_on: [16-03, 16-VERIFICATION]
files_modified:
  - backend/src/modules/approval/process.route.ts
  - backend/src/modules/approval/__tests__/process-config.service.test.ts
autonomous: true
requirements: [CFG-02, CFG-03]

must_haves:
  truths:
    - "Full process edit cannot deactivate a process bound by published APPROVAL_REQUIRED templates"
    - "The existing status and delete binding guards remain unchanged"
    - "A regression test proves PUT-style update cannot disable a bound published process"
  artifacts:
    - path: "backend/src/modules/approval/process.route.ts"
      provides: "Approval process full-edit disable guard"
      contains: "APPROVAL_PROCESS_BOUND_BY_PUBLISHED_TEMPLATE"
    - path: "backend/src/modules/approval/__tests__/process-config.service.test.ts"
      provides: "Regression coverage for full edit bound-process deactivation"
      contains: "full process update cannot deactivate a process bound by a published approval template"
  key_links:
    - from: "updateApprovalProcessConfig"
      to: "FormTemplate businessMode/status binding guard"
      via: "assertNotBoundByPublishedApprovalTemplate before writing isActive=false"
      pattern: "input.isActive === false"
---

<objective>
Close the Phase 16 verification gap by applying the published-template binding guard to the full approval process update path.

Purpose: The verifier found that `PATCH /approval/processes/:id/status` and `DELETE /approval/processes/:id` block deactivating/deleting bound published approval processes, but `PUT /approval/processes/:id` can still set `isActive: false`.
Output: The full update service rejects that state transition and a regression test covers the route helper used by the PUT handler.
</objective>

<execution_context>
@.planning/phases/16-process-config-template-binding/16-VERIFICATION.md
@.planning/phases/16-process-config-template-binding/16-03-PLAN.md
@.planning/phases/16-process-config-template-binding/16-03-SUMMARY.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Guard full process edit deactivation</name>
  <files>backend/src/modules/approval/process.route.ts</files>
  <read_first>
    - backend/src/modules/approval/process.route.ts
    - backend/src/modules/approval/process-config.service.ts
    - backend/src/modules/template/template.route.ts
    - .planning/phases/16-process-config-template-binding/16-VERIFICATION.md
  </read_first>
  <action>
Update `updateApprovalProcessConfig(processId, input)` so that when an existing active process is being changed to inactive through the full update payload, it calls `assertNotBoundByPublishedApprovalTemplate(processId)` before the transaction mutates the process or nodes.

The guard should only run for the active -> inactive transition. It should not block updating an already inactive process, it should not block active edits, and it should preserve the existing transactional validation behavior.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/approval/__tests__/process-config.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `updateApprovalProcessConfig` calls `assertNotBoundByPublishedApprovalTemplate`
    - The call is conditioned on `existing.isActive === true` and `input.isActive === false`
    - Existing `PATCH /status` and `DELETE` guard behavior remains intact
  </acceptance_criteria>
  <done>Full process edit uses the same published-template deactivation protection as status toggle and delete.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage and verify</name>
  <files>backend/src/modules/approval/__tests__/process-config.service.test.ts</files>
  <read_first>
    - backend/src/modules/approval/__tests__/process-config.service.test.ts
    - backend/prisma/schema.prisma
  </read_first>
  <action>
Add a regression test named `full process update cannot deactivate a process bound by a published approval template`.

The test should:
1. Create a valid active process with one fixed-user node.
2. Create a published `APPROVAL_REQUIRED` `FormTemplate` bound to that process.
3. Call `updateApprovalProcessConfig(process.id, { ..., isActive: false, nodes: [...] })`.
4. Assert the call rejects with `APPROVAL_PROCESS_BOUND_BY_PUBLISHED_TEMPLATE` or the Chinese guard message.
5. Reload the process and ordered nodes to prove `isActive` and node data were not changed.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/approval/__tests__/process-config.service.test.ts && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains `full process update cannot deactivate a process bound by a published approval template`
    - Targeted process config tests pass
    - Backend build passes
  </acceptance_criteria>
  <done>The verifier gap has direct regression coverage.</done>
</task>

</tasks>

<verification>
```powershell
cd backend
bun test src/modules/approval/__tests__/process-config.service.test.ts
bun run build
```
</verification>

<success_criteria>
- Full process update blocks active-to-inactive changes for processes referenced by published `APPROVAL_REQUIRED` templates.
- The failed update leaves the prior process and node rows intact.
- Phase 16 targeted backend checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/16-process-config-template-binding/16-09-SUMMARY.md`.
</output>
