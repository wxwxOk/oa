---
phase: 08-share-public-fill
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - frontend/src/stores/template.ts
  - frontend/src/components/ShareDialog.vue
  - frontend/src/pages/TemplatePage.vue
  - frontend/src/pages/FormDesignerPage.vue
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-04]

must_haves:
  truths:
    - "模板列表页 PUBLISHED 状态的模板行有'分享'按钮"
    - "点击分享按钮弹出 ShareDialog，显示完整分享链接 URL 和二维码"
    - "复制链接按钮调用 navigator.clipboard.writeText 并显示成功通知"
    - "二维码通过 qrcode.toCanvas() 渲染到 canvas 元素"
    - "设计器页面有 requireIdentity QToggle 开关"
    - "Template store 有 createShareLink action"
  artifacts:
    - path: "frontend/src/components/ShareDialog.vue"
      provides: "分享弹窗组件（链接 + 二维码 + 复制）"
      contains: "QRCode.toCanvas"
    - path: "frontend/src/stores/template.ts"
      provides: "createShareLink action"
      contains: "createShareLink"
    - path: "frontend/src/pages/TemplatePage.vue"
      provides: "分享按钮 + ShareDialog 集成"
      contains: "ShareDialog"
    - path: "frontend/src/pages/FormDesignerPage.vue"
      provides: "requireIdentity 开关"
      contains: "requireIdentity"
  key_links:
    - from: "TemplatePage.vue"
      to: "ShareDialog.vue"
      via: "v-model + templateId prop"
      pattern: "ShareDialog"
    - from: "ShareDialog.vue"
      to: "template store"
      via: "createShareLink action"
      pattern: "createShareLink"
    - from: "FormDesignerPage.vue"
      to: "template store"
      via: "store.update with requireIdentity"
      pattern: "requireIdentity"
---

<objective>
前端管理端集成：新建 ShareDialog 组件（链接+二维码+复制），在 TemplatePage 添加分享按钮，在 FormDesignerPage 添加身份信息开关，扩展 template store。

Purpose: 让管理员可以为已发布模板生成分享链接和二维码，并配置是否要求填写者身份信息。
Output: 1 个新组件 + 2 个页面修改 + 1 个 store 扩展。
</objective>

<execution_context>
@.planning/phases/08-share-public-fill/08-UI-SPEC.md
@.planning/phases/08-share-public-fill/08-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- TemplatePage 现有 dialog 模式和按钮模式 -->
From frontend/src/pages/TemplatePage.vue:
```vue
<!-- 操作按钮模式 -->
<q-btn v-if="props.row.status === 'PUBLISHED'" v-perm="'form:template:publish'"
  size="sm" flat dense icon="unpublished" color="warning" @click="onOffline(props.row)" />

<!-- Dialog 模式 -->
<q-dialog v-model="createDialog">
  <q-card style="min-width: 400px">
    <q-card-section class="text-h6">创建模板</q-card-section>
    ...
  </q-card>
</q-dialog>
```

From frontend/src/stores/template.ts:
```typescript
export const useTemplateStore = defineStore('template', {
  actions: {
    async fetchOne(id: number) { ... },
    async update(id: number, payload: { name?: string; description?: string; schema?: FormField[] }) { ... },
    async changeStatus(id: number, action: 'publish' | 'offline') { ... },
  },
});
```

From frontend/src/pages/FormDesignerPage.vue:
```vue
<div v-else class="designer-body row no-wrap">
  <FieldPalette />
  <DesignerCanvas />
  <PropertyEditor />
</div>
```

From frontend/src/boot/axios.ts:
```typescript
const api: AxiosInstance = axios.create({ baseURL: process.env.API_BASE, timeout: 15000 });
export { api };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template store 扩展 + ShareDialog 组件</name>
  <files>frontend/src/stores/template.ts, frontend/src/components/ShareDialog.vue</files>
  <read_first>
    - frontend/src/stores/template.ts
    - frontend/src/boot/axios.ts
    - frontend/src/pages/TemplatePage.vue
  </read_first>
  <action>
1. 修改 `frontend/src/stores/template.ts`：

   a) 在 Template interface 中新增字段（在 updatedAt 之前）：
   ```typescript
   requireIdentity: boolean;
   ```

   b) 在 update action 的 payload 类型中新增：
   ```typescript
   async update(id: number, payload: { name?: string; description?: string; schema?: FormField[]; requireIdentity?: boolean }) {
   ```

   c) 在 actions 中新增 createShareLink：
   ```typescript
   async createShareLink(templateId: number) {
     const { data } = await api.post(`/templates/${templateId}/share-links`);
     return data;
   },
   ```

2. 创建 `frontend/src/components/ShareDialog.vue`：

```vue
<template>
  <q-dialog :model-value="modelValue" @update:model-value="$emit('update:modelValue', $event)">
    <q-card style="min-width: 400px; max-width: 480px; border-radius: 12px">
      <q-card-section class="text-h6">分享链接</q-card-section>
      <q-card-section>
        <q-input
          :model-value="shareUrl"
          outlined
          readonly
          dense
        >
          <template #append>
            <q-btn flat dense icon="content_copy" @click="copyLink" />
          </template>
        </q-input>
        <div class="flex flex-center q-mt-md">
          <canvas ref="qrCanvas" />
        </div>
        <div class="text-center text-caption q-mt-sm" style="color: var(--oa-text-secondary)">
          扫描二维码或复制链接分享
        </div>
      </q-card-section>
      <q-card-actions align="right">
        <q-btn flat label="关闭" v-close-popup />
      </q-card-actions>
    </q-card>
  </q-dialog>
</template>

<script setup lang="ts">
import { ref, watch, nextTick } from 'vue';
import QRCode from 'qrcode';
import { Notify } from 'quasar';
import { useTemplateStore } from 'src/stores/template';

const props = defineProps<{
  modelValue: boolean;
  templateId: number;
}>();

defineEmits<{
  'update:modelValue': [value: boolean];
}>();

const store = useTemplateStore();
const qrCanvas = ref<HTMLCanvasElement | null>(null);
const shareUrl = ref('');

watch(() => props.modelValue, async (open) => {
  if (!open) return;
  try {
    const link = await store.createShareLink(props.templateId);
    shareUrl.value = `${window.location.origin}/f/${link.code}`;
    await nextTick();
    if (qrCanvas.value) {
      await QRCode.toCanvas(qrCanvas.value, shareUrl.value, {
        width: 200,
        margin: 2,
        color: { dark: '#000000', light: '#ffffff' },
      });
    }
  } catch {
    Notify.create({ type: 'negative', message: '生成分享链接失败' });
  }
});

async function copyLink() {
  await navigator.clipboard.writeText(shareUrl.value);
  Notify.create({ type: 'positive', message: '链接已复制' });
}
</script>
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && grep -n "createShareLink\|requireIdentity" src/stores/template.ts && ls src/components/ShareDialog.vue</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/stores/template.ts 包含 `createShareLink` action
    - frontend/src/stores/template.ts 包含 `requireIdentity: boolean` 在 Template interface 中
    - frontend/src/stores/template.ts update action payload 包含 `requireIdentity?: boolean`
    - frontend/src/components/ShareDialog.vue 文件存在
    - ShareDialog.vue 包含 `QRCode.toCanvas`
    - ShareDialog.vue 包含 `navigator.clipboard.writeText`
    - ShareDialog.vue 包含 `Notify.create({ type: 'positive', message: '链接已复制' })`
    - ShareDialog.vue 包含 `window.location.origin`
    - ShareDialog.vue 使用 `v-close-popup` 关闭按钮
  </acceptance_criteria>
  <done>Template store 有 createShareLink action 和 requireIdentity 字段，ShareDialog 组件可生成链接、渲染二维码、复制链接</done>
</task>

<task type="auto">
  <name>Task 2: TemplatePage 分享按钮 + FormDesignerPage 身份开关</name>
  <files>frontend/src/pages/TemplatePage.vue, frontend/src/pages/FormDesignerPage.vue</files>
  <read_first>
    - frontend/src/pages/TemplatePage.vue
    - frontend/src/pages/FormDesignerPage.vue
    - frontend/src/components/ShareDialog.vue
    - frontend/src/stores/template.ts
  </read_first>
  <action>
1. 修改 `frontend/src/pages/TemplatePage.vue`：

   a) 在 script setup 导入区域新增：
   ```typescript
   import ShareDialog from 'src/components/ShareDialog.vue';
   ```

   b) 新增响应式变量（在 createForm 之后）：
   ```typescript
   const shareDialog = ref(false);
   const shareTemplateId = ref(0);
   ```

   c) 新增 openShare 函数：
   ```typescript
   function openShare(row: Template) {
     shareTemplateId.value = row.id;
     shareDialog.value = true;
   }
   ```

   d) 在 PC 表格的 body-cell-actions slot 中，在 edit_note 按钮之后、publish 按钮之前，新增分享按钮：
   ```vue
   <q-btn
     v-if="props.row.status === 'PUBLISHED'"
     v-perm="'form:template:share'"
     size="sm" flat dense icon="share" color="primary"
     @click="openShare(props.row)"
   />
   ```

   e) 在移动端卡片的 q-card-actions 中，在 publish 按钮之前，新增分享按钮：
   ```vue
   <q-btn
     v-if="t.status === 'PUBLISHED'"
     v-perm="'form:template:share'"
     flat dense icon="share" color="primary"
     @click="openShare(t)"
   />
   ```

   f) 在 template 末尾（createDialog 之后、</q-page> 之前）新增：
   ```vue
   <ShareDialog v-model="shareDialog" :template-id="shareTemplateId" />
   ```

2. 修改 `frontend/src/pages/FormDesignerPage.vue`：

   a) 在 designer-toolbar 中，"保存设计" 按钮之前，新增 requireIdentity 开关：
   ```vue
   <q-toggle
     v-if="store.current"
     v-model="store.current.requireIdentity"
     label="要求填写者提供身份信息"
     dense
     class="q-mr-md"
   />
   ```

   b) 修改 handleSave 函数，在 store.update 调用中新增 requireIdentity：
   将：
   ```typescript
   await store.update(templateId, { schema: store.current.schema });
   ```
   改为：
   ```typescript
   await store.update(templateId, {
     schema: store.current.schema,
     requireIdentity: store.current.requireIdentity,
   });
   ```
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && grep -n "ShareDialog\|shareDialog\|openShare" src/pages/TemplatePage.vue && grep -n "requireIdentity" src/pages/FormDesignerPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - TemplatePage.vue 包含 `import ShareDialog from 'src/components/ShareDialog.vue'`
    - TemplatePage.vue 包含 `shareDialog` ref 和 `shareTemplateId` ref
    - TemplatePage.vue 包含 `openShare` 函数
    - TemplatePage.vue PC 表格中 PUBLISHED 行有 `icon="share"` 按钮
    - TemplatePage.vue 移动端卡片中 PUBLISHED 行有 `icon="share"` 按钮
    - TemplatePage.vue 包含 `<ShareDialog v-model="shareDialog" :template-id="shareTemplateId" />`
    - FormDesignerPage.vue 包含 `v-model="store.current.requireIdentity"` 的 QToggle
    - FormDesignerPage.vue handleSave 包含 `requireIdentity: store.current.requireIdentity`
  </acceptance_criteria>
  <done>TemplatePage 有分享按钮和 ShareDialog 集成，FormDesignerPage 有 requireIdentity 开关且保存时同步到后端</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser clipboard | navigator.clipboard.writeText 需要 HTTPS 或 localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Spoofing | ShareDialog createShareLink | mitigate | 通过 template store 调用 api（带 token），后端 authGuard 校验权限 |
| T-08-10 | Information Disclosure | ShareDialog URL | accept | 分享链接 URL 包含 nanoid code，设计上就是可分享的，非敏感信息 |
</threat_model>

<verification>
- TemplatePage PUBLISHED 模板行显示分享图标按钮
- 点击分享按钮弹出 ShareDialog
- ShareDialog 显示完整 URL 和二维码
- 复制按钮复制 URL 到剪贴板并显示通知
- FormDesignerPage 显示 requireIdentity 开关
- 保存设计时 requireIdentity 值同步到后端
</verification>

<success_criteria>
管理员可在模板列表点击分享按钮生成链接和二维码，可在设计器中配置身份信息要求。
</success_criteria>

<output>
After completion, create `.planning/phases/08-share-public-fill/08-03-SUMMARY.md`
</output>
