---
phase: 10-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/schema.ts
  - backend/src/modules/template/schema.validation.ts
  - backend/src/modules/template/template.route.ts
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02

must_haves:
  truths:
    - "SchemaV2 类型定义包含 version:2 + items 数组（row/group/dynamic-table 三种 type）"
    - "SchemaField 包含 colSpan 字段（1-12），默认 12"
    - "flattenFields helper 可从 SchemaV2 提取所有 SchemaField"
    - "后端 PUT /templates/:id 接受 v2 schema 格式并通过 TypeBox 校验"
    - "非法 schema payload 被后端拒绝并返回错误"
  artifacts:
    - path: "frontend/src/types/schema.ts"
      provides: "v2 schema TypeScript 类型 + helper 函数"
      exports: ["FieldType", "SchemaField", "SchemaRow", "SchemaGroup", "SchemaDynamicTable", "SchemaItem", "SchemaV2", "flattenFields", "createEmptySchema"]
    - path: "backend/src/modules/template/schema.validation.ts"
      provides: "TypeBox v2 schema 校验定义"
      exports: ["SchemaV2Body"]
    - path: "backend/src/modules/template/template.route.ts"
      provides: "PUT route 使用新 SchemaV2Body 校验"
  key_links:
    - from: "backend/src/modules/template/template.route.ts"
      to: "backend/src/modules/template/schema.validation.ts"
      via: "import SchemaV2Body"
      pattern: "import.*SchemaV2Body.*from.*schema\\.validation"
---

<objective>
建立 v2 schema 类型体系和后端校验，为后续渲染器和页面重构提供类型基础。

Purpose: 所有后续 plan 依赖此 plan 产出的类型定义和后端校验
Output: schema.ts 类型文件 + schema.validation.ts TypeBox 校验 + route 更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-schema/10-CONTEXT.md
@.planning/phases/10-schema/10-RESEARCH.md

<interfaces>
<!-- 现有类型，将被替代 -->
From frontend/src/stores/template.ts:
```typescript
export interface FormField {
  id: string;
  type: 'text' | 'textarea' | 'radio' | 'checkbox' | 'date' | 'phone' | 'signature';
  label: string;
  required: boolean;
  placeholder?: string;
  options?: string[];
  sort: number;
}
```

From backend/src/modules/template/template.route.ts (PUT body.schema 当前校验):
```typescript
schema: t.Optional(
  t.Array(
    t.Object({
      id: t.String(),
      type: t.String(),
      label: t.String(),
      required: t.Boolean(),
      placeholder: t.Optional(t.String()),
      options: t.Optional(t.Array(t.String())),
      sort: t.Number(),
    }),
  ),
),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 v2 schema 类型定义和 helper 函数</name>
  <files>frontend/src/types/schema.ts, frontend/src/types/__tests__/schema.test.ts</files>
  <read_first>
    - frontend/src/stores/template.ts (现有 FormField 接口，理解字段结构)
    - frontend/src/components/designer/fieldRegistry.ts (7 种字段类型元数据)
  </read_first>
  <behavior>
    - flattenFields 从含 row+group 的 SchemaV2 中提取所有 SchemaField
    - flattenFields 对空 items 返回空数组
    - flattenFields 跳过 dynamic-table 类型
    - createEmptySchema 返回 { version: 2, items: [] }
    - SchemaField.colSpan 默认值为 12
  </behavior>
  <action>
创建 `frontend/src/types/schema.ts`，定义以下类型和函数（per D-01, D-02, D-03, D-04）：

类型定义：
- `FieldType = 'text' | 'textarea' | 'radio' | 'checkbox' | 'date' | 'phone' | 'signature'`
- `SchemaField { id: string; type: FieldType; label: string; required: boolean; colSpan: number; placeholder?: string; options?: string[] }`
  - 注意：去掉旧 FormField 的 `sort` 字段（位置由 row 内顺序隐含）
  - 新增 `colSpan` 字段（1-12，per D-02）
- `SchemaRow { type: 'row'; fields: SchemaField[] }`
- `SchemaGroup { type: 'group'; title: string; rows: SchemaRow[] }`
- `SchemaDynamicTable { type: 'dynamic-table'; label: string; colSpan: number; columns: Array<{ key: string; label: string; type: FieldType; width?: number }> }`
- `SchemaItem = SchemaRow | SchemaGroup | SchemaDynamicTable`
- `SchemaV2 { version: 2; items: SchemaItem[] }`

Helper 函数：
- `flattenFields(schema: SchemaV2): SchemaField[]` — 遍历 items，row 取 fields，group 遍历 rows 取 fields，跳过 dynamic-table
- `createEmptySchema(): SchemaV2` — 返回 `{ version: 2, items: [] }`

创建测试文件 `frontend/src/types/__tests__/schema.test.ts`，测试 flattenFields 和 createEmptySchema。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/types/__tests__/schema.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/schema.ts 存在且包含 `export type FieldType`
    - frontend/src/types/schema.ts 包含 `export interface SchemaField`，含 `colSpan: number`
    - frontend/src/types/schema.ts 包含 `export interface SchemaRow`，含 `type: 'row'`
    - frontend/src/types/schema.ts 包含 `export interface SchemaGroup`，含 `type: 'group'`
    - frontend/src/types/schema.ts 包含 `export interface SchemaDynamicTable`，含 `type: 'dynamic-table'`
    - frontend/src/types/schema.ts 包含 `export interface SchemaV2`，含 `version: 2`
    - frontend/src/types/schema.ts 包含 `export function flattenFields`
    - frontend/src/types/schema.ts 包含 `export function createEmptySchema`
    - frontend/src/types/schema.ts 不包含 `sort: number`（旧字段已移除）
    - 测试文件通过 vitest run
  </acceptance_criteria>
  <done>SchemaV2 类型体系完整导出，flattenFields 正确提取嵌套字段，测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 TypeBox v2 schema 校验并更新 PUT route</name>
  <files>backend/src/modules/template/schema.validation.ts, backend/src/modules/template/template.route.ts, backend/src/modules/template/__tests__/schema.validation.test.ts</files>
  <read_first>
    - backend/src/modules/template/template.route.ts (现有 PUT route body 校验，第 52-91 行)
    - frontend/src/types/schema.ts (Task 1 产出的类型定义，确保前后端一致)
  </read_first>
  <behavior>
    - 合法 v2 schema（version:2 + row items）通过校验
    - colSpan 超出 1-12 范围被拒绝
    - 缺少 required 字段的 SchemaField 被拒绝
    - 非法 type 值被拒绝
    - 空 items 数组通过校验（空模板合法）
    - group 类型含 title + rows 通过校验
    - dynamic-table 类型含 columns 通过校验
  </behavior>
  <action>
创建 `backend/src/modules/template/schema.validation.ts`（per D-08, D-09）：

```typescript
import { t } from 'elysia';

const FieldType = t.Union([
  t.Literal('text'), t.Literal('textarea'), t.Literal('radio'),
  t.Literal('checkbox'), t.Literal('date'), t.Literal('phone'), t.Literal('signature'),
]);

const SchemaField = t.Object({
  id: t.String(),
  type: FieldType,
  label: t.String(),
  required: t.Boolean(),
  colSpan: t.Integer({ minimum: 1, maximum: 12 }),
  placeholder: t.Optional(t.String()),
  options: t.Optional(t.Array(t.String())),
});

const SchemaRow = t.Object({
  type: t.Literal('row'),
  fields: t.Array(SchemaField, { minItems: 1 }),
});

const SchemaGroup = t.Object({
  type: t.Literal('group'),
  title: t.String(),
  rows: t.Array(SchemaRow),
});

const SchemaDynamicTable = t.Object({
  type: t.Literal('dynamic-table'),
  label: t.String(),
  colSpan: t.Integer({ minimum: 1, maximum: 12 }),
  columns: t.Array(t.Object({
    key: t.String(),
    label: t.String(),
    type: FieldType,
    width: t.Optional(t.Integer({ minimum: 1 })),
  })),
});

const SchemaItem = t.Union([SchemaRow, SchemaGroup, SchemaDynamicTable]);

export const SchemaV2Body = t.Object({
  version: t.Literal(2),
  items: t.Array(SchemaItem),
});
```

修改 `backend/src/modules/template/template.route.ts` PUT route（第 75-88 行）：
- 将 `body.schema` 的校验从旧的 `t.Array(t.Object({...}))` 替换为 `t.Optional(SchemaV2Body)`
- 在文件顶部添加 `import { SchemaV2Body } from './schema.validation';`
- 删除旧的 inline schema 校验定义（id/type/label/required/placeholder/options/sort）

创建测试 `backend/src/modules/template/__tests__/schema.validation.test.ts`，使用 TypeBox Value.Check 验证合法/非法 payload。
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/template/__tests__/schema.validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/modules/template/schema.validation.ts 存在且包含 `export const SchemaV2Body`
    - backend/src/modules/template/schema.validation.ts 包含 `t.Literal(2)` (version 校验)
    - backend/src/modules/template/schema.validation.ts 包含 `minimum: 1, maximum: 12` (colSpan 范围)
    - backend/src/modules/template/template.route.ts 包含 `import { SchemaV2Body } from './schema.validation'`（新导入，per SCHEMA-02）
    - backend/src/modules/template/template.route.ts 不包含 `sort: t.Number()` (旧校验已移除)
    - backend/src/modules/template/template.route.ts 的 PUT body.schema 使用 SchemaV2Body
    - 测试文件通过 bun test
  </acceptance_criteria>
  <done>TypeBox 校验定义完整覆盖 v2 schema 结构，PUT route 使用新校验，旧 inline 校验已删除，测试通过</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/types/__tests__/schema.test.ts --reporter=verbose` 通过
- `cd backend && bun test src/modules/template/__tests__/schema.validation.test.ts` 通过
- `grep -n "export.*SchemaV2" frontend/src/types/schema.ts` 有输出
- `grep -n "SchemaV2Body" backend/src/modules/template/schema.validation.ts` 有输出
- `grep -n "import.*SchemaV2Body" backend/src/modules/template/template.route.ts` 有输出
</verification>

<success_criteria>
- v2 schema 类型体系完整（7 个导出类型 + 2 个 helper 函数）
- TypeBox 校验覆盖所有 SchemaItem 变体
- PUT route 使用新校验，旧 inline 校验已移除
- 前后端类型定义一致（字段名、类型、约束）
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/10-schema/10-01-SUMMARY.md`
</output>
