---
phase: 05-responsive
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - frontend/src/pages/LoginPage.vue
  - frontend/src/pages/DashboardPage.vue
  - frontend/src/pages/ForbiddenPage.vue
  - frontend/src/pages/NotFoundPage.vue
autonomous: true
requirements: [FR-6.1, FR-6.2, FR-6.3, NFR-1]
must_haves:
  truths:
    - "LoginPage 有渐变背景（light: #4F46E5→#6366F1 / dark: #312E81→#3730A3）+ 装饰圆 + 居中卡片"
    - "DashboardPage 显示时段欢迎词 + 3 张统计卡片（调 /dashboard/stats 接口）+ 快捷操作"
    - "DashboardPage 统计卡片有骨架屏加载态和错误态（显示 --）"
    - "ForbiddenPage 显示 72px 错误码 403 + 描述 + 返回首页按钮"
    - "NotFoundPage 显示 72px 错误码 404 + 描述 + 返回首页按钮"
    - "所有页面无硬编码颜色残留（grep 扫描为空）"
  artifacts:
    - path: "frontend/src/pages/LoginPage.vue"
      provides: "美化后的登录页"
      contains: "login-gradient"
    - path: "frontend/src/pages/DashboardPage.vue"
      provides: "重构后的 Dashboard 页"
      contains: "dashboard/stats"
    - path: "frontend/src/pages/ForbiddenPage.vue"
      provides: "美化后的 403 页"
      contains: "72px"
    - path: "frontend/src/pages/NotFoundPage.vue"
      provides: "美化后的 404 页"
      contains: "72px"
  key_links:
    - from: "frontend/src/pages/DashboardPage.vue"
      to: "/api/v1/dashboard/stats"
      via: "api.get('/dashboard/stats')"
      pattern: "dashboard/stats"
---

<objective>
美化 LoginPage（渐变背景+装饰圆+居中卡片）、重构 DashboardPage（欢迎词+统计卡片+快捷操作）、打磨 403/404 错误页（72px 错误码+描述+CTA）、最终硬编码颜色扫描。

Purpose: 完成 Phase 5 的 UI/UX 美化交付（D-15, D-16, D-17），确保所有页面视觉一致且暗色模式兼容。
Output: 4 个页面的美化/重构。
</objective>

<execution_context>
@.planning/phases/05-responsive/05-RESEARCH.md
@.planning/phases/05-responsive/05-UI-SPEC.md
@.planning/phases/05-responsive/05-PATTERNS.md
@.planning/phases/05-responsive/05-03-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
From frontend/src/boot/axios.ts:
```typescript
export { api }; // AxiosInstance, baseURL: '/api/v1'
```

From backend/src/modules/dashboard/dashboard.route.ts (Plan 03 创建):
```typescript
// GET /api/v1/dashboard/stats
// Response: { userCount: number, departmentCount: number, roleCount: number }
```

From frontend/src/stores/auth.ts:
```typescript
// useAuthStore().user: { realName: string, roles: string[], permissions: string[] }
// useAuthStore().hasPerm(code: string): boolean
```

From frontend/src/composables/useDarkMode.ts:
```typescript
export function useDarkMode(): { isDark: ComputedRef<boolean>; toggleDark: () => void }
```

From frontend/src/composables/useResponsive.ts:
```typescript
export function useResponsive(): { isDesktop: ComputedRef<boolean>; isMobile: ComputedRef<boolean> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 美化 LoginPage + 403/404 错误页</name>
  <files>
    frontend/src/pages/LoginPage.vue,
    frontend/src/pages/ForbiddenPage.vue,
    frontend/src/pages/NotFoundPage.vue
  </files>
  <read_first>
    frontend/src/pages/LoginPage.vue,
    frontend/src/pages/ForbiddenPage.vue,
    frontend/src/pages/NotFoundPage.vue,
    .planning/phases/05-responsive/05-UI-SPEC.md
  </read_first>
  <action>
**LoginPage.vue 美化（D-15, D-09）：**

重写 template，保留 script 中的 onLogin 逻辑不变。

Template 结构：
```vue
<template>
  <q-layout view="hHh lpR fFf">
    <q-page-container>
      <q-page class="login-page flex flex-center">
        <!-- 装饰圆形 -->
        <div class="login-decor login-decor--1"></div>
        <div class="login-decor login-decor--2"></div>

        <q-card class="login-card q-pa-lg" style="width: 400px; max-width: 90vw; border-radius: 12px; z-index: 1; background: var(--oa-surface)">
          <!-- 暗色切换按钮 -->
          <q-btn flat round dense size="sm" :icon="isDark ? 'light_mode' : 'dark_mode'"
                 @click="toggleDark" style="position: absolute; top: 12px; right: 12px" />

          <q-card-section class="text-center">
            <div style="font-size: 20px; font-weight: 600; color: var(--oa-text-primary)" class="text-primary">
              OA 管理系统
            </div>
            <div class="q-mt-xs" style="font-size: 14px; color: var(--oa-text-secondary)">
              请使用您的账号登录
            </div>
          </q-card-section>
          <q-form @submit="onLogin" class="q-gutter-md">
            <q-input v-model="form.username" label="用户名" outlined :rules="[(v) => !!v || '必填']" />
            <q-input v-model="form.password" label="密码" outlined
                     :type="showPwd ? 'text' : 'password'" :rules="[(v) => !!v || '必填']">
              <template #append>
                <q-icon :name="showPwd ? 'visibility_off' : 'visibility'" class="cursor-pointer" @click="showPwd = !showPwd" />
              </template>
            </q-input>
            <q-btn type="submit" color="primary" class="full-width" :loading="loading" label="立即登录" />
            <div class="text-center" style="font-size: 12px; color: var(--oa-text-secondary)">
              默认账号: admin / admin123
            </div>
          </q-form>
        </q-card>
      </q-page>
    </q-page-container>
  </q-layout>
</template>
```

Script 新增 imports（保留现有 onLogin 逻辑）：
```typescript
import { useDarkMode } from 'src/composables/useDarkMode';
const { isDark, toggleDark } = useDarkMode();
```

Style scoped 新增：
```scss
<style scoped>
.login-page {
  background: linear-gradient(135deg, var(--oa-login-gradient-start) 0%, var(--oa-login-gradient-end) 100%);
  position: relative;
  overflow: hidden;
  min-height: 100vh;
}
.login-decor {
  position: absolute;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
}
.body--dark .login-decor {
  background: rgba(255, 255, 255, 0.05);
}
.login-decor--1 {
  width: 200px;
  height: 200px;
  top: -60px;
  left: -60px;
}
.login-decor--2 {
  width: 150px;
  height: 150px;
  bottom: -40px;
  right: -40px;
}
</style>
```

关键变更：
- `text-grey` (L8, L24) → `var(--oa-text-secondary)` (D-09 #9, #10)
- 登录按钮文案从"登录"改为"立即登录"（UI-SPEC 文案契约）
- 卡片宽度 400px，圆角 12px，shadow-4（通过 Quasar class `shadow-4` 或默认 q-card 阴影）
- 渐变背景使用 CSS 变量（light/dark 自动切换）

**ForbiddenPage.vue 美化（D-17, D-09）：**

完全重写 template：
```vue
<template>
  <q-layout view="hHh lpR fFf">
    <q-page-container>
      <q-page class="flex flex-center" style="min-height: 100vh; background: var(--oa-bg)">
        <div class="text-center">
          <div style="font-size: 72px; font-weight: 600; line-height: 1; color: var(--oa-text-tertiary)">
            403
          </div>
          <div class="q-mt-md" style="font-size: 20px; font-weight: 600; color: var(--oa-text-primary)">
            无权限访问
          </div>
          <div class="q-mt-sm" style="font-size: 14px; color: var(--oa-text-secondary)">
            您没有权限访问此页面，请联系管理员获取相应权限
          </div>
          <q-btn color="primary" label="返回首页" to="/" class="q-mt-lg" style="border-radius: 8px" />
        </div>
      </q-page>
    </q-page-container>
  </q-layout>
</template>
```
- 错误码 72px/600（UI-SPEC Display Override）
- `text-grey-6` (L6) → `var(--oa-text-tertiary)` (D-09 #7)
- 背景使用 `var(--oa-bg)`
- 按钮圆角 8px

**NotFoundPage.vue 美化（D-17, D-09）：**

完全重写 template（结构同 ForbiddenPage，仅文案不同）：
```vue
<template>
  <q-layout view="hHh lpR fFf">
    <q-page-container>
      <q-page class="flex flex-center" style="min-height: 100vh; background: var(--oa-bg)">
        <div class="text-center">
          <div style="font-size: 72px; font-weight: 600; line-height: 1; color: var(--oa-text-tertiary)">
            404
          </div>
          <div class="q-mt-md" style="font-size: 20px; font-weight: 600; color: var(--oa-text-primary)">
            页面未找到
          </div>
          <div class="q-mt-sm" style="font-size: 14px; color: var(--oa-text-secondary)">
            您访问的页面不存在或已被移除
          </div>
          <q-btn color="primary" label="返回首页" to="/" class="q-mt-lg" style="border-radius: 8px" />
        </div>
      </q-page>
    </q-page-container>
  </q-layout>
</template>
```
- `text-grey-6` (L6) → `var(--oa-text-tertiary)` (D-09 #8)
  </action>
  <verify>
    <automated>cd frontend && grep "login-gradient" src/pages/LoginPage.vue && grep "72px" src/pages/ForbiddenPage.vue && grep "72px" src/pages/NotFoundPage.vue && grep -c "text-grey" src/pages/LoginPage.vue src/pages/ForbiddenPage.vue src/pages/NotFoundPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - LoginPage.vue 包含 `login-gradient-start` 或 `var(--oa-login-gradient-start)` 渐变背景
    - LoginPage.vue 包含 `login-decor` 装饰圆形
    - LoginPage.vue 包含 `import { useDarkMode }` 和暗色切换按钮
    - LoginPage.vue 不包含 `text-grey`（D-09 #9, #10 已替换）
    - LoginPage.vue 包含 `立即登录` 按钮文案
    - LoginPage.vue 卡片宽度为 `400px`，圆角 `12px`
    - ForbiddenPage.vue 包含 `font-size: 72px` 错误码
    - ForbiddenPage.vue 包含 `var(--oa-text-tertiary)` 替代 `text-grey-6`
    - ForbiddenPage.vue 包含 `var(--oa-bg)` 背景
    - ForbiddenPage.vue 包含 `返回首页` 按钮
    - NotFoundPage.vue 包含 `font-size: 72px` 错误码
    - NotFoundPage.vue 包含 `var(--oa-text-tertiary)` 替代 `text-grey-6`
    - NotFoundPage.vue 包含 `页面未找到` 标题
    - NotFoundPage.vue 包含 `返回首页` 按钮
  </acceptance_criteria>
  <done>
    LoginPage 有渐变背景+装饰圆+居中卡片+暗色切换；ForbiddenPage 和 NotFoundPage 有 72px 错误码+描述+返回首页按钮；所有硬编码色已替换。
  </done>
</task>

<task type="auto">
  <name>Task 2: 重构 DashboardPage — 欢迎词 + 统计卡片 + 快捷操作</name>
  <files>frontend/src/pages/DashboardPage.vue</files>
  <read_first>
    frontend/src/pages/DashboardPage.vue,
    .planning/phases/05-responsive/05-UI-SPEC.md,
    .planning/phases/05-responsive/05-PATTERNS.md,
    frontend/src/boot/axios.ts
  </read_first>
  <action>
完全重写 DashboardPage.vue。

Template：
```vue
<template>
  <q-page padding>
    <!-- 欢迎词 -->
    <div style="font-size: 20px; font-weight: 600; color: var(--oa-text-primary)">
      {{ greeting }}，{{ auth.user?.realName }}
    </div>
    <div class="q-mt-xs q-mb-lg" style="font-size: 14px; color: var(--oa-text-secondary)">
      欢迎回到 OA 管理系统
    </div>

    <!-- 统计卡片 -->
    <div class="row q-gutter-md q-mb-lg">
      <q-card v-for="item in statCards" :key="item.label"
              class="col-12 col-sm stat-card" flat bordered
              style="border-radius: 8px; min-height: 120px">
        <q-card-section>
          <div class="row items-center q-gutter-md">
            <div style="width: 48px; height: 48px; border-radius: 50%; display: flex; align-items: center; justify-content: center; background: var(--oa-stat-icon-bg)">
              <q-icon :name="item.icon" size="24px" color="primary" />
            </div>
            <div>
              <div style="font-size: 14px; color: var(--oa-text-secondary)">{{ item.label }}</div>
              <div v-if="statsLoading">
                <q-skeleton type="text" width="60px" height="32px" />
              </div>
              <div v-else style="font-size: 32px; font-weight: 600; line-height: 1; color: var(--oa-text-primary)">
                {{ statsError ? '--' : item.value }}
              </div>
            </div>
          </div>
        </q-card-section>
      </q-card>
    </div>

    <!-- 快捷操作 -->
    <div style="font-size: 20px; font-weight: 600; color: var(--oa-text-primary)" class="q-mb-md">
      快捷操作
    </div>
    <div class="row q-gutter-sm">
      <q-btn v-for="action in quickActions" :key="action.to"
             outline color="primary" :icon="action.icon" :label="action.label"
             :to="action.to" style="border-radius: 8px" />
    </div>
  </q-page>
</template>
```

Script：
```typescript
<script setup lang="ts">
import { ref, computed, onMounted } from 'vue';
import { api } from 'src/boot/axios';
import { useAuthStore } from 'src/stores/auth';
import { Notify } from 'quasar';

const auth = useAuthStore();

// 时段欢迎词
const greeting = computed(() => {
  const h = new Date().getHours();
  if (h >= 6 && h < 12) return '早上好';
  if (h >= 12 && h < 18) return '下午好';
  return '晚上好';
});

// 统计数据
const stats = ref({ userCount: 0, departmentCount: 0, roleCount: 0 });
const statsLoading = ref(true);
const statsError = ref(false);

const statCards = computed(() => [
  { icon: 'people', label: '用户总数', value: stats.value.userCount },
  { icon: 'account_tree', label: '部门总数', value: stats.value.departmentCount },
  { icon: 'security', label: '角色总数', value: stats.value.roleCount },
]);

// 快捷操作
const quickActions = computed(() => {
  const actions = [];
  if (auth.hasPerm('user:create')) actions.push({ icon: 'person_add', label: '新建用户', to: '/users' });
  if (auth.hasPerm('department:create')) actions.push({ icon: 'create_new_folder', label: '新建部门', to: '/departments' });
  if (auth.hasPerm('role:list')) actions.push({ icon: 'admin_panel_settings', label: '角色管理', to: '/roles' });
  return actions;
});

onMounted(async () => {
  try {
    const { data } = await api.get('/dashboard/stats');
    stats.value = data;
  } catch {
    statsError.value = true;
    Notify.create({ type: 'warning', message: '统计数据加载失败' });
  } finally {
    statsLoading.value = false;
  }
});
</script>
```

关键变更：
- 硬编码色替换（D-09 #4, #5）：`text-grey` → `var(--oa-text-secondary)`
- 欢迎词时段判断：06-12 早上好 / 12-18 下午好 / else 晚上好
- 统计卡片调 GET /dashboard/stats 接口（Plan 03 创建）
- 骨架屏：statsLoading 时显示 q-skeleton
- 错误态：statsError 时显示 "--" + Notify warning
- 统计数字 32px/600（UI-SPEC Display Override）
- 统计图标 48px 圆形背景 `var(--oa-stat-icon-bg)`
- 快捷操作按权限过滤
- 移动端：统计卡片 `col-12` 纵向堆叠（Quasar grid 自动响应）
  </action>
  <verify>
    <automated>cd frontend && grep "dashboard/stats" src/pages/DashboardPage.vue && grep "greeting" src/pages/DashboardPage.vue && grep "statsLoading" src/pages/DashboardPage.vue && grep -c "text-grey" src/pages/DashboardPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage.vue 包含 `api.get('/dashboard/stats')`
    - DashboardPage.vue 包含 `greeting` computed（时段欢迎词）
    - DashboardPage.vue 包含 `statsLoading` ref 和 `statsError` ref
    - DashboardPage.vue 包含 `q-skeleton` 骨架屏
    - DashboardPage.vue 包含 `font-size: 32px; font-weight: 600`（统计数字）
    - DashboardPage.vue 包含 `var(--oa-stat-icon-bg)`（统计图标背景）
    - DashboardPage.vue 包含 `stat-card` class（hover 微交互）
    - DashboardPage.vue 包含 `quickActions` computed（按权限过滤）
    - DashboardPage.vue 不包含 `text-grey`（D-09 #4, #5 已替换）
    - DashboardPage.vue 包含 `Notify.create({ type: 'warning', message: '统计数据加载失败' })`
  </acceptance_criteria>
  <done>
    DashboardPage 显示时段欢迎词 + 3 张统计卡片（骨架屏/数据/错误三态）+ 按权限过滤的快捷操作；所有硬编码色已替换；统计数据来自 /dashboard/stats 接口。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API /dashboard/stats | 需要有效 access token |
| DashboardPage 欢迎词 realName 渲染 | Vue 模板默认转义，无 XSS 风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Spoofing | DashboardPage 欢迎词 realName | accept | Vue 模板 `{{ auth.user?.realName }}` 默认 HTML 转义，不使用 v-html，无 XSS 风险 |
| T-05-07 | Information Disclosure | 统计数字展示 | accept | 仅显示聚合 count 数字，不含 PII。后端 authGuard 已保护接口。 |
</threat_model>

<verification>
```bash
# 最终硬编码颜色全扫描（Phase 5 完成后应为空）
grep -rn "bg-white\|bg-grey\|text-grey[^-]\|color=\"grey" frontend/src/pages/ frontend/src/layouts/ --include="*.vue"
# 期望: 无输出（或仅 Quasar 语义色）

# 注意：q-chip 中的 grey-4/grey-8 是状态指示色（禁用用户），属于 Quasar 语义用法，保留不改

# 验证 Dashboard 接口调用
grep "dashboard/stats" frontend/src/pages/DashboardPage.vue

# 验证错误页美化
grep "72px" frontend/src/pages/ForbiddenPage.vue frontend/src/pages/NotFoundPage.vue

# 验证登录页渐变
grep "login-gradient" frontend/src/pages/LoginPage.vue
```
</verification>

<success_criteria>
- LoginPage 有渐变背景 + 装饰圆 + 居中卡片 + 暗色切换
- DashboardPage 有时段欢迎词 + 统计卡片（调接口）+ 快捷操作
- ForbiddenPage 有 72px 错误码 + 描述 + 返回首页
- NotFoundPage 有 72px 错误码 + 描述 + 返回首页
- 全仓 grep 扫描无硬编码颜色残留（bg-white/bg-grey/text-grey 等）
</success_criteria>

<output>
After completion, create `.planning/phases/05-responsive/05-05-SUMMARY.md`
</output>
