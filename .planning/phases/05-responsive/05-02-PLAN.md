---
phase: 05-responsive
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - frontend/src/layouts/MainLayout.vue
autonomous: true
requirements: [FR-6.1, FR-6.2]
must_haves:
  truths:
    - "PC 端（>=1024px）显示固定侧边 Drawer + 顶栏（含暗色切换+用户下拉）"
    - "移动端（<1024px）显示底部 Tab 栏 + 顶栏精简（汉堡+标题+暗色+头像）"
    - "移动端汉堡菜单点击弹出 overlay Drawer（导航+用户信息+暗色切换+退出）"
    - "底部 Tab 栏选中项 primary 色高亮，未选中灰色"
    - "router-view 包裹 fade 过渡动画"
    - "MainLayout 不再包含任何 bg-grey-2 / bg-white / text-grey-9 硬编码色"
  artifacts:
    - path: "frontend/src/layouts/MainLayout.vue"
      provides: "PC/Mobile 双布局 + overlay Drawer + fade 过渡"
      contains: "useResponsive"
  key_links:
    - from: "frontend/src/layouts/MainLayout.vue"
      to: "frontend/src/composables/useResponsive.ts"
      via: "import { useResponsive }"
      pattern: "useResponsive"
    - from: "frontend/src/layouts/MainLayout.vue"
      to: "frontend/src/composables/useDarkMode.ts"
      via: "import { useDarkMode }"
      pattern: "useDarkMode"
---

<objective>
改造 MainLayout.vue 实现 PC/Mobile 双布局：PC 端固定 Drawer + 顶栏，移动端底部 Tab + overlay Drawer + 精简顶栏 + fade 页面过渡。

Purpose: 这是 FR-6.1 和 FR-6.2 的核心交付物 — 布局层的响应式切换。所有子页面都在 MainLayout 内渲染，布局改造是页面适配的前提。
Output: 改造后的 MainLayout.vue，支持双布局 + overlay Drawer + fade 过渡。
</objective>

<execution_context>
@.planning/phases/05-responsive/05-RESEARCH.md
@.planning/phases/05-responsive/05-UI-SPEC.md
@.planning/phases/05-responsive/05-PATTERNS.md
@.planning/phases/05-responsive/05-01-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
From frontend/src/composables/useResponsive.ts (Plan 01 创建):
```typescript
export function useResponsive(): {
  isDesktop: ComputedRef<boolean>;
  isMobile: ComputedRef<boolean>;
}
```

From frontend/src/composables/useDarkMode.ts (Plan 01 创建):
```typescript
export function useDarkMode(): {
  isDark: ComputedRef<boolean>;
  toggleDark: () => void;
}
```

From frontend/src/stores/auth.ts:
```typescript
// useAuthStore() 返回:
// - user: { realName, roles, permissions }
// - hasPerm(code: string): boolean
// - logout(): void
```

From frontend/src/layouts/MainLayout.vue (当前结构):
```typescript
const allMenus = [
  { path: '/dashboard', title: '首页', icon: 'dashboard', perm: '' },
  { path: '/departments', title: '部门', icon: 'account_tree', perm: 'department:list' },
  { path: '/users', title: '用户', icon: 'people', perm: 'user:list' },
  { path: '/roles', title: '角色', icon: 'security', perm: 'role:list' },
];
const visibleMenus = computed(() => allMenus.filter((m) => !m.perm || auth.hasPerm(m.perm)));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 MainLayout.vue — PC/Mobile 双布局 + overlay Drawer + fade 过渡</name>
  <files>frontend/src/layouts/MainLayout.vue</files>
  <read_first>
    frontend/src/layouts/MainLayout.vue,
    frontend/src/composables/useResponsive.ts,
    frontend/src/composables/useDarkMode.ts,
    .planning/phases/05-responsive/05-UI-SPEC.md
  </read_first>
  <action>
完全重写 MainLayout.vue template + script，保留 allMenus/visibleMenus/onLogout 逻辑。具体变更：

**Template 结构（从上到下）：**

1. `<q-layout view="hHh Lpr lff">` — 移除 `:class="$q.dark.isActive ? '' : 'bg-grey-2'"` 三元（D-09 #1，q-page 已有 var(--oa-bg)）

2. **Header（PC+Mobile 共用，但内容按断点切换）：**
   ```vue
   <q-header elevated class="bg-primary text-white">
     <q-toolbar>
       <!-- 移动端：汉堡菜单开关 overlay Drawer -->
       <q-btn v-if="isMobile" flat dense round icon="menu" @click="mobileDrawerOpen = !mobileDrawerOpen" />
       <!-- PC 端：汉堡菜单开关固定 Drawer -->
       <q-btn v-else flat dense round icon="menu" @click="drawerOpen = !drawerOpen" />
       <q-toolbar-title>OA 管理系统</q-toolbar-title>
       <q-space />
       <q-btn flat round dense :icon="isDark ? 'light_mode' : 'dark_mode'" @click="toggleDark" />
       <!-- PC 端：用户名下拉 -->
       <q-btn-dropdown v-if="isDesktop" flat :label="auth.user?.realName ?? ''" icon="account_circle">
         <q-list>
           <q-item clickable v-close-popup @click="onLogout">
             <q-item-section avatar><q-icon name="logout" /></q-item-section>
             <q-item-section>退出登录</q-item-section>
           </q-item>
         </q-list>
       </q-btn-dropdown>
       <!-- 移动端：仅头像圆形 -->
       <q-avatar v-else size="32px" color="primary" text-color="white" class="q-ml-sm">
         {{ auth.user?.realName?.charAt(0) ?? '' }}
       </q-avatar>
     </q-toolbar>
   </q-header>
   ```

3. **PC 端固定 Drawer（D-02）：**
   ```vue
   <q-drawer v-if="isDesktop" v-model="drawerOpen" show-if-above bordered :width="220">
     <q-list>
       <q-item-label header>导航</q-item-label>
       <q-item v-for="m in visibleMenus" :key="m.path" clickable v-ripple :to="m.path" active-class="text-primary">
         <q-item-section avatar><q-icon :name="m.icon" /></q-item-section>
         <q-item-section>{{ m.title }}</q-item-section>
       </q-item>
     </q-list>
   </q-drawer>
   ```

4. **移动端 overlay Drawer（D-02, D-12）：**
   ```vue
   <q-drawer v-if="isMobile" v-model="mobileDrawerOpen" overlay bordered :width="280">
     <q-list>
       <q-item-label header>导航</q-item-label>
       <q-item v-for="m in visibleMenus" :key="m.path" clickable v-ripple :to="m.path"
               active-class="text-primary" @click="mobileDrawerOpen = false">
         <q-item-section avatar><q-icon :name="m.icon" /></q-item-section>
         <q-item-section>{{ m.title }}</q-item-section>
       </q-item>
     </q-list>
     <q-space />
     <div style="border-top: 1px solid var(--oa-border)" class="q-pa-md">
       <div class="row items-center q-gutter-sm">
         <q-avatar size="36px" color="primary" text-color="white">
           {{ auth.user?.realName?.charAt(0) ?? '' }}
         </q-avatar>
         <div>
           <div style="font-size: 14px; font-weight: 600">{{ auth.user?.realName }}</div>
           <div style="font-size: 12px; color: var(--oa-text-secondary)">{{ auth.user?.roles?.join(', ') }}</div>
         </div>
       </div>
       <div class="row q-mt-md q-gutter-sm">
         <q-btn flat dense :icon="isDark ? 'light_mode' : 'dark_mode'" @click="toggleDark" />
         <q-space />
         <q-btn flat dense icon="logout" label="退出" @click="onLogout" />
       </div>
     </div>
   </q-drawer>
   ```

5. **Page container + fade 过渡（D-14）：**
   ```vue
   <q-page-container>
     <router-view v-slot="{ Component }">
       <transition name="fade" mode="out-in">
         <component :is="Component" />
       </transition>
     </router-view>
   </q-page-container>
   ```

6. **移动端底部 Tab（D-11）：** 替换 `bg-white text-grey-9` 为 CSS 变量（D-09 #3）
   ```vue
   <q-footer v-if="isMobile" bordered style="background: var(--oa-surface); color: var(--oa-text-primary)">
     <q-tabs v-model="activeTab" dense active-color="primary" indicator-color="primary"
             align="justify" @update:model-value="onTab">
       <q-tab v-for="m in visibleMenus" :key="m.path" :name="m.path" :icon="m.icon" :label="m.title" />
     </q-tabs>
   </q-footer>
   ```

**Script 变更：**
```typescript
import { ref, computed, watch } from 'vue';
import { useRouter, useRoute } from 'vue-router';
import { useAuthStore } from 'src/stores/auth';
import { useResponsive } from 'src/composables/useResponsive';
import { useDarkMode } from 'src/composables/useDarkMode';

const { isDesktop, isMobile } = useResponsive();
const { isDark, toggleDark } = useDarkMode();
const drawerOpen = ref(true);
const mobileDrawerOpen = ref(false);
const router = useRouter();
const route = useRoute();
const auth = useAuthStore();

// allMenus / visibleMenus / activeTab / onTab / onLogout 保持不变
```
  </action>
  <verify>
    <automated>cd frontend && grep -c "useResponsive\|useDarkMode\|mobileDrawerOpen\|overlay\|fade" src/layouts/MainLayout.vue</automated>
  </verify>
  <acceptance_criteria>
    - MainLayout.vue 包含 `import { useResponsive } from 'src/composables/useResponsive'`
    - MainLayout.vue 包含 `import { useDarkMode } from 'src/composables/useDarkMode'`
    - MainLayout.vue 包含 `const { isDesktop, isMobile } = useResponsive()`
    - MainLayout.vue 包含 `const { isDark, toggleDark } = useDarkMode()`
    - MainLayout.vue 包含 `v-if="isDesktop"` 用于 PC Drawer
    - MainLayout.vue 包含 `v-if="isMobile"` 用于 overlay Drawer 和底部 Tab
    - MainLayout.vue 包含 `overlay` 属性在移动端 Drawer 上
    - MainLayout.vue 包含 `mobileDrawerOpen` ref
    - MainLayout.vue 包含 `<transition name="fade" mode="out-in">`
    - MainLayout.vue 不包含 `bg-grey-2`（D-09 #1 已移除）
    - MainLayout.vue 不包含 `bg-white text-grey-9`（D-09 #3 已替换）
    - MainLayout.vue 不包含 `$q.screen.gt.sm` 或 `$q.screen.lt.md`（已统一为 composable）
    - MainLayout.vue 不包含 `$q.dark.toggle()`（已替换为 toggleDark）
    - 底部 Tab footer 使用 `style="background: var(--oa-surface); color: var(--oa-text-primary)"`
    - overlay Drawer 包含用户信息区（realName + roles）+ 退出按钮 + 暗色切换
  </acceptance_criteria>
  <done>
    MainLayout.vue 实现 PC/Mobile 双布局：PC 固定 Drawer + 完整顶栏，移动端 overlay Drawer + 底部 Tab + 精简顶栏；router-view 包裹 fade 过渡；所有硬编码色已替换为 CSS 变量；断点判断统一使用 useResponsive composable。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side auth state | visibleMenus 依赖 auth.hasPerm 过滤，前端权限仅控制 UI 显隐 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | overlay Drawer 显示 realName + roles | accept | 低威胁 — 仅对已登录用户自身显示，与 PC 端顶栏行为一致 |
</threat_model>

<verification>
```bash
# 验证断点统一
grep -c "\\$q.screen.gt.sm\|\\$q.screen.lt.md" frontend/src/layouts/MainLayout.vue
# 期望输出: 0（全部替换为 composable）

# 验证硬编码色清除
grep -c "bg-grey-2\|bg-white\|text-grey-9" frontend/src/layouts/MainLayout.vue
# 期望输出: 0

# 验证 composable 引用
grep "useResponsive\|useDarkMode" frontend/src/layouts/MainLayout.vue

# 验证 fade 过渡
grep "transition.*fade" frontend/src/layouts/MainLayout.vue
```
</verification>

<success_criteria>
- PC 端（>=1024px）显示固定 Drawer + 完整顶栏（暗色切换+用户下拉）
- 移动端（<1024px）显示底部 Tab + 精简顶栏 + overlay Drawer
- overlay Drawer 包含导航菜单 + 用户信息 + 暗色切换 + 退出按钮
- 底部 Tab 选中 primary 高亮，未选中灰色
- router-view 包裹 fade 过渡
- 无硬编码颜色残留
</success_criteria>

<output>
After completion, create `.planning/phases/05-responsive/05-02-SUMMARY.md`
</output>
