---
phase: 05-responsive
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - frontend/src/components/EmptyState.vue
  - frontend/src/components/FilterSheet.vue
  - frontend/src/pages/UserPage.vue
  - frontend/src/pages/DepartmentPage.vue
  - frontend/src/pages/RolePage.vue
autonomous: true
requirements: [FR-6.2, FR-6.3]
must_haves:
  truths:
    - "UserPage 移动端显示卡片列表而非表格（FR-6.3）"
    - "UserPage 移动端筛选栏折叠为筛选按钮，点击弹出底部 Sheet（D-06）"
    - "UserPage 移动端新建按钮为右下角 FAB（D-06）"
    - "DepartmentPage 移动端弹窗为全屏弹窗（D-04）"
    - "RolePage 移动端为单栏切换：角色列表 ↔ 权限分配视图（D-05）"
    - "所有页面的编辑/新建弹窗在移动端使用全屏弹窗（D-07）"
    - "EmptyState 公共组件替换各页面内联空态（D-17）"
    - "各页面使用骨架屏替代 spinner 加载态（D-14）"
    - "各页面硬编码颜色已替换为 CSS 变量（D-09）"
  artifacts:
    - path: "frontend/src/components/EmptyState.vue"
      provides: "统一空态组件"
      exports: ["default"]
    - path: "frontend/src/components/FilterSheet.vue"
      provides: "移动端底部 Sheet 筛选组件"
      exports: ["default"]
    - path: "frontend/src/pages/UserPage.vue"
      provides: "移动端适配后的用户管理页"
      contains: "useResponsive"
    - path: "frontend/src/pages/DepartmentPage.vue"
      provides: "移动端适配后的部门管理页"
      contains: "useResponsive"
    - path: "frontend/src/pages/RolePage.vue"
      provides: "移动端适配后的角色管理页"
      contains: "mobileView"
  key_links:
    - from: "frontend/src/pages/UserPage.vue"
      to: "frontend/src/components/EmptyState.vue"
      via: "import EmptyState"
      pattern: "EmptyState"
    - from: "frontend/src/pages/UserPage.vue"
      to: "frontend/src/components/FilterSheet.vue"
      via: "import FilterSheet"
      pattern: "FilterSheet"
---

<objective>
适配 UserPage/DepartmentPage/RolePage 的移动端体验：创建 EmptyState 和 FilterSheet 公共组件，各页面接入 useResponsive composable，实现全屏弹窗、底部 Sheet 筛选、RolePage 单栏切换、骨架屏、空态替换、硬编码色清理。

Purpose: 这是 FR-6.2（移动布局）和 FR-6.3（表格→卡片）的页面级交付。
Output: 2 个公共组件 + 3 个页面的移动端适配。
</objective>

<execution_context>
@.planning/phases/05-responsive/05-RESEARCH.md
@.planning/phases/05-responsive/05-UI-SPEC.md
@.planning/phases/05-responsive/05-PATTERNS.md
@.planning/phases/05-responsive/05-01-SUMMARY.md
@.planning/phases/05-responsive/05-02-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
From frontend/src/composables/useResponsive.ts:
```typescript
export function useResponsive(): {
  isDesktop: ComputedRef<boolean>;
  isMobile: ComputedRef<boolean>;
}
```

EmptyState.vue props (本 plan 创建):
```typescript
defineProps<{
  icon: string;      // Material icon 名称
  title: string;     // 标题文字
  description: string; // 描述文字
  ctaText?: string;  // CTA 按钮文字（可选）
}>();
defineEmits<{ action: [] }>();
```

FilterSheet.vue props (本 plan 创建):
```typescript
defineProps<{
  modelValue: boolean;
  keyword: string;
  departmentId: number | null;
  status: string;
  deptOptions: Array<{ label: string; value: number }>;
}>();
defineEmits<{
  'update:modelValue': [value: boolean];
  apply: [filters: { keyword: string; departmentId: number | null; status: string }];
  reset: [];
}>();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 EmptyState + FilterSheet 公共组件</name>
  <files>
    frontend/src/components/EmptyState.vue,
    frontend/src/components/FilterSheet.vue
  </files>
  <read_first>
    frontend/src/pages/UserPage.vue,
    frontend/src/pages/DepartmentPage.vue,
    .planning/phases/05-responsive/05-UI-SPEC.md,
    .planning/phases/05-responsive/05-PATTERNS.md
  </read_first>
  <action>
1. 创建 `frontend/src/components/EmptyState.vue`（per D-17, UI-SPEC 空态规格）：
   ```vue
   <template>
     <div class="flex flex-center" style="padding: 48px 16px">
       <div class="text-center">
         <q-icon :name="icon" size="64px" style="color: var(--oa-text-tertiary)" />
         <div class="q-mt-md" style="font-size: 20px; font-weight: 600; color: var(--oa-text-primary)">
           {{ title }}
         </div>
         <div class="q-mt-sm" style="font-size: 14px; color: var(--oa-text-secondary)">
           {{ description }}
         </div>
         <q-btn v-if="ctaText" color="primary" :label="ctaText" class="q-mt-md" @click="$emit('action')" />
       </div>
     </div>
   </template>

   <script setup lang="ts">
   defineProps<{
     icon: string;
     title: string;
     description: string;
     ctaText?: string;
   }>();
   defineEmits<{ action: [] }>();
   </script>
   ```
   - 图标 64px，`var(--oa-text-tertiary)` 色
   - 标题 20px/600，描述 14px/400
   - CTA 按钮可选，通过 `@action` emit 通知父组件

2. 创建 `frontend/src/components/FilterSheet.vue`（per D-06, UI-SPEC 底部 Sheet）：
   ```vue
   <template>
     <q-dialog :model-value="modelValue" position="bottom"
               @update:model-value="$emit('update:modelValue', $event)">
       <q-card style="width: 100%; border-radius: 16px 16px 0 0">
         <div class="flex flex-center q-pt-sm q-pb-xs">
           <div style="width: 40px; height: 4px; border-radius: 2px; background: var(--oa-border)"></div>
         </div>
         <q-card-section class="q-gutter-md">
           <q-input v-model="local.keyword" outlined dense label="搜索" clearable />
           <q-select v-if="deptOptions.length > 0" v-model="local.departmentId" :options="deptOptions"
                     label="部门" outlined dense emit-value map-options clearable />
           <q-btn-toggle v-model="local.status" toggle-color="primary" flat bordered
                         :options="[{ label: '全部', value: '' }, { label: '启用', value: 'ACTIVE' }, { label: '禁用', value: 'DISABLED' }]"
                         class="full-width" />
         </q-card-section>
         <q-card-actions>
           <q-btn flat label="重置筛选" @click="onReset" />
           <q-space />
           <q-btn color="primary" label="应用筛选" @click="onApply" v-close-popup />
         </q-card-actions>
       </q-card>
     </q-dialog>
   </template>

   <script setup lang="ts">
   import { reactive, watch } from 'vue';

   const props = defineProps<{
     modelValue: boolean;
     keyword: string;
     departmentId: number | null;
     status: string;
     deptOptions: Array<{ label: string; value: number }>;
   }>();

   const emit = defineEmits<{
     'update:modelValue': [value: boolean];
     apply: [filters: { keyword: string; departmentId: number | null; status: string }];
     reset: [];
   }>();

   const local = reactive({ keyword: '', departmentId: null as number | null, status: '' });

   // 打开时同步父组件筛选值
   watch(() => props.modelValue, (open) => {
     if (open) {
       local.keyword = props.keyword;
       local.departmentId = props.departmentId;
       local.status = props.status;
     }
   });

   function onApply() {
     emit('apply', { keyword: local.keyword, departmentId: local.departmentId, status: local.status });
   }

   function onReset() {
     local.keyword = '';
     local.departmentId = null;
     local.status = '';
     emit('reset');
     emit('update:modelValue', false);
   }
   </script>
   ```
   - 底部 Sheet 使用 `q-dialog position="bottom"`
   - 顶部圆角 16px，拖拽条 40x4px
   - 打开时同步父组件当前筛选值
  </action>
  <verify>
    <automated>cd frontend && ls src/components/EmptyState.vue src/components/FilterSheet.vue && grep "defineProps" src/components/EmptyState.vue && grep "position=\"bottom\"" src/components/FilterSheet.vue</automated>
  </verify>
  <acceptance_criteria>
    - EmptyState.vue 存在且包含 `defineProps<{ icon: string; title: string; description: string; ctaText?: string }>`
    - EmptyState.vue 包含 `size="64px"` 和 `var(--oa-text-tertiary)`
    - EmptyState.vue 包含 `font-size: 20px; font-weight: 600`（标题）
    - EmptyState.vue 包含 `defineEmits<{ action: [] }>`
    - FilterSheet.vue 存在且包含 `position="bottom"`
    - FilterSheet.vue 包含 `border-radius: 16px 16px 0 0`
    - FilterSheet.vue 包含 `emit('apply'` 和 `emit('reset'`
    - FilterSheet.vue 不包含任何硬编码颜色 class（bg-white, text-grey 等）
  </acceptance_criteria>
  <done>
    EmptyState 公共组件支持 icon/title/description/ctaText props + action emit；FilterSheet 底部 Sheet 组件支持筛选同步、应用、重置。
  </done>
</task>

<task type="auto">
  <name>Task 2: 适配 UserPage + DepartmentPage + RolePage 移动端</name>
  <files>
    frontend/src/pages/UserPage.vue,
    frontend/src/pages/DepartmentPage.vue,
    frontend/src/pages/RolePage.vue
  </files>
  <read_first>
    frontend/src/pages/UserPage.vue,
    frontend/src/pages/DepartmentPage.vue,
    frontend/src/pages/RolePage.vue,
    frontend/src/components/EmptyState.vue,
    frontend/src/components/FilterSheet.vue,
    .planning/phases/05-responsive/05-UI-SPEC.md,
    .planning/phases/05-responsive/05-PATTERNS.md
  </read_first>
  <action>
**UserPage.vue 改造（D-06, D-07, D-09, D-14, D-17）：**

1. 新增 imports：
   ```typescript
   import { useResponsive } from 'src/composables/useResponsive';
   import EmptyState from 'src/components/EmptyState.vue';
   import FilterSheet from 'src/components/FilterSheet.vue';
   const { isDesktop, isMobile } = useResponsive();
   const filterSheetOpen = ref(false);
   ```

2. 筛选栏改造（D-06）：PC 保持内联，移动端显示"筛选"按钮
   - 将现有筛选栏（L6-33）包裹在 `<template v-if="isDesktop">` 中
   - 移动端显示：
     ```vue
     <template v-else>
       <div class="text-h6">用户管理</div>
       <q-space />
       <q-btn flat icon="filter_list" label="筛选" @click="filterSheetOpen = true" />
     </template>
     ```
   - 在 template 末尾添加 FilterSheet 组件：
     ```vue
     <FilterSheet v-model="filterSheetOpen" :keyword="keyword" :department-id="deptFilter"
                  :status="statusFilter" :dept-options="deptFilterOptions"
                  @apply="onFilterApply" @reset="onFilterReset" />
     ```
   - 添加 onFilterApply 和 onFilterReset 方法：
     ```typescript
     function onFilterApply(f: { keyword: string; departmentId: number | null; status: string }) {
       keyword.value = f.keyword;
       deptFilter.value = f.departmentId;
       statusFilter.value = f.status;
       load(1);
     }
     function onFilterReset() {
       keyword.value = '';
       deptFilter.value = null;
       statusFilter.value = '';
       load(1);
     }
     ```

3. 骨架屏改造（D-14）：替换 L38-40 的 `<q-skeleton type="QTable" />`
   - PC 端：5 行表格骨架（`<q-skeleton type="rect" height="40px" />` + 5 个 `height="48px"`）
   - 移动端：3 张卡片骨架（每张含 3 行 text 骨架）

4. 空态替换（D-17）：替换 L49-56 内联空态为：
   ```vue
   <EmptyState icon="people" title="暂无用户" description="创建第一个用户以开始管理"
               :cta-text="canCreateUser ? '新建用户' : undefined" @action="openEdit(null)" />
   ```

5. 断点统一：L60 `$q.screen.gt.sm` → `isDesktop`

6. 硬编码色替换（D-09）：
   - L51 `color="grey-4"` → 已由 EmptyState 组件处理
   - L53 `text-grey-6` → 已由 EmptyState 组件处理
   - L96 `text-grey` → `style="color: var(--oa-text-secondary)"`

7. 弹窗全屏化（D-07）：L116 `<q-dialog>` 添加：
   ```vue
   <q-dialog v-model="dialog" :maximized="isMobile"
             :transition-show="isMobile ? 'slide-up' : 'scale'"
             :transition-hide="isMobile ? 'slide-down' : 'scale'">
     <q-card :style="isMobile ? '' : 'min-width: 400px'">
       <q-bar v-if="isMobile">
         <q-space />
         <q-btn dense flat icon="close" v-close-popup />
       </q-bar>
       <!-- 原有内容 -->
     </q-card>
   </q-dialog>
   ```

8. 移动端 FAB 新建按钮：
   ```vue
   <q-page-sticky v-if="isMobile && canCreateUser" position="bottom-right" :offset="[16, 72]">
     <q-btn fab icon="add" color="primary" @click="openEdit(null)" />
   </q-page-sticky>
   ```
   offset Y=72 避让底部 Tab 栏（56px + 16px 间距）

**DepartmentPage.vue 改造（D-04, D-09, D-14, D-17）：**

1. 新增 imports：
   ```typescript
   import { useResponsive } from 'src/composables/useResponsive';
   import EmptyState from 'src/components/EmptyState.vue';
   const { isMobile } = useResponsive();
   ```

2. 骨架屏（D-14）：替换 L10-12 spinner 为树形骨架：
   ```vue
   <div v-if="loading" class="q-gutter-sm q-pa-md">
     <q-skeleton type="rect" height="32px" />
     <q-skeleton type="rect" height="32px" style="margin-left: 24px" />
     <q-skeleton type="rect" height="32px" style="margin-left: 24px" />
     <q-skeleton type="rect" height="32px" style="margin-left: 48px" />
     <q-skeleton type="rect" height="32px" style="margin-left: 48px" />
   </div>
   ```

3. 空态替换（D-17）：替换 L21-27 为：
   ```vue
   <EmptyState v-else-if="tree.length === 0" icon="account_tree" title="暂无部门"
               description="建立组织架构第一步：添加顶级部门"
               cta-text="新建部门" @action="openEdit(null)" />
   ```
   注意：CTA 按钮的权限控制 — EmptyState 不内置 v-perm，由父组件决定是否传 ctaText。如果当前用户无 department:create 权限，不传 ctaText：
   ```vue
   :cta-text="auth.hasPerm('department:create') ? '新建部门' : undefined"
   ```
   需要在 script 中引入 auth store：`import { useAuthStore } from 'src/stores/auth'; const auth = useAuthStore();`

4. 硬编码色替换（D-09）：
   - L22 `color="grey-4"` → 已由 EmptyState 组件处理
   - L25 `text-grey-6` → 已由 EmptyState 组件处理

5. 弹窗全屏化（D-04）：L50 `<q-dialog>` 添加：
   ```vue
   <q-dialog v-model="dialog" :maximized="isMobile"
             :transition-show="isMobile ? 'slide-up' : 'scale'"
             :transition-hide="isMobile ? 'slide-down' : 'scale'">
     <q-card :style="isMobile ? '' : 'min-width: 320px'">
       <q-bar v-if="isMobile">
         <q-space />
         <q-btn dense flat icon="close" v-close-popup />
       </q-bar>
       <!-- 原有内容 -->
     </q-card>
   </q-dialog>
   ```

**RolePage.vue 改造（D-05, D-07, D-09）：**

1. 新增 imports：
   ```typescript
   import { useResponsive } from 'src/composables/useResponsive';
   import EmptyState from 'src/components/EmptyState.vue';
   const { isDesktop, isMobile } = useResponsive();
   const mobileView = ref<'list' | 'permissions'>('list');
   ```

2. 硬编码色替换（D-09）：
   - L11 `bg-white` → `style="background: var(--oa-surface)"`
   - L17 `bg-blue-1` → 改为 active-class 使用 CSS 变量：`active-class="text-primary"` + 自定义 style `style="background: var(--oa-hover)"` 通过 `:style="selected?.id === r.id ? 'background: var(--oa-hover)' : ''"`

3. 移动端单栏切换（D-05）：
   - 将现有 `<div class="row q-gutter-md">` 双栏布局包裹在 `<template v-if="isDesktop">` 中
   - 添加移动端视图：
     ```vue
     <template v-if="isMobile">
       <!-- 权限视图 -->
       <div v-if="mobileView === 'permissions' && selected">
         <div class="row items-center q-mb-md">
           <q-btn flat icon="arrow_back" label="返回角色列表" @click="mobileView = 'list'" />
         </div>
         <div style="font-size: 20px; font-weight: 600; color: var(--oa-text-primary)" class="q-mb-md">
           {{ selected.name }} 的权限
         </div>
         <!-- 权限 checkbox 区域（复用 groupedPerms 逻辑） -->
         <div v-for="(perms, module) in groupedPerms" :key="module" class="q-mb-md">
           <div class="text-subtitle2 text-primary q-mb-xs">{{ moduleLabel(module) }}</div>
           <q-option-group v-model="checkedIds"
             :options="perms.map((p: any) => ({ label: p.name + ' (' + p.code + ')', value: p.id }))"
             type="checkbox" />
         </div>
         <q-btn v-perm="'role:assign-permission'" color="primary" label="保存权限" @click="savePerms" />
       </div>
       <!-- 角色列表视图 -->
       <div v-else>
         <q-list bordered style="border-radius: 8px; background: var(--oa-surface)">
           <q-item v-for="r in roles" :key="r.id" clickable @click="selectRoleMobile(r)">
             <q-item-section>
               <q-item-label>{{ r.name }}</q-item-label>
               <q-item-label caption>{{ r.code }} · 成员: {{ r._count?.users ?? 0 }}</q-item-label>
             </q-item-section>
             <q-item-section side>
               <q-icon name="chevron_right" />
             </q-item-section>
           </q-item>
         </q-list>
       </div>
     </template>
     ```
   - 添加 selectRoleMobile 方法：
     ```typescript
     function selectRoleMobile(r: any) {
       selectRole(r);
       mobileView.value = 'permissions';
     }
     ```

4. 弹窗全屏化（D-07）：L73 `<q-dialog>` 添加 `:maximized="isMobile"` + 过渡动画 + q-bar

5. 空态：如果 roles 为空，显示 EmptyState（角色页通常有 seed 数据，但仍需处理）
  </action>
  <verify>
    <automated>cd frontend && grep -c "useResponsive" src/pages/UserPage.vue src/pages/DepartmentPage.vue src/pages/RolePage.vue && grep "EmptyState" src/pages/UserPage.vue && grep "FilterSheet" src/pages/UserPage.vue && grep "mobileView" src/pages/RolePage.vue</automated>
  </verify>
  <acceptance_criteria>
    - UserPage.vue 包含 `import { useResponsive }` 和 `const { isDesktop, isMobile } = useResponsive()`
    - UserPage.vue 包含 `import EmptyState from 'src/components/EmptyState.vue'`
    - UserPage.vue 包含 `import FilterSheet from 'src/components/FilterSheet.vue'`
    - UserPage.vue 包含 `<FilterSheet` 组件使用
    - UserPage.vue 包含 `filterSheetOpen` ref
    - UserPage.vue 包含 `:maximized="isMobile"` 在 q-dialog 上
    - UserPage.vue 包含 `<q-page-sticky` FAB 按钮
    - UserPage.vue 不包含 `$q.screen.gt.sm`（已替换为 isDesktop）
    - UserPage.vue 不包含 `color="grey-4"` 或 `text-grey-6`（已由 EmptyState 处理或替换）
    - UserPage.vue L96 `text-grey` 已替换为 `var(--oa-text-secondary)`
    - DepartmentPage.vue 包含 `import { useResponsive }`
    - DepartmentPage.vue 包含 `import EmptyState`
    - DepartmentPage.vue 包含 `<EmptyState` 替换内联空态
    - DepartmentPage.vue 包含 `:maximized="isMobile"` 在 q-dialog 上
    - DepartmentPage.vue 包含 `q-skeleton` 树形骨架（5 行）
    - DepartmentPage.vue 不包含 `color="grey-4"` 或 `text-grey-6`
    - RolePage.vue 包含 `import { useResponsive }`
    - RolePage.vue 包含 `mobileView` ref
    - RolePage.vue 包含 `selectRoleMobile` 方法
    - RolePage.vue 包含 `arrow_back` 返回按钮
    - RolePage.vue 不包含 `bg-white`（已替换为 `var(--oa-surface)`）
    - RolePage.vue 不包含 `bg-blue-1`（已替换为 `var(--oa-hover)`）
    - RolePage.vue 包含 `:maximized="isMobile"` 在 q-dialog 上
  </acceptance_criteria>
  <done>
    UserPage 移动端：底部 Sheet 筛选 + 卡片列表 + FAB 新建 + 全屏弹窗 + 骨架屏 + EmptyState。DepartmentPage 移动端：全屏弹窗 + 树形骨架屏 + EmptyState。RolePage 移动端：单栏切换（列表↔权限）+ 全屏弹窗。所有页面硬编码色已清理。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端权限控制 | EmptyState CTA 按钮通过父组件 v-perm/v-if 控制，不在组件内部鉴权 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Elevation of Privilege | EmptyState CTA 按钮绕过权限 | mitigate | EmptyState 不内置权限检查，父组件通过 `:cta-text="canCreate ? '新建' : undefined"` 控制按钮显隐，与现有 v-perm 模式一致 |
</threat_model>

<verification>
```bash
# 验证公共组件存在
ls frontend/src/components/EmptyState.vue frontend/src/components/FilterSheet.vue

# 验证断点统一（所有页面不再直接使用 $q.screen）
grep -c "\\$q.screen" frontend/src/pages/UserPage.vue frontend/src/pages/DepartmentPage.vue frontend/src/pages/RolePage.vue
# 期望: 0 0 0

# 验证硬编码色清除
grep -rn "bg-white\|bg-grey\|text-grey[^-]\|color=\"grey" frontend/src/pages/UserPage.vue frontend/src/pages/DepartmentPage.vue frontend/src/pages/RolePage.vue
# 期望: 无输出（或仅 Quasar 语义色如 color="primary"）

# 验证 EmptyState 使用
grep "EmptyState" frontend/src/pages/UserPage.vue frontend/src/pages/DepartmentPage.vue

# 验证 RolePage 单栏切换
grep "mobileView" frontend/src/pages/RolePage.vue
```
</verification>

<success_criteria>
- UserPage 移动端显示卡片列表 + 底部 Sheet 筛选 + FAB 新建 + 全屏弹窗
- DepartmentPage 移动端弹窗全屏 + 树形骨架屏
- RolePage 移动端单栏切换（角色列表 ↔ 权限视图）
- 所有页面使用 EmptyState 公共组件
- 所有页面无硬编码颜色残留
- 所有页面使用 useResponsive composable
</success_criteria>

<output>
After completion, create `.planning/phases/05-responsive/05-04-SUMMARY.md`
</output>
