---
phase: 13-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/renderer/GridFormRenderer.vue
  - frontend/src/components/renderer/GroupRenderer.vue
  - frontend/src/components/renderer/FieldRenderer.vue
  - frontend/src/components/renderer/DynamicTablePrint.vue
  - frontend/src/assets/print.css
  - frontend/src/components/submission/SubmissionDetail.vue
autonomous: true
requirements: [PDF-01, PDF-03]
must_haves:
  truths:
    - "GridFormRenderer mode='print' 渲染 HTML <table> 而非 CSS Grid"
    - "GroupRenderer mode='print' 渲染 <table> + 标题条背景色"
    - "FieldRenderer print 模式有 1px #000 边框和 8px padding（通过外层 td）"
    - "DynamicTablePrint 有 data-break 和 data-thead 属性标记"
    - "#print-area 声明中文字体栈"
    - "print.css 包含 .print-grid-table / .print-cell / .group-print 样式"
  artifacts:
    - path: "frontend/src/components/renderer/GridFormRenderer.vue"
      provides: "print 模式 table 渲染路径"
      contains: "print-grid-table"
    - path: "frontend/src/components/renderer/GroupRenderer.vue"
      provides: "print 模式 table + 标题条"
      contains: "group-print"
    - path: "frontend/src/assets/print.css"
      provides: "v2 栅格打印样式"
      contains: ".print-grid-table"
  key_links:
    - from: "GridFormRenderer.vue"
      to: "print.css"
      via: "CSS class .print-grid-table / .print-cell"
      pattern: "print-grid-table"
    - from: "SubmissionDetail.vue"
      to: "GridFormRenderer.vue"
      via: "mode='print' prop"
      pattern: "mode=\"print\""
---

<objective>
将 GridFormRenderer/GroupRenderer 的 print 模式从 CSS Grid 转换为 HTML table 结构，添加纸质表格边框样式、中文字体栈、分页标记属性。

Purpose: html2canvas 对 CSS Grid 支持不完整（GitHub issues #2405, #2729），转换为 table 确保 PDF 栅格布局 1:1 保真（PDF-01）；中文字体栈确保无乱码（PDF-03）。
Output: 6 个文件修改完成，print 模式渲染为 table + 纸质边框风格。
</objective>

<execution_context>
@.planning/phases/13-pdf/13-CONTEXT.md
@.planning/phases/13-pdf/13-RESEARCH.md
@.planning/phases/13-pdf/13-PATTERNS.md
@.planning/phases/13-pdf/13-UI-SPEC.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 关键类型定义 — executor 直接使用 -->

From frontend/src/types/schema.ts:
```typescript
export interface SchemaField {
  id: string;
  type: FieldType;
  label: string;
  required: boolean;
  colSpan: number;
  placeholder?: string;
  options?: string[];
}

export interface SchemaRow {
  type: 'row';
  fields: SchemaField[];
}

export interface SchemaGroup {
  type: 'group';
  id: string;
  title: string;
  rows: SchemaRow[];
}

export interface SchemaDynamicTable {
  type: 'dynamic-table';
  id: string;
  label: string;
  colSpan: number;
  columns: Array<{ key: string; label: string; type: DynamicTableColumnType; width?: number; options?: string[] }>;
}

export type SchemaItem = SchemaRow | SchemaGroup | SchemaDynamicTable;

export interface SchemaV2 {
  version: 2;
  items: SchemaItem[];
}
```

From frontend/src/components/renderer/GridFormRenderer.vue (current):
```typescript
const props = defineProps<{
  schema: SchemaV2;
  mode: 'designer' | 'fill' | 'print';
  modelValue?: Record<string, any>;
}>();
```

From frontend/src/components/renderer/GroupRenderer.vue (current):
```typescript
const props = defineProps<{
  group: SchemaGroup;
  mode: 'designer' | 'fill' | 'print';
  modelValue?: Record<string, any>;
}>();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GridFormRenderer + GroupRenderer print 模式 table 转换 + DynamicTablePrint data 属性</name>
  <read_first>
    - frontend/src/components/renderer/GridFormRenderer.vue
    - frontend/src/components/renderer/GroupRenderer.vue
    - frontend/src/components/renderer/DynamicTablePrint.vue
    - frontend/src/types/schema.ts
    - .planning/phases/13-pdf/13-CONTEXT.md
    - .planning/phases/13-pdf/13-UI-SPEC.md
  </read_first>
  <files>
    frontend/src/components/renderer/GridFormRenderer.vue,
    frontend/src/components/renderer/GroupRenderer.vue,
    frontend/src/components/renderer/DynamicTablePrint.vue
  </files>
  <action>
**GridFormRenderer.vue — print 模式 table 转换（per D-01, D-15, D-17）：**

在 template 中，当 `mode === 'print'` 时，用完全不同的渲染路径替代现有 CSS Grid。核心逻辑：按 `schema.items` 原始顺序遍历，将连续的 `type === 'row'` 合并到同一个 `<table class="print-grid-table">` 中，遇到 `group` 或 `dynamic-table` 时关闭当前 table、渲染独立 block、再开新 table。

具体实现：

1. 在 `<script setup>` 中添加 computed `printSegments`，将 `schema.items` 分段：
```typescript
import { computed } from 'vue';

// 将 schema.items 按类型分段：连续 rows 合并为一个 segment
type PrintSegment =
  | { kind: 'rows'; rows: SchemaRow[] }
  | { kind: 'group'; item: SchemaGroup }
  | { kind: 'dynamic-table'; item: SchemaDynamicTable };

const printSegments = computed<PrintSegment[]>(() => {
  const segments: PrintSegment[] = [];
  let currentRows: SchemaRow[] = [];
  for (const item of props.schema.items) {
    if (item.type === 'row') {
      currentRows.push(item);
    } else {
      if (currentRows.length) {
        segments.push({ kind: 'rows', rows: currentRows });
        currentRows = [];
      }
      if (item.type === 'group') {
        segments.push({ kind: 'group', item });
      } else if (item.type === 'dynamic-table') {
        segments.push({ kind: 'dynamic-table', item });
      }
    }
  }
  if (currentRows.length) {
    segments.push({ kind: 'rows', rows: currentRows });
  }
  return segments;
});
```

2. 添加 `rowRemainder` 辅助函数：
```typescript
function rowRemainder(row: SchemaRow): number {
  const used = row.fields.reduce((sum, f) => sum + f.colSpan, 0);
  return Math.max(0, 12 - used);
}
```

3. template 中 print 模式分支（在现有 `<template v-for>` 之前插入条件分支）：
```vue
<template v-if="mode === 'print'">
  <template v-for="(seg, sIdx) in printSegments" :key="'ps-' + sIdx">
    <!-- 连续 rows → 一个 table -->
    <table v-if="seg.kind === 'rows'" class="print-grid-table">
      <colgroup>
        <col v-for="i in 12" :key="i" style="width: 8.333%" />
      </colgroup>
      <tbody>
        <tr v-for="(row, rIdx) in seg.rows" :key="rIdx" data-break="row">
          <td
            v-for="field in row.fields"
            :key="field.id"
            :colspan="field.colSpan"
            class="print-cell"
          >
            <FieldRenderer :field="field" mode="print" :model-value="modelValue?.[field.id]" />
          </td>
          <td v-if="rowRemainder(row) > 0" :colspan="rowRemainder(row)" class="print-cell empty-cell" />
        </tr>
      </tbody>
    </table>

    <!-- Group → 独立 block -->
    <GroupRenderer
      v-else-if="seg.kind === 'group'"
      :group="seg.item"
      mode="print"
      :model-value="modelValue"
    />

    <!-- DynamicTable → 独立 block -->
    <div v-else-if="seg.kind === 'dynamic-table'" class="dynamic-table-print-wrapper" data-break="table">
      <DynamicTablePrint
        :label="seg.item.label"
        :columns="seg.item.columns"
        :rows="modelValue?.[seg.item.id] ?? []"
      />
    </div>
  </template>
</template>

<!-- 非 print 模式保持现有代码不变 -->
<template v-else>
  <!-- ... 现有 v-for schema.items 代码 ... -->
</template>
```

注意：print 模式不需要 fieldRefMap、groupRefs、validateFields、saveSignatures 等交互逻辑，但这些 expose 保持不变（不影响 print 路径）。

需要在 imports 中添加 `type SchemaRow, type SchemaGroup, type SchemaDynamicTable`：
```typescript
import { flattenFields, type SchemaV2, type SchemaRow, type SchemaGroup, type SchemaDynamicTable } from 'src/types/schema';
```

**GroupRenderer.vue — print 模式 table 转换（per D-16）：**

在 template 中，当 `mode === 'print'` 时，用不同的渲染路径：
```vue
<template v-if="mode === 'print'">
  <div class="group-print" data-break="group">
    <div class="group-header">{{ group.title }}</div>
    <div class="group-body">
      <table class="print-grid-table">
        <colgroup>
          <col v-for="i in 12" :key="i" style="width: 8.333%" />
        </colgroup>
        <tbody>
          <tr v-for="(row, idx) in group.rows" :key="idx" data-break="row">
            <td
              v-for="field in row.fields"
              :key="field.id"
              :colspan="field.colSpan"
              class="print-cell"
            >
              <FieldRenderer :field="field" mode="print" :model-value="modelValue?.[field.id]" />
            </td>
            <td v-if="rowRemainder(row) > 0" :colspan="rowRemainder(row)" class="print-cell empty-cell" />
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</template>
<template v-else>
  <!-- 现有 q-card 代码不变 -->
</template>
```

添加 `rowRemainder` 辅助函数（同 GridFormRenderer）：
```typescript
import type { SchemaGroup, SchemaRow } from 'src/types/schema';

function rowRemainder(row: SchemaRow): number {
  const used = row.fields.reduce((sum, f) => sum + f.colSpan, 0);
  return Math.max(0, 12 - used);
}
```

**DynamicTablePrint.vue — 添加 data 属性标记（per D-06）：**

1. 外层 `<div class="dynamic-table-print">` 添加 `data-break="table"` 属性
2. `<thead>` 添加 `data-thead` 属性
3. `<tbody>` 内每个 `<tr>` 添加 `data-break="table-row"` 属性

修改后的 template 关键行：
```vue
<div class="dynamic-table-print" data-break="table">
  ...
  <thead data-thead>
    ...
  </thead>
  <tbody>
    <tr v-for="(row, rowIdx) in rows" :key="rowIdx" data-break="table-row">
      ...
    </tr>
  </tbody>
</div>
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - GridFormRenderer.vue 包含 `class="print-grid-table"` 和 `v-if="mode === 'print'"`
    - GridFormRenderer.vue 包含 `<colgroup>` 和 `style="width: 8.333%"`
    - GridFormRenderer.vue 包含 `data-break="row"` 和 `class="print-cell"`
    - GridFormRenderer.vue 包含 `rowRemainder` 函数
    - GridFormRenderer.vue 包含 `printSegments` computed
    - GroupRenderer.vue 包含 `class="group-print"` 和 `data-break="group"`
    - GroupRenderer.vue 包含 `class="print-grid-table"` 和 `<colgroup>`
    - DynamicTablePrint.vue 包含 `data-break="table"` 和 `data-thead` 和 `data-break="table-row"`
    - 现有 vitest 测试套件通过（无回归）
  </acceptance_criteria>
  <done>
    GridFormRenderer/GroupRenderer 的 print 模式渲染 HTML table（12 列 colgroup + td colspan），DynamicTablePrint 有分页标记属性。非 print 模式代码不变。
  </done>
</task>

<task type="auto">
  <name>Task 2: print.css 纸质表格样式 + SubmissionDetail 字体栈 + FieldRenderer 签名边框</name>
  <read_first>
    - frontend/src/assets/print.css
    - frontend/src/css/assets/print.css
    - frontend/src/components/submission/SubmissionDetail.vue
    - frontend/src/components/renderer/FieldRenderer.vue
    - .planning/phases/13-pdf/13-UI-SPEC.md
    - .planning/phases/13-pdf/13-CONTEXT.md
  </read_first>
  <files>
    frontend/src/assets/print.css,
    frontend/src/components/submission/SubmissionDetail.vue,
    frontend/src/components/renderer/FieldRenderer.vue
  </files>
  <action>
**print.css — 追加 v2 栅格打印样式（per D-15, D-16, D-17, D-18, D-19）：**

在现有 `@media print { ... }` 块的末尾（`@page` 规则之前）追加以下规则。同时，在 `@media print` 块之外也添加相同的非 media-query 版本（因为 html2canvas 不解析 `@media print`，PDF 导出路径需要非 media-query 的 CSS 类）。

在文件顶部（`@media print` 之前）添加非 media-query 样式：
```css
/* ===== v2 栅格表单打印样式（PDF 导出 + 浏览器打印共用） ===== */

/* 打印区域中文字体栈（per D-12, D-13） */
#print-area {
  font-family: 'PingFang SC', 'Microsoft YaHei', 'Heiti SC', 'Source Han Sans SC', sans-serif;
}

/* 栅格表格（替代 CSS Grid，per D-15） */
.print-grid-table {
  width: 100%;
  border-collapse: collapse;
  table-layout: fixed;
}

.print-cell {
  border: 1px solid #000;
  padding: 8px;
  vertical-align: top;
}

.print-cell.empty-cell {
  /* 空单元格保留边框（per D-19） */
}

/* 分组打印样式（per D-16） */
.group-print {
  margin-bottom: 0;
}

.group-print .group-header {
  background: #f5f5f5;
  font-size: 16px;
  font-weight: 600;
  padding: 8px 12px;
  border: 1px solid #000;
}

.group-print .group-body {
  border: 1px solid #000;
  border-top: none;
  padding: 0;
}

/* 分组内部 table 不需要额外外框 */
.group-print .print-grid-table {
  border: none;
}
.group-print .print-grid-table .print-cell:first-child {
  border-left: none;
}
.group-print .print-grid-table .print-cell:last-child {
  border-right: none;
}
.group-print .print-grid-table tr:last-child .print-cell {
  border-bottom: none;
}

/* 字段标签/值覆盖样式（per D-17） */
#print-area .print-cell .field-label {
  font-size: 12px;
  color: #333;
  font-weight: 400;
  margin-bottom: 2px;
}
#print-area .print-cell .print-value {
  font-size: 14px;
  color: #000;
}
#print-area .print-cell .print-value.empty {
  color: #999;
}

/* 签名图片边框（per D-18） */
#print-area .print-cell img {
  max-height: 80px;
  border: 1px solid #000;
  object-fit: contain;
}

/* Quasar 组件 print 模式重置 */
#print-area .q-card {
  box-shadow: none !important;
  border-radius: 0 !important;
}
#print-area .q-card--bordered {
  border: none !important;
}
#print-area .q-separator {
  display: none !important;
}

/* 动态表格打印包装器 */
.dynamic-table-print-wrapper {
  margin: 0;
}
```

在 `@media print` 块内追加（`@page` 规则之前）：
```css
  /* v2 栅格表格 */
  .print-grid-table {
    width: 100%;
    border-collapse: collapse;
    table-layout: fixed;
  }
  .print-cell {
    border: 1px solid #000;
    padding: 8px;
    vertical-align: top;
  }

  /* 分组打印 */
  .group-print .group-header {
    background: #f5f5f5;
    font-size: 16px;
    font-weight: 600;
    padding: 8px 12px;
    border: 1px solid #000;
  }
  .group-print .group-body {
    border: 1px solid #000;
    border-top: none;
  }

  /* Quasar 重置 */
  #print-area .q-card {
    box-shadow: none !important;
    border-radius: 0 !important;
  }
```

**SubmissionDetail.vue — #print-area 添加 data 属性（per D-08, D-09, D-10）：**

修改 `<div id="print-area" class="detail-body">` 为：
```vue
<div
  id="print-area"
  class="detail-body"
  :data-form-title="templateName"
  :data-submit-time="formatDate(submission.createdAt)"
>
```

这两个 data 属性供 usePdfExport.ts 读取，用于 pdf.text() 注入页眉页脚。

**FieldRenderer.vue — 签名图片边框（per D-18）：**

修改 print 模式下签名 img 的 style，添加 1px 黑色边框：
```vue
<img v-if="modelValue" :src="modelValue" style="max-height: 80px; object-fit: contain; border: 1px solid #000" />
```

注意：签名边框也在 print.css 中通过 `#print-area .print-cell img` 声明了，但 inline style 作为双保险确保 html2canvas 截图时生效。
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - print.css 包含 `#print-area` 和 `font-family: 'PingFang SC'`
    - print.css 包含 `.print-grid-table` 和 `border-collapse: collapse`
    - print.css 包含 `.print-cell` 和 `border: 1px solid #000`
    - print.css 包含 `.group-print .group-header` 和 `background: #f5f5f5`
    - print.css 包含 `#print-area .q-card` 和 `box-shadow: none`
    - print.css 包含 `#print-area .print-cell img` 和 `border: 1px solid #000`
    - SubmissionDetail.vue 包含 `data-form-title` 和 `data-submit-time`
    - FieldRenderer.vue 签名 img 包含 `border: 1px solid #000`
    - 现有 vitest 测试套件通过（无回归）
  </acceptance_criteria>
  <done>
    print.css 包含完整的 v2 栅格打印样式（table 边框、分组背景、字段标签/值覆盖、签名边框、Quasar 重置、中文字体栈）。SubmissionDetail 传递表单名称和提交时间 data 属性。FieldRenderer 签名有黑色边框。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 本阶段纯客户端 CSS/模板修改，不涉及数据传输或用户输入处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | html2canvas useCORS | accept | 签名数据已是 base64 data URL，不涉及外部域名请求；useCORS 仅用于同源图片加载 |
| T-13-02 | Denial of Service | canvas 尺寸超限 | accept | 极端超长表单可能导致 canvas 超限，但这是浏览器限制而非安全漏洞；Plan 03 会添加高度检查 |
</threat_model>

<verification>
- `cd frontend && npx vitest run` 全套测试通过
- GridFormRenderer.vue 的 print 模式渲染 `<table>` 而非 CSS Grid（grep 验证 `print-grid-table`）
- print.css 包含所有 v2 打印样式（grep 验证 `.print-grid-table`, `.group-print`, `#print-area`）
</verification>

<success_criteria>
- print 模式下 GridFormRenderer 渲染 12 列 HTML table（colgroup + td colspan）
- print 模式下 GroupRenderer 渲染标题条 + 内部 table
- DynamicTablePrint 有 data-break/data-thead 属性
- print.css 有完整的纸质表格样式
- #print-area 有中文字体栈声明
- SubmissionDetail 传递 data-form-title/data-submit-time
- 现有测试无回归
</success_criteria>

<output>
After completion, create `.planning/phases/13-pdf/13-01-SUMMARY.md`
</output>
