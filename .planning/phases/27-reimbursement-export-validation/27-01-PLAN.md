---
phase: 27
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts
  - backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts
  - frontend/src/stores/__tests__/reimbursement.test.ts
  - frontend/src/pages/__tests__/ReimbursementPage.test.ts
autonomous: true
requirements: [EXPORT-01, EXPORT-02, EXPORT-03, PERM-01, PERM-02, UX-02]
user_setup:
  - "None. This wave adds focused source/store/page contracts only."
must_haves:
  truths:
    - "Wave 0 contracts pin a backend `reimbursement:export` XLSX route before `/:id`; frontend visibility is not trusted for authorization."
    - "Wave 0 contracts pin current-filter export, 2,000-row cap, fixed reimbursement detail columns, formula sanitization and review-result derivation from actions."
    - "Wave 0 contracts pin authenticated blob download from `/reimbursements/export`, object URL cleanup, clear feedback copy and negative scope boundaries."
  artifacts:
    - path: "backend/src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts"
      provides: "Export service contracts for workbook columns, sanitization, cap, filters and action-derived review fields"
    - path: "backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts"
      provides: "Route contract for `/export` ordering, `reimbursement:export` guard, query schema reuse and XLSX headers"
    - path: "frontend/src/stores/__tests__/reimbursement.test.ts"
      provides: "Pinia export method contract for blob response, loading state and filter params"
    - path: "frontend/src/pages/__tests__/ReimbursementPage.test.ts"
      provides: "List toolbar export entry, permission visibility, feedback and blob lifecycle source contracts"
  key_links:
    - from: ".planning/phases/27-reimbursement-export-validation/27-CONTEXT.md"
      to: "backend/src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts"
      via: "D-01 through D-12 export scope, columns and Excel safety decisions"
      pattern: "MAX_REIMBURSEMENT_EXPORT_ROWS|sanitizeReimbursementExcelCell|DEPARTMENT_APPROVE|FINANCE_APPROVE|completedAt"
    - from: ".planning/phases/27-reimbursement-export-validation/27-UI-SPEC.md"
      to: "frontend/src/pages/__tests__/ReimbursementPage.test.ts"
      via: "export entry, mobile accessibility, feedback and negative scope contract"
      pattern: "导出 Excel|reimbursement:export|createObjectURL|revokeObjectURL|window.open"
---

# Phase 27 Plan 01: Wave 0 Export Contracts

<objective>
Add failing-but-specific contracts for reimbursement Excel export, backend authorization, fixed workbook content, frontend blob download and Phase 27 negative scope boundaries.
</objective>

<context>
This is the Wave 0 contract plan. It must not implement export behavior yet. It extends the focused reimbursement backend/frontend tests so later plans can implement against exact service names, route ordering, UI labels and error copy.
</context>

<source_audit>
Read first:

- `.planning/phases/27-reimbursement-export-validation/27-CONTEXT.md`
- `.planning/phases/27-reimbursement-export-validation/27-RESEARCH.md`
- `.planning/phases/27-reimbursement-export-validation/27-UI-SPEC.md`
- `.planning/phases/27-reimbursement-export-validation/27-VALIDATION.md`
- `backend/src/modules/approval/archive-export.service.ts`
- `backend/src/modules/approval/archive.route.ts`
- `backend/src/modules/approval/__tests__/archive-export.test.ts`
- `backend/src/modules/reimbursement/reimbursement.service.ts`
- `backend/src/modules/reimbursement/reimbursement.route.ts`
- `backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts`
- `backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts`
- `frontend/src/stores/approvalArchive.ts`
- `frontend/src/pages/ApprovalArchivePage.vue`
- `frontend/src/stores/reimbursement.ts`
- `frontend/src/pages/ReimbursementPage.vue`
- `frontend/src/stores/__tests__/reimbursement.test.ts`
- `frontend/src/pages/__tests__/ReimbursementPage.test.ts`
</source_audit>

<tasks>

<task id="27-01-01" type="auto">
  <name>Add backend export service contracts</name>
  <files>backend/src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts</files>
  <read_first>
    - `backend/src/modules/approval/archive-export.service.ts` for cap, formula sanitization, frozen header and paged export patterns.
    - `backend/src/modules/approval/__tests__/archive-export.test.ts` for existing export contract style.
    - `backend/src/modules/reimbursement/reimbursement.service.ts` for `ReimbursementActor`, `ReimbursementListFilters`, statuses, actions and attachment count serialization.
    - `.planning/phases/27-reimbursement-export-validation/27-CONTEXT.md` decisions D-01 through D-12.
  </read_first>
  <action>
    Create `backend/src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts`. Import the expected future exports from `../reimbursement-export.service`: `MAX_REIMBURSEMENT_EXPORT_ROWS`, `sanitizeReimbursementExcelCell`, `buildReimbursementWorkbook`, and `exportReimbursementsExcel`.

    Add a test named `pins export cap and formula sanitization` asserting `MAX_REIMBURSEMENT_EXPORT_ROWS` is `2000` and strings beginning with `=`, `+`, `-`, `@`, tab and carriage return are prefixed with a single quote. Assert normal strings, numbers, null and undefined are returned unchanged.

    Add a test named `pins fixed reimbursement workbook columns` that builds a workbook from one representative row and asserts the first worksheet uses frozen header row (`views[0].state === 'frozen'` and `ySplit === 1`). Assert the header labels are exactly: `申请编号`, `标题`, `类别`, `金额`, `发生日期`, `事由`, `收款信息`, `备注`, `申请人`, `申请部门`, `提交时间`, `附件数量`, `当前状态`, `部门审核结果`, `部门审核人`, `部门审核时间`, `部门审核意见`, `财务审核结果`, `财务审核人`, `财务审核时间`, `财务审核意见`, `最终通过时间`.

    Add a test named `pins review result columns from latest actions` using actions that include older and newer `DEPARTMENT_APPROVE`/`DEPARTMENT_REJECT` and `FINANCE_APPROVE`/`FINANCE_REJECT` records. Assert department and finance result/person/time/comment cells are derived from the latest matching action for each node, not from any client-supplied fields. Assert `最终通过时间` is populated only when the row status is `APPROVED`.

    Add a test named `pins current-filter full export paging` for `exportReimbursementsExcel(actor, filters, dependencies)`. Use dependency injection so the test can assert `status`, `category`, `dateFrom`, `dateTo`, and `keyword` are preserved while incoming `page` and `size` are ignored in favor of export paging. Assert the implementation throws the existing oversize message `当前筛选结果超过导出上限，请缩小筛选范围后重试。` when the matching total exceeds 2,000.

    Keep the contracts focused on reimbursement export. Do not add dashboard, statistics, payment, OCR or accounting assertions except as negative source strings.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "MAX_REIMBURSEMENT_EXPORT_ROWS|sanitizeReimbursementExcelCell|buildReimbursementWorkbook|exportReimbursementsExcel" backend/src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts` finds the expected service exports.
    - `rg "申请编号|附件数量|部门审核结果|财务审核结果|最终通过时间" backend/src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts` finds the fixed header contract.
    - `rg "DEPARTMENT_APPROVE|DEPARTMENT_REJECT|FINANCE_APPROVE|FINANCE_REJECT|completedAt" backend/src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts` finds the action-derived review contract.
    - `rg "当前筛选结果超过导出上限|status|category|dateFrom|dateTo|keyword" backend/src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts` finds the cap and filter-paging contract.
    - `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts` exits non-zero until Plan 27-02 implements the missing export service, then exits 0.
  </acceptance_criteria>
  <done>Backend export service contracts pin the workbook, safety and current-filter export behavior.</done>
</task>

<task id="27-01-02" type="auto">
  <name>Extend backend route export contracts</name>
  <files>backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts</files>
  <read_first>
    - `backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts` for existing route signature and source contract helpers.
    - `backend/src/modules/reimbursement/reimbursement.route.ts` for current route order, guards and `reimbursementListQuery`.
    - `backend/src/modules/approval/archive.route.ts` for XLSX response header shape.
    - `.planning/phases/27-reimbursement-export-validation/27-CONTEXT.md` decisions D-02, D-03, D-10 and D-12.
  </read_first>
  <action>
    Update `backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts` so route signatures include `GET /export`. Add a route-order assertion that `/export` appears before `GET /:id` in the route source/signature list so the dynamic detail route cannot swallow export.

    Add source assertions that `reimbursement.route.ts` imports/calls `exportReimbursementsExcel`, uses `authGuard('reimbursement:export')`, reuses `reimbursementListQuery`, calls `workbook.xlsx.writeBuffer()`, sets `Content-Type` to `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`, sets `Content-Disposition` with `attachment` and `reimbursements-export`, and does not contain frontend-only `responseType` or `window.open`.

    Keep existing Phase 24-26 route assertions intact.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "GET /export|authGuard\('reimbursement:export'\)|exportReimbursementsExcel|reimbursementListQuery" backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts` finds route contract assertions.
    - `rg "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet|Content-Disposition|reimbursements-export|writeBuffer" backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts` finds XLSX response assertions.
    - `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.route.test.ts` exits non-zero until Plan 27-02 wires the route, then exits 0.
  </acceptance_criteria>
  <done>Backend route contracts pin export route ordering, permission guard and XLSX response shape.</done>
</task>

<task id="27-01-03" type="auto">
  <name>Extend frontend store and page export contracts</name>
  <files>frontend/src/stores/__tests__/reimbursement.test.ts, frontend/src/pages/__tests__/ReimbursementPage.test.ts</files>
  <read_first>
    - `frontend/src/stores/approvalArchive.ts` for `exportLoading`, `exportExcel(filters)` and `responseType: 'blob'` pattern.
    - `frontend/src/pages/ApprovalArchivePage.vue` for object URL download and Notify copy.
    - `frontend/src/stores/reimbursement.ts` for current filter request builder and API call style.
    - `frontend/src/pages/ReimbursementPage.vue` for toolbar insertion point and current filter state.
    - `.planning/phases/27-reimbursement-export-validation/27-UI-SPEC.md`.
  </read_first>
  <action>
    Update `frontend/src/stores/__tests__/reimbursement.test.ts` with contracts for `exportLoading` and `exportExcel(filters)`. Assert `exportExcel` calls `api.get('/reimbursements/export', { params, responseType: 'blob' })`, returns the blob, toggles `exportLoading` back to `false` on success and failure, and passes only `status`, `category`, `dateFrom`, `dateTo`, and `keyword`. Assert `page`, `size` and `reviewScope` are not sent as export params.

    Update `frontend/src/pages/__tests__/ReimbursementPage.test.ts` with source contracts for the export toolbar entry. Assert `ReimbursementPage.vue` contains `导出 Excel`, `导出报销 Excel`, `auth.hasPerm('reimbursement:export')`, `store.exportLoading`, `exportReimbursements`, `store.exportExcel(currentFilters`, `URL.createObjectURL`, `URL.revokeObjectURL`, `link.click()`, `Excel 导出已开始`, `当前筛选结果超过导出上限，请缩小筛选范围后重试。`, and `导出失败，请稍后重试。` or equivalent clear fallback.

    Add negative source assertions that `ReimbursementPage.vue` does not contain `window.open`, direct `/api/v1/reimbursements/export` string construction, statistics dashboard copy, OCR, `发票验真`, payment/accounting/voucher copy, or `/approval/applications` as an export host.
  </action>
  <verify>
    <automated>cd frontend && bun test src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "exportLoading|exportExcel|/reimbursements/export|responseType: 'blob'|reviewScope" frontend/src/stores/__tests__/reimbursement.test.ts` finds store export contracts.
    - `rg "导出 Excel|导出报销 Excel|reimbursement:export|createObjectURL|revokeObjectURL|Excel 导出已开始" frontend/src/pages/__tests__/ReimbursementPage.test.ts` finds page export contracts.
    - `rg "window.open|OCR|发票验真|统计看板|付款|会计凭证" frontend/src/pages/__tests__/ReimbursementPage.test.ts` finds negative scope assertions.
    - `cd frontend && bun test src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts` exits non-zero until Plan 27-03 implements the frontend export UX, then exits 0.
  </acceptance_criteria>
  <done>Frontend contracts pin store blob export and permission-gated toolbar behavior.</done>
</task>

<task id="27-01-04" type="auto">
  <name>Run Wave 0 focused contract gate</name>
  <files>backend/src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts, backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts, frontend/src/stores/__tests__/reimbursement.test.ts, frontend/src/pages/__tests__/ReimbursementPage.test.ts</files>
  <read_first>
    - `.planning/phases/27-reimbursement-export-validation/27-VALIDATION.md` Wave 0 requirements.
    - All files changed by Tasks 27-01-01 through 27-01-03.
  </read_first>
  <action>
    Run the Wave 0 focused backend and frontend contract commands. Confirm the failures are specific to missing Phase 27 implementation exports/routes/UI and not syntax errors. Fix only contract mistakes in the test files; do not implement application code in this plan.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts src/modules/reimbursement/__tests__/reimbursement.route.test.ts</automated>
    <automated>cd frontend && bun test src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Backend contract tests fail only because `reimbursement-export.service.ts` and `/export` wiring are not implemented yet.
    - Frontend contract tests fail only because `exportLoading`, `exportExcel` and page export UX are not implemented yet.
    - No production source file is changed by this plan.
  </acceptance_criteria>
  <done>Wave 0 red contracts are precise and ready for backend/frontend implementation waves.</done>
</task>

</tasks>

<final_verification>
- `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement-export.service.test.ts src/modules/reimbursement/__tests__/reimbursement.route.test.ts`
- `cd frontend && bun test src/stores/__tests__/reimbursement.test.ts src/pages/__tests__/ReimbursementPage.test.ts`
- Expected after this plan: red, but only for missing Phase 27 implementation.
</final_verification>

<handoff>
Next plan: `27-02-PLAN.md` implements the backend export service and route to make backend Wave 0 contracts green.
</handoff>
