---
phase: 24-api
plan: 2
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/20260503013000_add_reimbursements/migration.sql
  - backend/prisma/seed.ts
  - .gitignore
  - docker-compose.yml
autonomous: true
requirements: [REIM-01, REIM-02, INV-01, INV-02, INV-04, PERM-01, PERM-02, PERM-03, NFR-01, NFR-02]
user_setup:
  - "A PostgreSQL database reachable through backend/.env or ../.env is required for migrate/generate verification."
must_haves:
  truths:
    - "Prisma schema has fixed reimbursement application, attachment and action models with explicit columns, enum status values and filter indexes."
    - "Permission seed exports the exact reimbursement permission codes locked by Wave 0 and gives EMPLOYEE only create, own and attachment defaults."
    - "Local upload storage is ignored by Git and persisted by Docker through REIMBURSEMENT_UPLOAD_DIR and an oa_uploads volume."
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Reimbursement enums, models, relations, Decimal amount and indexes"
    - path: "backend/prisma/migrations/20260503013000_add_reimbursements/migration.sql"
      provides: "Database migration for reimbursement enums, tables, FKs and indexes"
    - path: "backend/prisma/seed.ts"
      provides: "REIMBURSEMENT_PERMISSION_CODES, PERMISSIONS rows and EMPLOYEE baseline grants"
    - path: ".gitignore"
      provides: "uploads/ is not committed"
    - path: "docker-compose.yml"
      provides: "REIMBURSEMENT_UPLOAD_DIR and oa_uploads persistence"
  key_links:
    - from: "backend/src/modules/reimbursement/__tests__/reimbursement.schema.test.ts"
      to: "backend/prisma/schema.prisma"
      via: "schema source contract"
      pattern: "ReimbursementStatus|ReimbursementApplication|@db.Decimal(12, 2)"
    - from: "backend/src/modules/role/__tests__/reimbursement-permissions.seed.test.ts"
      to: "backend/prisma/seed.ts"
      via: "permission seed contract"
      pattern: "REIMBURSEMENT_PERMISSION_CODES"
---

<objective>
Close Phase 24 Wave 1 gaps by adding the reimbursement data model, Prisma migration, reimbursement permission seed and upload storage baseline.

Purpose: make the Wave 0 schema and permission contracts green while preparing durable local attachment storage for later API work.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-api/24-CONTEXT.md
@.planning/phases/24-api/24-RESEARCH.md
@.planning/phases/24-api/24-VALIDATION.md
@.planning/phases/24-api/24-VERIFICATION.md
@.planning/phases/24-api/24-01-PLAN.md
@backend/prisma/schema.prisma
@backend/prisma/seed.ts
@docker-compose.yml
@.gitignore
</context>

<source_audit>
## Phase 24 Gap Coverage

| Gap Source | Missing Item | Covered By |
|------------|--------------|------------|
| 24-VERIFICATION | Prisma schema expresses applications, attachments, actions and statuses | Task 1 schema + migration |
| 24-VERIFICATION | Permission seed exports reimbursement create/view/review/attachment/export | Task 2 seed update |
| 24-VERIFICATION | File upload baseline absent | Task 3 Git/Docker upload storage config |
| 24-VALIDATION | 24-02-01 Prisma validation pending | Task 1 verify gate |
| 24-VALIDATION | 24-02-02 seed test pending | Task 2 focused seed test |
| 24-VALIDATION | 24-02-03 storage config pending | Task 3 grep check |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add reimbursement Prisma schema and migration</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations/20260503013000_add_reimbursements/migration.sql</files>
  <read_first>
    - @backend/prisma/schema.prisma for existing enum, model, relation, timestamp and index conventions.
    - @backend/src/modules/reimbursement/__tests__/reimbursement.schema.test.ts for the locked schema contract.
    - @.planning/phases/24-api/24-CONTEXT.md decisions D-01 through D-05 and D-21 through D-23.
    - @.planning/phases/24-api/24-RESEARCH.md Recommended Target Contracts section.
  </read_first>
  <action>
    Update `backend/prisma/schema.prisma` with exact reimbursement enums and models. Add `enum ReimbursementStatus { DRAFT DEPARTMENT_REVIEW FINANCE_REVIEW APPROVED REJECTED }` and `enum ReimbursementActionType { SUBMIT DEPARTMENT_APPROVE DEPARTMENT_REJECT FINANCE_APPROVE FINANCE_REJECT }`. Add these relation fields to `User`: `reimbursementApplications ReimbursementApplication[] @relation("ReimbursementApplicant")`, `reimbursementAttachments ReimbursementAttachment[] @relation("ReimbursementAttachmentUploader")`, and `reimbursementActions ReimbursementAction[] @relation("ReimbursementActionActor")`. Add this relation field to `Department`: `reimbursementApplications ReimbursementApplication[] @relation("ReimbursementApplicantDepartment")`.

    Add `model ReimbursementApplication` with fields: `id Int @id @default(autoincrement())`, `applicationNo String @unique`, `title String`, `category String`, `occurredAt DateTime`, `amount Decimal @db.Decimal(12, 2)`, `reason String`, `payeeInfo String?`, `remark String?`, `status ReimbursementStatus @default(DRAFT)`, `applicantId Int`, `applicant User @relation("ReimbursementApplicant", fields: [applicantId], references: [id])`, `applicantName String`, `applicantDepartmentId Int?`, `applicantDepartment Department? @relation("ReimbursementApplicantDepartment", fields: [applicantDepartmentId], references: [id])`, `applicantDepartmentName String?`, `submittedAt DateTime?`, `completedAt DateTime?`, `attachments ReimbursementAttachment[]`, `actions ReimbursementAction[]`, `createdAt DateTime @default(now())`, and `updatedAt DateTime @updatedAt`. Add indexes exactly for `[applicantId]`, `[applicantDepartmentId]`, `[status]`, `[category]`, `[occurredAt]`, and `[createdAt]`. Do not add `formData Json`, `data Json`, `payload Json @default`, or any relation to `ApprovalApplication` on this model.

    Add `model ReimbursementAttachment` with fields: `id Int @id @default(autoincrement())`, `applicationId Int`, `application ReimbursementApplication @relation(fields: [applicationId], references: [id], onDelete: Cascade)`, `originalName String`, `storedName String`, `relativePath String`, `mimeType String`, `size Int`, `uploaderId Int`, `uploader User @relation("ReimbursementAttachmentUploader", fields: [uploaderId], references: [id])`, and `createdAt DateTime @default(now())`. Add indexes for `[applicationId]` and `[uploaderId]`.

    Add `model ReimbursementAction` with fields: `id Int @id @default(autoincrement())`, `applicationId Int`, `application ReimbursementApplication @relation(fields: [applicationId], references: [id], onDelete: Cascade)`, `actorId Int`, `actor User @relation("ReimbursementActionActor", fields: [actorId], references: [id])`, `actorName String`, `type ReimbursementActionType`, `nodeName String?`, `comment String?`, `signatureRelativePath String?`, `signatureMimeType String?`, `signatureSize Int?`, and `createdAt DateTime @default(now())`. Add indexes for `[applicationId]`, `[actorId]`, `[type]`, and `[createdAt]`.

    Create migration file `backend/prisma/migrations/20260503013000_add_reimbursements/migration.sql` with SQL that creates both enums, all three tables, foreign keys and indexes matching the Prisma schema. Use quoted PostgreSQL enum/table names generated by Prisma style: `"ReimbursementStatus"`, `"ReimbursementActionType"`, `"ReimbursementApplication"`, `"ReimbursementAttachment"`, `"ReimbursementAction"`.
  </action>
  <verify>
    <automated>cd backend && bun --env-file=../.env prisma validate && bun --env-file=../.env prisma generate</automated>
    <automated>cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "enum ReimbursementStatus|DEPARTMENT_REVIEW|FINANCE_REVIEW|enum ReimbursementActionType|FINANCE_REJECT" backend/prisma/schema.prisma` finds the two enums and required status/action values.
    - `rg "model ReimbursementApplication|applicationNo\s+String\s+@unique|amount\s+Decimal\s+@db\.Decimal\(12,\s*2\)|@@index\(\[applicantId\]\)|@@index\(\[createdAt\]\)" backend/prisma/schema.prisma` finds the application model, Decimal amount and indexes.
    - `rg "model ReimbursementAttachment|originalName|storedName|relativePath|mimeType|uploaderId" backend/prisma/schema.prisma` finds attachment metadata fields.
    - `rg "model ReimbursementAction|signatureRelativePath|DEPARTMENT_APPROVE|FINANCE_APPROVE" backend/prisma/schema.prisma` finds audit trail and signature-ready fields.
    - `rg "CREATE TYPE \"ReimbursementStatus\"|CREATE TABLE \"ReimbursementApplication\"|CREATE TABLE \"ReimbursementAttachment\"|CREATE TABLE \"ReimbursementAction\"" backend/prisma/migrations/20260503013000_add_reimbursements/migration.sql` finds migration DDL.
    - `cd backend && bun --env-file=../.env prisma validate && bun --env-file=../.env prisma generate` exits 0.
    - `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.schema.test.ts` exits 0.
  </acceptance_criteria>
  <done>Prisma model, enum, relation, index and migration gaps are closed.</done>
</task>

<task type="auto">
  <name>Task 2: Add reimbursement permission seed implementation</name>
  <files>backend/prisma/seed.ts</files>
  <read_first>
    - @backend/prisma/seed.ts for current `APPROVAL_PERMISSION_CODES`, `VISIT_PERMISSION_CODES`, `EMPLOYEE_PERMISSION_CODES`, `PERMISSIONS`, and ADMIN all-permissions flow.
    - @backend/src/modules/role/__tests__/reimbursement-permissions.seed.test.ts for exact expected exports and role grant assertions.
    - @backend/src/modules/role/__tests__/approval-permissions.seed.test.ts for DB-backed seed cleanup and role permission test style.
    - @.planning/phases/24-api/24-CONTEXT.md decisions D-06 through D-09.
  </read_first>
  <action>
    Update `backend/prisma/seed.ts` as the single source of reimbursement permissions. Export `REIMBURSEMENT_PERMISSION_CODES` exactly as `['reimbursement:create', 'reimbursement:own', 'reimbursement:list', 'reimbursement:department-review', 'reimbursement:finance-review', 'reimbursement:attachment', 'reimbursement:export']`. Add `reimbursement:create`, `reimbursement:own`, and `reimbursement:attachment` to `EMPLOYEE_PERMISSION_CODES`; do not add `reimbursement:list`, `reimbursement:department-review`, `reimbursement:finance-review`, or `reimbursement:export` to the employee baseline. Add seven `PERMISSIONS` rows under a `// 报销模块` section with `module: 'reimbursement'` and names: `创建报销申请`, `查看我的报销`, `查看全部报销`, `部门审核报销`, `财务复核报销`, `访问报销附件`, and `导出报销数据`. Do not change the ADMIN seed flow: it must still read `const allPerms = await prisma.permission.findMany()` and assign `allPerms.map((p) => ({ roleId: adminRole.id, permissionId: p.id }))`.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/role/__tests__/reimbursement-permissions.seed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export const REIMBURSEMENT_PERMISSION_CODES|reimbursement:create|reimbursement:own|reimbursement:list|reimbursement:department-review|reimbursement:finance-review|reimbursement:attachment|reimbursement:export" backend/prisma/seed.ts` finds the exported codes.
    - `rg "创建报销申请|查看我的报销|查看全部报销|部门审核报销|财务复核报销|访问报销附件|导出报销数据|module: 'reimbursement'" backend/prisma/seed.ts` finds all permission rows and module grouping.
    - `rg "EMPLOYEE_PERMISSION_CODES[\s\S]*reimbursement:create[\s\S]*reimbursement:own[\s\S]*reimbursement:attachment" backend/prisma/seed.ts --multiline` finds the employee baseline grants.
    - `rg "const allPerms = await prisma.permission.findMany\(\)|allPerms\.map\(\(p\) => \(\{ roleId: adminRole\.id, permissionId: p\.id \}\)\)" backend/prisma/seed.ts` confirms ADMIN inheritance is unchanged.
    - `cd backend && bun test src/modules/role/__tests__/reimbursement-permissions.seed.test.ts` exits 0 with a reachable test database.
  </acceptance_criteria>
  <done>Permission seed gaps for PERM-01, PERM-02 and PERM-03 are closed.</done>
</task>

<task type="auto">
  <name>Task 3: Add reimbursement upload storage baseline</name>
  <files>.gitignore, docker-compose.yml</files>
  <read_first>
    - @.gitignore for current ignored runtime directories.
    - @docker-compose.yml for backend service environment and volume conventions.
    - @.planning/phases/24-api/24-CONTEXT.md decisions D-15 through D-20.
    - @.planning/phases/24-api/24-RESEARCH.md Attachment Implementation Notes section.
  </read_first>
  <action>
    Add `uploads/` to `.gitignore` so local attachment files are never committed. Update `docker-compose.yml` backend service with environment key `REIMBURSEMENT_UPLOAD_DIR: ${REIMBURSEMENT_UPLOAD_DIR:-/app/uploads/reimbursements}`. Add backend volume mapping `- oa_uploads:/app/uploads` under the backend service `volumes:` block. Add top-level named volume `oa_uploads:` next to existing `oa_pgdata:`. Do not change PostgreSQL, frontend, JWT or port settings.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path '.gitignore','docker-compose.yml' -Pattern 'uploads/|REIMBURSEMENT_UPLOAD_DIR|oa_uploads'"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "^uploads/$" .gitignore` finds the Git ignore rule.
    - `rg "REIMBURSEMENT_UPLOAD_DIR: \$\{REIMBURSEMENT_UPLOAD_DIR:-/app/uploads/reimbursements\}" docker-compose.yml` finds the backend environment setting.
    - `rg "oa_uploads:/app/uploads|oa_uploads:" docker-compose.yml` finds the backend mount and named volume.
    - `rg "oa_pgdata:" docker-compose.yml` still finds the existing PostgreSQL volume.
  </acceptance_criteria>
  <done>Local upload files are ignored and Docker persists `/app/uploads`.</done>
</task>

</tasks>

<threat_model>
| Threat | Area | Mitigation in this plan |
|--------|------|-------------------------|
| Generic JSON reimbursement data | Schema | Explicit columns only; no `formData/data/payload Json @default` on `ReimbursementApplication`. |
| Permission drift | Seed | Exact exported `REIMBURSEMENT_PERMISSION_CODES` and module rows are tested. |
| Uploaded files committed or lost on rebuild | Storage | `.gitignore` adds `uploads/`; Docker mounts `oa_uploads:/app/uploads`. |
</threat_model>

<verification>
Wave 1 gate: `cd backend && bun --env-file=../.env prisma validate && bun --env-file=../.env prisma generate`, schema contract test, reimbursement permission seed test, and storage config grep all pass.
</verification>

<success_criteria>
Phase 24 schema, migration, seed and storage-baseline gaps are closed without implementing route/service or attachment IO behavior from later waves.
</success_criteria>

<output>
After completion, create `.planning/phases/24-api/24-02-SUMMARY.md`.
</output>
