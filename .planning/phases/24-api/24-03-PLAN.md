---
phase: 24-api
plan: 3
type: execute
wave: 2
depends_on: [24-02]
files_modified:
  - backend/src/modules/reimbursement/reimbursement.state.ts
  - backend/src/modules/reimbursement/reimbursement.service.ts
  - backend/src/modules/reimbursement/reimbursement.route.ts
  - backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts
  - backend/src/index.ts
autonomous: true
requirements: [REIM-01, REIM-02, REIM-03, REIM-04, INV-02, PERM-01, PERM-02, PERM-03, NFR-02]
user_setup: []
must_haves:
  truths:
    - "The reimbursement service derives applicant identity and department snapshots from currentUser/prisma user data, never from request body trusted fields."
    - "Drafts can be created/edited by the applicant while status is DRAFT; submit moves the record to DEPARTMENT_REVIEW and appends a SUBMIT audit action."
    - "List/detail APIs enforce object visibility for own/list/department-review/finance-review/admin scopes and return paginated rows/total/page/size responses."
  artifacts:
    - path: "backend/src/modules/reimbursement/reimbursement.state.ts"
      provides: "Reimbursement status transition guard for DRAFT -> DEPARTMENT_REVIEW"
    - path: "backend/src/modules/reimbursement/reimbursement.service.ts"
      provides: "Create, update, submit, list, detail, serialization and object-authorization helpers"
    - path: "backend/src/modules/reimbursement/reimbursement.route.ts"
      provides: "Elysia /reimbursements module, strict schemas, route signatures and guards"
    - path: "backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts"
      provides: "Focused service/state validation for write input, filters, transitions and object auth"
    - path: "backend/src/index.ts"
      provides: "Registers reimbursementModule under /api/v1"
  key_links:
    - from: "backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts"
      to: "backend/src/modules/reimbursement/reimbursement.route.ts"
      via: "route module and schema contract"
      pattern: "reimbursementModule|reimbursementWriteBody|reimbursementListQuery"
    - from: "backend/src/modules/reimbursement/reimbursement.route.ts"
      to: "backend/src/modules/reimbursement/reimbursement.service.ts"
      via: "route delegates to service and object auth helpers"
      pattern: "assertCanViewReimbursement|assertCanMutateDraftReimbursement|currentUser.id"
---

<objective>
Close Phase 24 Wave 2 gaps by implementing the reimbursement application state helper, service layer, route module and API registration.

Purpose: make the Wave 0 route contract green for application list/detail/create/edit/submit while leaving file IO behavior to Wave 3.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-api/24-CONTEXT.md
@.planning/phases/24-api/24-RESEARCH.md
@.planning/phases/24-api/24-VALIDATION.md
@.planning/phases/24-api/24-VERIFICATION.md
@.planning/phases/24-api/24-01-PLAN.md
@.planning/phases/24-api/24-02-PLAN.md
@backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts
@backend/src/modules/visit/visit.route.ts
@backend/src/modules/approval/application-submission.service.ts
@backend/src/modules/approval/application.route.ts
@backend/src/modules/approval/state-machine.ts
@backend/src/index.ts
</context>

<source_audit>
## Phase 24 Gap Coverage

| Gap Source | Missing Item | Covered By |
|------------|--------------|------------|
| 24-VERIFICATION | API supports create/submit/list/detail | Tasks 2 and 3 service + route |
| 24-VERIFICATION | Object access validation missing | Task 2 authorization helpers |
| 24-VERIFICATION | Amount/required/list filter validation missing | Tasks 1 and 2 service validation |
| 24-VALIDATION | 24-03-01 state/service test pending | Task 1 test + Task 2 implementation |
| 24-VALIDATION | 24-03-02 route/API test pending | Task 3 route + registration |
| 24-CONTEXT D-13 | Draft create/edit/submit contract | Tasks 2 and 3 |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add reimbursement service and state tests</name>
  <files>backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts</files>
  <read_first>
    - @backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts for locked route/service names.
    - @backend/src/modules/approval/state-machine.ts for compact transition guard assertions.
    - @backend/src/modules/approval/application-submission.service.ts for pagination and date filter helper expectations.
    - @.planning/phases/24-api/24-CONTEXT.md decisions D-01 through D-14 and D-21 through D-23.
  </read_first>
  <action>
    Create `backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts` with `bun:test`. Import these future exports from `../reimbursement.service` and `../reimbursement.state`: `REIMBURSEMENT_DEPARTMENT_REVIEW_NODE`, `assertReimbursementTransition`, `canViewReimbursement`, `normalizeReimbursementListFilters`, `normalizeReimbursementWriteInput`, and `serializeReimbursementDetail`. Add tests that assert: `assertReimbursementTransition('DRAFT', 'DEPARTMENT_REVIEW')` does not throw; `assertReimbursementTransition('DEPARTMENT_REVIEW', 'DRAFT')` throws `BizError`; `normalizeReimbursementWriteInput` trims `title`, `category`, `reason`, `payeeInfo`, `remark`, parses `occurredAt` into a `Date`, converts amount `'123.456'` to `'123.46'`, and rejects amount `0`, blank title, blank category, blank reason, and invalid date; `normalizeReimbursementListFilters({ page: '0', size: '999', dateFrom: '2026-05-01', dateTo: '2026-05-02' })` returns `page: 1`, `size: 100`, `dateFrom: new Date('2026-05-01T00:00:00.000Z')`, and `dateTo: new Date('2026-05-02T23:59:59.999Z')`; `canViewReimbursement` returns true for applicant, ADMIN role, `reimbursement:list`, same-department `reimbursement:department-review` on `DEPARTMENT_REVIEW`, and `reimbursement:finance-review` on `FINANCE_REVIEW`, but false for unrelated employee; and `serializeReimbursementDetail` returns ISO date strings plus `attachments` and `actions` arrays.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path 'backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts'"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "normalizeReimbursementWriteInput|normalizeReimbursementListFilters|canViewReimbursement|assertReimbursementTransition|serializeReimbursementDetail" backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts` finds all service/state helper contracts.
    - `rg "123\.46|2026-05-02T23:59:59\.999Z|reimbursement:department-review|reimbursement:finance-review|REIMBURSEMENT_DEPARTMENT_REVIEW_NODE" backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts` finds exact validation and visibility assertions.
    - `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts` fails only until Tasks 2 and 3 implement the service exports.
  </acceptance_criteria>
  <done>Service and state behavior is pinned before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reimbursement state, service and object authorization</name>
  <files>backend/src/modules/reimbursement/reimbursement.state.ts, backend/src/modules/reimbursement/reimbursement.service.ts</files>
  <read_first>
    - @backend/src/modules/reimbursement/__tests__/reimbursement.service.test.ts for exact helper exports and expected behavior.
    - @backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts for route-visible helper names.
    - @backend/src/modules/approval/application-submission.service.ts for applicant snapshot, pagination, date filters and generated number style.
    - @backend/src/modules/approval/application.service.ts for transaction-bound action append style.
    - @backend/src/modules/approval/state-machine.ts for `BizError` transition guard style.
    - @backend/src/utils/errors.ts for `BizError`, `notFound` and forbidden status patterns.
  </read_first>
  <action>
    Create `backend/src/modules/reimbursement/reimbursement.state.ts`. Export `REIMBURSEMENT_DEPARTMENT_REVIEW_NODE = '部门初审'`, `TERMINAL_REIMBURSEMENT_STATUSES = ['APPROVED', 'REJECTED'] as const`, `canTransitionReimbursement(from, to)`, and `assertReimbursementTransition(from, to)`. The allowed transition map must be: `DRAFT -> DEPARTMENT_REVIEW`, `DEPARTMENT_REVIEW -> FINANCE_REVIEW or REJECTED`, `FINANCE_REVIEW -> APPROVED or REJECTED`, and terminal statuses have no outgoing transitions. Throw `new BizError(\`非法报销状态流转: ${from} -> ${to}\`, 400, 'INVALID_REIMBURSEMENT_TRANSITION')` when invalid.

    Create `backend/src/modules/reimbursement/reimbursement.service.ts`. Export `MAX_REIMBURSEMENT_PAGE_SIZE = 100`, `ReimbursementActor = { id: number; name: string; roleCodes: string[]; permissions: string[] }`, `ReimbursementWriteInput`, `ReimbursementListFilters`, `normalizeReimbursementWriteInput`, `normalizeReimbursementListFilters`, `canViewReimbursement`, `assertCanViewReimbursement`, `assertCanMutateDraftReimbursement`, `serializeReimbursementRow`, `serializeReimbursementDetail`, `listReimbursements`, `getReimbursementDetail`, `createReimbursementDraft`, `updateReimbursementDraft`, and `submitReimbursementDraft`.

    `normalizeReimbursementWriteInput` must only accept `title`, `category`, `occurredAt`, `amount`, `reason`, `payeeInfo`, and `remark`. Trim strings. Require non-empty `title`, `category`, and `reason`; parse `occurredAt` as a valid `Date`; parse `amount` as a finite number greater than 0 and return amount as `amount.toFixed(2)`. Throw `BizError` with clear Chinese messages for invalid title/category/reason/date/amount.

    `normalizeReimbursementListFilters` must return `{ page, size, status, category, dateFrom, dateTo, keyword }`, default page to 1, cap size at 100, accept only `DRAFT`, `DEPARTMENT_REVIEW`, `FINANCE_REVIEW`, `APPROVED`, `REJECTED` or blank status, trim optional category/keyword, parse `dateFrom` to start-of-day, and parse `dateTo` to end-of-day.

    `canViewReimbursement(actor, application, actorDepartmentId?)` must return true when actor has role `ADMIN`, has `reimbursement:list`, is the applicant, has `reimbursement:department-review` and `application.status === 'DEPARTMENT_REVIEW'` and `actorDepartmentId === application.applicantDepartmentId`, or has `reimbursement:finance-review` and `application.status` is `FINANCE_REVIEW`, `APPROVED`, or `REJECTED`. `assertCanViewReimbursement` must throw `new BizError('无权查看该报销申请', 403, 'REIMBURSEMENT_VIEW_FORBIDDEN')` if false. `assertCanMutateDraftReimbursement` must allow only ADMIN or the applicant when `status === 'DRAFT'`; otherwise throw `REIMBURSEMENT_DRAFT_MUTATE_FORBIDDEN` or `INVALID_REIMBURSEMENT_STATUS`.

    `createReimbursementDraft(actor, input)` must load the applicant from `prisma.user.findUnique` selecting `id`, `realName`, `departmentId`, `department.name`, and `status`. It must reject missing/disabled users, generate `applicationNo` as `REIM-${YYYYMMDD}-${nanoid(8).toUpperCase()}`, create a `reimbursementApplication` with applicant and department snapshots, `status: 'DRAFT'`, and normalized fields. `updateReimbursementDraft` must load by id, call `assertCanMutateDraftReimbursement`, and update only normalized writable fields. `submitReimbursementDraft` must run in `prisma.$transaction`, call `assertReimbursementTransition('DRAFT', 'DEPARTMENT_REVIEW')`, update status to `DEPARTMENT_REVIEW`, set `submittedAt`, and create a `reimbursementAction` row with `type: 'SUBMIT'`, `nodeName: REIMBURSEMENT_DEPARTMENT_REVIEW_NODE`, `actorId`, `actorName`, and `comment: null`. `listReimbursements` must build a Prisma `where` with visibility OR conditions, filters for `status`, `category`, `occurredAt.gte/lte`, and keyword OR on `applicationNo`, `title`, `category`, `reason`, `applicantName`, `applicantDepartmentName`, use `skip/take`, order by `createdAt desc`, include `_count.attachments`, and return `{ rows, total, page, size }`. `getReimbursementDetail` must include `attachments` and `actions`, call `assertCanViewReimbursement`, and return serialized detail.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "REIMBURSEMENT_DEPARTMENT_REVIEW_NODE|INVALID_REIMBURSEMENT_TRANSITION|DRAFT.*DEPARTMENT_REVIEW|FINANCE_REVIEW.*APPROVED" backend/src/modules/reimbursement/reimbursement.state.ts` finds the state guard constants and transitions.
    - `rg "normalizeReimbursementWriteInput|amount\.toFixed\(2\)|MAX_REIMBURSEMENT_PAGE_SIZE = 100|normalizeReimbursementListFilters|canViewReimbursement|assertCanViewReimbursement|assertCanMutateDraftReimbursement" backend/src/modules/reimbursement/reimbursement.service.ts` finds validation and auth helpers.
    - `rg "REIM-\$\{date\}-\$\{nanoid\(8\)\.toUpperCase\(\)\}|prisma\.\$transaction|reimbursementAction\.create|type: 'SUBMIT'|status: 'DEPARTMENT_REVIEW'" backend/src/modules/reimbursement/reimbursement.service.ts` finds number generation and submit action behavior.
    - `rg "applicantName|applicantDepartmentName|_count|attachments|actions|rows, total, page, size" backend/src/modules/reimbursement/reimbursement.service.ts` finds serialization and list/detail behavior.
    - `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts` exits 0.
  </acceptance_criteria>
  <done>Application service, state transition and object authorization gaps are closed.</done>
</task>

<task type="auto">
  <name>Task 3: Add reimbursement route module and register it</name>
  <files>backend/src/modules/reimbursement/reimbursement.route.ts, backend/src/index.ts</files>
  <read_first>
    - @backend/src/modules/reimbursement/reimbursement.service.ts for exported service functions and auth helpers.
    - @backend/src/modules/reimbursement/__tests__/reimbursement.route.test.ts for exact schema, serializer, source-string and route signature contracts.
    - @backend/src/modules/visit/visit.route.ts for Elysia route module, TypeBox schema and serializer style.
    - @backend/src/modules/approval/application.route.ts for applicant-owned create/list/detail/submit route patterns.
    - @backend/src/index.ts for existing `/api/v1` module registration order.
  </read_first>
  <action>
    Create `backend/src/modules/reimbursement/reimbursement.route.ts`. Import `Elysia, t`, `authGuard`, `BizError`, and service functions. Export `reimbursementListQuery = t.Object({ page: t.Optional(t.String()), size: t.Optional(t.String()), status: t.Optional(t.String()), category: t.Optional(t.String()), dateFrom: t.Optional(t.String()), dateTo: t.Optional(t.String()), keyword: t.Optional(t.String()) })`. Export `reimbursementWriteBody` as `t.Object({ title: t.String({ minLength: 1 }), category: t.String({ minLength: 1 }), occurredAt: t.String(), amount: t.Union([t.String(), t.Number()]), reason: t.String({ minLength: 1 }), payeeInfo: t.Optional(t.Union([t.String(), t.Null()])), remark: t.Optional(t.Union([t.String(), t.Null()])) }, { additionalProperties: false })`. Do not include `id`, `applicationNo`, `status`, `applicantId`, `applicantName`, `applicantDepartmentId`, `applicantDepartmentName`, `submittedAt`, `completedAt`, `createdAt`, `updatedAt`, `attachments`, or `actions` in `reimbursementWriteBody`.

    Add `toReimbursementActor(currentUser)` returning `{ id: currentUser.id, name: currentUser.realName || currentUser.username || String(currentUser.id), roleCodes: currentUser.roleCodes ?? [], permissions: currentUser.permissions ?? [] }`. Export `serializeReimbursementListResponse(response)` that returns `{ rows: response.rows.map(serializeReimbursementRow), total: response.total, page: response.page, size: response.size }`. Keep the literal source expression `dateTo + 'T23:59:59.999Z'` in route-level filter normalization or a small comment adjacent to date handling so the Wave 0 route contract remains stable.

    Export `reimbursementModule = new Elysia({ prefix: '/reimbursements' })`. Add `GET /` guarded by `authGuard('reimbursement:own')` and returning `serializeReimbursementListResponse(await listReimbursements(toReimbursementActor(currentUser), query))`. Add `GET /:id` guarded by `authGuard('reimbursement:own')`, calling `getReimbursementDetail`, and ensure the route source contains `assertCanViewReimbursement`. Add `POST /` guarded by `authGuard('reimbursement:create')`, passing `body` and `currentUser.id`-derived actor to `createReimbursementDraft`; do not write `data: body` or `applicantId: body`. Add `PUT /:id` guarded by `authGuard('reimbursement:create')`, calling `assertCanMutateDraftReimbursement` through the service update flow and `updateReimbursementDraft`. Add `POST /:id/submit` guarded by `authGuard('reimbursement:create')`, calling `submitReimbursementDraft`.

    Declare Wave 3 attachment route signatures now so the route contract stays green: `POST /:id/attachments`, `GET /:id/attachments/:attachmentId/preview`, `GET /:id/attachments/:attachmentId/download`, and `DELETE /:id/attachments/:attachmentId`, each guarded by `authGuard('reimbursement:attachment')` and temporarily throwing `new BizError('报销附件接口待实现', 501, 'REIMBURSEMENT_ATTACHMENT_NOT_READY')`. Do not import `reimbursement-file.service.ts` in this plan; Wave 3 replaces these handlers with real file IO.

    Update `backend/src/index.ts`: import `{ reimbursementModule } from './modules/reimbursement/reimbursement.route'` and add `.use(reimbursementModule)` inside the `/api/v1` group after `.use(visitModule)` or near other business modules.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.route.test.ts src/modules/reimbursement/__tests__/reimbursement.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export const reimbursementListQuery|export const reimbursementWriteBody|additionalProperties: false|serializeReimbursementListResponse|export const reimbursementModule" backend/src/modules/reimbursement/reimbursement.route.ts` finds route exports and strict schema.
    - `rg "authGuard\('reimbursement:own'\)|authGuard\('reimbursement:create'\)|authGuard\('reimbursement:list'\)|authGuard\('reimbursement:attachment'\)|currentUser\.id|assertCanViewReimbursement|assertCanMutateDraftReimbursement" backend/src/modules/reimbursement/reimbursement.route.ts` finds guards and object-auth hooks. If `authGuard('reimbursement:list')` is not used on a separate route because `/` uses own-scope plus service visibility, include a source comment `// authGuard('reimbursement:list') is enforced through service-level visibility for privileged list users.` so the Wave 0 source contract remains explicit.
    - `rg "GET /|POST /|PUT /:id|POST /:id/submit|attachments/:attachmentId/preview|attachments/:attachmentId/download" backend/src/modules/reimbursement/reimbursement.route.ts` finds route signatures.
    - `rg "reimbursementModule|modules/reimbursement/reimbursement.route|use\(reimbursementModule\)" backend/src/index.ts` confirms `/api/v1` registration.
    - `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.route.test.ts src/modules/reimbursement/__tests__/reimbursement.service.test.ts` exits 0 after Wave 2 implementation.
  </acceptance_criteria>
  <done>Application route signatures, schemas, serialization and `/api/v1` registration are complete.</done>
</task>

</tasks>

<threat_model>
| Threat | Area | Mitigation in this plan |
|--------|------|-------------------------|
| Trusted-field tampering | Route/service | Strict `reimbursementWriteBody`; service derives applicant fields from current user. |
| Object access bypass | List/detail | Centralized `canViewReimbursement` and assert helpers used by routes and later attachment handlers. |
| Submitted core field mutation | State | `assertCanMutateDraftReimbursement` blocks non-DRAFT edits. |
| Missing audit trail | Submit | Transaction appends `ReimbursementAction` `SUBMIT` when moving to department review. |
</threat_model>

<verification>
Wave 2 gate: `cd backend && bun test src/modules/reimbursement/__tests__/reimbursement.service.test.ts src/modules/reimbursement/__tests__/reimbursement.route.test.ts` passes, and `backend/src/index.ts` registers `reimbursementModule` under `/api/v1`.
</verification>

<success_criteria>
Phase 24 application API gaps are closed for create, edit, submit, list, detail, pagination, filter parsing, serialization and object authorization, while attachment file IO remains explicitly scoped to Wave 3.
</success_criteria>

<output>
After completion, create `.planning/phases/24-api/24-03-SUMMARY.md`.
</output>
