---
phase: 09-data-view-print-stats
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - frontend/src/components/submission/SubmissionDetail.vue
  - frontend/src/pages/SubmissionPage.vue
  - frontend/src/router/routes.ts
  - frontend/src/pages/TemplatePage.vue
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]
must_haves:
  truths:
    - "用户在模板列表页点击'查看数据'按钮可导航到 /templates/:id/submissions"
    - "提交列表页展示 QTable 含序号/填写者姓名/手机号/提交时间/分享人/操作列，支持分页和筛选"
    - "点击'查看'打开右侧 QDrawer 展示提交详情（表格式字段名+值 + 签名图片）"
    - "抽屉内'打印'按钮调用 window.print()，打印排版仅含 #print-area 内容"
    - "抽屉内'导出 PDF'按钮调用 exportToPdf，下载 {模板名称}_{提交时间}.pdf"
    - "列表页支持多选 + 批量导出 PDF，含进度对话框"
  artifacts:
    - path: "frontend/src/components/submission/SubmissionDetail.vue"
      provides: "提交详情组件（抽屉内容 + 打印区域）"
      min_lines: 80
    - path: "frontend/src/pages/SubmissionPage.vue"
      provides: "提交列表页（QTable + 筛选 + QDrawer + 批量导出）"
      min_lines: 150
    - path: "frontend/src/router/routes.ts"
      provides: "新增 submissions 路由"
      contains: "templates/:id/submissions"
    - path: "frontend/src/pages/TemplatePage.vue"
      provides: "新增'查看数据'按钮"
      contains: "form:submission:list"
  key_links:
    - from: "frontend/src/pages/SubmissionPage.vue"
      to: "frontend/src/stores/submission.ts"
      via: "useSubmissionStore()"
      pattern: "useSubmissionStore"
    - from: "frontend/src/pages/SubmissionPage.vue"
      to: "frontend/src/composables/usePdfExport.ts"
      via: "import { exportToPdf, exportBatchToPdf }"
      pattern: "exportToPdf"
    - from: "frontend/src/pages/SubmissionPage.vue"
      to: "frontend/src/components/submission/SubmissionDetail.vue"
      via: "component import"
      pattern: "SubmissionDetail"
---

<objective>
创建提交数据列表页和详情抽屉，实现数据查看、筛选、浏览器打印和 PDF 导出（单条+批量）。

Purpose: 实现 DATA-01（列表+筛选）、DATA-02（详情查看）、DATA-03（浏览器打印）、DATA-04（PDF 导出）的完整前端交互。
Output: SubmissionDetail.vue + SubmissionPage.vue + 路由注册 + TemplatePage 入口按钮
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-view-print-stats/09-RESEARCH.md
@.planning/phases/09-data-view-print-stats/09-CONTEXT.md
@.planning/phases/09-data-view-print-stats/09-PATTERNS.md
@.planning/phases/09-data-view-print-stats/09-UI-SPEC.md
@.planning/phases/09-data-view-print-stats/09-02-SUMMARY.md

<interfaces>
<!-- Plan 02 创建的接口 — 执行者直接使用 -->

From frontend/src/stores/submission.ts:
```typescript
export interface SubmissionRow {
  id: number;
  data: Record<string, any>;
  schemaVersion: number;
  submitterName: string | null;
  submitterPhone: string | null;
  createdAt: string;
  shareLink: {
    id: number;
    creator: { id: number; realName: string };
  };
}

export interface SubmissionDetail extends SubmissionRow {
  template: {
    name: string;
    schema: any[];
    schemaVersion: number;
  };
}

export interface SharerOption {
  id: number;
  realName: string;
}

export const useSubmissionStore = defineStore('submission', {
  state: () => ({ rows, total, loading, page, size }),
  actions: {
    fetchList(templateId, filters?),
    fetchDetail(templateId, submissionId): Promise<SubmissionDetail>,
    fetchSharers(templateId): Promise<SharerOption[]>,
  },
});
```

From frontend/src/composables/usePdfExport.ts:
```typescript
export async function exportToPdf(element: HTMLElement, filename: string): Promise<void>;
export async function exportBatchToPdf(
  renderFn: (index: number) => Promise<HTMLElement>,
  total: number,
  filename: string,
  onProgress?: (current: number, total: number) => void,
  cancelRef?: { value: boolean },
): Promise<boolean>;
```

From frontend/src/composables/useResponsive.ts:
```typescript
export function useResponsive(): { isDesktop: ComputedRef<boolean>; isMobile: ComputedRef<boolean> };
```

From frontend/src/components/EmptyState.vue:
```typescript
// props: icon, title, description, ctaText?
// emit: action
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SubmissionDetail.vue 详情组件</name>
  <files>frontend/src/components/submission/SubmissionDetail.vue</files>
  <read_first>
    frontend/src/components/public-fill/FormFieldRenderer.vue
    frontend/src/stores/submission.ts
    frontend/src/assets/print.css
  </read_first>
  <action>
创建 `frontend/src/components/submission/SubmissionDetail.vue`（需先创建 `frontend/src/components/submission/` 目录）。

这是只读详情展示组件，同时作为打印区域（#print-area）。

Props 定义：
```typescript
const props = defineProps<{
  submission: SubmissionDetail;
  templateName: string;
}>();

const emit = defineEmits<{
  print: [];
  exportPdf: [];
  close: [];
}>();
```

模板结构（严格遵循 09-UI-SPEC.md Surface B）：

```html
<div class="drawer-content">
  <!-- 抽屉头部 — 打印时隐藏 -->
  <div class="drawer-header row items-center justify-between q-mb-md">
    <div style="font-size: 20px; font-weight: 600">提交详情</div>
    <q-btn flat round icon="close" @click="emit('close')" />
  </div>

  <!-- 操作按钮 — 打印时隐藏 -->
  <div class="drawer-actions row q-gutter-sm q-mb-md">
    <q-btn outline icon="print" label="打印" @click="emit('print')" aria-label="打印当前提交数据" />
    <q-btn outline icon="picture_as_pdf" label="导出 PDF" @click="emit('exportPdf')" />
  </div>

  <!-- 打印区域 -->
  <div id="print-area" class="detail-body">
    <!-- 打印表头 -->
    <div class="print-header" style="text-align: center; margin-bottom: 16px">
      <div style="font-size: 16pt; font-weight: 600">{{ templateName }}</div>
      <div style="font-size: 12pt; color: var(--oa-text-secondary)">
        提交时间：{{ formatDate(submission.createdAt) }}
      </div>
    </div>

    <!-- 字段表格 -->
    <table class="detail-table" style="width: 100%; border-collapse: collapse">
      <tr v-for="field in displayFields" :key="field.id">
        <th style="...">{{ field.label }}</th>
        <td>{{ formatFieldValue(field) }}</td>
      </tr>
    </table>

    <!-- 签名区域 -->
    <div v-if="signatureField" class="signature-section" style="margin-top: 16px">
      <div style="font-size: 14px; font-weight: 600; margin-bottom: 8px">签名</div>
      <img :src="signatureField.value" style="max-width: 300px; max-height: 150px; border: 1px solid var(--oa-border)" alt="手写签名" />
    </div>

    <!-- 版本不一致提示 -->
    <q-banner v-if="versionMismatch" class="q-mt-md" type="warning" dense>
      字段定义可能已更新，显示为原始提交数据
    </q-banner>
  </div>
</div>
```

Script 逻辑：

```typescript
import { computed } from 'vue';
import type { SubmissionDetail } from 'src/stores/submission';

// 版本是否一致
const versionMismatch = computed(() =>
  props.submission.schemaVersion !== props.submission.template.schemaVersion
);

// 解析字段展示列表
const displayFields = computed(() => {
  const schema = props.submission.template.schema as any[];
  const data = props.submission.data as Record<string, any>;

  if (!versionMismatch.value && Array.isArray(schema)) {
    // 版本一致：用 schema 字段定义渲染
    return schema
      .filter((f: any) => f.type !== 'signature')
      .map((f: any) => ({
        id: f.id,
        label: f.label || f.id,
        value: data[f.id],
        type: f.type,
        options: f.options,
      }));
  } else {
    // 版本不一致：直接展示 key-value
    return Object.entries(data)
      .filter(([key]) => !key.startsWith('signature'))
      .map(([key, value]) => ({
        id: key,
        label: key,
        value,
        type: 'text',
        options: undefined,
      }));
  }
});

// 签名字段
const signatureField = computed(() => {
  const schema = props.submission.template.schema as any[];
  const data = props.submission.data as Record<string, any>;
  const sigField = schema?.find((f: any) => f.type === 'signature');
  if (sigField && data[sigField.id]) {
    return { value: data[sigField.id] };
  }
  // fallback: 查找 data 中以 signature 开头的 key
  const sigKey = Object.keys(data).find(k => k.startsWith('signature'));
  if (sigKey && data[sigKey]) {
    return { value: data[sigKey] };
  }
  return null;
});

// 格式化字段值
function formatFieldValue(field: { value: any; type: string; options?: any[] }) {
  if (field.value == null || field.value === '') return '—';
  if (field.type === 'checkbox' && Array.isArray(field.value)) {
    return field.value.join('、');
  }
  return String(field.value);
}

// 格式化日期
function formatDate(dateStr: string) {
  const d = new Date(dateStr);
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')} ${String(d.getHours()).padStart(2, '0')}:${String(d.getMinutes()).padStart(2, '0')}`;
}
```

屏幕上的 detail-table 样式（非打印时）需要内联或 scoped style：
```css
.detail-table th,
.detail-table td {
  padding: 12px 16px;
  text-align: left;
  border-bottom: 1px solid var(--oa-border, #e5e7eb);
}
.detail-table th {
  font-size: 14px;
  font-weight: 600;
  width: 30%;
  color: var(--oa-text-secondary);
  background: var(--oa-hover, #f8fafc);
}
.detail-table td {
  font-size: 16px;
}
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && grep -n "id=\"print-area\"" src/components/submission/SubmissionDetail.vue && grep -n "detail-table" src/components/submission/SubmissionDetail.vue && grep -n "signature-section" src/components/submission/SubmissionDetail.vue && grep -n "versionMismatch" src/components/submission/SubmissionDetail.vue</automated>
  </verify>
  <acceptance_criteria>
    - SubmissionDetail.vue 存在于 frontend/src/components/submission/ 目录
    - 组件包含 `id="print-area"` 的打印区域 div
    - 组件包含 `class="detail-table"` 的字段表格
    - 组件包含 `class="signature-section"` 的签名区域，内含 `<img :src=`
    - 组件包含 `versionMismatch` computed，比较 submission.schemaVersion 与 template.schemaVersion
    - 组件包含 `class="drawer-header"` 和 `class="drawer-actions"`（打印时被 print.css 隐藏）
    - 组件 emit print, exportPdf, close 三个事件
    - 组件 props 接收 submission: SubmissionDetail 和 templateName: string
    - 版本不一致时显示 QBanner 警告"字段定义可能已更新，显示为原始提交数据"
  </acceptance_criteria>
  <done>SubmissionDetail.vue 完整实现只读详情展示，含打印区域、签名图片、版本不一致提示</done>
</task>

<task type="auto">
  <name>Task 2: SubmissionPage.vue + 路由注册 + TemplatePage 入口按钮</name>
  <files>frontend/src/pages/SubmissionPage.vue, frontend/src/router/routes.ts, frontend/src/pages/TemplatePage.vue</files>
  <read_first>
    frontend/src/pages/UserPage.vue
    frontend/src/pages/TemplatePage.vue
    frontend/src/router/routes.ts
    frontend/src/components/submission/SubmissionDetail.vue
    frontend/src/stores/submission.ts
    frontend/src/composables/usePdfExport.ts
    frontend/src/components/EmptyState.vue
  </read_first>
  <action>
1. 创建 `frontend/src/pages/SubmissionPage.vue`，严格遵循 UserPage.vue 的 QTable + 分页 + 筛选模式，结合 09-UI-SPEC.md Surface A 规范。

核心结构：

```html
<template>
  <q-page padding>
    <!-- 页面头部 -->
    <div class="row items-center q-mb-md">
      <q-btn flat icon="arrow_back" @click="$router.push('/templates')" />
      <div style="font-size: 20px; font-weight: 600">提交数据 — {{ templateName }}</div>
    </div>

    <!-- 筛选栏（PC 水平排列） -->
    <div v-if="isDesktop" class="row items-center q-mb-md q-gutter-sm">
      <q-input v-model="filters.submitterName" outlined dense label="填写者姓名" style="width: 160px" @keyup.enter="load(1)" clearable />
      <q-input v-model="filters.submitterPhone" outlined dense label="手机号" style="width: 160px" @keyup.enter="load(1)" clearable />
      <q-input v-model="filters.dateFrom" outlined dense label="开始日期" style="width: 150px">
        <template #append><q-icon name="event" class="cursor-pointer"><q-popup-proxy><q-date v-model="filters.dateFrom" mask="YYYY-MM-DD" /></q-popup-proxy></q-icon></template>
      </q-input>
      <q-input v-model="filters.dateTo" outlined dense label="结束日期" style="width: 150px">
        <template #append><q-icon name="event" class="cursor-pointer"><q-popup-proxy><q-date v-model="filters.dateTo" mask="YYYY-MM-DD" /></q-popup-proxy></q-icon></template>
      </q-input>
      <q-select v-model="filters.sharerId" :options="sharerOptions" label="分享人" outlined dense emit-value map-options clearable style="width: 160px" />
      <q-btn color="primary" label="查询" dense @click="load(1)" />
      <q-btn flat label="重置" dense @click="resetFilters" />
    </div>

    <!-- 加载骨架屏 -->
    <div v-if="firstLoading" class="q-pa-md">
      <q-skeleton type="rect" height="40px" class="q-mb-sm" />
      <q-skeleton v-for="i in 5" :key="i" type="rect" height="48px" class="q-mb-xs" />
    </div>

    <!-- 空状态 -->
    <EmptyState v-else-if="store.rows.length === 0 && !store.loading" icon="inbox" title="暂无提交数据" description="该模板还没有收到任何提交" />

    <!-- QTable（PC） -->
    <q-table v-else-if="isDesktop"
      :rows="store.rows" :columns="columns" row-key="id"
      :loading="store.loading" :pagination="pagination"
      @request="onReq" flat bordered
      selection="multiple" v-model:selected="selected"
    >
      <template #body-cell-index="props">
        <q-td :props="props">{{ props.rowIndex + 1 + (pagination.page - 1) * pagination.rowsPerPage }}</q-td>
      </template>
      <template #body-cell-submitterName="props">
        <q-td :props="props">{{ props.row.submitterName || '—' }}</q-td>
      </template>
      <template #body-cell-submitterPhone="props">
        <q-td :props="props">{{ props.row.submitterPhone || '—' }}</q-td>
      </template>
      <template #body-cell-createdAt="props">
        <q-td :props="props">{{ formatDate(props.row.createdAt) }}</q-td>
      </template>
      <template #body-cell-sharer="props">
        <q-td :props="props">{{ props.row.shareLink?.creator?.realName || '—' }}</q-td>
      </template>
      <template #body-cell-actions="props">
        <q-td :props="props">
          <q-btn size="sm" flat dense icon="visibility" @click="openDetail(props.row)" />
          <q-btn size="sm" flat dense icon="print" @click="openAndPrint(props.row)" />
          <q-btn size="sm" flat dense icon="picture_as_pdf" @click="openAndExport(props.row)" />
        </q-td>
      </template>
    </q-table>

    <!-- 移动端卡片列表 -->
    <div v-else class="q-gutter-sm">
      <q-card v-for="row in store.rows" :key="row.id" flat bordered @click="openDetail(row)">
        <q-card-section>
          <div class="row items-center">
            <div class="text-subtitle1">{{ row.submitterName || '匿名' }}</div>
            <q-space />
            <div class="text-caption">{{ formatDate(row.createdAt) }}</div>
          </div>
          <div class="text-caption q-mt-xs">分享人：{{ row.shareLink?.creator?.realName || '—' }}</div>
        </q-card-section>
      </q-card>
    </div>

    <!-- 批量操作栏 -->
    <div v-if="selected.length > 0" class="fixed-bottom q-pa-md" style="background: var(--oa-surface); border-top: 1px solid var(--oa-border); z-index: 100">
      <div class="row items-center justify-between">
        <span>已选 {{ selected.length }} 条</span>
        <q-btn color="primary" icon="picture_as_pdf" label="批量导出 PDF" :loading="batchExporting" @click="handleBatchExport" />
      </div>
    </div>

    <!-- QDrawer 详情抽屉 -->
    <q-drawer v-model="drawerOpen" side="right" :width="isMobile ? undefined : 480" bordered overlay :maximized="isMobile">
      <SubmissionDetail v-if="currentDetail"
        :submission="currentDetail" :template-name="templateName"
        @print="handlePrint" @export-pdf="handleExportPdf" @close="drawerOpen = false"
      />
    </q-drawer>

    <!-- 批量导出进度对话框 -->
    <q-dialog v-model="batchDialogOpen" persistent no-backdrop-dismiss>
      <q-card style="min-width: 320px">
        <q-card-section>
          <div style="font-size: 16px; font-weight: 600">正在导出 PDF...</div>
          <div style="font-size: 14px" class="q-mt-sm">{{ batchProgress.current }}/{{ batchProgress.total }} 条</div>
          <q-linear-progress :value="batchProgress.current / batchProgress.total" color="primary" size="8px" class="q-mt-md" />
        </q-card-section>
        <q-card-actions align="right">
          <q-btn flat label="取消" @click="cancelBatch" />
        </q-card-actions>
      </q-card>
    </q-dialog>
  </q-page>
</template>
```

Script 逻辑要点：
- `const templateId = computed(() => Number(route.params.id))`
- `const store = useSubmissionStore()`
- `const { isDesktop, isMobile } = useResponsive()`
- columns 定义：序号(index)、填写者姓名(submitterName)、手机号(submitterPhone)、提交时间(createdAt)、分享人(sharer)、操作(actions)
- pagination ref: `{ page: 1, rowsPerPage: 20, rowsNumber: 0 }`
- filters reactive: `{ submitterName: '', submitterPhone: '', dateFrom: '', dateTo: '', sharerId: null }`
- onMounted: fetchSharers + load(1)
- load(page): 调用 store.fetchList(templateId, filters)，更新 pagination.rowsNumber = store.total
- openDetail(row): 调用 store.fetchDetail → 设置 currentDetail → drawerOpen = true
- handlePrint: `window.print()`
- handleExportPdf: `exportToPdf(document.getElementById('print-area')!, filename)`，filename = `${templateName}_${formatDate(currentDetail.createdAt)}.pdf`
- handleBatchExport: 限制 50 条，调用 exportBatchToPdf
- 批量导出 renderFn: 逐条 fetchDetail → 设置 currentDetail → nextTick → 获取 #print-area 元素
- sharerOptions: 从 fetchSharers 结果映射为 `[{ label: realName, value: id }]`

2. 在 `frontend/src/router/routes.ts` 的 MainLayout children 数组中，在 `templates/:id/design` 路由之后追加：
```typescript
{ path: 'templates/:id/submissions', component: () => import('pages/SubmissionPage.vue'), meta: { title: '提交数据', perm: 'form:submission:list' } },
```

3. 在 `frontend/src/pages/TemplatePage.vue` 的 QTable body-cell-actions 模板中，在现有分享按钮之后追加"查看数据"按钮：
```html
<q-btn v-perm="'form:submission:list'" size="sm" flat dense icon="visibility" color="primary"
       @click="$router.push(`/templates/${props.row.id}/submissions`)" />
```
注意：此按钮不需要 `v-if="props.row.status === 'PUBLISHED'"` 条件，因为即使模板下线也应能查看历史提交数据。
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && grep -n "SubmissionPage" src/router/routes.ts && grep -n "form:submission:list" src/router/routes.ts && grep -n "form:submission:list" src/pages/TemplatePage.vue && grep -n "useSubmissionStore" src/pages/SubmissionPage.vue && grep -n "SubmissionDetail" src/pages/SubmissionPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - SubmissionPage.vue 存在且包含 `useSubmissionStore` 导入
    - SubmissionPage.vue 包含 QTable 组件，columns 含 index/submitterName/submitterPhone/createdAt/sharer/actions
    - SubmissionPage.vue 包含 `selection="multiple"` 和 `v-model:selected`
    - SubmissionPage.vue 包含 QDrawer 组件 `side="right"` 内嵌 SubmissionDetail
    - SubmissionPage.vue 包含批量导出进度 QDialog（"正在导出 PDF..."）
    - SubmissionPage.vue 包含 `window.print()` 调用
    - SubmissionPage.vue 包含 `exportToPdf` 和 `exportBatchToPdf` 导入
    - SubmissionPage.vue 包含 EmptyState 组件（icon="inbox", title="暂无提交数据"）
    - routes.ts 包含 `path: 'templates/:id/submissions'` 路由，meta.perm = 'form:submission:list'
    - TemplatePage.vue body-cell-actions 模板包含 `v-perm="'form:submission:list'"` 的 visibility 按钮
    - TemplatePage.vue 该按钮 @click 导航到 `/templates/${props.row.id}/submissions`
  </acceptance_criteria>
  <done>SubmissionPage 完整实现列表+筛选+详情抽屉+打印+PDF导出+批量导出，路由和入口按钮已注册</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route params → API call | templateId 从 URL 参数获取，传入 API 调用 |
| submission.data → DOM render | 用户提交的数据渲染到页面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Spoofing | route params | mitigate | templateId 通过 Number() 转换，非数字值变为 NaN，API 返回 404 |
| T-09-10 | Tampering | submission.data render | mitigate | Vue 模板默认转义 HTML，{{ }} 不执行脚本；签名图片使用 :src 绑定 base64 data URI |
| T-09-11 | Information Disclosure | SubmissionPage | mitigate | 路由 meta.perm = 'form:submission:list'，前端路由守卫 + 后端 authGuard 双重保护 |
| T-09-12 | Denial of Service | batch PDF export | mitigate | 批量导出上限 50 条，超出显示警告并截取；逐条处理释放内存 |
</threat_model>

<verification>
1. 访问 `/templates/1/submissions` 页面加载无报错
2. QTable 显示列表数据，分页可切换
3. 点击"查看"打开抽屉，显示详情
4. 抽屉内"打印"按钮触发浏览器打印对话框
5. 抽屉内"导出 PDF"按钮下载 PDF 文件
</verification>

<success_criteria>
- 提交列表页完整可用：筛选、分页、详情查看、打印、PDF 导出
- 路由 /templates/:id/submissions 可访问
- TemplatePage 有"查看数据"入口按钮
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-view-print-stats/09-03-SUMMARY.md`
</output>
