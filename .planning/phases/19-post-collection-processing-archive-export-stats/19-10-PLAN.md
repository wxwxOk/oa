---
phase: 19-post-collection-processing-archive-export-stats
plan: 10
type: execute
wave: 5
depends_on: [19-02, 19-04, 19-06, 19-08]
files_modified:
  - frontend/src/layouts/MainLayout.vue
autonomous: true
requirements: [OPS-07]
user_setup: []
must_haves:
  truths:
    - "Logged-in users see an in-app notification button with unread count in the main header."
    - "Unread count refreshes after login/page focus/fixed interval polling without WebSocket/SSE."
    - "Users can open notifications, navigate to the target record, and mark notifications read."
  artifacts:
    - path: "frontend/src/layouts/MainLayout.vue"
      provides: "Notification header badge, dropdown/dialog list, polling, and mark-read UI"
  key_links:
    - from: "frontend/src/layouts/MainLayout.vue"
      to: "frontend/src/stores/notification.ts"
      via: "notification store actions"
      pattern: "useNotificationStore"
    - from: "frontend/src/layouts/MainLayout.vue"
      to: "/approval/tasks/:id and /approval/applications/:id"
      via: "notification targetRoute navigation"
      pattern: "targetRoute"
---

<objective>
Add in-app notification UI to the main layout.

Purpose: complete OPS-07 on the frontend with quiet polling, unread badge, notification list, and mark-read behavior.
Output: header notification control integrated into `MainLayout.vue`.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-post-collection-processing-archive-export-stats/19-CONTEXT.md decisions D-26 through D-30
@.planning/phases/19-post-collection-processing-archive-export-stats/19-UI-SPEC.md In-App Notifications
@.claude/skills/ui-ux-pro-max/SKILL.md accessibility and touch guidance
@frontend/src/layouts/MainLayout.vue
@frontend/src/stores/notification.ts
@frontend/src/types/notification.ts
@frontend/src/layouts/__tests__/MainLayoutNotification.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add header unread badge and notification list</name>
  <files>frontend/src/layouts/MainLayout.vue</files>
  <read_first>
    - @frontend/src/layouts/MainLayout.vue current toolbar and drawer layout
    - @frontend/src/stores/notification.ts store actions from plan 19-04
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-UI-SPEC.md Header badge and notification menu/list contract
    - @frontend/src/layouts/__tests__/MainLayoutNotification.test.ts Wave 0 layout contract
  </read_first>
  <behavior>
    - Header button uses icon `notifications` and `aria-label="站内通知"`.
    - Floating badge shows unread count and caps above 99 as `99+`.
    - Desktop uses a `q-menu`; mobile uses a full-screen dialog or route-like full-screen panel, not a crowded toolbar menu.
  </behavior>
  <action>
    In `MainLayout.vue`, import `useNotificationStore` and render a notification icon button near the dark-mode/user controls. Add `q-badge floating color="negative"` when `notification.unreadCount > 0`, displaying `99+` above 99. Desktop opens a `q-menu` width about 360px containing title `站内通知`, skeleton rows while loading, rows with unread dot/title/summary/created time, empty copy `暂无站内通知` and `新的待办和审批结果会显示在这里。`, and `全部标为已读` only when unread count > 0. Mobile opens a full-screen dialog with the same list and touch-safe 56px rows. Every icon-only button needs aria-label or tooltip.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/layouts/__tests__/MainLayoutNotification.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "notifications|aria-label=\"站内通知\"|q-badge|99\\+|全部标为已读|暂无站内通知" frontend/src/layouts/MainLayout.vue` finds notification UI.
    - `rg "min-height: 56px|q-menu|q-dialog" frontend/src/layouts/MainLayout.vue` finds list layout/touch handling.
    - Layout contract test exits 0.
  </acceptance_criteria>
  <done>Notification list and unread badge are visible and accessible in the main layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire polling, focus refresh, navigation, and mark-read</name>
  <files>frontend/src/layouts/MainLayout.vue</files>
  <read_first>
    - @frontend/src/stores/notification.ts actions `fetchUnreadCount`, `fetchList`, `markRead`, `markAllRead`
    - @frontend/src/router/routes.ts target route availability
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-CONTEXT.md decision D-30
  </read_first>
  <behavior>
    - Unread count refreshes on layout mount after login, on `window` focus, and fixed interval polling.
    - Polling does not steal focus or interrupt active forms.
    - Clicking a notification routes to `targetRoute` and marks it read after navigation intent is accepted.
  </behavior>
  <action>
    Add lifecycle logic to fetch unread count when authenticated, add a `window.focus` listener to refresh unread count, and start a fixed interval poll at 60 seconds while logged in. Clean listeners/interval on unmount. When opening the menu/dialog, call `notification.fetchList()`. On notification row click, route to `item.targetRoute` if present and then call `notification.markRead(item.id)`; refresh unread count after read changes. Add `全部标为已读` action calling `markAllRead`. Do not use WebSocket/SSE and do not add external notification channels.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/stores/__tests__/notification.test.ts src/layouts/__tests__/MainLayoutNotification.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "setInterval|addEventListener\\('focus'|removeEventListener\\('focus'|markRead|markAllRead|targetRoute" frontend/src/layouts/MainLayout.vue` finds polling/navigation/read wiring.
    - `rg "WebSocket|EventSource|SSE|钉钉|企业微信|短信|邮件" frontend/src/layouts/MainLayout.vue` finds nothing.
    - Notification store/layout tests and frontend build exit 0.
  </acceptance_criteria>
  <done>Notification unread count, list, navigation, and read-state updates work without realtime or external channels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout->notification API | Client polls current-user notification data from authenticated endpoints. |
| notification targetRoute->router | Backend-provided route strings are used for in-app navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-IDOR | I/E | notification list UI | mitigate | UI displays only rows returned by user-scoped backend endpoints. |
| T-19-TAMPER | T | notification read actions | mitigate | UI sends only notification id; backend constrains by current user. |
| T-19-AUDIT | R/T | notification UI | accept | Notifications are not audit records; approval/archive events remain source of truth. |
| T-19-CSV-INJECTION | T/I | notification UI | accept | No spreadsheet export in this plan. |
| T-19-EXPORT-DOS | D | polling | mitigate | Poll interval is fixed at 60 seconds and list pagination remains bounded by backend. |
| T-19-NOTIFICATION-LEAK | I | MainLayout.vue | mitigate | No client-supplied user id; display depends on current-user store results only. |
| T-19-DATA-SEPARATION | T/I | notification UI | accept | No processing/correction data mutation in this plan. |
</threat_model>

<verification>
Run notification store/layout tests and frontend build. Manual UAT should verify desktop and mobile badge placement with unread counts.
</verification>

<success_criteria>
OPS-07 frontend notification behavior is complete: badge, unread count, list, mark-read, mark-all-read, target navigation, focus refresh, and 60-second polling.
</success_criteria>

<output>
After completion, create `.planning/phases/19-post-collection-processing-archive-export-stats/19-10-SUMMARY.md`
</output>
