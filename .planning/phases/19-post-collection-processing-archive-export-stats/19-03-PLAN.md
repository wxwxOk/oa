---
phase: 19-post-collection-processing-archive-export-stats
plan: 3
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - backend/package.json
  - backend/bun.lock
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/20260426090000_add_archive_operations_notifications/migration.sql
  - backend/prisma/seed.ts
  - backend/src/modules/role/__tests__/approval-permissions.seed.test.ts
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06, OPS-07]
user_setup: []
must_haves:
  truths:
    - "Prisma can store archive operational metadata without mutating submitted form JSON."
    - "Processing field configuration exists on templates and stays separate from formal schema versioning."
    - "Notification rows are user-scoped and indexable by unread state."
    - "Only `exceljs@4.4.0` is added as a backend dependency."
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Archive metadata, archive events, processing schema, and user notification models"
      contains: "model ArchiveRecordMeta"
    - path: "backend/prisma/migrations/20260426090000_add_archive_operations_notifications/migration.sql"
      provides: "Database migration for Phase 19 schema changes"
    - path: "backend/prisma/seed.ts"
      provides: "Phase 19 operation permission seed codes"
      contains: "approval:archive:edit"
    - path: "backend/package.json"
      provides: "ExcelJS dependency"
      contains: "exceljs"
  key_links:
    - from: "backend/prisma/schema.prisma"
      to: "backend/src/modules/approval/archive.service.ts"
      via: "ArchiveRecordMeta and ArchiveEvent Prisma models"
      pattern: "archiveRecordMeta|archiveEvent"
    - from: "backend/prisma/schema.prisma"
      to: "backend/src/modules/approval/notification.service.ts"
      via: "UserNotification Prisma model"
      pattern: "userNotification"
---

<objective>
Add the Phase 19 data model, permissions, Excel dependency, and blocking Prisma gate.

Purpose: provide the database/API foundation needed by all archive, export, stats, processing-field, and notification implementation plans.
Output: updated Prisma schema/migration, generated client gate, `exceljs@4.4.0`, and approval permission seeds.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-post-collection-processing-archive-export-stats/19-CONTEXT.md
@.planning/phases/19-post-collection-processing-archive-export-stats/19-RESEARCH.md
@.planning/phases/19-post-collection-processing-archive-export-stats/19-PATTERNS.md
@backend/prisma/schema.prisma
@backend/prisma/seed.ts
@backend/src/modules/role/__tests__/approval-permissions.seed.test.ts
@backend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ExcelJS exactly once</name>
  <files>backend/package.json, backend/bun.lock</files>
  <read_first>
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-RESEARCH.md Standard Stack and dependency constraints
    - @backend/package.json
  </read_first>
  <action>
    In `backend`, install exactly `exceljs@4.4.0` using Bun. Do not upgrade Prisma, Elysia, Vue, Quasar, Chart.js, jsPDF, or html2canvas. The only new dependency in this plan is `exceljs`; let `bun.lock` update mechanically. This implements D-18 through D-21 and the dependency constraint that PDF remains frontend `html2canvas + jsPDF`.
  </action>
  <verify>
    <automated>cd backend && bun pm ls exceljs</automated>
  </verify>
  <acceptance_criteria>
    - `rg '"exceljs": "\\^?4\\.4\\.0"' backend/package.json` finds the dependency.
    - `rg "exceljs" backend/bun.lock` finds the lockfile entry.
    - `git diff -- backend/package.json` shows no unrelated dependency upgrades.
  </acceptance_criteria>
  <done>`exceljs@4.4.0` is installed and no other dependency is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add archive operations and notification Prisma models</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations/20260426090000_add_archive_operations_notifications/migration.sql</files>
  <read_first>
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-CONTEXT.md decisions D-01, D-05, D-08, D-09, D-11, D-28
    - @backend/prisma/schema.prisma current `FormTemplate`, `ApprovalApplication`, `Submission`, `ApprovalAction`, `ApprovalTimelineEvent`, and `User` models
  </read_first>
  <behavior>
    - Processing fields are stored on `FormTemplate.processingSchema` and do not change `FormTemplate.schema` or approval/public submission data.
    - Current tags, processing values, and correction overlays are stored on an archive metadata row keyed to exactly one source record.
    - Archive events are append-only and record actor, source, action, reason/comment, payload, and time.
    - User notifications are scoped to `userId` and can be counted by unread state.
  </behavior>
  <action>
    Extend `schema.prisma` with `FormTemplate.processingSchema Json @default("[]")`; `enum ArchiveSourceType { APPROVAL COLLECTION }`; `enum ArchiveEventType { TAGS_UPDATED NOTE_ADDED CONTROLLED_EDIT PROCESSING_UPDATED }`; `enum UserNotificationType { NEW_TASK APPROVED REJECTED }`; `model ArchiveRecordMeta`; `model ArchiveEvent`; and `model UserNotification`. `ArchiveRecordMeta` must include `sourceType`, optional unique `approvalApplicationId`, optional unique `submissionId`, `tags String[] @default([])`, `processingData Json @default("{}")`, `correctionData Json @default("{}")`, timestamps, and `events ArchiveEvent[]`. `ArchiveEvent` must include `metadataId`, `sourceType`, optional source IDs, optional `actorId`, `actorName`, `type`, optional `reason`, optional `comment`, optional `payload`, and `createdAt`; it must not expose update/delete semantics. `UserNotification` must include `userId`, `type`, `title`, optional `summary`, optional `sourceType`, optional `approvalApplicationId`, optional `approvalTaskId`, `targetRoute`, optional `readAt`, and `createdAt`. Add relation fields on `User`, `ApprovalApplication`, `ApprovalTask`, `Submission`, and `FormTemplate` as needed. Create the migration SQL at the exact path in `<files>` and include indexes for source lookup, metadata updatedAt, event source/date, and notification `userId/readAt` plus `userId/createdAt`. If Prisma cannot express exact-one-source as a model constraint, add a SQL `CHECK` constraint in the migration and still enforce it in services.
  </action>
  <verify>
    <automated>cd backend && bun run prisma:generate && bun run prisma:migrate</automated>
  </verify>
  <acceptance_criteria>
    - `rg "model ArchiveRecordMeta|model ArchiveEvent|model UserNotification|processingSchema" backend/prisma/schema.prisma` finds all schema additions.
    - `rg "CHECK|ArchiveRecordMeta|UserNotification|ArchiveEvent" backend/prisma/migrations/20260426090000_add_archive_operations_notifications/migration.sql` finds migration DDL and source invariant.
    - `cd backend && bun run prisma:generate` exits 0 before any dependent backend service plan runs.
    - `cd backend && bun run prisma:migrate` exits 0 against the configured PostgreSQL database before any dependent backend service plan runs.
  </acceptance_criteria>
  <done>Prisma schema and migration support operational metadata, append-only events, processing fields, and notifications without mutating formal submitted data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seed Phase 19 operation permissions</name>
  <files>backend/prisma/seed.ts, backend/src/modules/role/__tests__/approval-permissions.seed.test.ts</files>
  <read_first>
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-CONTEXT.md decisions D-03 and D-04
    - @backend/prisma/seed.ts existing `APPROVAL_PERMISSION_CODES` and `EMPLOYEE_PERMISSION_CODES`
    - @backend/src/modules/role/__tests__/approval-permissions.seed.test.ts updated Wave 0 contracts
  </read_first>
  <behavior>
    - ADMIN receives all approval/archive permissions.
    - EMPLOYEE keeps approval application create/own permissions but does not receive archive edit, mark, stats, export, or task-handle permissions by default.
  </behavior>
  <action>
    Add `approval:archive:edit`, `approval:archive:mark`, and `approval:archive:stats` to `APPROVAL_PERMISSION_CODES` and the `PERMISSIONS` list with module `approval` and Chinese names `归档受控编辑`, `归档标记备注`, and `归档统计`. Keep `approval:export` unchanged for Excel export. Update the seed test to use Phase 19 approval codes and to assert EMPLOYEE does not contain `approval:archive:edit`, `approval:archive:mark`, `approval:archive:stats`, `approval:export`, or `approval:task:handle`.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/role/__tests__/approval-permissions.seed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "approval:archive:edit|approval:archive:mark|approval:archive:stats" backend/prisma/seed.ts backend/src/modules/role/__tests__/approval-permissions.seed.test.ts` finds all new permission codes.
    - `rg "not\\.toContain\\('approval:archive" backend/src/modules/role/__tests__/approval-permissions.seed.test.ts` confirms EMPLOYEE exclusion assertions.
    - `cd backend && bun test src/modules/role/__tests__/approval-permissions.seed.test.ts` exits 0.
  </acceptance_criteria>
  <done>Phase 19 permission seeds are test-covered and ready for route/action gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma schema->service layer | Database can store source-linked operational rows; services still enforce exact source and visibility. |
| seed permissions->route guards | New operation permissions are granted only through role seed data and route guards. |
| backend export->spreadsheet | `exceljs` introduces spreadsheet generation where user-controlled values must be sanitized later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-IDOR | I/E | ArchiveRecordMeta source keys | mitigate | Schema separates source IDs; later service plans enforce source-specific visibility before use. |
| T-19-TAMPER | T | processing/correction storage | mitigate | Schema stores overlays separately; later route schemas reject trusted-field over-posting. |
| T-19-AUDIT | R/T | ArchiveEvent | mitigate | Append-only event model has no planned update/delete API and records actor/source/time. |
| T-19-CSV-INJECTION | T/I | ExcelJS dependency | mitigate | Dependency is installed only; export plan implements `sanitizeExcelCell`. |
| T-19-EXPORT-DOS | D | ExcelJS dependency | mitigate | Export plan caps rows at 2,000 before workbook generation. |
| T-19-NOTIFICATION-LEAK | I | UserNotification | mitigate | Notification model is keyed by `userId` and indexed for user-scoped unread queries. |
| T-19-DATA-SEPARATION | T/I | FormTemplate/ArchiveRecordMeta | mitigate | `processingSchema`, `processingData`, and `correctionData` are outside formal submit JSON. |
</threat_model>

<verification>
Blocking gate: `cd backend && bun run prisma:generate && bun run prisma:migrate` must pass before plans 19-05, 19-06, and 19-07 execute.
</verification>

<success_criteria>
The backend data model, migration, permission seed, and Excel dependency are complete, generated, migrated, and covered by seed tests.
</success_criteria>

<output>
After completion, create `.planning/phases/19-post-collection-processing-archive-export-stats/19-03-SUMMARY.md`
</output>
