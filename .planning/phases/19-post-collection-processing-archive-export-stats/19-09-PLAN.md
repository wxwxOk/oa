---
phase: 19-post-collection-processing-archive-export-stats
plan: 9
type: execute
wave: 4
depends_on: [19-02, 19-04, 19-05]
files_modified:
  - frontend/src/stores/template.ts
  - frontend/src/pages/FormDesignerPage.vue
autonomous: true
requirements: [OPS-03]
user_setup: []
must_haves:
  truths:
    - "Administrators can configure lightweight processing fields on a template."
    - "Processing fields are visually and technically separate from applicant-submitted schema fields."
    - "Unsupported processing field types are not offered in the UI."
  artifacts:
    - path: "frontend/src/stores/template.ts"
      provides: "Template processingSchema DTO/update payload support"
    - path: "frontend/src/pages/FormDesignerPage.vue"
      provides: "Template designer processing-field configuration UI"
  key_links:
    - from: "frontend/src/pages/FormDesignerPage.vue"
      to: "frontend/src/stores/template.ts"
      via: "store.update processingSchema payload"
      pattern: "processingSchema"
---

<objective>
Add template processing-field configuration UI.

Purpose: complete the administrator side of OPS-03 so internal processing fields can be enabled without changing formal submit fields.
Output: template store DTO support and Form Designer controls for lightweight processing fields.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-post-collection-processing-archive-export-stats/19-CONTEXT.md decisions D-11 through D-13
@.planning/phases/19-post-collection-processing-archive-export-stats/19-UI-SPEC.md Processing information and Component Inventory
@frontend/src/stores/template.ts
@frontend/src/pages/FormDesignerPage.vue
@frontend/src/types/approvalArchive.ts
@backend/src/modules/template/template.route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add processing schema DTO support to template store</name>
  <files>frontend/src/stores/template.ts</files>
  <read_first>
    - @frontend/src/stores/template.ts current `Template` and `TemplateUpdatePayload`
    - @frontend/src/types/approvalArchive.ts `ProcessingFieldConfig` and supported field types from plan 19-04
    - @backend/src/modules/template/template.route.ts processingSchema API from plan 19-05
  </read_first>
  <behavior>
    - Template fetch/update round-trips `processingSchema`.
    - Processing schema payload is separate from `schema`.
  </behavior>
  <action>
    Import or define the shared `ProcessingFieldConfig` type from `src/types/approvalArchive`. Add `processingSchema: ProcessingFieldConfig[]` to `Template` and optional `processingSchema?: ProcessingFieldConfig[]` to `TemplateUpdatePayload`. Do not include `processingSchema` in designer field getters for formal `SchemaV2`; it is not applicant form schema.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/types/__tests__/approvalArchive.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "processingSchema|ProcessingFieldConfig" frontend/src/stores/template.ts` finds DTO/update support.
    - `rg "flattenFields\\(.*processingSchema" frontend/src/stores/template.ts` finds nothing.
    - Frontend build exits 0.
  </acceptance_criteria>
  <done>Template store can load and save processing field configuration without mixing it into formal schema helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add processing field editor in Form Designer</name>
  <files>frontend/src/pages/FormDesignerPage.vue</files>
  <read_first>
    - @frontend/src/pages/FormDesignerPage.vue current toolbar/save flow
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-UI-SPEC.md Processing information and Accessibility rules
    - @frontend/src/types/approvalArchive.ts supported processing field types
  </read_first>
  <behavior>
    - UI offers only `text`, `textarea`, `date`, `radio`, `checkbox`, and `phone` processing field types.
    - UI copy states processing fields do not overwrite formal submitted content.
    - Saving processing fields does not claim template schemaVersion changed.
  </behavior>
  <action>
    Add a compact `处理字段` control in `FormDesignerPage.vue`, near existing template purpose controls or as a right-side/dialog section that matches current Quasar designer style. The editor must list existing processing fields with label/type, support add/remove/reorder/edit, and expose supported type labels: `文本`, `多行文本`, `日期`, `单选`, `多选`, `手机号`. For radio/checkbox fields, allow options editing. Do not offer signature, dynamic table, or attachment. Add explanatory copy: `处理字段仅用于内部后续处理，不会覆盖申请人或填写者的正式提交内容。` Save by including `processingSchema: store.current.processingSchema` in existing `store.update`. Keep 44px touch targets for add/remove/save controls and visible labels for inputs.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "处理字段|处理字段仅用于内部后续处理|processingSchema|保存设计" frontend/src/pages/FormDesignerPage.vue` finds UI and save integration.
    - `rg "signature|dynamic-table|attachment" frontend/src/pages/FormDesignerPage.vue` does not show these as offered processing types.
    - `rg "text|textarea|date|radio|checkbox|phone" frontend/src/pages/FormDesignerPage.vue` finds supported type options.
    - Frontend build exits 0.
  </acceptance_criteria>
  <done>Admins can configure template processing fields in the designer without changing applicant formal schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| designer UI->template API | Admin-supplied processing field config is untrusted and must be validated by backend. |
| processing config->archive detail | Processing fields influence internal UI only, not applicant submitted content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-IDOR | I/E | FormDesignerPage | accept | Existing template route permissions govern access; no archive source reads here. |
| T-19-TAMPER | T | processingSchema payload | mitigate | UI offers allowed types only; backend plan 19-05 validates and rejects unsupported/over-posted fields. |
| T-19-AUDIT | R/T | processing config | accept | Config changes use existing template update path; operational value changes are audited in archive plan. |
| T-19-CSV-INJECTION | T/I | processing config | accept | No export generation here; export plan sanitizes cell values. |
| T-19-EXPORT-DOS | D | processing config | accept | No bulk export or unbounded query here. |
| T-19-NOTIFICATION-LEAK | I | processing config | accept | Notifications are not touched. |
| T-19-DATA-SEPARATION | T/I | template store/designer | mitigate | Processing schema stays separate from formal `schema` and uses explicit separation copy. |
</threat_model>

<verification>
Run frontend build and inspect the Form Designer source for processing type restrictions and separation copy.
</verification>

<success_criteria>
Template processing fields are configurable by administrators, restricted to supported lightweight types, and saved separately from applicant schema/data.
</success_criteria>

<output>
After completion, create `.planning/phases/19-post-collection-processing-archive-export-stats/19-09-SUMMARY.md`
</output>
