---
phase: 19-post-collection-processing-archive-export-stats
plan: 2
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/types/__tests__/approvalArchive.test.ts
  - frontend/src/stores/__tests__/approvalArchive.test.ts
  - frontend/src/stores/__tests__/notification.test.ts
  - frontend/src/pages/__tests__/ApprovalArchivePage.test.ts
  - frontend/src/pages/__tests__/ApprovalArchiveDetailPage.test.ts
  - frontend/src/layouts/__tests__/MainLayoutNotification.test.ts
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06, OPS-07]
user_setup: []
must_haves:
  truths:
    - "Frontend Phase 19 contracts pin route strings, DTO keys, and critical UI copy before implementation."
    - "Archive primary screen focal point is tested as filter row plus results table with `查询归档` as the primary action."
    - "Notification unread count and mark-read behavior are covered by automated frontend tests."
  artifacts:
    - path: "frontend/src/types/__tests__/approvalArchive.test.ts"
      provides: "Archive type helper and payload key guardrail contracts"
    - path: "frontend/src/stores/__tests__/approvalArchive.test.ts"
      provides: "Archive store endpoint and loading-state contracts"
    - path: "frontend/src/pages/__tests__/ApprovalArchivePage.test.ts"
      provides: "Archive query focal point, filters, table, mobile sheet, and stats contracts"
    - path: "frontend/src/pages/__tests__/ApprovalArchiveDetailPage.test.ts"
      provides: "Full-page detail, print/PDF reuse, processing separation, and controlled edit contracts"
    - path: "frontend/src/layouts/__tests__/MainLayoutNotification.test.ts"
      provides: "Header notification badge and polling entry contracts"
  key_links:
    - from: "frontend/src/stores/__tests__/approvalArchive.test.ts"
      to: "frontend/src/stores/approvalArchive.ts"
      via: "mocked axios calls"
      pattern: "/approval/archive"
    - from: "frontend/src/pages/__tests__/ApprovalArchivePage.test.ts"
      to: "frontend/src/pages/ApprovalArchivePage.vue"
      via: "static SFC contract"
      pattern: "查询归档"
    - from: "frontend/src/layouts/__tests__/MainLayoutNotification.test.ts"
      to: "frontend/src/layouts/MainLayout.vue"
      via: "static SFC contract"
      pattern: "notifications|站内通知"
---

<objective>
Create Wave 0 frontend validation contracts for Phase 19.

Purpose: pin the operational UI/API contracts, accessibility strings, and notification behavior before frontend implementation.
Output: frontend tests that initially fail until types, stores, pages, and layout integrations exist.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/19-post-collection-processing-archive-export-stats/19-CONTEXT.md
@.planning/phases/19-post-collection-processing-archive-export-stats/19-VALIDATION.md
@.planning/phases/19-post-collection-processing-archive-export-stats/19-UI-SPEC.md
@.claude/skills/ui-ux-pro-max/SKILL.md
@frontend/src/stores/__tests__/approvalTask.test.ts
@frontend/src/pages/__tests__/ApprovalTaskDetailPage.test.ts
@frontend/src/layouts/MainLayout.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add archive type and store tests</name>
  <files>frontend/src/types/__tests__/approvalArchive.test.ts, frontend/src/stores/__tests__/approvalArchive.test.ts</files>
  <read_first>
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-UI-SPEC.md sections Archive Query List, Archive Detail, Tags, Controlled Edit, Excel And PDF Export, Basic Archive Statistics
    - @frontend/src/types/__tests__/approvalTask.test.ts for helper test style
    - @frontend/src/stores/__tests__/approvalTask.test.ts for Pinia + mocked axios style
  </read_first>
  <action>
    Create tests for future `src/types/approvalArchive.ts` helpers and future `useApprovalArchiveStore`. Type tests must assert `sourceTypeLabel('approval') === '审批申请'`, `sourceTypeLabel('collection') === '公开收集'`, collection status label `已收集`, recommended tags `待跟进`、`已核对`、`资料不全`、`重点`, payload key constants for tag/note/processing/correction requests, and no trusted source/actor/form-data keys in those payload constants. Store tests must mock `src/boot/axios` and assert endpoints: `GET /approval/archive/meta`, `GET /approval/archive`, `GET /approval/archive/{sourceType}/{id}`, `PUT /approval/archive/{sourceType}/{id}/tags`, `POST /approval/archive/{sourceType}/{id}/notes`, `PUT /approval/archive/{sourceType}/{id}/processing`, `POST /approval/archive/{sourceType}/{id}/corrections`, `GET /approval/archive/export` with `responseType: 'blob'`, and `GET /approval/archive/stats`. Include loading/actionLoading/exportLoading reset tests.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path 'frontend/src/types/__tests__/approvalArchive.test.ts'; Test-Path 'frontend/src/stores/__tests__/approvalArchive.test.ts'"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "待跟进|已核对|资料不全|重点|sourceTypeLabel|COLLECTED" frontend/src/types/__tests__/approvalArchive.test.ts` finds helper contracts.
    - `rg "/approval/archive|responseType.*blob|corrections|processing|stats" frontend/src/stores/__tests__/approvalArchive.test.ts` finds all required store endpoints.
    - Tests include `sourceType`, `templateId`, `departmentId`, `personName`, `status`, `dateFrom`, `dateTo`, and `tags` filter params.
  </acceptance_criteria>
  <done>Frontend archive DTO/store contracts cover OPS-01 through OPS-06.</done>
</task>

<task type="auto">
  <name>Task 2: Add archive page and detail page UI contract tests</name>
  <files>frontend/src/pages/__tests__/ApprovalArchivePage.test.ts, frontend/src/pages/__tests__/ApprovalArchiveDetailPage.test.ts</files>
  <read_first>
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-UI-SPEC.md full contract
    - @frontend/src/pages/__tests__/ApprovalTaskDetailPage.test.ts for static SFC contract style
    - @frontend/src/pages/ApprovalTaskPage.vue for desktop table/mobile card/filter sheet pattern
    - @frontend/src/pages/ApprovalTaskDetailPage.vue for full-page detail and mobile sticky safety pattern
  </read_first>
  <action>
    Add source-text tests for future `ApprovalArchivePage.vue` and `ApprovalArchiveDetailPage.vue`. The list-page test must explicitly assert the primary screen focal point: visible copy `归档查询`, filter labels `来源`、`状态`、`模板`、`部门`、`申请人/填写者`、`开始日期`、`结束日期`、`标签/标记`, primary button `查询归档`, `q-table`, mobile `filter_list` button, bottom sheet title `筛选归档`, and export button `导出 Excel`. The detail-page test must assert route/detail copy `归档详情`, `正式提交内容`, `后续处理信息`, `字段修正历史`, `标签/标记`, `内部备注`, `处理动态`, `#print-area`, `GridFormRenderer`, `mode=\"print\"`, `导出 PDF`, `修正提交数据`, mandatory reason copy `修正原因`, `保存修正`, and separation copy `以下信息为内部后续处理字段，不会覆盖申请人或填写者的正式提交内容。`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path 'frontend/src/pages/__tests__/ApprovalArchivePage.test.ts'; Test-Path 'frontend/src/pages/__tests__/ApprovalArchiveDetailPage.test.ts'"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "查询归档|q-table|筛选归档|导出 Excel" frontend/src/pages/__tests__/ApprovalArchivePage.test.ts` finds focal-point assertions.
    - `rg "print-area|GridFormRenderer|修正原因|后续处理字段|导出 PDF" frontend/src/pages/__tests__/ApprovalArchiveDetailPage.test.ts` finds detail/PDF/separation assertions.
    - Tests assert 44px touch target or `min-height: 44px` and mobile bottom padding for detail actions.
  </acceptance_criteria>
  <done>Frontend UI contracts cover archive list/detail accessibility, layout, print/PDF reuse, and controlled edit safeguards.</done>
</task>

<task type="auto">
  <name>Task 3: Add notification store and layout contract tests</name>
  <files>frontend/src/stores/__tests__/notification.test.ts, frontend/src/layouts/__tests__/MainLayoutNotification.test.ts</files>
  <read_first>
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-CONTEXT.md decisions D-26 through D-30
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-UI-SPEC.md In-App Notifications section
    - @frontend/src/layouts/MainLayout.vue for header insertion point
    - @frontend/src/stores/__tests__/approvalTask.test.ts for store mocking style
  </read_first>
  <action>
    Create tests for future `useNotificationStore` endpoints `GET /notifications/unread-count`, `GET /notifications`, `PATCH /notifications/{id}/read`, and `POST /notifications/mark-all-read`, with user-scoped payloads and loading-state resets. Create a static layout contract test asserting `MainLayout.vue` contains a header `q-btn` with icon `notifications`, `aria-label=\"站内通知\"`, floating `q-badge` with `99+` cap, copy `全部标为已读`, `暂无站内通知`, `新的待办和审批结果会显示在这里。`, page-focus refresh, and fixed interval polling. The tests must not require WebSocket/SSE.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path 'frontend/src/stores/__tests__/notification.test.ts'; Test-Path 'frontend/src/layouts/__tests__/MainLayoutNotification.test.ts'"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "/notifications/unread-count|mark-all-read|PATCH" frontend/src/stores/__tests__/notification.test.ts` finds endpoint contracts.
    - `rg "notifications|站内通知|99\\+|全部标为已读|setInterval|focus" frontend/src/layouts/__tests__/MainLayoutNotification.test.ts` finds layout/polling contracts.
    - Tests mention T-19-NOTIFICATION-LEAK or assert no client-side target `userId` is sent.
  </acceptance_criteria>
  <done>Frontend notification contracts cover OPS-07 and UI accessibility requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->archive API | Frontend sends filter/action payloads to authenticated archive endpoints. |
| browser->notifications API | Frontend polls unread counts and marks notifications read for the current user only. |
| archive detail->PDF | Browser exports only rendered detail DOM through the existing `#print-area` path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-IDOR | I/E | approvalArchive store tests | mitigate | Store tests pin source-specific route params while backend plans enforce visibility. |
| T-19-TAMPER | T | approvalArchive type tests | mitigate | Payload key tests forbid trusted fields in client operation payloads. |
| T-19-AUDIT | R/T | detail page tests | mitigate | Detail tests require field correction history and processing timeline sections. |
| T-19-CSV-INJECTION | T/I | export store tests | mitigate | Store tests require server-side Excel endpoint; backend export plan sanitizes cells. |
| T-19-EXPORT-DOS | D | export UI tests | mitigate | Page tests require row-cap error copy and disabled duplicate export state. |
| T-19-NOTIFICATION-LEAK | I | notification store/layout tests | mitigate | Store tests require current-user notification endpoints without client-supplied user IDs. |
| T-19-DATA-SEPARATION | T/I | detail page tests | mitigate | Detail tests require formal content and processing information separated by explicit copy. |
</threat_model>

<verification>
Wave 0 verification is contract scaffolding. These tests are expected to fail until later plans implement the referenced frontend types, stores, pages, and layout behavior.
</verification>

<success_criteria>
All frontend Wave 0 test files exist and pin the archive focal point, responsive layout, PDF reuse, operation payload guardrails, and notification unread behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/19-post-collection-processing-archive-export-stats/19-02-SUMMARY.md`
</output>
