---
phase: 19-post-collection-processing-archive-export-stats
plan: 7
type: execute
wave: 3
depends_on: [19-01, 19-03, 19-05]
files_modified:
  - backend/src/modules/approval/archive-export.service.ts
  - backend/src/modules/approval/archive-stats.service.ts
  - backend/src/modules/approval/archive.route.ts
autonomous: true
requirements: [OPS-05, OPS-06]
user_setup: []
must_haves:
  truths:
    - "Authorized staff can export the currently filtered archive list as Excel under their visible scope."
    - "Excel export sanitizes spreadsheet formula-leading strings and caps rows at 2,000."
    - "Administrators can retrieve basic archive stats by template, status, department, month, and source type."
  artifacts:
    - path: "backend/src/modules/approval/archive-export.service.ts"
      provides: "ExcelJS workbook generation and cell sanitization"
      exports: ["MAX_ARCHIVE_EXPORT_ROWS", "sanitizeExcelCell", "buildArchiveWorkbook", "exportArchiveExcel"]
    - path: "backend/src/modules/approval/archive-stats.service.ts"
      provides: "Archive statistics aggregation"
      exports: ["getArchiveStats"]
    - path: "backend/src/modules/approval/archive.route.ts"
      provides: "Export and stats routes under /approval/archive"
  key_links:
    - from: "backend/src/modules/approval/archive-export.service.ts"
      to: "backend/src/modules/approval/archive.service.ts"
      via: "reuse filtered visible archive query"
      pattern: "listArchiveRecords"
    - from: "backend/src/modules/approval/archive.route.ts"
      to: "backend/src/modules/approval/archive-export.service.ts"
      via: "GET /export route"
      pattern: "exportArchiveExcel"
    - from: "backend/src/modules/approval/archive.route.ts"
      to: "backend/src/modules/approval/archive-stats.service.ts"
      via: "GET /stats route"
      pattern: "getArchiveStats"
---

<objective>
Implement backend Excel export and archive statistics.

Purpose: complete OPS-05 and OPS-06 using existing archive visibility, ExcelJS, and lightweight aggregation patterns.
Output: export/stats services and `/approval/archive/export` plus `/approval/archive/stats` routes.
</objective>

<execution_context>
@C:/Users/11828/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-post-collection-processing-archive-export-stats/19-CONTEXT.md decisions D-18 through D-25
@.planning/phases/19-post-collection-processing-archive-export-stats/19-VALIDATION.md threats T-19-CSV-INJECTION and T-19-EXPORT-DOS
@backend/src/modules/approval/archive.service.ts
@backend/src/modules/approval/archive.route.ts
@backend/src/modules/form-stats/form-stats.route.ts
@backend/src/modules/approval/__tests__/archive-export.test.ts
@backend/src/modules/approval/__tests__/archive-stats.test.ts
@backend/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Excel export service</name>
  <files>backend/src/modules/approval/archive-export.service.ts</files>
  <read_first>
    - @backend/src/modules/approval/archive.service.ts archive row/detail DTOs
    - @backend/src/modules/approval/__tests__/archive-export.test.ts Wave 0 contracts
    - @.planning/phases/19-post-collection-processing-archive-export-stats/19-RESEARCH.md ExcelJS and OWASP CSV injection notes
  </read_first>
  <behavior>
    - Export reuses current filters and current actor visibility.
    - Export includes metadata, status, department/person, tags, processing fields, and flattened dynamic form fields.
    - Full audit history is not included in list export.
  </behavior>
  <action>
    Create `archive-export.service.ts` with `MAX_ARCHIVE_EXPORT_ROWS = 2000`, `sanitizeExcelCell(value)`, `buildArchiveWorkbook(rows, options)`, and `exportArchiveExcel(actor, filters)`. Use ExcelJS workbook/worksheet APIs. Prefix any exported string beginning with `=`, `+`, `-`, `@`, tab, or carriage return with a safe apostrophe. Before workbook generation, count/filter visible archive rows and throw `BizError('当前筛选结果超过导出上限，请缩小筛选范围后重试。', 400, 'ARCHIVE_EXPORT_TOO_LARGE')` when over 2,000. Flatten formal fields from `schemaSnapshot`/source schema and effective data; include processing fields; omit field-level edit history. Use Chinese headers from UI-SPEC: 编号, 来源, 模板, 申请人/填写者, 部门, 状态, 标签/标记, 更新时间, followed by processing and form field columns.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/approval/__tests__/archive-export.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "MAX_ARCHIVE_EXPORT_ROWS = 2000|sanitizeExcelCell|ARCHIVE_EXPORT_TOO_LARGE|ExcelJS" backend/src/modules/approval/archive-export.service.ts` finds export controls.
    - `rg "ApprovalAction|ApprovalTimelineEvent|ArchiveEvent" backend/src/modules/approval/archive-export.service.ts` does not show audit history being exported as list columns.
    - Export tests exit 0.
  </acceptance_criteria>
  <done>Excel export service satisfies OPS-05 and mitigates CSV injection/export DoS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add archive statistics service</name>
  <files>backend/src/modules/approval/archive-stats.service.ts</files>
  <read_first>
    - @backend/src/modules/form-stats/form-stats.route.ts current groupBy aggregation pattern
    - @backend/src/modules/approval/archive.service.ts source visibility helpers
    - @backend/src/modules/approval/__tests__/archive-stats.test.ts Wave 0 contracts
  </read_first>
  <behavior>
    - Draft approvals are excluded.
    - Collections count as `COLLECTED`.
    - Stats respect the same visible source scope as archive list.
  </behavior>
  <action>
    Create `archive-stats.service.ts` export `getArchiveStats(actor, filters)`. Support filters `sourceType`, `dateFrom`, `dateTo`, optional `templateId`, and optional `departmentId`. Return datasets `byTemplate`, `byStatus`, `byDepartment`, `byMonth`, and `bySourceType`, each with count totals and approval/collection splits where relevant. Use Prisma `groupBy` where practical and service-side merging where source records differ. Approval records must exclude `DRAFT`; collection records map to status `COLLECTED`.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/approval/__tests__/archive-stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "getArchiveStats|byTemplate|byStatus|byDepartment|byMonth|bySourceType|COLLECTED" backend/src/modules/approval/archive-stats.service.ts` finds required datasets.
    - `rg "DRAFT" backend/src/modules/approval/archive-stats.service.ts` shows explicit draft exclusion.
    - Stats tests exit 0.
  </acceptance_criteria>
  <done>Archive statistics service satisfies OPS-06 with required grouping dimensions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add export and stats routes</name>
  <files>backend/src/modules/approval/archive.route.ts</files>
  <read_first>
    - @backend/src/modules/approval/archive.route.ts existing archive route module from 19-05
    - @backend/src/modules/approval/__tests__/archive.route.test.ts
    - @backend/src/modules/approval/__tests__/archive-export.test.ts
    - @backend/src/modules/approval/__tests__/archive-stats.test.ts
  </read_first>
  <behavior>
    - `GET /approval/archive/export` requires `approval:export`.
    - `GET /approval/archive/stats` requires `approval:archive:stats`.
    - Both routes reuse the same query filter names as archive list.
  </behavior>
  <action>
    Extend `archive.route.ts` with `GET /export` and `GET /stats`. The export route uses `authGuard('approval:export')`, calls `exportArchiveExcel`, and sets XLSX headers: `Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` and `Content-Disposition` with filename beginning `archive-export-` and ending `.xlsx`. The stats route uses `authGuard('approval:archive:stats')`, calls `getArchiveStats`, and returns JSON. Both routes must reject trusted body fields by using query-only schemas and currentUser-derived actor data.
  </action>
  <verify>
    <automated>cd backend && bun test src/modules/approval/__tests__/archive.route.test.ts src/modules/approval/__tests__/archive-export.test.ts src/modules/approval/__tests__/archive-stats.test.ts && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "approval:export|approval:archive:stats|Content-Disposition|archive-export-" backend/src/modules/approval/archive.route.ts` finds route gates and headers.
    - `cd backend && bun test src/modules/approval/__tests__/archive.route.test.ts src/modules/approval/__tests__/archive-export.test.ts src/modules/approval/__tests__/archive-stats.test.ts` exits 0.
  </acceptance_criteria>
  <done>Archive export and stats endpoints are route-gated, test-covered, and share archive filters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive filters->export service | User-controlled filters select records but backend enforces visible scope. |
| archive data->Excel cells | User-controlled form/processing strings become spreadsheet cells. |
| stats API->aggregate data | Aggregate counts must not reveal records outside source visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-IDOR | I/E | export/stats services | mitigate | Reuse archive visibility for exported rows and stats datasets. |
| T-19-TAMPER | T | export/stats routes | mitigate | Routes accept query filters only; actor and permissions derive from JWT. |
| T-19-AUDIT | R/T | export service | accept | List export intentionally omits audit history per D-19; detail retains history. |
| T-19-CSV-INJECTION | T/I | `sanitizeExcelCell` | mitigate | Sanitize strings beginning with `=`, `+`, `-`, `@`, tab, or carriage return. |
| T-19-EXPORT-DOS | D | `MAX_ARCHIVE_EXPORT_ROWS` | mitigate | Enforce 2,000 row cap before workbook generation. |
| T-19-NOTIFICATION-LEAK | I | export/stats | accept | Notification endpoints are not touched. |
| T-19-DATA-SEPARATION | T/I | export service | mitigate | Export uses effective display values and processing fields without mutating formal data. |
</threat_model>

<verification>
Run archive route, export, and stats tests plus backend build after route changes.
</verification>

<success_criteria>
Authorized Excel export and archive stats work under current filters, source permissions, row cap, and spreadsheet sanitization.
</success_criteria>

<output>
After completion, create `.planning/phases/19-post-collection-processing-archive-export-stats/19-07-SUMMARY.md`
</output>
