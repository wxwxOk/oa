---
phase: 03-crud
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/css/quasar.variables.scss
  - frontend/src/css/app.scss
autonomous: true
requirements: [NFR-3]

must_haves:
  truths:
    - "Quasar primary 色为 #4F46E5（indigo-600）"
    - "页面背景色为 #F8FAFC（slate-50），暗色模式为 #0F172A（slate-900）"
    - "中文优先字体栈已设置"
    - "CSS 自定义变量 --oa-bg/--oa-surface/--oa-border 等在 :root 和 .body--dark 中定义"
  artifacts:
    - path: "frontend/src/css/quasar.variables.scss"
      provides: "Quasar 色彩变量覆盖（Slate + Indigo 色系）"
      contains: "$primary   : #4F46E5"
    - path: "frontend/src/css/app.scss"
      provides: "CSS 自定义变量 + 字体栈 + typography 覆盖 + 微交互"
      contains: "--oa-bg"
  key_links:
    - from: "quasar.variables.scss"
      to: "Quasar 组件"
      via: "SCSS 变量注入"
      pattern: "\\$primary"
    - from: "app.scss :root"
      to: "页面组件"
      via: "CSS 自定义属性"
      pattern: "--oa-bg"
---

<objective>
按 03-UI-SPEC.md 设计契约覆盖 Quasar 默认色彩变量，设置 CSS 自定义属性、中文字体栈、typography 覆盖和微交互样式。

Purpose: 设计系统是前端页面的视觉基础，Plan 03 的 UserPage/DepartmentPage 依赖这些变量。
Output: 两个 CSS 文件更新完成，Slate + Indigo 色系生效。
</objective>

<execution_context>
@.planning/phases/03-crud/03-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-crud/03-CONTEXT.md
@.planning/phases/03-crud/03-UI-SPEC.md

<interfaces>
<!-- UI-SPEC 定义的色彩契约 -->

Quasar 变量覆盖值（from 03-UI-SPEC.md）:
```scss
$primary   : #4F46E5;  // indigo-600
$secondary : #475569;  // slate-600
$accent    : #6366F1;  // indigo-500
$dark      : #0F172A;  // slate-900
$dark-page : #0F172A;  // slate-900
$positive  : #16A34A;  // green-600
$negative  : #DC2626;  // red-600
$info      : #3B82F6;  // blue-500
$warning   : #F59E0B;  // amber-500
```

CSS 自定义变量（from 03-UI-SPEC.md）:
```scss
:root {
  --oa-bg: #F8FAFC;
  --oa-surface: #FFFFFF;
  --oa-border: #E2E8F0;
  --oa-text-primary: #0F172A;
  --oa-text-secondary: #475569;
  --oa-text-tertiary: #64748B;
  --oa-hover: #EEF2FF;
  --oa-focus-ring: #4F46E5;
}
.body--dark {
  --oa-bg: #0F172A;
  --oa-surface: #1E293B;
  --oa-border: #334155;
  --oa-text-primary: #F8FAFC;
  --oa-text-secondary: #94A3B8;
  --oa-text-tertiary: #64748B;
  --oa-hover: #1E1B4B;
  --oa-focus-ring: #6366F1;
}
```

字体栈: `-apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', 'Source Han Sans CN', 'Noto Sans CJK SC', sans-serif`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quasar 色彩变量覆盖</name>
  <files>frontend/src/css/quasar.variables.scss</files>
  <read_first>
    - frontend/src/css/quasar.variables.scss
    - .planning/phases/03-crud/03-UI-SPEC.md
  </read_first>
  <action>
将 `frontend/src/css/quasar.variables.scss` 全部内容替换为 UI-SPEC 定义的 Slate + Indigo 色系：

```scss
// OA Design System — Slate + Indigo (03-UI-SPEC.md)
$primary   : #4F46E5;  // indigo-600
$secondary : #475569;  // slate-600
$accent    : #6366F1;  // indigo-500（dark mode 备用）

$dark      : #0F172A;  // slate-900
$dark-page : #0F172A;  // slate-900

$positive  : #16A34A;  // green-600
$negative  : #DC2626;  // red-600
$info      : #3B82F6;  // blue-500
$warning   : #F59E0B;  // amber-500
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa && grep "#4F46E5" frontend/src/css/quasar.variables.scss && grep "#DC2626" frontend/src/css/quasar.variables.scss && grep "#0F172A" frontend/src/css/quasar.variables.scss</automated>
  </verify>
  <acceptance_criteria>
    - quasar.variables.scss 包含 `$primary   : #4F46E5`
    - quasar.variables.scss 包含 `$negative  : #DC2626`
    - quasar.variables.scss 包含 `$dark      : #0F172A`
    - 不再包含旧值 `#1976d2`、`#26a69a`、`#9c27b0`
  </acceptance_criteria>
  <done>Quasar 色彩变量全部替换为 Slate + Indigo 色系</done>
</task>

<task type="auto">
  <name>Task 2: CSS 自定义变量 + 字体栈 + typography + 微交互</name>
  <files>frontend/src/css/app.scss</files>
  <read_first>
    - frontend/src/css/app.scss
    - .planning/phases/03-crud/03-UI-SPEC.md
  </read_first>
  <action>
在 `frontend/src/css/app.scss` 现有 `.full-page` 规则之后追加以下内容（保留 .full-page 不变）：

```scss
// === OA Design System (03-UI-SPEC.md) ===

// 中文优先字体栈
body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC',
    'Hiragino Sans GB', 'Microsoft YaHei', 'Source Han Sans CN',
    'Noto Sans CJK SC', sans-serif;
}

// CSS 自定义属性 — Light Mode
:root {
  --oa-bg: #F8FAFC;
  --oa-surface: #FFFFFF;
  --oa-border: #E2E8F0;
  --oa-text-primary: #0F172A;
  --oa-text-secondary: #475569;
  --oa-text-tertiary: #64748B;
  --oa-hover: #EEF2FF;
  --oa-focus-ring: #4F46E5;
}

// CSS 自定义属性 — Dark Mode
.body--dark {
  --oa-bg: #0F172A;
  --oa-surface: #1E293B;
  --oa-border: #334155;
  --oa-text-primary: #F8FAFC;
  --oa-text-secondary: #94A3B8;
  --oa-text-tertiary: #64748B;
  --oa-hover: #1E1B4B;
  --oa-focus-ring: #6366F1;
}

// Typography 覆盖 — 对话框标题 20px/600
.text-h6 {
  font-size: 20px !important;
  font-weight: 600 !important;
  line-height: 1.2 !important;
}

// 微交互 — 表格行 hover
.q-table tbody tr:hover {
  background-color: var(--oa-hover);
  transition: background-color 150ms ease;
}

// 微交互 — Focus ring（无障碍可见焦点）
.q-field--outlined .q-field__control:focus-within {
  outline: 2px solid var(--oa-focus-ring);
  outline-offset: 2px;
}

// 微交互 — 按钮点击反馈
.q-btn:active {
  transform: scale(0.98);
  transition: transform 100ms ease;
}

// 页面背景色
.q-page {
  background-color: var(--oa-bg);
}
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa && grep "--oa-bg" frontend/src/css/app.scss && grep "PingFang SC" frontend/src/css/app.scss && grep "text-h6" frontend/src/css/app.scss && grep ".full-page" frontend/src/css/app.scss</automated>
  </verify>
  <acceptance_criteria>
    - app.scss 包含 `--oa-bg: #F8FAFC` 在 `:root` 块中
    - app.scss 包含 `--oa-bg: #0F172A` 在 `.body--dark` 块中
    - app.scss 包含 `PingFang SC` 在 body font-family 中
    - app.scss 包含 `.text-h6` 覆盖为 `font-size: 20px`
    - app.scss 包含 `var(--oa-hover)` 在表格行 hover 中
    - app.scss 保留原有 `.full-page { min-height: 100vh; }`
  </acceptance_criteria>
  <done>CSS 自定义变量、字体栈、typography 覆盖、微交互样式全部就位</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 纯 CSS 配置变更，无信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | N/A | CSS 文件 | accept | 纯样式变更，无安全风险 |
</threat_model>

<verification>
```bash
# 验证 Quasar 变量覆盖
grep "$primary" frontend/src/css/quasar.variables.scss | grep "#4F46E5"
# 期望: 匹配

# 验证 CSS 自定义变量
grep -c "--oa-" frontend/src/css/app.scss
# 期望: >= 16（:root 8 + .body--dark 8）

# 验证字体栈
grep "PingFang SC" frontend/src/css/app.scss
# 期望: 匹配
```
</verification>

<success_criteria>
- Quasar 色彩变量为 Slate + Indigo 色系
- CSS 自定义变量在 :root 和 .body--dark 中完整定义
- 中文优先字体栈已设置
- typography 覆盖和微交互样式就位
</success_criteria>

<output>
After completion, create `.planning/phases/03-crud/03-02-SUMMARY.md`
</output>
