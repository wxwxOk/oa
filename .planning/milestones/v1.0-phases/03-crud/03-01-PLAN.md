---
phase: 03-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/user/user.route.ts
  - backend/src/modules/department/department.route.ts
autonomous: true
requirements: [FR-2.1, FR-2.2, FR-2.3, FR-3.1, FR-3.2, NFR-1]

must_haves:
  truths:
    - "GET /api/v1/users?status=ACTIVE 仅返回启用用户"
    - "GET /api/v1/users?status=DISABLED 仅返回禁用用户"
    - "POST /api/v1/users/:id/reset-password 需要 user:reset-password 权限，不再受 user:update 保护"
    - "PUT /api/v1/departments/:id 将 parentId 设为子孙部门时返回 400"
  artifacts:
    - path: "backend/src/modules/user/user.route.ts"
      provides: "用户 CRUD API（含 status 筛选 + 权限码分离）"
      contains: "authGuard('user:reset-password')"
    - path: "backend/src/modules/department/department.route.ts"
      provides: "部门 CRUD API（含循环引用深度校验）"
      contains: "getDescendantIds"
  key_links:
    - from: "user.route.ts GET /"
      to: "prisma.user.findMany"
      via: "where.status 条件"
      pattern: "where\\.status"
    - from: "department.route.ts PUT /:id"
      to: "getDescendantIds"
      via: "循环引用校验"
      pattern: "descendants\\.has"
---

<objective>
修复后端 3 个已知缺陷：(1) 用户列表缺少 status 筛选参数（D-03/D-16）；(2) reset-password 端点权限码错误（D-05）；(3) 部门编辑仅校验直接自引用，未排除子孙部门（D-11）。

Purpose: 后端 API 是前端页面的基础，必须先修正才能进行前端对齐。
Output: 两个后端路由文件修复完成，API 行为符合 CONTEXT.md 决策。
</objective>

<execution_context>
@.planning/phases/03-crud/03-RESEARCH.md
@.planning/phases/03-crud/03-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-crud/03-CONTEXT.md

<interfaces>
<!-- 后端共享模式，executor 直接使用 -->

From backend/src/utils/errors.ts:
```typescript
export class BizError extends Error {
  status: number;
  code: string;
  constructor(message: string, status = 400, code = 'BIZ_ERROR');
}
export const unauthorized = (msg?: string) => BizError;
export const forbidden = (msg?: string) => BizError;
export const notFound = (msg?: string) => BizError;
```

From backend/src/middlewares/auth.ts:
```typescript
export const authGuard = (requiredPerm?: string) => Elysia;
// 用法：.use(authGuard('user:reset-password'))
// ADMIN 角色跳过权限检查
```

From backend/prisma/schema.prisma:
```prisma
enum UserStatus { ACTIVE DISABLED }
model User { status UserStatus @default(ACTIVE) ... }
model Department { parentId Int? parent Department? @relation("DeptTree") children Department[] @relation("DeptTree") ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用户路由修复 — status 筛选 + reset-password 权限码分离</name>
  <files>backend/src/modules/user/user.route.ts</files>
  <read_first>
    - backend/src/modules/user/user.route.ts
    - backend/src/middlewares/auth.ts
    - backend/prisma/seed.ts
  </read_first>
  <action>
修改 `backend/src/modules/user/user.route.ts`，两处变更：

**变更 1：GET / 增加 status 筛选（D-03/D-16）**

在第 29 行 `const departmentId = ...` 之后，增加 status 参数解析：
```typescript
const status = query.status as string | undefined;
```

在第 38 行 `if (departmentId) where.departmentId = departmentId;` 之后，增加 status 条件：
```typescript
if (status === 'ACTIVE' || status === 'DISABLED') where.status = status;
```

在第 52 行 query schema 的 t.Object 中增加：
```typescript
status: t.Optional(t.String())
```

最终 query schema 为：
```typescript
{ query: t.Object({ page: t.Optional(t.String()), pageSize: t.Optional(t.String()), keyword: t.Optional(t.String()), departmentId: t.Optional(t.String()), status: t.Optional(t.String()) }) }
```

**变更 2：reset-password 从 user:update guard 中拆出（D-05）**

当前结构（第 85-121 行）：user:update guard 包含 PUT /:id 和 POST /:id/reset-password。

需要将 reset-password 拆为独立 guard 块。修改后的结构：

```typescript
// user:update guard — 仅包含 PUT /:id
.guard({ beforeHandle: [] }, (app) =>
  app
    .use(authGuard('user:update'))
    .put('/:id', /* 保持原有 PUT 逻辑不变 */)
)
// user:reset-password guard — 独立
.guard({ beforeHandle: [] }, (app) =>
  app
    .use(authGuard('user:reset-password'))
    .post('/:id/reset-password', async ({ params, body }: any) => {
      const id = Number(params.id);
      const newPwd = body.password || '123456';
      await prisma.user.update({ where: { id }, data: { password: bcrypt.hashSync(newPwd, 10) } });
      return { password: newPwd };
    }, { body: t.Object({ password: t.Optional(t.String()) }) })
)
```

注意：PUT /:id 的 body schema 和处理逻辑保持不变（第 88-113 行）。仅将第 115-120 行的 reset-password 端点移到新的 guard 块中。
  </action>
  <verify>
    <automated>cd E:/webspace/oa && grep -n "authGuard('user:reset-password')" backend/src/modules/user/user.route.ts && grep -n "status: t.Optional" backend/src/modules/user/user.route.ts && grep -n "where.status" backend/src/modules/user/user.route.ts</automated>
  </verify>
  <acceptance_criteria>
    - user.route.ts 包含 `authGuard('user:reset-password')` 独立 guard 块
    - user.route.ts 包含 `status: t.Optional(t.String())` 在 query schema 中
    - user.route.ts 包含 `if (status === 'ACTIVE' || status === 'DISABLED') where.status = status;`
    - reset-password 端点不再位于 `authGuard('user:update')` 的 guard 块内
    - PUT /:id 仍在 `authGuard('user:update')` guard 内，逻辑不变
  </acceptance_criteria>
  <done>GET /users 支持 status 筛选参数；reset-password 使用独立 user:reset-password 权限码</done>
</task>

<task type="auto">
  <name>Task 2: 部门路由修复 — 循环引用深度校验</name>
  <files>backend/src/modules/department/department.route.ts</files>
  <read_first>
    - backend/src/modules/department/department.route.ts
    - backend/src/utils/errors.ts
  </read_first>
  <action>
修改 `backend/src/modules/department/department.route.ts`，增加 `getDescendantIds` 函数并在 PUT /:id 中使用。

**变更 1：在 buildTree 函数之后（第 28 行后），export 之前，新增辅助函数：**

```typescript
// 获取某部门的所有子孙 ID（全量查询 + 内存递归，部门数 < 1000 性能可接受）
async function getDescendantIds(deptId: number): Promise<Set<number>> {
  const all = await prisma.department.findMany({
    select: { id: true, parentId: true },
  });
  const ids = new Set<number>();
  const collect = (pid: number) => {
    for (const d of all) {
      if (d.parentId === pid && !ids.has(d.id)) {
        ids.add(d.id);
        collect(d.id);
      }
    }
  };
  collect(deptId);
  return ids;
}
```

**变更 2：修改 PUT /:id 处理函数（当前第 51-54 行），替换为：**

```typescript
async ({ params, body }: any) => {
  const id = Number(params.id);
  if (body.parentId !== undefined && body.parentId !== null) {
    if (body.parentId === id) throw new BizError('上级部门不能是自己');
    const descendants = await getDescendantIds(id);
    if (descendants.has(body.parentId)) {
      throw new BizError('不能将部门移动到其子部门下');
    }
  }
  return prisma.department.update({ where: { id }, data: body });
},
```

注意保留原有的 params 和 body schema 不变。仅修改处理函数体。
  </action>
  <verify>
    <automated>cd E:/webspace/oa && grep -n "getDescendantIds" backend/src/modules/department/department.route.ts && grep -n "descendants.has" backend/src/modules/department/department.route.ts && grep -n "不能将部门移动到其子部门下" backend/src/modules/department/department.route.ts</automated>
  </verify>
  <acceptance_criteria>
    - department.route.ts 包含 `async function getDescendantIds(deptId: number): Promise<Set<number>>`
    - department.route.ts 包含 `descendants.has(body.parentId)` 校验逻辑
    - department.route.ts 包含错误消息 `不能将部门移动到其子部门下`
    - 原有 `body.parentId === id` 直接自引用校验仍保留
    - buildTree 函数和删除校验逻辑不变
  </acceptance_criteria>
  <done>PUT /departments/:id 在 parentId 变更时递归校验所有子孙，防止循环引用</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | 不可信输入：status 参数、parentId 参数 |
| API → Database | Prisma 参数化查询，无 SQL 注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | reset-password 端点 | mitigate | 独立 authGuard('user:reset-password') 保护，不再共享 user:update 权限 |
| T-03-02 | Denial of Service | getDescendantIds 递归 | accept | 部门数 < 1000，全量查询 + 内存递归性能可接受；OA 内部系统无外部攻击面 |
| T-03-03 | Tampering | status 参数注入 | mitigate | 仅接受 'ACTIVE' 或 'DISABLED' 枚举值，其他值忽略 |
| T-03-04 | Tampering | parentId 循环引用 | mitigate | getDescendantIds 递归校验，拒绝将部门移到自身子孙下 |
</threat_model>

<verification>
```bash
# 验证 status 筛选参数存在
grep -c "where.status" backend/src/modules/user/user.route.ts
# 期望: 1

# 验证 reset-password 独立权限码
grep -c "authGuard('user:reset-password')" backend/src/modules/user/user.route.ts
# 期望: 1

# 验证循环引用校验函数存在
grep -c "getDescendantIds" backend/src/modules/department/department.route.ts
# 期望: >= 2（定义 + 调用）

# 验证 reset-password 不在 user:update guard 内
# user:update guard 块中不应包含 reset-password
grep -A 30 "authGuard('user:update')" backend/src/modules/user/user.route.ts | grep -c "reset-password"
# 期望: 0
```
</verification>

<success_criteria>
- 用户列表 API 支持 status=ACTIVE|DISABLED 筛选
- reset-password 端点使用独立 user:reset-password 权限码
- 部门编辑时 parentId 变更经过子孙递归校验
- 所有现有功能不受影响（CRUD、分页、删除校验等）
</success_criteria>

<output>
After completion, create `.planning/phases/03-crud/03-01-SUMMARY.md`
</output>
