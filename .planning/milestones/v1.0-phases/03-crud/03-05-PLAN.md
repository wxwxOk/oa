---
phase: 03-crud
plan: 05
type: execute
wave: 3
depends_on: [03-03, 03-04]
files_modified: []
autonomous: false
requirements: [FR-2.1, FR-2.2, FR-2.3, FR-3.1, FR-3.2, FR-3.3, NFR-1, NFR-3]

must_haves:
  truths:
    - "用户 CRUD 端到端可用"
    - "部门 CRUD 端到端可用"
    - "UI 视觉符合 Slate + Indigo 设计系统"
  artifacts: []
  key_links: []
---

<objective>
端到端验证：启动 Docker 环境，验证用户和部门 CRUD 全流程可用，UI 视觉符合设计契约。

Purpose: 确保 Phase 3 所有变更在真实环境中端到端工作。
Output: 验证通过或发现需要修复的问题。
</objective>

<execution_context>
@.planning/phases/03-crud/03-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Phase 3 端到端人工验证</name>
  <files></files>
  <action>
启动 Docker 环境并按以下步骤验证 Phase 3 全部变更：

Phase 3 已完成的变更：
- 后端：用户列表 status 筛选、reset-password 独立权限码、部门循环引用深度校验
- 设计系统：Slate + Indigo 色彩、CSS 变量、字体栈、微交互
- 前端：DepartmentPage 父部门选择器 + 表单校验 + 空态
- 前端：UserPage status 筛选 + 表单校验 + 密码弹窗 + 空态 + 状态徽章

验证步骤：
1. 启动环境：`docker compose up -d`，等待后端就绪
2. 访问 http://localhost:9000，用 admin/admin123 登录

部门管理验证：
3. 进入「部门管理」页面
4. 验证 Slate + Indigo 色系生效（primary 按钮为靛蓝色 #4F46E5）
5. 点击「新建部门」，验证对话框标题为"新建部门"，名称和排序有红星号
6. 不填名称直接保存，验证出现"请输入部门名称"校验提示
7. 创建"研发部"（sort=1），再在研发部下创建"前端组"（sort=1）
8. 编辑"研发部"，验证对话框有"上级部门"选择器，且选项中不包含"研发部"和"前端组"
9. 删除"前端组"，验证确认框文案包含"此操作不可恢复"

用户管理验证：
10. 进入「用户管理」页面
11. 验证状态筛选（全部/启用/禁用）q-btn-toggle 存在
12. 点击"禁用"筛选，验证列表仅显示禁用用户
13. 点击「新建用户」，验证用户名和真实姓名有红星号
14. 创建测试用户（username: test1, realName: 测试用户），验证成功
15. 验证状态列显示 q-chip 徽章（绿色"启用"）
16. 点击重置密码按钮，确认后验证弹窗显示"密码已重置"
17. 删除测试用户，验证确认框文案包含"此操作不可恢复"

用户输入 "approved" 表示验证通过，或描述发现的问题。
  </action>
  <verify>人工验证通过即为成功</verify>
  <done>Phase 3 全部功能端到端验证通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 验证计划，无代码变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | N/A |
</threat_model>

<verification>
人工验证通过即为成功。
</verification>

<success_criteria>
- 部门 CRUD 全流程可用（创建、编辑含父部门变更、删除含校验）
- 用户 CRUD 全流程可用（创建、编辑、删除、重置密码、状态切换）
- status 筛选正常工作
- 表单校验正常触发
- UI 视觉符合 Slate + Indigo 设计系统
</success_criteria>

<output>
After completion, create `.planning/phases/03-crud/03-05-SUMMARY.md`
</output>
