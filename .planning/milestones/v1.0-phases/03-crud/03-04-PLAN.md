---
phase: 03-crud
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - frontend/src/pages/UserPage.vue
autonomous: true
requirements: [FR-2.1, FR-2.2, FR-2.3, FR-3.3]

must_haves:
  truths:
    - "用户列表包含 status 筛选（全部/启用/禁用 q-btn-toggle）"
    - "用户表单有 Quasar rules 校验（用户名 >=2、真实姓名必填、邮箱正则、手机数字、密码 >=4）"
    - "删除确认文案为'将永久删除用户 {username}。此操作不可恢复。'"
    - "重置密码成功后弹窗显示密码 + 复制按钮"
    - "空态显示 q-banner 含图标 + 文案 + 新建按钮"
    - "状态列显示 q-chip 徽章（启用 positive / 禁用 grey）"
    - "部门筛选下拉使用树形扁平化选项"
  artifacts:
    - path: "frontend/src/pages/UserPage.vue"
      provides: "用户管理页面（含 status 筛选、表单校验、密码弹窗、空态、状态徽章）"
      contains: "statusFilter"
  key_links:
    - from: "UserPage.vue load"
      to: "GET /api/v1/users"
      via: "params 包含 status"
      pattern: "status.*statusFilter"
    - from: "UserPage.vue onReset"
      to: "Dialog.create"
      via: "密码显示弹窗"
      pattern: "密码已重置"
---

<objective>
补全 UserPage.vue：增加 status 筛选 UI（D-03）、部门树形筛选、表单校验（D-06/D-07）、删除确认文案（D-02）、重置密码弹窗 + 复制（D-13）、空态展示（D-18）、状态徽章、UI-SPEC 文案对齐。

Purpose: 用户管理是 OA 系统最核心的页面，需要完整的 CRUD 体验。
Output: UserPage.vue 完全符合 CONTEXT.md 21 项决策和 UI-SPEC 设计契约。
</objective>

<execution_context>
@.planning/phases/03-crud/03-UI-SPEC.md
@.planning/phases/03-crud/03-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-crud/03-CONTEXT.md
@.planning/phases/03-crud/03-UI-SPEC.md

<interfaces>
<!-- UserPage 使用的 API -->

GET /api/v1/users?page=1&pageSize=20&keyword=&departmentId=&status= 返回:
```typescript
{ total: number, items: User[], page: number, pageSize: number }
// User 包含: id, username, realName, email, phone, avatar, status, departmentId,
//   department: { id, name }, roles: [{ role: { id, code, name } }], createdAt
```

POST /api/v1/users — body: { username, password?, realName, email?, phone?, departmentId?, roleIds? }
PUT /api/v1/users/:id — body: { realName?, email?, phone?, status?, departmentId?, roleIds? }
DELETE /api/v1/users/:id
POST /api/v1/users/:id/reset-password — body: { password? } → { password: string }

GET /api/v1/departments — 返回 Department[] (flat)
GET /api/v1/departments/tree — 返回 DeptNode[] (nested)
GET /api/v1/roles — 返回 Role[]

<!-- UI-SPEC 表单校验规则 -->
```typescript
const userRules = {
  username: [
    (v: string) => !!v || '请输入用户名',
    (v: string) => v.length >= 2 || '至少 2 个字符',
  ],
  password: [
    (v: string) => !!v || '请输入密码',
    (v: string) => v.length >= 4 || '至少 4 个字符',
  ],
  realName: [(v: string) => !!v || '请输入真实姓名'],
  email: [(v: string) => !v || /^\S+@\S+\.\S+$/.test(v) || '邮箱格式不正确'],
  phone: [(v: string) => !v || /^\d{6,15}$/.test(v) || '手机号格式不正确'],
}
```

<!-- UI-SPEC Copywriting -->
页面标题: 用户管理
主要 CTA: 新建用户 (color="primary", icon="add")
搜索 placeholder: 搜索用户名/姓名
状态筛选: 全部 / 启用 / 禁用 (q-btn-toggle)
部门筛选 placeholder: 选择部门
空态标题: 暂无用户
空态正文: 创建第一个用户以开始管理
对话框标题: 新建用户 / 编辑用户
保存按钮: 保存用户
删除确认: 将永久删除用户 {username}。此操作不可恢复。
重置密码确认: 密码将重置为 123456，用户下次登录需立即修改。
重置成功标题: 密码已重置
重置成功内容: 新密码：123456 + 复制按钮
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UserPage template 重构 — 筛选栏 + 空态 + 状态徽章 + 对话框表单校验</name>
  <files>frontend/src/pages/UserPage.vue</files>
  <read_first>
    - frontend/src/pages/UserPage.vue
    - .planning/phases/03-crud/03-UI-SPEC.md
    - .planning/phases/03-crud/03-CONTEXT.md
  </read_first>
  <action>
重写 `frontend/src/pages/UserPage.vue` 的 template 和 script 部分。以下是完整变更清单：

**template 变更：**

1. 顶部工具栏改造：
   - 标题改为 "用户管理"（已正确）
   - 按钮 label 改为 "新建用户"
   - 在搜索框和按钮之间增加部门筛选下拉和 status 筛选：
   ```vue
   <div class="row items-center q-mb-md q-gutter-sm">
     <div class="text-h6">用户管理</div>
     <q-space />
     <q-input v-model="keyword" outlined dense placeholder="搜索用户名/姓名" @keyup.enter="load(1)" clearable style="width: 200px">
       <template #append><q-icon name="search" class="cursor-pointer" @click="load(1)" /></template>
     </q-input>
     <q-select
       v-model="deptFilter"
       :options="deptFilterOptions"
       label="选择部门"
       outlined
       dense
       emit-value
       map-options
       clearable
       style="width: 160px"
       @update:model-value="load(1)"
     />
     <q-btn-toggle
       v-model="statusFilter"
       toggle-color="primary"
       flat
       bordered
       :options="[
         { label: '全部', value: '' },
         { label: '启用', value: 'ACTIVE' },
         { label: '禁用', value: 'DISABLED' },
       ]"
       @update:model-value="load(1)"
     />
     <q-btn v-perm="'user:create'" color="primary" icon="add" label="新建用户" @click="openEdit(null)" />
   </div>
   ```

2. 在 q-table 之前增加空态和错误态判断：
   ```vue
   <!-- 加载中（首次） -->
   <div v-if="firstLoading" class="q-pa-xl">
     <q-skeleton type="QTable" />
   </div>
   <!-- 错误态 -->
   <div v-else-if="error" class="flex flex-center q-pa-xl">
     <div class="text-center">
       <div class="text-body1">加载失败，请检查网络后重试</div>
       <q-btn color="primary" label="重试" class="q-mt-md" @click="load(1)" />
     </div>
   </div>
   <!-- 空态 -->
   <div v-else-if="rows.length === 0 && !loading" class="flex flex-center q-pa-xl">
     <div class="text-center">
       <q-icon name="people" size="4em" color="grey-4" />
       <div class="text-h6 q-mt-md">暂无用户</div>
       <div class="text-body2 text-grey-6 q-mt-sm">创建第一个用户以开始管理</div>
       <q-btn v-perm="'user:create'" color="primary" label="新建用户" icon="add" class="q-mt-md" @click="openEdit(null)" />
     </div>
   </div>
   <!-- 数据态 -->
   <template v-else>
     <q-table v-if="$q.screen.gt.sm" ... >
   ```

3. q-table 增加 status 列的自定义渲染（状态徽章）：
   ```vue
   <template #body-cell-status="props">
     <q-td :props="props">
       <q-chip
         :color="props.row.status === 'ACTIVE' ? 'positive' : 'grey-4'"
         :text-color="props.row.status === 'ACTIVE' ? 'white' : 'grey-8'"
         dense
         size="sm"
       >
         {{ props.row.status === 'ACTIVE' ? '启用' : '禁用' }}
       </q-chip>
     </q-td>
   </template>
   ```

4. 移动端卡片增加状态徽章和重置密码按钮：
   ```vue
   <q-card v-for="u in rows" :key="u.id" flat bordered>
     <q-card-section>
       <div class="row items-center">
         <div class="text-subtitle1">{{ u.realName }} <span class="text-caption text-grey">({{ u.username }})</span></div>
         <q-space />
         <q-chip
           :color="u.status === 'ACTIVE' ? 'positive' : 'grey-4'"
           :text-color="u.status === 'ACTIVE' ? 'white' : 'grey-8'"
           dense size="sm"
         >{{ u.status === 'ACTIVE' ? '启用' : '禁用' }}</q-chip>
       </div>
       <div class="text-caption q-mt-xs">部门: {{ u.department?.name ?? '-' }}</div>
       <div class="text-caption">角色: {{ u.roles.map((r: any) => r.role.name).join(', ') || '-' }}</div>
     </q-card-section>
     <q-card-actions align="right">
       <q-btn v-perm="'user:update'" flat dense icon="edit" @click="openEdit(u)" />
       <q-btn v-perm="'user:reset-password'" flat dense icon="vpn_key" @click="onReset(u)" />
       <q-btn v-perm="'user:delete'" flat dense icon="delete" color="negative" @click="onDelete(u)" />
     </q-card-actions>
   </q-card>
   ```

5. 对话框表单增加 rules 校验和必填红星号：
   ```vue
   <q-dialog v-model="dialog">
     <q-card style="min-width: 400px">
       <q-card-section class="text-h6">{{ form.id ? '编辑用户' : '新建用户' }}</q-card-section>
       <q-card-section class="q-gutter-sm">
         <q-input v-if="!form.id" v-model="form.username" outlined lazy-rules="ondemand"
           :rules="[(v: string) => !!v || '请输入用户名', (v: string) => v.length >= 2 || '至少 2 个字符']">
           <template #label>用户名 <span class="text-negative">*</span></template>
         </q-input>
         <q-input v-if="!form.id" v-model="form.password" outlined lazy-rules="ondemand"
           :rules="[(v: string) => !!v || '请输入密码', (v: string) => v.length >= 4 || '至少 4 个字符']">
           <template #label>初始密码 (默认 123456)</template>
         </q-input>
         <q-input v-model="form.realName" outlined lazy-rules="ondemand"
           :rules="[(v: string) => !!v || '请输入真实姓名']">
           <template #label>真实姓名 <span class="text-negative">*</span></template>
         </q-input>
         <q-input v-model="form.email" outlined lazy-rules="ondemand"
           :rules="[(v: string) => !v || /^\S+@\S+\.\S+$/.test(v) || '邮箱格式不正确']"
           label="邮箱" />
         <q-input v-model="form.phone" outlined lazy-rules="ondemand"
           :rules="[(v: string) => !v || /^\d{6,15}$/.test(v) || '手机号格式不正确']"
           label="手机" />
         <q-select v-model="form.departmentId" :options="deptOptions" label="部门" outlined emit-value map-options clearable />
         <q-select v-model="form.roleIds" :options="roleOptions" label="角色" outlined multiple emit-value map-options />
         <q-toggle v-if="form.id" v-model="form.statusActive" label="启用" />
       </q-card-section>
       <q-card-actions align="right">
         <q-btn flat label="取消" v-close-popup />
         <q-btn color="primary" label="保存用户" @click="onSave" />
       </q-card-actions>
     </q-card>
   </q-dialog>
   ```

**script 变更：**

1. 增加 imports：
```typescript
import { ref, reactive, onMounted } from 'vue';
import { api } from 'src/boot/axios';
import { Dialog, Notify, useQuasar, copyToClipboard } from 'quasar';
```

2. 增加新的 ref：
```typescript
const $q = useQuasar();
const statusFilter = ref('');
const deptFilter = ref<number | null>(null);
const deptFilterOptions = ref<Array<{ label: string; value: number }>>([]);
const firstLoading = ref(true);
const error = ref(false);
```

3. 修改 `load` 函数，增加 status 和 departmentId 参数：
```typescript
async function load(page = pagination.value.page) {
  loading.value = true;
  try {
    const { data } = await api.get('/users', {
      params: {
        page,
        pageSize: pagination.value.rowsPerPage,
        keyword: keyword.value || undefined,
        departmentId: deptFilter.value || undefined,
        status: statusFilter.value || undefined,
      },
    });
    rows.value = data.items;
    pagination.value.rowsNumber = data.total;
    pagination.value.page = data.page;
    error.value = false;
  } catch {
    error.value = true;
  } finally {
    loading.value = false;
    firstLoading.value = false;
  }
}
```

4. 修改 `loadMeta` 函数，部门选项使用树形扁平化：
```typescript
interface DeptNode {
  id: number;
  name: string;
  parentId: number | null;
  sort: number;
  children: DeptNode[];
}

function flattenTreeForFilter(nodes: DeptNode[], depth = 0): Array<{ label: string; value: number }> {
  const result: Array<{ label: string; value: number }> = [];
  for (const node of nodes) {
    const indent = depth > 0 ? '\u3000'.repeat(depth) + '\u2514 ' : '';
    result.push({ label: indent + node.name, value: node.id });
    if (node.children?.length) {
      result.push(...flattenTreeForFilter(node.children, depth + 1));
    }
  }
  return result;
}

async function loadMeta() {
  const [d, r] = await Promise.all([api.get('/departments/tree'), api.get('/roles')]);
  deptFilterOptions.value = flattenTreeForFilter(d.data);
  deptOptions.value = flattenTreeForFilter(d.data);
  roleOptions.value = r.data.map((x: any) => ({ label: x.name, value: x.id }));
}
```

5. 修改 `onSave` 函数，departmentId 清空时传 null：
```typescript
async function onSave() {
  if (form.id) {
    await api.put(`/users/${form.id}`, {
      realName: form.realName,
      email: form.email || undefined,
      phone: form.phone || undefined,
      departmentId: form.departmentId ?? null,
      roleIds: form.roleIds,
      status: form.statusActive ? 'ACTIVE' : 'DISABLED',
    });
  } else {
    await api.post('/users', {
      username: form.username,
      password: form.password || undefined,
      realName: form.realName,
      email: form.email || undefined,
      phone: form.phone || undefined,
      departmentId: form.departmentId ?? null,
      roleIds: form.roleIds,
    });
  }
  Notify.create({ type: 'positive', message: '保存成功' });
  dialog.value = false;
  await load();
}
```

6. 修改 `onDelete` 函数，使用 UI-SPEC 文案：
```typescript
function onDelete(row: any) {
  Dialog.create({
    title: '删除用户',
    message: `将永久删除用户 ${row.username}。此操作不可恢复。`,
    cancel: true,
    ok: { label: '确认删除', color: 'negative' },
  }).onOk(async () => {
    await api.delete(`/users/${row.id}`);
    Notify.create({ type: 'positive', message: '已删除' });
    await load();
  });
}
```

7. 修改 `onReset` 函数，使用 UI-SPEC 文案 + 密码弹窗 + 复制按钮（D-13）：
```typescript
function onReset(row: any) {
  Dialog.create({
    title: '重置密码',
    message: '密码将重置为 123456，用户下次登录需立即修改。',
    cancel: true,
    ok: { label: '确认重置', color: 'primary' },
  }).onOk(async () => {
    const { data } = await api.post(`/users/${row.id}/reset-password`, {});
    // 自动复制到剪贴板
    copyToClipboard(data.password).catch(() => {});
    Dialog.create({
      title: '密码已重置',
      message: `新密码：<code style="font-size:16px;padding:2px 8px;background:#f1f5f9;border-radius:4px">${data.password}</code>`,
      html: true,
      ok: '关闭',
      persistent: true,
    });
    Notify.create({ type: 'positive', message: '密码已复制到剪贴板', timeout: 2000 });
  });
}
```

8. columns 中 status 列移除 format 函数（改用 slot 渲染 q-chip）：
```typescript
{ name: 'status', label: '状态', field: 'status', align: 'center' as const },
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa && grep -n "statusFilter" frontend/src/pages/UserPage.vue && grep -n "q-btn-toggle" frontend/src/pages/UserPage.vue && grep -n "暂无用户" frontend/src/pages/UserPage.vue && grep -n "此操作不可恢复" frontend/src/pages/UserPage.vue && grep -n "密码已重置" frontend/src/pages/UserPage.vue && grep -n "请输入用户名" frontend/src/pages/UserPage.vue && grep -n "q-chip" frontend/src/pages/UserPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - UserPage.vue 包含 `statusFilter` ref 和 `q-btn-toggle` 组件
    - UserPage.vue load 函数 params 包含 `status: statusFilter.value || undefined`
    - UserPage.vue 包含空态文案 `暂无用户` 和 `创建第一个用户以开始管理`
    - UserPage.vue onDelete 文案为 `将永久删除用户 ${row.username}。此操作不可恢复。`
    - UserPage.vue onReset 成功后调用 `Dialog.create` 显示密码，标题为 `密码已重置`
    - UserPage.vue 包含 `copyToClipboard` 调用
    - UserPage.vue 表单包含 `:rules` 校验（用户名 >=2、真实姓名必填、邮箱正则、手机数字）
    - UserPage.vue 必填字段 label 包含 `<span class="text-negative">*</span>`
    - UserPage.vue status 列使用 `q-chip` 渲染状态徽章
    - UserPage.vue 部门筛选使用树形扁平化选项（`flattenTreeForFilter`）
    - UserPage.vue onSave 中 departmentId 使用 `?? null` 而非 `?? undefined`
  </acceptance_criteria>
  <done>UserPage.vue 完整实现 status 筛选、表单校验、删除确认文案、密码弹窗复制、空态、状态徽章、部门树形筛选、UI-SPEC 文案对齐</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | 用户输入经 Quasar rules 前端校验 + Elysia t.Object 后端校验双重保护 |
| Clipboard API | 仅在 HTTPS/localhost 下可用，降级为静默失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | 密码弹窗 | accept | 密码仅在创建/重置时一次性显示，不持久化；管理员操作场景可接受 |
| T-03-08 | Tampering | 前端表单校验绕过 | mitigate | 前端 rules 为 UX 辅助，后端 Elysia t.Object 做强制校验（D-08） |
| T-03-09 | Spoofing | v-perm 指令绕过 | mitigate | v-perm 仅控制 UI 显隐，后端 authGuard 做强制权限校验 |
</threat_model>

<verification>
```bash
# 验证 status 筛选
grep -c "statusFilter" frontend/src/pages/UserPage.vue
# 期望: >= 3（定义 + 绑定 + 使用）

# 验证表单校验
grep -c ":rules" frontend/src/pages/UserPage.vue
# 期望: >= 5（username, password, realName, email, phone）

# 验证密码弹窗
grep -c "密码已重置" frontend/src/pages/UserPage.vue
# 期望: 1

# 验证空态
grep -c "暂无用户" frontend/src/pages/UserPage.vue
# 期望: 1

# 验证状态徽章
grep -c "q-chip" frontend/src/pages/UserPage.vue
# 期望: >= 2（PC 表格 + 移动卡片）

# 验证删除确认文案
grep "此操作不可恢复" frontend/src/pages/UserPage.vue
# 期望: 匹配
```
</verification>

<success_criteria>
- 用户列表支持 status 筛选（全部/启用/禁用）
- 部门筛选使用树形扁平化下拉
- 表单所有字段有 rules 校验，必填字段有红星号
- 删除确认文案符合 UI-SPEC
- 重置密码成功后弹窗显示密码 + 自动复制
- 空态正确显示
- 状态列使用 q-chip 徽章
</success_criteria>

<output>
After completion, create `.planning/phases/03-crud/03-04-SUMMARY.md`
</output>
