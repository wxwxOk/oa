---
phase: 03-crud
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - frontend/src/pages/DepartmentPage.vue
autonomous: true
requirements: [FR-3.1, FR-3.2, FR-3.3]

must_haves:
  truths:
    - "部门编辑对话框包含父部门选择器（q-select 树形下拉）"
    - "编辑时父部门选择器排除自身及其子部门"
    - "部门表单有 Quasar rules 校验（名称必填、排序必填整数）"
    - "空态显示 q-banner 含图标 + 文案 + 新建按钮"
    - "删除确认文案包含部门名称和不可恢复提示"
    - "编辑保存时传递 parentId"
  artifacts:
    - path: "frontend/src/pages/DepartmentPage.vue"
      provides: "部门管理页面（含父部门选择器、表单校验、空态、UI 对齐）"
      contains: "flattenTreeForSelect"
  key_links:
    - from: "DepartmentPage.vue onSave"
      to: "PUT /api/v1/departments/:id"
      via: "api.put 包含 parentId"
      pattern: "parentId.*form\\.parentId"
    - from: "DepartmentPage.vue flattenTreeForSelect"
      to: "q-select :options"
      via: "树形数据扁平化为选项"
      pattern: "flattenTreeForSelect"
---

<objective>
补全 DepartmentPage.vue：增加父部门选择器（D-09）、编辑时排除自身及子部门（D-11）、表单校验（D-06）、空态展示、删除确认文案对齐（D-02）、编辑时传递 parentId（D-09）。

Purpose: 部门管理页面是组织架构的核心，需要完整的树形编辑能力。
Output: DepartmentPage.vue 完全符合 CONTEXT.md 决策和 UI-SPEC 设计契约。
</objective>

<execution_context>
@.planning/phases/03-crud/03-UI-SPEC.md
@.planning/phases/03-crud/03-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-crud/03-CONTEXT.md
@.planning/phases/03-crud/03-UI-SPEC.md

<interfaces>
<!-- DepartmentPage 使用的 API -->

GET /api/v1/departments/tree 返回:
```typescript
type DeptNode = {
  id: number;
  name: string;
  parentId: number | null;
  sort: number;
  children: DeptNode[];
}
// 返回 DeptNode[]
```

POST /api/v1/departments — body: { name: string, parentId?: number | null, sort?: number }
PUT /api/v1/departments/:id — body: { name?: string, parentId?: number | null, sort?: number }
DELETE /api/v1/departments/:id — 存在子部门或用户时返回 400

<!-- UI-SPEC 表单校验规则 -->
```typescript
const deptRules = {
  name: [(v: string) => !!v || '请输入部门名称'],
  sort: [
    (v: any) => v !== null && v !== undefined || '请输入排序数字',
    (v: any) => Number.isInteger(Number(v)) || '必须是整数',
  ],
}
```

<!-- UI-SPEC Copywriting -->
页面标题: 部门管理
主要 CTA: 新建部门 (color="primary", icon="add")
空态标题: 暂无部门
空态正文: 建立组织架构第一步：添加顶级部门
对话框标题: 新建部门 / 编辑部门
保存按钮: 保存部门
父部门选择器 label: 上级部门
删除确认: 将永久删除部门 {name}。子部门或用户存在时删除会失败。此操作不可恢复。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DepartmentPage 完整补全</name>
  <files>frontend/src/pages/DepartmentPage.vue</files>
  <read_first>
    - frontend/src/pages/DepartmentPage.vue
    - .planning/phases/03-crud/03-UI-SPEC.md
    - .planning/phases/03-crud/03-CONTEXT.md
  </read_first>
  <action>
重写 `frontend/src/pages/DepartmentPage.vue`，在现有骨架基础上补全以下功能：

**template 部分变更：**

1. 页面标题栏保持不变（"部门管理" + "新建部门" 按钮），按钮 label 改为 "新建部门"（已正确）。

2. 在 q-tree 之前增加空态判断：
```vue
<!-- 加载中 -->
<div v-if="loading" class="flex flex-center q-pa-xl">
  <q-spinner color="primary" size="3em" />
</div>
<!-- 空态 -->
<div v-else-if="tree.length === 0" class="flex flex-center q-pa-xl">
  <div class="text-center">
    <q-icon name="account_tree" size="4em" color="grey-4" />
    <div class="text-h6 q-mt-md">暂无部门</div>
    <div class="text-body2 text-grey-6 q-mt-sm">建立组织架构第一步：添加顶级部门</div>
    <q-btn v-perm="'department:create'" color="primary" label="新建部门" icon="add" class="q-mt-md" @click="openEdit(null)" />
  </div>
</div>
<!-- 数据态 -->
<q-tree v-else ... >
```

3. 在 q-tree 上增加 `v-else`。

4. 对话框表单变更：
   - 增加 `ref="formRef"` 到 q-card 外层或使用 q-form
   - 部门名称 q-input 增加 `:rules="[(v: string) => !!v || '请输入部门名称']"`，label 改为模板：
     ```vue
     <template #label>部门名称 <span class="text-negative">*</span></template>
     ```
   - 排序 q-input 增加 `:rules="[(v: any) => v !== null && v !== undefined || '请输入排序数字', (v: any) => Number.isInteger(Number(v)) || '必须是整数']"`，label 改为模板：
     ```vue
     <template #label>排序 <span class="text-negative">*</span></template>
     ```
   - 在排序 q-input 之前，增加父部门选择器：
     ```vue
     <q-select
       v-model="form.parentId"
       :options="parentOptions"
       label="上级部门"
       outlined
       emit-value
       map-options
       clearable
     />
     ```
   - 保存按钮 label 改为 "保存部门"
   - 对话框标题改为 `{{ form.id ? '编辑部门' : '新建部门' }}`

5. 所有 q-input 增加 `lazy-rules="ondemand"` 属性。

**script 部分变更：**

1. 增加 `loading` ref：
```typescript
const loading = ref(false);
```

2. 增加 `parentOptions` computed 或 ref，以及辅助函数：
```typescript
import { ref, onMounted, reactive, computed } from 'vue';
import { useQuasar } from 'quasar';

const $q = useQuasar();

interface DeptNode {
  id: number;
  name: string;
  parentId: number | null;
  sort: number;
  children: DeptNode[];
}

// 将树形数据扁平化为 q-select options，带缩进前缀
function flattenTreeForSelect(
  nodes: DeptNode[],
  depth = 0,
  excludeIds = new Set<number>()
): Array<{ label: string; value: number }> {
  const result: Array<{ label: string; value: number }> = [];
  for (const node of nodes) {
    if (excludeIds.has(node.id)) continue;
    const indent = depth > 0 ? '\u3000'.repeat(depth) + '\u2514 ' : '';
    result.push({ label: indent + node.name, value: node.id });
    if (node.children?.length) {
      result.push(...flattenTreeForSelect(node.children, depth + 1, excludeIds));
    }
  }
  return result;
}

// 获取节点及其所有子孙的 ID 集合（D-11 前端辅助过滤）
function getSubtreeIds(nodeId: number, nodes: DeptNode[]): Set<number> {
  const ids = new Set<number>([nodeId]);
  const collectChildren = (children: DeptNode[]) => {
    for (const c of children) {
      ids.add(c.id);
      if (c.children?.length) collectChildren(c.children);
    }
  };
  const findNode = (list: DeptNode[]): DeptNode | undefined => {
    for (const n of list) {
      if (n.id === nodeId) return n;
      if (n.children?.length) {
        const found = findNode(n.children);
        if (found) return found;
      }
    }
    return undefined;
  };
  const target = findNode(nodes);
  if (target?.children?.length) collectChildren(target.children);
  return ids;
}

// 父部门选项：编辑时排除自身及子孙（D-11），新建时显示全部
const parentOptions = computed(() => {
  const excludeIds = form.id ? getSubtreeIds(form.id, tree.value as DeptNode[]) : new Set<number>();
  return flattenTreeForSelect(tree.value as DeptNode[], 0, excludeIds);
});
```

3. 修改 `load` 函数增加 loading 状态：
```typescript
async function load() {
  loading.value = true;
  try {
    const { data } = await api.get('/departments/tree');
    tree.value = data;
  } finally {
    loading.value = false;
  }
}
```

4. 修改 `openEdit` 函数，编辑时正确填充 parentId：
```typescript
function openEdit(node: any) {
  Object.assign(form, { id: null, name: '', parentId: null, sort: 0 });
  if (node?.id && node?.name) {
    // 编辑模式：填充所有字段包括 parentId
    Object.assign(form, { id: node.id, name: node.name, parentId: node.parentId ?? null, sort: node.sort ?? 0 });
  } else if (node?.parentId) {
    // 新建子部门模式
    form.parentId = node.parentId;
  }
  dialog.value = true;
}
```

5. 修改 `onSave` 函数，编辑时也传 parentId：
```typescript
async function onSave() {
  // 表单校验（如果使用 q-form ref）
  if (form.id) {
    await api.put(`/departments/${form.id}`, {
      name: form.name,
      parentId: form.parentId ?? null,
      sort: form.sort,
    });
  } else {
    await api.post('/departments', {
      name: form.name,
      parentId: form.parentId ?? null,
      sort: form.sort,
    });
  }
  Notify.create({ type: 'positive', message: '保存成功' });
  dialog.value = false;
  await load();
}
```

6. 修改 `onDelete` 函数，使用 UI-SPEC 文案并包含部门名称：
```typescript
function onDelete(node: any) {
  Dialog.create({
    title: '删除部门',
    message: `将永久删除部门 ${node.name}。子部门或用户存在时删除会失败。此操作不可恢复。`,
    cancel: true,
    ok: { label: '确认删除', color: 'negative' },
  }).onOk(async () => {
    await api.delete(`/departments/${node.id}`);
    Notify.create({ type: 'positive', message: '已删除' });
    await load();
  });
}
```

7. 修改 q-tree 节点模板中 delete 按钮的 @click.stop，传递整个 node 而非 node.id：
```vue
<q-btn v-perm="'department:delete'" size="sm" flat dense icon="delete" color="negative" @click.stop="onDelete(props.node)" />
```

8. 增加网络错误处理：在 load 函数的 try 块中增加 catch：
```typescript
async function load() {
  loading.value = true;
  try {
    const { data } = await api.get('/departments/tree');
    tree.value = data;
    error.value = false;
  } catch {
    error.value = true;
  } finally {
    loading.value = false;
  }
}
```
增加 `const error = ref(false);`，在模板中空态之前增加错误态：
```vue
<div v-else-if="error" class="flex flex-center q-pa-xl">
  <div class="text-center">
    <div class="text-body1">加载失败，请检查网络后重试</div>
    <q-btn color="primary" label="重试" class="q-mt-md" @click="load" />
  </div>
</div>
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa && grep -n "flattenTreeForSelect" frontend/src/pages/DepartmentPage.vue && grep -n "getSubtreeIds" frontend/src/pages/DepartmentPage.vue && grep -n "parentOptions" frontend/src/pages/DepartmentPage.vue && grep -n "请输入部门名称" frontend/src/pages/DepartmentPage.vue && grep -n "暂无部门" frontend/src/pages/DepartmentPage.vue && grep -n "parentId.*form" frontend/src/pages/DepartmentPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - DepartmentPage.vue 包含 `function flattenTreeForSelect` 辅助函数
    - DepartmentPage.vue 包含 `function getSubtreeIds` 辅助函数
    - DepartmentPage.vue 包含 `parentOptions` computed 属性
    - DepartmentPage.vue 包含 q-select 父部门选择器绑定 `parentOptions`
    - DepartmentPage.vue 包含 `:rules="[(v: string) => !!v || '请输入部门名称']"` 在名称输入框
    - DepartmentPage.vue 包含空态文案 `暂无部门` 和 `建立组织架构第一步：添加顶级部门`
    - DepartmentPage.vue onSave 编辑时传递 `parentId: form.parentId ?? null`
    - DepartmentPage.vue onDelete 文案包含 `此操作不可恢复`
    - DepartmentPage.vue 包含 loading 和 error 状态处理
  </acceptance_criteria>
  <done>DepartmentPage.vue 完整实现父部门选择器、循环引用前端过滤、表单校验、空态、错误态、UI-SPEC 文案对齐</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | parentId 由用户选择，后端已有 getDescendantIds 校验兜底 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | parentId 前端过滤绕过 | mitigate | 前端 getSubtreeIds 仅为 UX 辅助，后端 getDescendantIds 做强制校验（Plan 01 Task 2） |
</threat_model>

<verification>
```bash
# 验证父部门选择器
grep -c "flattenTreeForSelect" frontend/src/pages/DepartmentPage.vue
# 期望: >= 2（定义 + 使用）

# 验证编辑时排除自身及子部门
grep -c "getSubtreeIds" frontend/src/pages/DepartmentPage.vue
# 期望: >= 2

# 验证表单校验
grep -c "请输入部门名称" frontend/src/pages/DepartmentPage.vue
# 期望: 1

# 验证空态
grep -c "暂无部门" frontend/src/pages/DepartmentPage.vue
# 期望: 1

# 验证编辑时传 parentId
grep "parentId.*form.parentId" frontend/src/pages/DepartmentPage.vue
# 期望: 匹配
```
</verification>

<success_criteria>
- 部门编辑对话框包含父部门选择器，选项为树形缩进
- 编辑时父部门选择器自动排除自身及子部门
- 表单必填字段有红星号和 rules 校验
- 空态正确显示图标 + 文案 + 新建按钮
- 编辑保存时正确传递 parentId
- 删除确认文案符合 UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/03-crud/03-03-SUMMARY.md`
</output>
