---
phase: 05-responsive
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/dashboard/dashboard.route.ts
  - backend/src/index.ts
autonomous: true
requirements: [NFR-1]
must_haves:
  truths:
    - "GET /api/v1/dashboard/stats 返回 { userCount, departmentCount, roleCount }"
    - "接口需要登录（authGuard 无参数），不需要特定权限"
    - "接口 p95 响应时间 < 500ms（3 个 count 查询并行）"
  artifacts:
    - path: "backend/src/modules/dashboard/dashboard.route.ts"
      provides: "Dashboard 统计接口"
      exports: ["dashboardModule"]
    - path: "backend/src/index.ts"
      provides: "注册 dashboardModule"
      contains: "dashboardModule"
  key_links:
    - from: "backend/src/modules/dashboard/dashboard.route.ts"
      to: "backend/src/middlewares/auth.ts"
      via: "authGuard()"
      pattern: "authGuard"
    - from: "backend/src/index.ts"
      to: "backend/src/modules/dashboard/dashboard.route.ts"
      via: "import { dashboardModule }"
      pattern: "dashboardModule"
---

<objective>
新增后端 Dashboard 统计接口 GET /api/v1/dashboard/stats，返回用户数/部门数/角色数。

Purpose: DashboardPage 统计卡片的数据源（D-16）。接口仅需登录，不需要特定权限码。
Output: dashboard.route.ts + index.ts 注册。
</objective>

<execution_context>
@.planning/phases/05-responsive/05-RESEARCH.md
@.planning/phases/05-responsive/05-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
From backend/src/middlewares/auth.ts:
```typescript
// authGuard(requiredPerm?: string) — 不传参数 = 仅验证登录，不检查权限
export const authGuard = (requiredPerm?: string) =>
  new Elysia({ name: `auth-guard-${requiredPerm ?? 'any'}` })
    .derive({ as: 'scoped' }, async ({ accessJwt, headers }: any) => { /* ... */ });
```

From backend/src/modules/department/department.route.ts (路由模块模式):
```typescript
import { Elysia, t } from 'elysia';
import { prisma } from '../../plugins/prisma';
import { authGuard } from '../../middlewares/auth';

export const departmentModule = new Elysia({ prefix: '/departments' })
  .use(authGuard('department:list'))
  .get('/', async () => { /* ... */ });
```

From backend/src/index.ts L54-61 (模块注册模式):
```typescript
.group('/api/v1', (app) =>
  app
    .use(authModule)
    .use(userModule)
    .use(departmentModule)
    .use(roleModule)
    .use(permissionModule),
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 dashboard.route.ts + 注册到 index.ts</name>
  <files>
    backend/src/modules/dashboard/dashboard.route.ts,
    backend/src/index.ts
  </files>
  <read_first>
    backend/src/index.ts,
    backend/src/middlewares/auth.ts,
    backend/src/modules/department/department.route.ts
  </read_first>
  <action>
1. 创建目录 `backend/src/modules/dashboard/`

2. 创建 `backend/src/modules/dashboard/dashboard.route.ts`：
   ```typescript
   import { Elysia } from 'elysia';
   import { prisma } from '../../plugins/prisma';
   import { authGuard } from '../../middlewares/auth';

   export const dashboardModule = new Elysia({ prefix: '/dashboard' })
     .use(authGuard())
     .get('/stats', async () => {
       const [userCount, departmentCount, roleCount] = await Promise.all([
         prisma.user.count(),
         prisma.department.count(),
         prisma.role.count(),
       ]);
       return { userCount, departmentCount, roleCount };
     });
   ```
   关键点：
   - `authGuard()` 不传参数 = 仅验证登录，所有登录用户可查看统计
   - `Promise.all` 并行 3 个 count 查询，满足 NFR-1 p95 < 500ms
   - 返回值类型 `{ userCount: number, departmentCount: number, roleCount: number }`

3. 编辑 `backend/src/index.ts`：
   - 在 import 区域（约 L9 后）添加：
     `import { dashboardModule } from './modules/dashboard/dashboard.route';`
   - 在 `.group('/api/v1', ...)` 内的 `.use(permissionModule)` 后追加：
     `.use(dashboardModule)`
  </action>
  <verify>
    <automated>cd backend && grep "dashboardModule" src/index.ts && grep "Promise.all" src/modules/dashboard/dashboard.route.ts && grep "authGuard()" src/modules/dashboard/dashboard.route.ts</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/modules/dashboard/dashboard.route.ts 存在
    - dashboard.route.ts 包含 `export const dashboardModule`
    - dashboard.route.ts 包含 `.use(authGuard())` （无参数，仅登录验证）
    - dashboard.route.ts 包含 `prefix: '/dashboard'`
    - dashboard.route.ts 包含 `.get('/stats'`
    - dashboard.route.ts 包含 `Promise.all([` 并行查询
    - dashboard.route.ts 包含 `prisma.user.count()`
    - dashboard.route.ts 包含 `prisma.department.count()`
    - dashboard.route.ts 包含 `prisma.role.count()`
    - backend/src/index.ts 包含 `import { dashboardModule }`
    - backend/src/index.ts 包含 `.use(dashboardModule)`
  </acceptance_criteria>
  <done>
    GET /api/v1/dashboard/stats 接口可用，返回 { userCount, departmentCount, roleCount }，需登录但不需要特定权限。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API /dashboard/stats | 需要有效 access token，否则 401 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | GET /dashboard/stats 未鉴权 | mitigate | authGuard() 强制要求有效 access token，未登录返回 401。接口仅返回聚合 count 数字，不含用户详情。 |
| T-05-04 | Information Disclosure | 统计数字泄露组织规模 | accept | 低威胁 — 仅返回 count 数字（如 userCount: 128），不含任何 PII。所有登录用户均可查看，与业务需求一致。 |
</threat_model>

<verification>
```bash
# 验证文件存在
ls backend/src/modules/dashboard/dashboard.route.ts

# 验证模块注册
grep "dashboardModule" backend/src/index.ts

# 验证接口结构
grep "stats" backend/src/modules/dashboard/dashboard.route.ts
```
</verification>

<success_criteria>
- GET /api/v1/dashboard/stats 接口存在，需登录，返回 { userCount, departmentCount, roleCount }
- 接口使用 Promise.all 并行查询，满足 NFR-1 性能要求
- dashboardModule 已注册到 index.ts 的 /api/v1 路由组
</success_criteria>

<output>
After completion, create `.planning/phases/05-responsive/05-03-SUMMARY.md`
</output>
