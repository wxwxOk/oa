---
phase: 4
plan: "04-01"
title: "后端 RBAC 保护 — ADMIN 锁死 + 角色删除拒绝 + userCount"
wave: 1
depends_on: []
requirements_addressed: [FR-4.1, FR-4.3, FR-4.4, NFR-1, NFR-3]
files_modified:
  - backend/src/modules/role/role.route.ts
autonomous: true
must_haves:
  truths:
    - "DELETE /api/v1/roles/{adminId} 返回 400 且 message 包含 '系统角色不可删除'"
    - "DELETE /api/v1/roles/{id} 对有挂载用户的角色返回 400 且 message 包含 '请先解绑'"
    - "PUT /api/v1/roles/{adminId}/permissions body {permissionIds:[]} 返回 400 且 message 包含 '不能清空'"
    - "GET /api/v1/roles 每个角色对象包含 _count.users 数值字段"
  artifacts:
    - path: "backend/src/modules/role/role.route.ts"
      provides: "ADMIN 锁死 + 挂载检查 + userCount"
      contains: "BizError"
  key_links:
    - from: "role.route.ts DELETE handler"
      to: "BizError"
      via: "throw new BizError('系统角色不可删除')"
    - from: "role.route.ts GET / handler"
      to: "prisma _count"
      via: "include: { _count: { select: { users: true } } }"
---

<objective>
为 role.route.ts 添加三层后端保护：(1) ADMIN 角色不可删除 (D-01)；(2) 被用户挂载的角色不可删除 (D-06)；(3) ADMIN 角色权限不能清空为空数组 (D-02)。同时在 GET /roles 列表中返回 _count.users 字段 (D-07)。

Purpose: 后端硬校验是安全底线，防止绕过前端直接 curl 操作 (D-03)。
Output: 修改后的 role.route.ts，包含完整的 ADMIN 保护 + 挂载检查 + userCount 聚合。
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | 不可信输入：用户可绕过前端禁用按钮直接发 HTTP 请求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | DELETE /roles/:id | mitigate | 后端检查 role.code === 'ADMIN'，返回 400 BizError (D-01) |
| T-04-02 | Tampering | DELETE /roles/:id | mitigate | 后端检查 _count.users > 0，返回 400 BizError (D-06) |
| T-04-03 | Elevation of Privilege | PUT /roles/:id/permissions | mitigate | 后端检查 ADMIN + permissionIds.length === 0，返回 400 BizError (D-02) |
| T-04-04 | Information Disclosure | GET /roles | accept | _count.users 是非敏感聚合数据，无需额外保护 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: role.route.ts — 导入 BizError + GET 列表增加 _count.users + DELETE 增加 ADMIN/挂载双重保护 + PUT permissions 增加 ADMIN 空分配拒绝</name>

  <read_first>
    - backend/src/modules/role/role.route.ts (当前完整实现，修改目标)
    - backend/src/modules/department/department.route.ts (BizError 导入 + _count + 删除保护的 analog 模式)
    - backend/src/utils/errors.ts (BizError 构造签名: message, status=400, code='BIZ_ERROR')
  </read_first>

  <files>backend/src/modules/role/role.route.ts</files>

  <action>
修改 `backend/src/modules/role/role.route.ts`，共 4 处变更：

**变更 1 — 导入 BizError (line 4)**
将:
```typescript
import { notFound } from '../../utils/errors';
```
改为:
```typescript
import { BizError, notFound } from '../../utils/errors';
```

**变更 2 — GET / 列表增加 _count (lines 9-12)**
将 findMany 的 include 从:
```typescript
prisma.role.findMany({
  include: { permissions: { include: { permission: { select: { id: true, code: true } } } } },
  orderBy: { id: 'asc' },
})
```
改为:
```typescript
prisma.role.findMany({
  include: {
    permissions: { include: { permission: { select: { id: true, code: true } } } },
    _count: { select: { users: true } },
  },
  orderBy: { id: 'asc' },
})
```
`_count` 与 `permissions` 同级，都在 `include` 内部。返回体每个 role 对象新增 `_count: { users: number }`。

**变更 3 — DELETE /:id 增加 ADMIN + 挂载双重保护 (lines 72-76)**
将整个 delete handler 从:
```typescript
app.use(authGuard('role:delete')).delete('/:id', async ({ params }: any) => {
  await prisma.role.delete({ where: { id: Number(params.id) } });
  return { ok: true };
}),
```
改为:
```typescript
app.use(authGuard('role:delete')).delete('/:id', async ({ params }: any) => {
  const id = Number(params.id);
  const role = await prisma.role.findUnique({
    where: { id },
    include: { _count: { select: { users: true } } },
  });
  if (!role) throw notFound('角色不存在');
  // D-08: ADMIN 检查优先于挂载检查
  if (role.code === 'ADMIN') throw new BizError('系统角色不可删除');
  // D-06: 挂载用户检查
  if (role._count.users > 0) {
    throw new BizError(`该角色仍有 ${role._count.users} 个用户，请先解绑`);
  }
  await prisma.role.delete({ where: { id } });
  return { ok: true };
}),
```
检查顺序严格遵循 D-08：先 ADMIN code 检查，再 _count.users 检查。

**变更 4 — PUT /:id/permissions 增加 ADMIN 空分配拒绝 (lines 55-63)**
在 `const roleId = Number(params.id);` 之后、`await prisma.rolePermission.deleteMany` 之前插入:
```typescript
// D-02: ADMIN 角色不能清空所有权限
const role = await prisma.role.findUnique({ where: { id: roleId } });
if (role?.code === 'ADMIN' && body.permissionIds.length === 0) {
  throw new BizError('ADMIN 角色不能清空所有权限');
}
```
仅拒绝 permissionIds 为空数组的情况，允许 ADMIN 增减权限。
  </action>

  <verify>
    <automated>
cd /e/webspace/oa && grep "BizError" backend/src/modules/role/role.route.ts && grep "_count.*select.*users" backend/src/modules/role/role.route.ts && grep "role.code.*ADMIN" backend/src/modules/role/role.route.ts && grep "permissionIds.length === 0" backend/src/modules/role/role.route.ts
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "BizError, notFound" backend/src/modules/role/role.route.ts` 匹配到导入行
    - `grep "_count: { select: { users: true } }" backend/src/modules/role/role.route.ts` 至少匹配 2 处（GET list + DELETE handler）
    - `grep "role.code === 'ADMIN'" backend/src/modules/role/role.route.ts` 至少匹配 2 处（DELETE + PUT permissions）
    - `grep "role._count.users > 0" backend/src/modules/role/role.route.ts` 匹配 1 处
    - `grep "permissionIds.length === 0" backend/src/modules/role/role.route.ts` 匹配 1 处
    - `grep "系统角色不可删除" backend/src/modules/role/role.route.ts` 匹配 1 处
    - `grep "请先解绑" backend/src/modules/role/role.route.ts` 匹配 1 处
    - `grep "不能清空所有权限" backend/src/modules/role/role.route.ts` 匹配 1 处
  </acceptance_criteria>

  <done>
    role.route.ts 包含完整的 ADMIN 删除保护、挂载用户删除保护、ADMIN 空权限拒绝、角色列表 _count.users 聚合。所有 BizError 抛出使用默认 status=400。
  </done>
</task>

</tasks>

<verification>
手动 curl 验证（需先获取 admin token）：
1. `curl -X DELETE -H "Authorization: Bearer $TOKEN" http://localhost:3000/api/v1/roles/1` → 400 "系统角色不可删除"
2. `curl -X PUT -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"permissionIds":[]}' http://localhost:3000/api/v1/roles/1/permissions` → 400 "ADMIN 角色不能清空所有权限"
3. `curl -H "Authorization: Bearer $TOKEN" http://localhost:3000/api/v1/roles` → 每个 role 含 `_count: { users: N }`
</verification>

<success_criteria>
后端 role.route.ts 的 4 处变更全部到位，grep 验证通过。ADMIN 角色在 DELETE 和 PUT permissions 两个端点均受保护。角色列表返回 userCount 聚合。
</success_criteria>

<output>
After completion, create `.planning/phases/04-rbac/04-01-SUMMARY.md`
</output>
