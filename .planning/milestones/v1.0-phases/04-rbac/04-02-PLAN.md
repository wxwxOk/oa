---
phase: 4
plan: "04-02"
title: "前端 RolePage 补全 — 成员数展示 + 按钮禁用逻辑"
wave: 2
depends_on: ["04-01"]
requirements_addressed: [FR-4.1, FR-4.2, FR-4.3]
files_modified:
  - frontend/src/pages/RolePage.vue
autonomous: true
must_haves:
  truths:
    - "角色列表每行显示 '成员: N' 文本"
    - "ADMIN 角色的删除按钮处于 disabled 状态且有 tooltip '系统角色不可删除'"
    - "有挂载用户的角色删除按钮处于 disabled 状态且有 tooltip '请先解绑 N 个用户'"
    - "选中 ADMIN 角色且清空所有权限勾选时，保存权限按钮处于 disabled 状态"
  artifacts:
    - path: "frontend/src/pages/RolePage.vue"
      provides: "成员数展示 + 按钮禁用 + isAdminSelected computed"
      contains: "isAdminSelected"
  key_links:
    - from: "RolePage.vue 删除按钮"
      to: "r._count?.users"
      via: ":disable 绑定"
    - from: "RolePage.vue 保存权限按钮"
      to: "isAdminSelected && checkedIds.length === 0"
      via: ":disable 绑定"
---

<objective>
补全 RolePage.vue 的三项前端体验：(1) 角色列表每行展示成员数 (D-07)；(2) ADMIN 行和有挂载用户行的删除按钮禁用 + tooltip (D-01/D-06/D-08)；(3) 选中 ADMIN 且权限全清空时保存按钮禁用 (D-02)。

Purpose: 前端禁用按钮改善用户体验，配合后端硬校验形成双保险 (D-03)。
Output: 修改后的 RolePage.vue，包含成员数展示、按钮禁用逻辑、isAdminSelected computed。
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM | 前端禁用仅为 UX 辅助，不构成安全边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | 前端 :disable 属性 | accept | 前端禁用可被 DevTools 绕过，后端 04-01 已有硬校验兜底 (D-03) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: RolePage.vue — caption 行追加成员数 + 删除按钮 :disable + tooltip + 保存权限按钮 :disable + isAdminSelected computed</name>

  <read_first>
    - frontend/src/pages/RolePage.vue (当前完整实现，修改目标)
    - .planning/phases/04-rbac/04-PATTERNS.md (RolePage 改造的精确代码片段)
  </read_first>

  <files>frontend/src/pages/RolePage.vue</files>

  <action>
修改 `frontend/src/pages/RolePage.vue`，共 4 处变更：

**变更 1 — caption 行追加成员数 (template line 22)**
将:
```vue
<q-item-label caption>{{ r.code }}</q-item-label>
```
改为:
```vue
<q-item-label caption>{{ r.code }} · 成员: {{ r._count?.users ?? 0 }}</q-item-label>
```

**变更 2 — 删除按钮增加 :disable + q-tooltip (template line 27)**
将:
```vue
<q-btn v-perm="'role:delete'" size="sm" flat dense icon="delete" color="negative" @click.stop="onDelete(r)" />
```
改为:
```vue
<q-btn
  v-perm="'role:delete'"
  size="sm" flat dense icon="delete" color="negative"
  :disable="r.code === 'ADMIN' || (r._count?.users ?? 0) > 0"
  @click.stop="onDelete(r)"
>
  <q-tooltip v-if="r.code === 'ADMIN'">系统角色不可删除</q-tooltip>
  <q-tooltip v-else-if="(r._count?.users ?? 0) > 0">
    请先解绑 {{ r._count.users }} 个用户
  </q-tooltip>
</q-btn>
```
D-08: ADMIN 条件在前（v-if），挂载条件在后（v-else-if）。

**变更 3 — 保存权限按钮增加 :disable + q-tooltip (template line 48)**
将:
```vue
<q-btn v-perm="'role:assign-permission'" color="primary" label="保存权限" @click="savePerms" />
```
改为:
```vue
<q-btn
  v-perm="'role:assign-permission'"
  color="primary"
  label="保存权限"
  :disable="isAdminSelected && checkedIds.length === 0"
  @click="savePerms"
>
  <q-tooltip v-if="isAdminSelected && checkedIds.length === 0">
    ADMIN 角色不能清空所有权限
  </q-tooltip>
</q-btn>
```

**变更 4 — script 新增 isAdminSelected computed**
在 `const groupedPerms = computed(...)` 之后（约 line 89 后）新增:
```typescript
const isAdminSelected = computed(() => selected.value?.code === 'ADMIN');
```
  </action>

  <verify>
    <automated>
cd /e/webspace/oa && grep "isAdminSelected" frontend/src/pages/RolePage.vue && grep "_count" frontend/src/pages/RolePage.vue && grep "系统角色不可删除" frontend/src/pages/RolePage.vue && grep "不能清空所有权限" frontend/src/pages/RolePage.vue
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "isAdminSelected" frontend/src/pages/RolePage.vue` 至少匹配 3 处（computed 定义 + 保存按钮 :disable + tooltip v-if）
    - `grep "_count" frontend/src/pages/RolePage.vue` 至少匹配 4 处（caption 展示 + 删除按钮 :disable + 两个 tooltip 条件）
    - `grep "成员:" frontend/src/pages/RolePage.vue` 匹配 1 处
    - `grep "系统角色不可删除" frontend/src/pages/RolePage.vue` 匹配 1 处
    - `grep "请先解绑" frontend/src/pages/RolePage.vue` 匹配 1 处
    - `grep "不能清空所有权限" frontend/src/pages/RolePage.vue` 匹配 1 处
    - `grep "r.code === 'ADMIN'" frontend/src/pages/RolePage.vue` 至少匹配 2 处（:disable + v-if）
  </acceptance_criteria>

  <done>
    RolePage.vue 展示成员数、ADMIN/挂载角色删除按钮禁用带 tooltip、ADMIN 空权限保存按钮禁用带 tooltip。isAdminSelected computed 已定义。
  </done>
</task>

</tasks>

<verification>
浏览器打开 http://localhost:9000/#/roles：
1. 角色列表每行 caption 显示 "ADMIN · 成员: 1"（或对应数字）
2. ADMIN 行删除按钮灰色不可点击，hover 显示 "系统角色不可删除"
3. 选中 ADMIN 角色 → 取消所有权限勾选 → 保存权限按钮灰色不可点击
</verification>

<success_criteria>
RolePage.vue 的 4 处变更全部到位，grep 验证通过。成员数展示、删除按钮禁用、保存权限按钮禁用三项功能均可在浏览器中验证。
</success_criteria>

<output>
After completion, create `.planning/phases/04-rbac/04-02-SUMMARY.md`
</output>
