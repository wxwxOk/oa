---
phase: 06-docker-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-env.sh
  - scripts/check-env.ps1
  - scripts/init.sh
  - scripts/init.ps1
  - scripts/backup.sh
  - scripts/backup.ps1
  - scripts/restore.sh
  - scripts/restore.ps1
  - scripts/upgrade.sh
  - scripts/upgrade.ps1
  - scripts/health.sh
  - scripts/health.ps1
autonomous: true
requirements: [NFR-2, NFR-4]

must_haves:
  truths:
    - "init 脚本从 .env.example 生成 .env 并填入随机 JWT_SECRET 和 POSTGRES_PASSWORD"
    - "check-env 脚本检测 JWT_SECRET 长度 >=32 且非默认值"
    - "check-env 脚本检测 POSTGRES_PASSWORD 非默认值"
    - "backup 脚本通过 docker exec 调用 pg_dump 导出到 backups/ 目录"
    - "restore 脚本从指定 SQL 文件恢复数据库"
    - "upgrade 脚本执行 pull + build + migrate"
    - "health 脚本检查 postgres/backend/frontend 三个服务状态"
    - "Bash 和 PowerShell 脚本功能对等"
  artifacts:
    - path: "scripts/check-env.sh"
      provides: "环境变量校验"
      contains: "JWT_SECRET"
    - path: "scripts/check-env.ps1"
      provides: "环境变量校验 (PowerShell)"
      contains: "JWT_SECRET"
    - path: "scripts/init.sh"
      provides: "首次部署初始化"
      contains: "openssl rand"
    - path: "scripts/init.ps1"
      provides: "首次部署初始化 (PowerShell)"
      contains: "GeneratePassword"
    - path: "scripts/backup.sh"
      provides: "数据库备份"
      contains: "pg_dump"
    - path: "scripts/backup.ps1"
      provides: "数据库备份 (PowerShell)"
      contains: "pg_dump"
    - path: "scripts/restore.sh"
      provides: "数据库恢复"
      contains: "psql"
    - path: "scripts/restore.ps1"
      provides: "数据库恢复 (PowerShell)"
      contains: "psql"
    - path: "scripts/upgrade.sh"
      provides: "服务升级"
      contains: "docker compose"
    - path: "scripts/upgrade.ps1"
      provides: "服务升级 (PowerShell)"
      contains: "docker compose"
    - path: "scripts/health.sh"
      provides: "健康检查"
      contains: "curl"
    - path: "scripts/health.ps1"
      provides: "健康检查 (PowerShell)"
      contains: "Invoke-WebRequest"
  key_links:
    - from: "scripts/init.sh"
      to: "scripts/check-env.sh"
      via: "init 调用 check-env"
      pattern: "check-env"
    - from: "scripts/init.sh"
      to: ".env.example"
      via: "复制为 .env"
      pattern: ".env.example"
    - from: "scripts/backup.sh"
      to: "docker exec oa-postgres"
      via: "pg_dump"
      pattern: "docker exec.*pg_dump"
---

<objective>
创建 Bash + PowerShell 双份部署脚本，覆盖 init/backup/restore/upgrade/health/check-env 六类操作。

Purpose: 自动化部署流程，防止人为错误（如忘记改默认密码），满足 NFR-4 一条命令部署。
Output: 12 个脚本文件（6 .sh + 6 .ps1）
</objective>

<execution_context>
@C:/Users/11828/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-docker-docs/06-CONTEXT.md
@.planning/phases/06-docker-docs/06-RESEARCH.md

<interfaces>
From .env.example:
```
POSTGRES_USER=oa
POSTGRES_PASSWORD=oa_pass_change_me
POSTGRES_DB=oa_db
POSTGRES_PORT=5432
BACKEND_PORT=3000
DATABASE_URL=postgresql://oa:oa_pass_change_me@postgres:5432/oa_db?schema=public
JWT_SECRET=please-change-this-to-a-long-random-string
JWT_EXPIRES_IN=2h
JWT_REFRESH_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:9000
FRONTEND_PORT=9000
VITE_API_BASE=http://localhost:3000/api/v1
```

From docker-compose.yml:
- Container names: oa-postgres, oa-backend, oa-frontend
- Backend health endpoint: http://localhost:3000/health
- Frontend: http://localhost:80/ (inside container), mapped to FRONTEND_PORT
- Postgres: pg_isready -U ${POSTGRES_USER:-oa} -d ${POSTGRES_DB:-oa_db}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: check-env + init 脚本（Bash + PowerShell）</name>
  <read_first>
    - .env.example
    - docker-compose.yml
  </read_first>
  <files>
    scripts/check-env.sh
    scripts/check-env.ps1
    scripts/init.sh
    scripts/init.ps1
  </files>
  <action>
    创建 `scripts/` 目录。

    **Per D-13: scripts/check-env.sh**
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
    PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
    ENV_FILE="$PROJECT_DIR/.env"

    if [ ! -f "$ENV_FILE" ]; then
      echo "ERROR: .env not found. Run init script first."
      exit 1
    fi

    set -a; source "$ENV_FILE"; set +a

    ERRORS=0
    if [ ${#JWT_SECRET} -lt 32 ]; then
      echo "ERROR: JWT_SECRET must be >= 32 chars (current: ${#JWT_SECRET})"
      ERRORS=1
    fi
    if [ "$JWT_SECRET" = "please-change-this-to-a-long-random-string" ]; then
      echo "ERROR: JWT_SECRET is default value"
      ERRORS=1
    fi
    if [ "$POSTGRES_PASSWORD" = "oa_pass_change_me" ]; then
      echo "ERROR: POSTGRES_PASSWORD is default value"
      ERRORS=1
    fi
    if [ $ERRORS -ne 0 ]; then exit 1; fi
    echo "Environment check passed."
    ```

    **Per D-13: scripts/check-env.ps1**
    等价 PowerShell 实现：读取 .env 文件，逐行解析 KEY=VALUE，检查 JWT_SECRET 长度 >=32 且不等于 `please-change-this-to-a-long-random-string`，POSTGRES_PASSWORD 不等于 `oa_pass_change_me`。失败时 exit 1。

    **Per D-12: scripts/init.sh**
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
    PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

    echo "=== OA Init ==="

    # 生成 .env
    if [ ! -f "$PROJECT_DIR/.env" ]; then
      cp "$PROJECT_DIR/.env.example" "$PROJECT_DIR/.env"
      JWT_SECRET=$(openssl rand -base64 32)
      PG_PASS=$(openssl rand -base64 16)
      # sed 替换默认值
      sed -i "s|please-change-this-to-a-long-random-string|$JWT_SECRET|" "$PROJECT_DIR/.env"
      sed -i "s|oa_pass_change_me|$PG_PASS|g" "$PROJECT_DIR/.env"
      # 同步 DATABASE_URL 中的密码
      sed -i "s|postgresql://oa:oa_pass_change_me@|postgresql://oa:$PG_PASS@|" "$PROJECT_DIR/.env"
      echo "Generated .env with random secrets"
    else
      echo ".env already exists, skipping generation"
    fi

    # 校验
    bash "$SCRIPT_DIR/check-env.sh"

    # 启动
    docker compose -f "$PROJECT_DIR/docker-compose.yml" up -d --build
    echo "Waiting for services..."
    sleep 15
    bash "$SCRIPT_DIR/health.sh"
    echo "=== Init complete ==="
    ```

    注意 sed -i 在 macOS 上需要 `sed -i ''`，但此脚本主要面向 Linux 部署环境。Windows 用户使用 .ps1 版本。

    **Per D-12: scripts/init.ps1**
    等价 PowerShell 实现：
    - 检查 .env 是否存在，不存在则从 .env.example 复制
    - 使用 `[System.Security.Cryptography.RandomNumberGenerator]` 生成 32 字节 base64 作为 JWT_SECRET
    - 使用同样方式生成 16 字节 base64 作为 POSTGRES_PASSWORD
    - 用 `(Get-Content .env) -replace 'old','new' | Set-Content .env` 替换默认值
    - 同步替换 DATABASE_URL 中的密码
    - 调用 `& "$PSScriptRoot/check-env.ps1"`
    - 执行 `docker compose up -d --build`
    - Start-Sleep 15
    - 调用 `& "$PSScriptRoot/health.ps1"`
  </action>
  <verify>
    <automated>
      test -f scripts/check-env.sh && test -f scripts/check-env.ps1 && test -f scripts/init.sh && test -f scripts/init.ps1 && grep -q "JWT_SECRET" scripts/check-env.sh && grep -q "openssl rand" scripts/init.sh && grep -q "check-env" scripts/init.sh && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - scripts/check-env.sh contains `JWT_SECRET` length check (`${#JWT_SECRET} -lt 32`)
    - scripts/check-env.sh contains default value check for `please-change-this-to-a-long-random-string`
    - scripts/check-env.sh contains default value check for `oa_pass_change_me`
    - scripts/check-env.ps1 contains equivalent JWT_SECRET and POSTGRES_PASSWORD checks
    - scripts/init.sh contains `openssl rand -base64 32` for JWT_SECRET generation
    - scripts/init.sh contains `openssl rand -base64 16` for POSTGRES_PASSWORD generation
    - scripts/init.sh calls `check-env.sh`
    - scripts/init.sh calls `docker compose` with `up -d --build`
    - scripts/init.ps1 contains random secret generation and calls check-env.ps1
    - All 4 files have shebang (bash) or proper PowerShell structure
  </acceptance_criteria>
  <done>4 个脚本创建完成：check-env 校验 JWT_SECRET>=32 + 非默认值 + PG 密码非默认值；init 从 .env.example 生成 .env 并填入随机密码后启动服务</done>
</task>

<task type="auto">
  <name>Task 2: backup/restore/upgrade/health 脚本（Bash + PowerShell）</name>
  <read_first>
    - docker-compose.yml
    - .env.example
    - scripts/check-env.sh (from Task 1)
  </read_first>
  <files>
    scripts/backup.sh
    scripts/backup.ps1
    scripts/restore.sh
    scripts/restore.ps1
    scripts/upgrade.sh
    scripts/upgrade.ps1
    scripts/health.sh
    scripts/health.ps1
  </files>
  <action>
    **Per D-12: scripts/backup.sh**
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
    PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
    BACKUP_DIR="$PROJECT_DIR/backups"
    mkdir -p "$BACKUP_DIR"
    FILENAME="$BACKUP_DIR/$(date +%Y-%m-%d_%H%M%S).sql"
    set -a; source "$PROJECT_DIR/.env"; set +a
    docker exec oa-postgres pg_dump -U "${POSTGRES_USER:-oa}" "${POSTGRES_DB:-oa_db}" > "$FILENAME"
    echo "Backup saved: $FILENAME"
    ```

    **scripts/backup.ps1** — 等价 PowerShell：
    - 创建 backups/ 目录
    - 文件名格式 `backups/yyyy-MM-dd_HHmmss.sql`
    - 从 .env 读取 POSTGRES_USER 和 POSTGRES_DB
    - `docker exec oa-postgres pg_dump -U $user $db | Out-File -Encoding utf8 $filename`

    **Per D-12: scripts/restore.sh**
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
    PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
    if [ -z "${1:-}" ]; then
      echo "Usage: restore.sh <backup-file.sql>"
      exit 1
    fi
    BACKUP_FILE="$1"
    if [ ! -f "$BACKUP_FILE" ]; then
      echo "ERROR: File not found: $BACKUP_FILE"
      exit 1
    fi
    set -a; source "$PROJECT_DIR/.env"; set +a
    cat "$BACKUP_FILE" | docker exec -i oa-postgres psql -U "${POSTGRES_USER:-oa}" "${POSTGRES_DB:-oa_db}"
    echo "Restore complete from: $BACKUP_FILE"
    ```

    **scripts/restore.ps1** — 等价 PowerShell：接受参数 `$BackupFile`，验证文件存在，`Get-Content $BackupFile | docker exec -i oa-postgres psql -U $user $db`

    **Per D-12: scripts/upgrade.sh**
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
    PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
    echo "=== OA Upgrade ==="
    echo "Pulling latest images..."
    docker compose -f "$PROJECT_DIR/docker-compose.yml" pull
    echo "Rebuilding and restarting..."
    docker compose -f "$PROJECT_DIR/docker-compose.yml" up -d --build
    echo "Waiting for services..."
    sleep 15
    bash "$SCRIPT_DIR/health.sh"
    echo "=== Upgrade complete ==="
    ```
    注意：migrate deploy 已在后端 CMD 中自动执行，无需脚本额外调用。

    **scripts/upgrade.ps1** — 等价 PowerShell：docker compose pull + up -d --build + Start-Sleep 15 + 调用 health.ps1

    **Per D-12: scripts/health.sh**
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
    PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
    set -a; source "$PROJECT_DIR/.env"; set +a
    ERRORS=0
    # Postgres
    if docker exec oa-postgres pg_isready -U "${POSTGRES_USER:-oa}" > /dev/null 2>&1; then
      echo "[OK] PostgreSQL"
    else
      echo "[FAIL] PostgreSQL"
      ERRORS=1
    fi
    # Backend
    if curl -sf "http://localhost:${BACKEND_PORT:-3000}/health" > /dev/null 2>&1; then
      echo "[OK] Backend"
    else
      echo "[FAIL] Backend"
      ERRORS=1
    fi
    # Frontend
    if curl -sf "http://localhost:${FRONTEND_PORT:-9000}/" > /dev/null 2>&1; then
      echo "[OK] Frontend"
    else
      echo "[FAIL] Frontend"
      ERRORS=1
    fi
    if [ $ERRORS -ne 0 ]; then
      echo "Some services are unhealthy!"
      exit 1
    fi
    echo "All services healthy."
    ```

    **scripts/health.ps1** — 等价 PowerShell：
    - Postgres: `docker exec oa-postgres pg_isready -U $user`
    - Backend: `Invoke-WebRequest -Uri "http://localhost:$backendPort/health" -UseBasicParsing`
    - Frontend: `Invoke-WebRequest -Uri "http://localhost:$frontendPort/" -UseBasicParsing`
    - 打印 [OK]/[FAIL] 状态，任一失败则 exit 1
  </action>
  <verify>
    <automated>
      test -f scripts/backup.sh && test -f scripts/restore.sh && test -f scripts/upgrade.sh && test -f scripts/health.sh && test -f scripts/backup.ps1 && test -f scripts/restore.ps1 && test -f scripts/upgrade.ps1 && test -f scripts/health.ps1 && grep -q "pg_dump" scripts/backup.sh && grep -q "psql" scripts/restore.sh && grep -q "docker compose" scripts/upgrade.sh && grep -q "pg_isready" scripts/health.sh && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - scripts/backup.sh contains `docker exec oa-postgres pg_dump`
    - scripts/backup.sh creates file in `backups/` with date-stamped name
    - scripts/backup.ps1 contains equivalent pg_dump via docker exec
    - scripts/restore.sh accepts backup file as $1 argument
    - scripts/restore.sh contains `docker exec -i oa-postgres psql`
    - scripts/restore.ps1 accepts $BackupFile parameter
    - scripts/upgrade.sh contains `docker compose pull` and `docker compose up -d --build`
    - scripts/upgrade.sh calls health.sh at the end
    - scripts/upgrade.ps1 contains equivalent docker compose commands
    - scripts/health.sh checks postgres (pg_isready), backend (/health), frontend (/)
    - scripts/health.sh prints [OK] or [FAIL] per service
    - scripts/health.ps1 uses Invoke-WebRequest for backend/frontend checks
    - All .sh files start with `#!/usr/bin/env bash` and `set -euo pipefail`
  </acceptance_criteria>
  <done>8 个脚本创建完成：backup 通过 docker exec pg_dump 导出，restore 从 SQL 文件恢复，upgrade 执行 pull+build+health，health 检查三个服务状态。Bash 和 PowerShell 功能对等</done>
</task>

</tasks>

<verification>
- `ls scripts/*.sh scripts/*.ps1 | wc -l` — 返回 12
- `grep -l "pg_dump" scripts/*.sh` — 返回 backup.sh
- `grep -l "JWT_SECRET" scripts/check-env.*` — 返回 check-env.sh 和 check-env.ps1
- `head -1 scripts/*.sh` — 每个文件均为 `#!/usr/bin/env bash`
</verification>

<success_criteria>
- 12 个脚本文件全部创建（6 .sh + 6 .ps1）
- check-env 校验 JWT_SECRET >=32 + 非默认值 + PG 密码非默认值
- init 生成 .env + 随机密码 + 调用 check-env + 启动服务
- backup 通过 docker exec pg_dump 导出到 backups/
- restore 从指定 SQL 文件恢复
- upgrade 执行 pull + build + health
- health 检查 postgres/backend/frontend 三个服务
- Bash 和 PowerShell 功能对等
</success_criteria>

<output>
After completion, create `.planning/phases/06-docker-docs/06-02-SUMMARY.md`
</output>
