---
phase: 07-template-designer
plan: 05
type: execute
wave: 3
depends_on: [07-01, 07-02, 07-03, 07-04]
files_modified: []
autonomous: false
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05, TMPL-01, TMPL-02, TMPL-03, TMPL-04, TMPL-05]

must_haves:
  truths:
    - "Full template lifecycle works: create -> design -> save -> publish -> edit -> version bump -> unpublish"
    - "All 7 field types can be dragged to canvas and configured"
    - "Signature field shows 400x200 placeholder in designer"
    - "Property editor updates canvas in real-time"
    - "Only draft templates can be deleted"
    - "Mobile shows template list without designer access"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the complete template management and form designer feature.

Purpose: Human verifies the full workflow before marking Phase 7 complete.
Output: Verification confirmation or issue list.
</objective>

<execution_context>
@C:/Users/11828/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-template-designer/07-CONTEXT.md
@.planning/phases/07-template-designer/07-UI-SPEC.md
@.planning/phases/07-template-designer/07-01-SUMMARY.md
@.planning/phases/07-template-designer/07-02-SUMMARY.md
@.planning/phases/07-template-designer/07-03-SUMMARY.md
@.planning/phases/07-template-designer/07-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Full template + designer walkthrough</name>
  <files>none</files>
  <action>
Human verifies the complete template management and form designer feature by walking through all 28 test steps covering template CRUD, designer drag-drop, field configuration, save/publish lifecycle, delete policy, mobile view, and dark mode.

What was built:
- Backend: FormTemplate Prisma model, CRUD + publish/unpublish API, RBAC permissions
- Frontend: Template list page (QTable + mobile cards), status filter, create/delete/publish dialogs
- Frontend: 3-panel form designer with drag-drop field palette, WYSIWYG canvas, property editor
- 7 field types: text, textarea, radio, checkbox, date, phone, signature
- Sidebar navigation with "模板管理" entry

How to verify — start both backend and frontend dev servers, then:

Template List (PC):
1. Navigate to /templates via sidebar "模板管理"
2. Verify empty state shows "暂无模板" with "创建模板" button
3. Click "创建模板", fill name "测试模板", click "保存模板"
4. Verify template appears in table with status "草稿", version "v1"
5. Verify status filter buttons work (全部/草稿/已发布/已下线)

Designer (PC):
6. Click "设计" action on the template row
7. Verify 3-panel layout: field palette (left), canvas (center), property editor (right)
8. Drag "文本" from palette to canvas — verify field card appears
9. Drag "单选" from palette — verify it appears with default options
10. Drag "手写签名" from palette — verify 400x200 placeholder shows "签名区域"
11. Click a field on canvas — verify property editor populates
12. Change field label in property editor — verify canvas updates in real-time
13. Toggle "必填" — verify red "*" badge appears on canvas field
14. For radio field: add/remove options — verify canvas preview updates
15. Drag to reorder fields on canvas — verify order changes
16. Click delete button on a field — verify it's removed

Save + Publish:
17. Click "保存设计" — verify "保存成功" notification
18. Click "发布模板" — confirm dialog — verify status changes, button becomes "下线模板"
19. Make a change and save again — verify "模板已更新至 v2" notification
20. Click "下线模板" — confirm — verify button reverts to "发布模板"

Delete Policy:
21. Go back to template list
22. Verify published/offline templates do NOT show delete button
23. Create a new draft template — verify delete button appears
24. Delete it — confirm — verify "已删除" notification

Mobile:
25. Resize browser to mobile width (< 1024px)
26. Verify template list shows as cards (not table)
27. Verify no "设计" action on mobile cards

Dark Mode:
28. Toggle dark mode — verify designer panels use correct dark theme colors

Resume signal: Type "approved" if all checks pass, or describe any issues found.
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human verification"</automated>
  </verify>
  <done>User confirms "approved" after completing all 28 verification steps, covering TMPL-01~05 and DSGN-01~05</done>
</task>

</tasks>

<verification>
Human walkthrough covers all 10 requirements (TMPL-01~05, DSGN-01~05).
</verification>

<success_criteria>
All verification steps pass. User confirms "approved".
</success_criteria>

<output>
After completion, create `.planning/phases/07-template-designer/07-05-SUMMARY.md`
</output>
