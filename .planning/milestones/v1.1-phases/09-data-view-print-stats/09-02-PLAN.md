---
phase: 09-data-view-print-stats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/stores/submission.ts
  - frontend/src/assets/print.css
  - frontend/src/composables/usePdfExport.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]
must_haves:
  truths:
    - "html2canvas, jspdf, vue-chartjs, chart.js 已安装到 frontend/package.json"
    - "useSubmissionStore 提供 fetchList 和 fetchDetail 方法"
    - "print.css 包含 @media print 规则，隐藏非打印区域并格式化 #print-area"
    - "usePdfExport 导出 exportToPdf 函数，接收 HTMLElement 和 filename 参数"
  artifacts:
    - path: "frontend/src/stores/submission.ts"
      provides: "提交数据 Pinia store"
      exports: ["useSubmissionStore", "SubmissionRow"]
    - path: "frontend/src/assets/print.css"
      provides: "@media print 全局样式"
      contains: "@media print"
    - path: "frontend/src/composables/usePdfExport.ts"
      provides: "PDF 导出 composable"
      exports: ["exportToPdf"]
  key_links:
    - from: "frontend/src/stores/submission.ts"
      to: "/api/v1/templates/:templateId/submissions"
      via: "api.get"
      pattern: "api\\.get.*submissions"
    - from: "frontend/src/composables/usePdfExport.ts"
      to: "html2canvas + jsPDF"
      via: "import"
      pattern: "import html2canvas"
---

<objective>
安装前端新依赖（html2canvas, jsPDF, vue-chartjs, chart.js），创建提交数据 store、打印样式表和 PDF 导出 composable。

Purpose: 为 Wave 2 的前端页面（SubmissionPage、FormStatsPanel）提供数据层和工具层基础。
Output: 4 个 npm 包安装 + submission store + print.css + usePdfExport.ts
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-view-print-stats/09-RESEARCH.md
@.planning/phases/09-data-view-print-stats/09-CONTEXT.md
@.planning/phases/09-data-view-print-stats/09-PATTERNS.md
@.planning/phases/09-data-view-print-stats/09-UI-SPEC.md

<interfaces>
<!-- 现有 store 模式 — 执行者需要遵循的模式 -->

From frontend/src/stores/template.ts:
```typescript
import { defineStore } from 'pinia';
import { api } from 'src/boot/axios';

export interface Template {
  id: number;
  name: string;
  // ...
}

export const useTemplateStore = defineStore('template', {
  state: () => ({
    rows: [] as Template[],
    total: 0,
    loading: false,
    page: 1,
    size: 10,
    statusFilter: '' as string,
  }),
  actions: {
    async fetchList() { /* ... */ },
    async fetchOne(id: number) { /* ... */ },
  },
});
```

From frontend/src/composables/useResponsive.ts:
```typescript
import { computed } from 'vue';
import { useQuasar } from 'quasar';

export function useResponsive() {
  const $q = useQuasar();
  const isDesktop = computed(() => $q.screen.gt.sm);
  const isMobile = computed(() => !$q.screen.gt.sm);
  return { isDesktop, isMobile };
}
```

From frontend/src/boot/axios.ts:
```typescript
export const api = axios.create({ baseURL: '/api/v1' });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装前端依赖 + 创建 submission store</name>
  <files>frontend/package.json, frontend/src/stores/submission.ts</files>
  <read_first>
    frontend/package.json
    frontend/src/stores/template.ts
    frontend/src/boot/axios.ts
  </read_first>
  <action>
1. 在 `frontend/` 目录下安装依赖：
   ```bash
   cd frontend && npm install html2canvas jspdf vue-chartjs chart.js
   ```

2. 创建 `frontend/src/stores/submission.ts`，严格遵循 template.ts 的 store 模式：

```typescript
import { defineStore } from 'pinia';
import { api } from 'src/boot/axios';

export interface SubmissionRow {
  id: number;
  data: Record<string, any>;
  schemaVersion: number;
  submitterName: string | null;
  submitterPhone: string | null;
  createdAt: string;
  shareLink: {
    id: number;
    creator: { id: number; realName: string };
  };
}

export interface SubmissionDetail extends SubmissionRow {
  template: {
    name: string;
    schema: any[];
    schemaVersion: number;
  };
}

export interface SharerOption {
  id: number;
  realName: string;
}

export const useSubmissionStore = defineStore('submission', {
  state: () => ({
    rows: [] as SubmissionRow[],
    total: 0,
    loading: false,
    page: 1,
    size: 20,
  }),
  actions: {
    async fetchList(templateId: number, filters?: Record<string, any>) {
      this.loading = true;
      try {
        const params: Record<string, unknown> = {
          page: this.page,
          size: this.size,
          ...filters,
        };
        const { data } = await api.get(
          `/templates/${templateId}/submissions`,
          { params },
        );
        this.rows = data.rows;
        this.total = data.total;
      } finally {
        this.loading = false;
      }
    },
    async fetchDetail(templateId: number, submissionId: number): Promise<SubmissionDetail> {
      const { data } = await api.get(
        `/templates/${templateId}/submissions/${submissionId}`,
      );
      return data;
    },
    async fetchSharers(templateId: number): Promise<SharerOption[]> {
      const { data } = await api.get(
        `/templates/${templateId}/submissions/sharers`,
      );
      return data;
    },
  },
});
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && grep "html2canvas" package.json && grep "jspdf" package.json && grep "vue-chartjs" package.json && grep "chart.js" package.json && grep -n "export const useSubmissionStore" src/stores/submission.ts</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json dependencies 包含 html2canvas, jspdf, vue-chartjs, chart.js
    - frontend/src/stores/submission.ts 存在且导出 `useSubmissionStore`
    - store 导出 `SubmissionRow`, `SubmissionDetail`, `SharerOption` 接口
    - store 包含 `fetchList(templateId, filters?)` 方法，调用 `api.get('/templates/${templateId}/submissions')`
    - store 包含 `fetchDetail(templateId, submissionId)` 方法
    - store 包含 `fetchSharers(templateId)` 方法
    - store state 包含 rows, total, loading, page, size 字段
  </acceptance_criteria>
  <done>4 个 npm 包已安装，submission store 导出含 fetchList/fetchDetail/fetchSharers 三个 action</done>
</task>

<task type="auto">
  <name>Task 2: 打印样式表 + PDF 导出 composable</name>
  <files>frontend/src/assets/print.css, frontend/src/composables/usePdfExport.ts</files>
  <read_first>
    frontend/src/composables/useResponsive.ts
    frontend/src/pages/DashboardPage.vue
  </read_first>
  <action>
1. 创建 `frontend/src/assets/print.css`，包含完整的 @media print 规则（来自 09-UI-SPEC.md Print Stylesheet Contract）：

```css
@media print {
  /* 隐藏所有非打印内容 */
  body > *:not(#print-area),
  .q-drawer__backdrop,
  .q-header,
  .q-footer,
  .q-page-sticky,
  .q-layout__section--marginal,
  .no-print,
  .drawer-header,
  .drawer-actions {
    display: none !important;
  }

  /* 打印区域全屏 */
  #print-area {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    display: block !important;
  }

  /* 表格排版 */
  .detail-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 12pt;
  }
  .detail-table th,
  .detail-table td {
    border: 1px solid #333;
    padding: 8px 12px;
    text-align: left;
  }
  .detail-table th {
    background: #f5f5f5;
    font-weight: 600;
    width: 30%;
  }

  /* 签名图片 */
  .signature-section img {
    max-width: 300px;
    max-height: 150px;
  }

  /* 页面设置 */
  @page {
    margin: 15mm;
    size: A4;
  }
}
```

然后在 `frontend/src/css/app.css`（或项目全局 CSS 入口）中 import 此文件。如果项目使用 quasar.config 的 css 数组，则在该数组中添加 `'src/assets/print.css'`。检查 `quasar.config.ts` 或 `quasar.config.js` 的 css 配置项。

2. 创建 `frontend/src/composables/usePdfExport.ts`，实现 PDF 导出功能（来自 09-RESEARCH.md Pattern 4）：

```typescript
import html2canvas from 'html2canvas';
import { jsPDF } from 'jspdf';

/**
 * 将 DOM 元素导出为 PDF 文件
 * @param element 要截图的 DOM 元素（#print-area）
 * @param filename 输出文件名（含 .pdf 后缀）
 */
export async function exportToPdf(
  element: HTMLElement,
  filename: string,
): Promise<void> {
  const canvas = await html2canvas(element, {
    scale: 2,
    useCORS: true,
    logging: false,
    backgroundColor: '#ffffff',
  });

  const imgData = canvas.toDataURL('image/jpeg', 0.95);
  const pdf = new jsPDF('p', 'mm', 'a4');
  const pageWidth = pdf.internal.pageSize.getWidth();
  const pageHeight = pdf.internal.pageSize.getHeight();
  const margin = 15; // mm
  const contentWidth = pageWidth - margin * 2;
  const imgHeight = (canvas.height * contentWidth) / canvas.width;
  const maxContentHeight = pageHeight - margin * 2;

  if (imgHeight <= maxContentHeight) {
    pdf.addImage(imgData, 'JPEG', margin, margin, contentWidth, imgHeight);
  } else {
    // 分页处理
    let remainingHeight = imgHeight;
    let sourceY = 0;
    while (remainingHeight > 0) {
      const sliceHeight = Math.min(remainingHeight, maxContentHeight);
      const sliceCanvas = document.createElement('canvas');
      sliceCanvas.width = canvas.width;
      sliceCanvas.height = (sliceHeight / contentWidth) * canvas.width;
      const ctx = sliceCanvas.getContext('2d')!;
      ctx.drawImage(
        canvas,
        0, sourceY, canvas.width, sliceCanvas.height,
        0, 0, sliceCanvas.width, sliceCanvas.height,
      );
      const sliceData = sliceCanvas.toDataURL('image/jpeg', 0.95);
      if (sourceY > 0) pdf.addPage();
      pdf.addImage(sliceData, 'JPEG', margin, margin, contentWidth, sliceHeight);
      sourceY += sliceCanvas.height;
      remainingHeight -= sliceHeight;
    }
  }

  pdf.save(filename);
}

/**
 * 批量导出多个提交为单个 PDF
 * @param renderFn 渲染函数，接收 index 返回要截图的 DOM 元素
 * @param total 总条数
 * @param filename 输出文件名
 * @param onProgress 进度回调 (current, total)
 * @returns 是否完成（false 表示被取消）
 */
export async function exportBatchToPdf(
  renderFn: (index: number) => Promise<HTMLElement>,
  total: number,
  filename: string,
  onProgress?: (current: number, total: number) => void,
  cancelRef?: { value: boolean },
): Promise<boolean> {
  const pdf = new jsPDF('p', 'mm', 'a4');
  const pageWidth = pdf.internal.pageSize.getWidth();
  const pageHeight = pdf.internal.pageSize.getHeight();
  const margin = 15;
  const contentWidth = pageWidth - margin * 2;
  const maxContentHeight = pageHeight - margin * 2;

  for (let i = 0; i < total; i++) {
    if (cancelRef?.value) return false;
    onProgress?.(i + 1, total);

    const element = await renderFn(i);
    const canvas = await html2canvas(element, {
      scale: 1.5, // 批量时降低 scale 节省内存
      useCORS: true,
      logging: false,
      backgroundColor: '#ffffff',
    });

    const imgData = canvas.toDataURL('image/jpeg', 0.9);
    const imgHeight = (canvas.height * contentWidth) / canvas.width;

    if (i > 0) pdf.addPage();

    if (imgHeight <= maxContentHeight) {
      pdf.addImage(imgData, 'JPEG', margin, margin, contentWidth, imgHeight);
    } else {
      let remainingHeight = imgHeight;
      let sourceY = 0;
      let firstSlice = true;
      while (remainingHeight > 0) {
        const sliceHeight = Math.min(remainingHeight, maxContentHeight);
        const sliceCanvas = document.createElement('canvas');
        sliceCanvas.width = canvas.width;
        sliceCanvas.height = (sliceHeight / contentWidth) * canvas.width;
        const ctx = sliceCanvas.getContext('2d')!;
        ctx.drawImage(
          canvas,
          0, sourceY, canvas.width, sliceCanvas.height,
          0, 0, sliceCanvas.width, sliceCanvas.height,
        );
        const sliceData = sliceCanvas.toDataURL('image/jpeg', 0.9);
        if (!firstSlice) pdf.addPage();
        firstSlice = false;
        pdf.addImage(sliceData, 'JPEG', margin, margin, contentWidth, sliceHeight);
        sourceY += sliceCanvas.height;
        remainingHeight -= sliceHeight;
      }
    }
  }

  pdf.save(filename);
  return true;
}
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && grep -n "@media print" src/assets/print.css && grep -n "export async function exportToPdf" src/composables/usePdfExport.ts && grep -n "export async function exportBatchToPdf" src/composables/usePdfExport.ts</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/assets/print.css 存在且包含 `@media print {` 规则
    - print.css 包含 `#print-area { position: fixed;` 规则
    - print.css 包含 `.detail-table` 表格排版规则（border: 1px solid #333）
    - print.css 包含 `.signature-section img` 签名图片规则
    - print.css 包含 `@page { margin: 15mm; size: A4; }`
    - print.css 已被全局引入（通过 quasar.config css 数组或 app.css import）
    - usePdfExport.ts 导出 `exportToPdf(element: HTMLElement, filename: string)` 函数
    - usePdfExport.ts 导出 `exportBatchToPdf(renderFn, total, filename, onProgress?, cancelRef?)` 函数
    - exportToPdf 使用 `html2canvas(element, { scale: 2 })` 和 `new jsPDF('p', 'mm', 'a4')`
    - exportBatchToPdf 使用 `scale: 1.5`（批量降低质量节省内存）
  </acceptance_criteria>
  <done>print.css 全局引入含完整 @media print 规则，usePdfExport.ts 导出单条和批量 PDF 导出函数</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM → html2canvas | 用户可见的 DOM 内容被截图，无额外安全风险 |
| store → API | 前端 store 通过 axios 调用后端 API，token 自动附加 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | usePdfExport | accept | html2canvas 渲染 DOM 而非 innerHTML，天然防 XSS；PDF 内容来自已渲染的安全 DOM |
| T-09-07 | Denial of Service | exportBatchToPdf | mitigate | 批量导出使用 scale: 1.5 降低内存，逐条处理后释放 Canvas，上限 50 条 |
| T-09-08 | Information Disclosure | print.css | accept | 打印内容与屏幕显示内容一致，无额外信息泄露 |
</threat_model>

<verification>
1. `cd frontend && grep "html2canvas" package.json` 确认依赖安装
2. `grep "@media print" src/assets/print.css` 确认打印样式存在
3. `grep "exportToPdf\|exportBatchToPdf" src/composables/usePdfExport.ts` 确认导出函数
</verification>

<success_criteria>
- 4 个 npm 包在 package.json 中
- submission store 可被 import 使用
- print.css 全局生效
- usePdfExport 可被 import 调用
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-view-print-stats/09-02-SUMMARY.md`
</output>
