---
phase: 09-data-view-print-stats
plan: 04
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - frontend/src/components/submission/FormStatsPanel.vue
  - frontend/src/pages/DashboardPage.vue
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "Dashboard 页面在现有统计卡片下方显示'表单统计'区域（需 form:stats:view 权限）"
    - "统计区域包含 QTable（员工姓名/分享次数/收集数量）和 Bar 柱状图"
    - "支持时间筛选：本周/本月快捷按钮 + 自定义日期范围"
    - "柱状图双柱：分享次数(#4F46E5) + 收集数量(#16A34A)"
  artifacts:
    - path: "frontend/src/components/submission/FormStatsPanel.vue"
      provides: "表单统计面板组件（表格+图表+时间筛选）"
      min_lines: 100
    - path: "frontend/src/pages/DashboardPage.vue"
      provides: "嵌入 FormStatsPanel 组件"
      contains: "FormStatsPanel"
  key_links:
    - from: "frontend/src/components/submission/FormStatsPanel.vue"
      to: "/api/v1/form-stats"
      via: "api.get('/form-stats')"
      pattern: "api\\.get.*form-stats"
    - from: "frontend/src/pages/DashboardPage.vue"
      to: "frontend/src/components/submission/FormStatsPanel.vue"
      via: "component import"
      pattern: "FormStatsPanel"
---

<objective>
创建表单统计面板组件并嵌入 Dashboard 页面，展示每个员工的分享次数和收集数量。

Purpose: 实现 DATA-05（基础统计：每个员工的分享次数和收集数量），让管理者直观了解员工工作量。
Output: FormStatsPanel.vue + DashboardPage.vue 嵌入
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-view-print-stats/09-RESEARCH.md
@.planning/phases/09-data-view-print-stats/09-CONTEXT.md
@.planning/phases/09-data-view-print-stats/09-PATTERNS.md
@.planning/phases/09-data-view-print-stats/09-UI-SPEC.md
@.planning/phases/09-data-view-print-stats/09-02-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的 API 接口 -->

GET /api/v1/form-stats?dateFrom=YYYY-MM-DD&dateTo=YYYY-MM-DD
Response: Array<{
  userId: number;
  realName: string;
  shareCount: number;
  submissionCount: number;
}>

<!-- vue-chartjs 使用模式（Plan 02 已安装） -->
```typescript
import { Chart as ChartJS, Title, Tooltip, Legend, BarElement, CategoryScale, LinearScale } from 'chart.js';
import { Bar } from 'vue-chartjs';
ChartJS.register(Title, Tooltip, Legend, BarElement, CategoryScale, LinearScale);
```

<!-- DashboardPage.vue 现有结构 -->
```html
<q-page padding>
  <!-- 欢迎词 -->
  <!-- 统计卡片 -->
  <!-- 快捷操作 -->
  <!-- 此处追加 FormStatsPanel -->
</q-page>
```

From frontend/src/boot/axios.ts:
```typescript
export const api = axios.create({ baseURL: '/api/v1' });
```

From frontend/src/stores/auth.ts:
```typescript
// auth.hasPerm(code: string): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FormStatsPanel.vue 统计面板组件</name>
  <files>frontend/src/components/submission/FormStatsPanel.vue</files>
  <read_first>
    frontend/src/pages/DashboardPage.vue
    frontend/src/composables/useResponsive.ts
    frontend/src/boot/axios.ts
  </read_first>
  <action>
创建 `frontend/src/components/submission/FormStatsPanel.vue`，严格遵循 09-UI-SPEC.md Surface C 规范。

模板结构：

```html
<template>
  <div class="form-stats-section q-mt-lg">
    <!-- 区域标题 + 时间筛选 -->
    <div class="row items-center justify-between q-mb-md">
      <div style="font-size: 20px; font-weight: 600; color: var(--oa-text-primary)">表单统计</div>
      <div class="row items-center q-gutter-sm">
        <q-btn-group outline>
          <q-btn :flat="period !== 'week'" :color="period === 'week' ? 'primary' : undefined" label="本周" @click="setPeriod('week')" dense />
          <q-btn :flat="period !== 'month'" :color="period === 'month' ? 'primary' : undefined" label="本月" @click="setPeriod('month')" dense />
        </q-btn-group>
        <q-input v-model="customRange" outlined dense label="自定义范围" style="width: 220px" readonly>
          <template #append>
            <q-icon name="date_range" class="cursor-pointer">
              <q-popup-proxy>
                <q-date v-model="customRange" range mask="YYYY-MM-DD" @update:model-value="onCustomRange" />
              </q-popup-proxy>
            </q-icon>
          </template>
        </q-input>
      </div>
    </div>

    <!-- 加载骨架 -->
    <div v-if="statsLoading" class="row q-gutter-md">
      <q-card :class="isDesktop ? 'col-12 col-md-6' : 'col-12'" flat bordered>
        <q-card-section><q-skeleton type="rect" height="200px" /></q-card-section>
      </q-card>
      <q-card :class="isDesktop ? 'col-12 col-md-6' : 'col-12'" flat bordered>
        <q-card-section><q-skeleton type="rect" height="200px" /></q-card-section>
      </q-card>
    </div>

    <!-- 空状态 -->
    <div v-else-if="stats.length === 0" class="text-center q-pa-lg" style="color: var(--oa-text-secondary)">
      暂无统计数据
    </div>

    <!-- 统计内容：表格 + 图表 -->
    <div v-else class="row q-gutter-md">
      <!-- 统计表格 -->
      <q-card :class="isDesktop ? 'col-12 col-md-6' : 'col-12'" flat bordered style="border-radius: 8px">
        <q-card-section>
          <q-table :rows="stats" :columns="tableColumns" row-key="userId" dense flat hide-bottom :pagination="{ rowsPerPage: 0 }" />
        </q-card-section>
      </q-card>

      <!-- 柱状图 -->
      <q-card :class="isDesktop ? 'col-12 col-md-6' : 'col-12'" flat bordered style="border-radius: 8px">
        <q-card-section>
          <Bar :data="chartData" :options="chartOptions" :style="{ height: '300px' }" role="img" aria-label="员工分享与收集数量柱状图" />
        </q-card-section>
      </q-card>
    </div>
  </div>
</template>
```

Script 逻辑：

```typescript
<script lang="ts" setup>
import { ref, computed, onMounted } from 'vue';
import { api } from 'src/boot/axios';
import { useResponsive } from 'src/composables/useResponsive';
import { Notify } from 'quasar';
import {
  Chart as ChartJS,
  Title, Tooltip, Legend,
  BarElement, CategoryScale, LinearScale,
} from 'chart.js';
import { Bar } from 'vue-chartjs';

ChartJS.register(Title, Tooltip, Legend, BarElement, CategoryScale, LinearScale);

const { isDesktop } = useResponsive();

interface StatRow {
  userId: number;
  realName: string;
  shareCount: number;
  submissionCount: number;
}

const stats = ref<StatRow[]>([]);
const statsLoading = ref(true);
const period = ref<'week' | 'month' | 'custom'>('month');
const customRange = ref<{ from: string; to: string } | null>(null);

const tableColumns = [
  { name: 'realName', label: '员工姓名', field: 'realName', align: 'left' as const },
  { name: 'shareCount', label: '分享次数', field: 'shareCount', align: 'center' as const },
  { name: 'submissionCount', label: '收集数量', field: 'submissionCount', align: 'center' as const },
];

const chartData = computed(() => ({
  labels: stats.value.map(s => s.realName),
  datasets: [
    {
      label: '分享次数',
      backgroundColor: '#4F46E5',
      data: stats.value.map(s => s.shareCount),
    },
    {
      label: '收集数量',
      backgroundColor: '#16A34A',
      data: stats.value.map(s => s.submissionCount),
    },
  ],
}));

const chartOptions = {
  responsive: true,
  maintainAspectRatio: false,
  plugins: {
    legend: { position: 'top' as const },
  },
  scales: {
    y: { beginAtZero: true, ticks: { stepSize: 1 } },
  },
};

function getDateRange(): { dateFrom: string; dateTo: string } {
  const now = new Date();
  if (period.value === 'week') {
    const day = now.getDay() || 7; // 周日=7
    const monday = new Date(now);
    monday.setDate(now.getDate() - day + 1);
    return {
      dateFrom: formatDateStr(monday),
      dateTo: formatDateStr(now),
    };
  }
  if (period.value === 'month') {
    const first = new Date(now.getFullYear(), now.getMonth(), 1);
    return {
      dateFrom: formatDateStr(first),
      dateTo: formatDateStr(now),
    };
  }
  // custom
  if (customRange.value) {
    return {
      dateFrom: customRange.value.from,
      dateTo: customRange.value.to,
    };
  }
  // fallback: 本月
  const first = new Date(now.getFullYear(), now.getMonth(), 1);
  return { dateFrom: formatDateStr(first), dateTo: formatDateStr(now) };
}

function formatDateStr(d: Date): string {
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
}

async function fetchStats() {
  statsLoading.value = true;
  try {
    const { dateFrom, dateTo } = getDateRange();
    const { data } = await api.get('/form-stats', { params: { dateFrom, dateTo } });
    stats.value = data;
  } catch {
    Notify.create({ type: 'warning', message: '统计数据加载失败' });
  } finally {
    statsLoading.value = false;
  }
}

function setPeriod(p: 'week' | 'month') {
  period.value = p;
  customRange.value = null;
  fetchStats();
}

function onCustomRange(val: { from: string; to: string } | null) {
  if (val && val.from && val.to) {
    period.value = 'custom';
    fetchStats();
  }
}

onMounted(fetchStats);
</script>
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && grep -n "export default" src/components/submission/FormStatsPanel.vue; grep -n "Bar" src/components/submission/FormStatsPanel.vue && grep -n "form-stats" src/components/submission/FormStatsPanel.vue && grep -n "chartData" src/components/submission/FormStatsPanel.vue</automated>
  </verify>
  <acceptance_criteria>
    - FormStatsPanel.vue 存在于 frontend/src/components/submission/ 目录
    - 组件导入并注册 vue-chartjs 的 Bar 组件和 chart.js 的 BarElement/CategoryScale/LinearScale/Title/Tooltip/Legend
    - 组件调用 `api.get('/form-stats', { params: { dateFrom, dateTo } })`
    - 组件包含 QBtnGroup 时间筛选（"本周"/"本月"按钮）
    - 组件包含 QDate range 自定义日期范围选择
    - 组件包含 QTable 展示 员工姓名/分享次数/收集数量 三列
    - 组件包含 Bar 图表，datasets 包含 分享次数(#4F46E5) 和 收集数量(#16A34A)
    - chartOptions 包含 `beginAtZero: true` 和 `stepSize: 1`
    - 组件包含加载骨架（QSkeleton）和空状态（"暂无统计数据"）
    - Bar 组件包含 `role="img" aria-label="员工分享与收集数量柱状图"`
  </acceptance_criteria>
  <done>FormStatsPanel.vue 完整实现统计表格+柱状图+时间筛选+加载/空状态</done>
</task>

<task type="auto">
  <name>Task 2: DashboardPage.vue 嵌入统计面板</name>
  <files>frontend/src/pages/DashboardPage.vue</files>
  <read_first>
    frontend/src/pages/DashboardPage.vue
    frontend/src/stores/auth.ts
    frontend/src/components/submission/FormStatsPanel.vue
  </read_first>
  <action>
修改 `frontend/src/pages/DashboardPage.vue`，在"快捷操作"区域之后追加 FormStatsPanel 组件，受 `form:stats:view` 权限控制。

1. 在 script setup 中添加导入：
```typescript
import FormStatsPanel from 'src/components/submission/FormStatsPanel.vue';
```
（useAuthStore 已导入，auth 已定义）

2. 在 template 中，"快捷操作" div 之后（`</q-page>` 之前）追加：
```html
    <!-- 表单统计 — 需要 form:stats:view 权限 -->
    <FormStatsPanel v-if="auth.hasPerm('form:stats:view')" />
```

注意：
- 使用 `auth.hasPerm('form:stats:view')` 控制可见性，无权限用户看不到此区域
- FormStatsPanel 是自包含组件，内部自行获取数据，DashboardPage 无需传 props
- 组件放在快捷操作之后，作为 Dashboard 的最后一个区域
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && grep -n "FormStatsPanel" src/pages/DashboardPage.vue && grep -n "form:stats:view" src/pages/DashboardPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage.vue 包含 `import FormStatsPanel from 'src/components/submission/FormStatsPanel.vue'`
    - DashboardPage.vue template 包含 `<FormStatsPanel v-if="auth.hasPerm('form:stats:view')" />`
    - FormStatsPanel 位于快捷操作区域之后
    - 无权限用户（auth.hasPerm 返回 false）看不到统计面板
  </acceptance_criteria>
  <done>DashboardPage 嵌入 FormStatsPanel，受 form:stats:view 权限控制</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → form-stats API | 认证用户请求统计数据 |
| API response → chart render | 统计数据渲染到图表 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Information Disclosure | FormStatsPanel | mitigate | v-if="auth.hasPerm('form:stats:view')" 前端隐藏 + 后端 authGuard 双重保护 |
| T-09-14 | Tampering | chart data | accept | 图表数据来自后端 API 响应，前端仅展示不修改；chart.js 不执行数据中的脚本 |
| T-09-15 | Denial of Service | form-stats API | accept | 统计 API 接受 dateFrom/dateTo 限制查询范围，当前数据量小 |
</threat_model>

<verification>
1. 以有 form:stats:view 权限的用户登录，Dashboard 页面显示"表单统计"区域
2. 统计表格显示员工姓名、分享次数、收集数量
3. 柱状图显示双柱（蓝色分享次数 + 绿色收集数量）
4. 切换"本周"/"本月"重新加载数据
5. 以无权限用户登录，Dashboard 不显示统计区域
</verification>

<success_criteria>
- Dashboard 页面有权限用户可见统计面板
- 统计表格和图表正确展示数据
- 时间筛选切换正常工作
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-view-print-stats/09-04-SUMMARY.md`
</output>
