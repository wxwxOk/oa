---
phase: 08-share-public-fill
plan: 04
type: execute
wave: 3
depends_on: [08-02, 08-03]
files_modified:
  - frontend/src/pages/PublicFillPage.vue
  - frontend/src/components/public-fill/FormFieldRenderer.vue
  - frontend/src/router/routes.ts
autonomous: false
requirements: [SHARE-01, SHARE-02, SHARE-03, SHARE-04, SHARE-05]

must_haves:
  truths:
    - "外部用户访问 /f/:code 可看到表单并填写提交"
    - "填写页独立于 MainLayout，卡片式居中布局，背景色 #F1F5F9"
    - "requireIdentity 为 true 时填写页顶部显示姓名+手机号字段"
    - "所有字段类型（text/textarea/radio/checkbox/date/phone/signature）可交互填写"
    - "提交成功后显示静态成功页（check_circle 图标 + 提交成功文字）"
    - "链接无效或模板下线时显示对应错误页"
  artifacts:
    - path: "frontend/src/pages/PublicFillPage.vue"
      provides: "公开填写页（独立 layout + 表单 + 成功页 + 错误页）"
      contains: "public-fill-page"
    - path: "frontend/src/components/public-fill/FormFieldRenderer.vue"
      provides: "可交互的表单字段渲染器"
      contains: "FormFieldRenderer"
    - path: "frontend/src/router/routes.ts"
      provides: "/f/:code 公开路由"
      contains: "/f/:code"
  key_links:
    - from: "routes.ts"
      to: "PublicFillPage.vue"
      via: "路由懒加载"
      pattern: "import.*PublicFillPage"
    - from: "PublicFillPage.vue"
      to: "FormFieldRenderer.vue"
      via: "v-for 渲染 schema 字段"
      pattern: "FormFieldRenderer"
    - from: "PublicFillPage.vue"
      to: "/api/public/f/:code"
      via: "axios GET + POST"
      pattern: "publicApi"
---

<objective>
公开填写页完整实现：新建 PublicFillPage.vue（独立 layout、表单渲染、提交、成功页、错误页），新建 FormFieldRenderer.vue（可交互字段渲染），注册 /f/:code 公开路由。

Purpose: 让外部人员通过分享链接免登录填写表单并提交数据。
Output: 2 个新组件 + 1 个路由修改 + 人工验证 checkpoint。
</objective>

<execution_context>
@.planning/phases/08-share-public-fill/08-UI-SPEC.md
@.planning/phases/08-share-public-fill/08-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-share-public-fill/08-02-SUMMARY.md
@.planning/phases/08-share-public-fill/08-03-SUMMARY.md

<interfaces>
From frontend/src/stores/template.ts:
```typescript
export interface FormField {
  id: string;
  type: 'text' | 'textarea' | 'radio' | 'checkbox' | 'date' | 'phone' | 'signature';
  label: string;
  required: boolean;
  placeholder?: string;
  options?: string[];
  sort: number;
}
```

From frontend/src/router/routes.ts — 公开路由模式:
```typescript
{ path: '/login', component: () => import('pages/LoginPage.vue'), meta: { public: true } },
{ path: '/403', component: () => import('pages/ForbiddenPage.vue'), meta: { public: true } },
```

From frontend/src/boot/axios.ts:
```typescript
const api: AxiosInstance = axios.create({ baseURL: process.env.API_BASE, timeout: 15000 });
```

From frontend/src/components/designer/fields/SignatureField.vue:
```typescript
// preview=false 模式初始化 signature_pad canvas
const props = withDefaults(defineProps<{ preview?: boolean; modelValue?: string; }>(),
  { preview: true, modelValue: '' });
defineExpose({ clear, save, isEmpty: () => pad?.isEmpty() ?? true });
```

From backend/src/modules/public/public.route.ts — API 响应格式:
```typescript
// GET /api/public/f/:code 返回:
{ templateName: string, description: string|null, schema: FormField[], requireIdentity: boolean }
// POST /api/public/f/:code/submit body:
{ data: Record<string,any>, submitterName?: string, submitterPhone?: string }
// POST 返回: { id: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FormFieldRenderer 可交互字段渲染器</name>
  <files>frontend/src/components/public-fill/FormFieldRenderer.vue</files>
  <read_first>
    - frontend/src/components/designer/DesignerCanvas.vue
    - frontend/src/components/designer/fields/SignatureField.vue
    - frontend/src/stores/template.ts
  </read_first>
  <action>
创建 `frontend/src/components/public-fill/FormFieldRenderer.vue`（需先创建 public-fill 目录）。

该组件接收单个 FormField 定义和 v-model 绑定值，渲染可交互的表单字段。

Props:
- `field: FormField` — 字段定义
- `modelValue: any` — 字段值（v-model）

Emit: `update:modelValue`

按字段类型渲染（参考 DesignerCanvas 的字段预览，但去掉 disabled，改为可交互）：

```vue
<template>
  <div class="form-field q-mb-md">
    <!-- 字段标签 -->
    <div class="field-label q-mb-xs" style="font-size: 14px; font-weight: 600; line-height: 1.4">
      {{ field.label }}
      <span v-if="field.required" style="color: #DC2626"> *</span>
    </div>

    <!-- text -->
    <q-input
      v-if="field.type === 'text'"
      :model-value="modelValue"
      @update:model-value="$emit('update:modelValue', $event)"
      outlined
      :placeholder="field.placeholder"
      :rules="field.required ? [requiredRule] : []"
    />

    <!-- textarea -->
    <q-input
      v-else-if="field.type === 'textarea'"
      :model-value="modelValue"
      @update:model-value="$emit('update:modelValue', $event)"
      outlined
      type="textarea"
      rows="3"
      :placeholder="field.placeholder"
      :rules="field.required ? [requiredRule] : []"
    />

    <!-- radio -->
    <template v-else-if="field.type === 'radio'">
      <q-option-group
        type="radio"
        :model-value="modelValue"
        @update:model-value="$emit('update:modelValue', $event)"
        :options="mapOptions(field.options)"
      />
      <div v-if="field.required && radioError" class="text-negative text-caption q-mt-xs">请选择一项</div>
    </template>

    <!-- checkbox -->
    <template v-else-if="field.type === 'checkbox'">
      <q-option-group
        type="checkbox"
        :model-value="modelValue || []"
        @update:model-value="$emit('update:modelValue', $event)"
        :options="mapOptions(field.options)"
      />
      <div v-if="field.required && checkboxError" class="text-negative text-caption q-mt-xs">请至少选择一项</div>
    </template>

    <!-- date -->
    <q-input
      v-else-if="field.type === 'date'"
      :model-value="modelValue"
      @update:model-value="$emit('update:modelValue', $event)"
      outlined
      placeholder="请选择日期"
      readonly
      :rules="field.required ? [(v: string) => !!v || '请选择日期'] : []"
    >
      <template #append>
        <q-icon name="calendar_today" class="cursor-pointer">
          <q-popup-proxy cover transition-show="scale" transition-hide="scale">
            <q-date
              :model-value="modelValue"
              @update:model-value="$emit('update:modelValue', $event)"
              mask="YYYY-MM-DD"
            />
          </q-popup-proxy>
        </q-icon>
      </template>
    </q-input>

    <!-- phone -->
    <q-input
      v-else-if="field.type === 'phone'"
      :model-value="modelValue"
      @update:model-value="$emit('update:modelValue', $event)"
      outlined
      type="tel"
      mask="###########"
      :placeholder="field.placeholder || '请输入手机号'"
      :rules="field.required ? [(v: string) => /^1\d{10}$/.test(v) || '请输入有效手机号'] : []"
    />

    <!-- signature -->
    <template v-else-if="field.type === 'signature'">
      <SignatureField
        ref="sigRef"
        :preview="false"
        :model-value="modelValue"
        @update:model-value="$emit('update:modelValue', $event)"
      />
      <div v-if="field.required && sigError" class="text-negative text-caption q-mt-xs">请签名</div>
    </template>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue';
import type { FormField } from 'src/stores/template';
import SignatureField from 'src/components/designer/fields/SignatureField.vue';

defineProps<{
  field: FormField;
  modelValue: any;
}>();

defineEmits<{
  'update:modelValue': [value: any];
}>();

const sigRef = ref<InstanceType<typeof SignatureField> | null>(null);
const requiredRule = (v: string) => !!v?.trim() || '此项为必填';
const radioError = ref(false);
const checkboxError = ref(false);
const sigError = ref(false);

function mapOptions(opts?: string[]) {
  return (opts ?? []).map(o => ({ label: o, value: o }));
}

// 暴露 validate 方法供父组件调用
function validate(value: any, field: FormField): boolean {
  if (!field.required) return true;
  if (field.type === 'radio') {
    radioError.value = value == null;
    return !radioError.value;
  }
  if (field.type === 'checkbox') {
    checkboxError.value = !value?.length;
    return !checkboxError.value;
  }
  if (field.type === 'signature') {
    sigError.value = !value;
    return !sigError.value;
  }
  return true; // QInput 自带 rules 验证
}

// 暴露签名保存方法
function saveSignature() {
  return sigRef.value?.save?.() ?? '';
}

defineExpose({ validate, saveSignature, sigRef });
</script>
```
  </action>
  <verify>
    <automated>ls E:/webspace/oa/frontend/src/components/public-fill/FormFieldRenderer.vue && cd E:/webspace/oa/frontend && grep -c "field.type" src/components/public-fill/FormFieldRenderer.vue</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/public-fill/FormFieldRenderer.vue 文件存在
    - FormFieldRenderer.vue 包含 7 种字段类型渲染（text, textarea, radio, checkbox, date, phone, signature）
    - FormFieldRenderer.vue 导入 SignatureField 组件（preview=false 模式）
    - FormFieldRenderer.vue 包含 `mapOptions` 函数
    - FormFieldRenderer.vue 包含 `defineExpose({ validate, saveSignature, sigRef })`
    - FormFieldRenderer.vue 每个 QInput 使用 outlined 样式
    - FormFieldRenderer.vue required 字段有红色星号 `*`
    - FormFieldRenderer.vue date 字段使用 QPopupProxy + QDate
  </acceptance_criteria>
  <done>FormFieldRenderer 支持全部 7 种字段类型的可交互渲染，含验证逻辑和签名保存</done>
</task>

<task type="auto">
  <name>Task 2: PublicFillPage + 路由注册</name>
  <files>frontend/src/pages/PublicFillPage.vue, frontend/src/router/routes.ts</files>
  <read_first>
    - frontend/src/pages/LoginPage.vue
    - frontend/src/router/routes.ts
    - frontend/src/components/public-fill/FormFieldRenderer.vue
    - frontend/src/boot/axios.ts
    - frontend/src/composables/useResponsive.ts
  </read_first>
  <action>
1. 创建 `frontend/src/pages/PublicFillPage.vue`：

该页面使用独立 q-layout（不嵌套在 MainLayout 中），参考 LoginPage 模式。
使用独立 axios 实例（无 token 拦截器）调用公开 API。

页面有 4 个状态：loading / error / form / success，通过响应式变量切换。

```vue
<template>
  <q-layout view="hHh lpR fFf">
    <q-page-container>
      <q-page class="public-fill-page flex flex-center">
        <div class="form-wrapper" :style="wrapperStyle">

          <!-- 加载中 -->
          <q-card v-if="pageState === 'loading'" flat class="form-card">
            <q-card-section>
              <q-skeleton type="text" width="60%" />
              <q-skeleton type="text" width="40%" class="q-mt-sm" />
            </q-card-section>
            <q-separator />
            <q-card-section>
              <q-skeleton v-for="i in 3" :key="i" type="rect" height="56px" class="q-mb-md" />
            </q-card-section>
          </q-card>

          <!-- 错误页 -->
          <q-card v-else-if="pageState === 'error'" flat class="form-card text-center q-pa-xl">
            <q-icon name="error_outline" size="64px" color="grey-5" />
            <div class="text-h6 q-mt-md">{{ errorTitle }}</div>
            <div class="text-body2 q-mt-sm" style="color: var(--oa-text-secondary, #64748B)">{{ errorBody }}</div>
          </q-card>

          <!-- 成功页 (per D-06: 静态成功页) -->
          <q-card v-else-if="pageState === 'success'" flat class="form-card text-center q-pa-xl">
            <q-icon name="check_circle" size="80px" color="positive" />
            <div style="font-size: 28px; font-weight: 600; margin-top: 24px">提交成功</div>
            <div class="q-mt-sm" style="font-size: 14px; color: var(--oa-text-secondary, #64748B)">
              您的信息已成功提交，感谢填写
            </div>
          </q-card>

          <!-- 表单 -->
          <q-card v-else flat class="form-card">
            <q-card-section>
              <div style="font-size: 20px; font-weight: 600; line-height: 1.2">{{ templateData.templateName }}</div>
              <div v-if="templateData.description" class="q-mt-xs" style="font-size: 14px; color: var(--oa-text-secondary, #64748B)">
                {{ templateData.description }}
              </div>
            </q-card-section>
            <q-separator />
            <q-card-section>
              <q-form ref="formRef" greedy>
                <!-- 身份信息区域 (per D-09/D-12: 单个开关控制) -->
                <div v-if="templateData.requireIdentity" class="identity-section q-mb-md" style="border-bottom: 1px solid var(--oa-border, #E2E8F0); padding-bottom: 16px">
                  <q-input
                    v-model="identity.name"
                    outlined
                    label="姓名"
                    :rules="[(v: string) => !!v?.trim() || '请输入姓名']"
                  />
                  <q-input
                    v-model="identity.phone"
                    outlined
                    label="手机号"
                    type="tel"
                    mask="###########"
                    class="q-mt-sm"
                    :rules="[(v: string) => /^1\d{10}$/.test(v) || '请输入有效手机号']"
                  />
                </div>

                <!-- 动态表单字段 -->
                <FormFieldRenderer
                  v-for="field in schema"
                  :key="field.id"
                  ref="fieldRefs"
                  :field="field"
                  :model-value="formData[field.id]"
                  @update:model-value="formData[field.id] = $event"
                />
              </q-form>
            </q-card-section>
            <q-card-section>
              <q-btn
                color="primary"
                label="提交"
                :loading="submitting"
                class="full-width"
                size="lg"
                style="min-height: 44px"
                @click="handleSubmit"
              />
            </q-card-section>
          </q-card>

        </div>
      </q-page>
    </q-page-container>
  </q-layout>
</template>

<script setup lang="ts">
import { ref, reactive, computed, onMounted } from 'vue';
import { useRoute } from 'vue-router';
import type { QForm } from 'quasar';
import { Notify } from 'quasar';
import axios from 'axios';
import { useResponsive } from 'src/composables/useResponsive';
import FormFieldRenderer from 'src/components/public-fill/FormFieldRenderer.vue';
import type { FormField } from 'src/stores/template';

// 独立 axios 实例，无 token 拦截器 (per PATTERNS: 公开 API 不使用 api 实例)
const publicApi = axios.create({ baseURL: process.env.API_BASE, timeout: 15000 });

const route = useRoute();
const { isMobile } = useResponsive();
const code = route.params.code as string;

const pageState = ref<'loading' | 'error' | 'form' | 'success'>('loading');
const errorTitle = ref('');
const errorBody = ref('');
const submitting = ref(false);
const formRef = ref<QForm | null>(null);
const fieldRefs = ref<InstanceType<typeof FormFieldRenderer>[]>([]);

const templateData = reactive({
  templateName: '',
  description: '' as string | null,
  requireIdentity: false,
});
const schema = ref<FormField[]>([]);
const formData = reactive<Record<string, any>>({});
const identity = reactive({ name: '', phone: '' });

const wrapperStyle = computed(() => ({
  maxWidth: '640px',
  width: '100%',
  padding: isMobile.value ? '16px' : '64px 0',
}));

onMounted(async () => {
  try {
    const { data } = await publicApi.get(`/public/f/${code}`);
    templateData.templateName = data.templateName;
    templateData.description = data.description;
    templateData.requireIdentity = data.requireIdentity;
    schema.value = (data.schema as FormField[]).sort((a, b) => a.sort - b.sort);
    // 初始化 formData
    for (const f of schema.value) {
      formData[f.id] = f.type === 'checkbox' ? [] : (f.type === 'signature' ? '' : '');
    }
    pageState.value = 'form';
  } catch (err: any) {
    const status = err.response?.status;
    const errCode = err.response?.data?.code;
    if (status === 404) {
      errorTitle.value = '链接无效';
      errorBody.value = '该链接不存在或已失效，请联系分享者获取新链接';
    } else if (errCode === 'TEMPLATE_OFFLINE' || status === 410) {
      errorTitle.value = '该表单已停止收集';
      errorBody.value = '此表单已被管理员关闭，暂时无法填写';
    } else {
      errorTitle.value = '加载失败';
      errorBody.value = '加载失败，请检查网络后重试';
    }
    pageState.value = 'error';
  }
});

async function handleSubmit() {
  // QForm 验证（QInput rules）
  const formValid = await formRef.value?.validate();

  // 手动验证 radio/checkbox/signature 字段
  let customValid = true;
  for (let i = 0; i < schema.value.length; i++) {
    const field = schema.value[i];
    const renderer = fieldRefs.value[i];
    if (renderer?.validate) {
      const ok = renderer.validate(formData[field.id], field);
      if (!ok) customValid = false;
    }
  }

  if (!formValid || !customValid) return;

  // 保存签名字段数据
  for (let i = 0; i < schema.value.length; i++) {
    const field = schema.value[i];
    if (field.type === 'signature') {
      const renderer = fieldRefs.value[i];
      const sigData = renderer?.saveSignature?.();
      if (sigData) formData[field.id] = sigData;
    }
  }

  submitting.value = true;
  try {
    await publicApi.post(`/public/f/${code}/submit`, {
      data: { ...formData },
      submitterName: templateData.requireIdentity ? identity.name : undefined,
      submitterPhone: templateData.requireIdentity ? identity.phone : undefined,
    });
    pageState.value = 'success';
  } catch {
    Notify.create({ type: 'negative', message: '提交失败，请稍后重试' });
  } finally {
    submitting.value = false;
  }
}
</script>

<style scoped>
.public-fill-page {
  background: #F1F5F9;
  min-height: 100vh;
}
.form-wrapper {
  margin: 0 auto;
}
.form-card {
  background: #FFFFFF;
  border-radius: 12px;
  box-shadow: 0 1px 5px rgba(0,0,0,0.1), 0 2px 2px rgba(0,0,0,0.06);
}
@media (max-width: 599px) {
  .form-card {
    border-radius: 8px;
  }
  .form-wrapper {
    padding: 16px !important;
  }
}
@media (min-width: 600px) and (max-width: 1023px) {
  .form-card {
    padding: 24px;
  }
}
@media (min-width: 1024px) {
  .form-card {
    padding: 32px;
  }
}
</style>
```

2. 修改 `frontend/src/router/routes.ts`：

在 `/403` 路由之后、`/` 路由之前，新增公开填写路由：
```typescript
{
  path: '/f/:code',
  component: () => import('pages/PublicFillPage.vue'),
  meta: { public: true },
},
```
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && grep -n "PublicFillPage\|/f/:code" src/router/routes.ts && grep -c "pageState" src/pages/PublicFillPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/PublicFillPage.vue 文件存在
    - PublicFillPage.vue 包含 `q-layout view="hHh lpR fFf"`（独立 layout）
    - PublicFillPage.vue 包含 `background: #F1F5F9`
    - PublicFillPage.vue 包含 4 个状态：loading, error, form, success
    - PublicFillPage.vue 包含 `publicApi = axios.create`（独立 axios 实例，无 token）
    - PublicFillPage.vue 包含 `check_circle` 图标和 "提交成功" 文字
    - PublicFillPage.vue 包含 "链接无效" 和 "该表单已停止收集" 错误文案
    - PublicFillPage.vue 包含 identity section（姓名+手机号）条件渲染
    - PublicFillPage.vue 提交按钮 `class="full-width"` 且 `style="min-height: 44px"`
    - routes.ts 包含 `path: '/f/:code'` 且 `meta: { public: true }`
  </acceptance_criteria>
  <done>PublicFillPage 完整实现（加载/错误/表单/成功 4 状态），路由已注册为公开路由</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 端到端人工验证</name>
  <what-built>
    Phase 8 完整功能：分享链接生成、二维码展示、公开填写页、表单提交、数据归档。
  </what-built>
  <how-to-verify>
    1. 启动后端和前端开发服务器
    2. 登录管理端，进入模板管理页面
    3. 选择一个已发布模板，点击"分享"按钮
    4. 验证弹窗显示完整链接 URL 和二维码
    5. 点击"复制链接"按钮，验证剪贴板内容和成功通知
    6. 在新的浏览器标签页（或无痕模式）打开复制的链接
    7. 验证填写页显示模板标题、描述和所有字段
    8. 如果模板开启了身份信息要求，验证顶部显示姓名和手机号字段
    9. 填写所有字段（包括签名），点击提交
    10. 验证显示"提交成功"页面（绿色 check_circle 图标）
    11. 在设计器中切换 requireIdentity 开关，保存，验证填写页相应变化
    12. 测试错误场景：访问无效链接（如 /f/invalid123），验证显示"链接无效"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public browser → /api/public/f/:code | 匿名用户提交表单数据，需输入验证 |
| Public page → formData | 用户输入的表单数据，需前端验证后提交 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | PublicFillPage formData | mitigate | QForm rules 前端验证 + 后端 Elysia t.Object schema 验证双重保障 |
| T-08-12 | Denial of Service | signature data size | mitigate | signature_pad toDataURL 输出通常 < 500KB，后端 Elysia 默认请求体限制提供保护 |
| T-08-13 | Information Disclosure | publicApi 实例 | mitigate | 使用独立 axios 实例（无 token 拦截器），不会意外发送管理端 JWT |
| T-08-14 | Spoofing | identity fields | accept | D-11 锁定手机号不做短信验证，仅格式校验；这是有意的设计决策以降低填写门槛 |
</threat_model>

<verification>
- 访问 /f/{valid_code} 显示表单
- 访问 /f/{invalid_code} 显示"链接无效"
- 填写并提交表单成功后显示成功页
- requireIdentity=true 时填写页显示身份字段
- 移动端布局正确（卡片全宽，44px 触摸目标）
</verification>

<success_criteria>
外部用户可通过分享链接免登录填写表单并提交，提交数据正确归档，所有字段类型可交互，错误状态正确展示。
</success_criteria>

<output>
After completion, create `.planning/phases/08-share-public-fill/08-04-SUMMARY.md`
</output>
