---
phase: 08-share-public-fill
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - backend/src/modules/template/template.route.ts
  - backend/src/modules/public/public.route.ts
  - backend/src/index.ts
autonomous: true
requirements: [SHARE-01, SHARE-03, SHARE-04, SHARE-05]

must_haves:
  truths:
    - "POST /api/v1/templates/:id/share-links 需要 authGuard 鉴权，返回包含 code 的 ShareLink 对象"
    - "GET /api/public/f/:code 无需鉴权，返回模板名称、描述、schema、requireIdentity"
    - "POST /api/public/f/:code/submit 无需鉴权，创建 Submission 记录并关联 schemaVersion"
    - "公开 API 不暴露 creatorId、updatedAt 等内部字段"
    - "模板非 PUBLISHED 状态时公开 API 返回错误"
  artifacts:
    - path: "backend/src/modules/template/template.route.ts"
      provides: "POST /:id/share-links 端点"
      contains: "share-links"
    - path: "backend/src/modules/public/public.route.ts"
      provides: "公开填写 API（GET /:code + POST /:code/submit）"
      exports: ["publicFillModule"]
    - path: "backend/src/index.ts"
      provides: "publicFillModule 注册"
      contains: "publicFillModule"
  key_links:
    - from: "template.route.ts"
      to: "prisma.shareLink.create"
      via: "nanoid 生成 code"
      pattern: "nanoid"
    - from: "public.route.ts"
      to: "prisma.submission.create"
      via: "POST submit 端点"
      pattern: "prisma\\.submission\\.create"
    - from: "index.ts"
      to: "publicFillModule"
      via: ".use(publicFillModule)"
      pattern: "publicFillModule"
---

<objective>
后端 API 实现：在 template.route.ts 新增分享链接创建端点，新建 public.route.ts 提供公开填写和提交 API，在 index.ts 注册公开模块。

Purpose: 提供分享链接生成和公开表单填写的完整后端能力。
Output: 3 个后端文件（1 修改 + 1 新建 + 1 修改）。
</objective>

<execution_context>
@.planning/phases/08-share-public-fill/08-RESEARCH.md
@.planning/phases/08-share-public-fill/08-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-share-public-fill/08-01-SUMMARY.md

<interfaces>
<!-- 现有 template.route.ts 模块声明和 guard 模式 -->
From backend/src/modules/template/template.route.ts:
```typescript
import { Elysia, t } from 'elysia';
import { prisma } from '../../plugins/prisma';
import { authGuard } from '../../middlewares/auth';
import { BizError, notFound } from '../../utils/errors';

export const formTemplateModule = new Elysia({ prefix: '/templates' })
  .use(authGuard('form:template:list'))
  // ... 现有端点
  .guard({}, (app) =>
    app.use(authGuard('form:template:create')).post('/', ...)
  )
```

From backend/src/index.ts — 路由注册模式:
```typescript
.group('/api/v1', (app) =>
  app
    .use(authModule)
    .use(userModule)
    .use(departmentModule)
    .use(roleModule)
    .use(permissionModule)
    .use(dashboardModule)
    .use(formTemplateModule),
)
.listen(Number(process.env.PORT ?? 3000));
```

From backend/src/utils/errors.ts:
```typescript
export class BizError extends Error { status: number; code: string; ... }
export const notFound = (msg = '资源不存在') => new BizError(msg, 404, 'NOT_FOUND');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: template.route.ts 新增分享链接端点</name>
  <files>backend/src/modules/template/template.route.ts</files>
  <read_first>
    - backend/src/modules/template/template.route.ts
    - backend/prisma/schema.prisma
    - backend/src/middlewares/auth.ts
  </read_first>
  <action>
在 template.route.ts 文件顶部新增 nanoid 导入：
```typescript
import { nanoid } from 'nanoid';
```

在文件末尾（最后一个 `.guard(...)` 之后、分号之前）新增分享链接创建端点：

```typescript
// 创建分享链接 (per D-01, D-04: 每次生成独立链接，nanoid 12位短码)
.guard({}, (app) =>
  app.use(authGuard('form:template:share')).post(
    '/:id/share-links',
    async ({ params, currentUser }: any) => {
      const templateId = Number(params.id);
      const tpl = await prisma.formTemplate.findUnique({ where: { id: templateId } });
      if (!tpl) throw notFound('模板不存在');
      if (tpl.status !== 'PUBLISHED') throw new BizError('仅已发布模板可生成分享链接');
      const link = await prisma.shareLink.create({
        data: {
          code: nanoid(12),
          templateId,
          creatorId: currentUser.id,
        },
      });
      return link;
    },
    { params: t.Object({ id: t.String() }) },
  ),
)
```

同时在 PUT /:id 端点的 body schema 中新增 requireIdentity 可选字段（per D-09）：
在 body: t.Object({ ... }) 中新增：
```typescript
requireIdentity: t.Optional(t.Boolean()),
```

在 PUT handler 的 data 构建逻辑中新增：
```typescript
if (body.requireIdentity !== undefined) data.requireIdentity = body.requireIdentity;
```
（放在 `if (body.schema !== undefined)` 块之后）
  </action>
  <verify>
    <automated>cd E:/webspace/oa/backend && grep -n "share-links\|nanoid\|requireIdentity" src/modules/template/template.route.ts</automated>
  </verify>
  <acceptance_criteria>
    - template.route.ts 包含 `import { nanoid } from 'nanoid'`
    - template.route.ts 包含 `'/:id/share-links'`
    - template.route.ts 包含 `authGuard('form:template:share')`
    - template.route.ts 包含 `nanoid(12)`
    - template.route.ts 包含 `tpl.status !== 'PUBLISHED'` 校验
    - template.route.ts PUT 端点 body 包含 `requireIdentity: t.Optional(t.Boolean())`
    - template.route.ts PUT handler 包含 `body.requireIdentity !== undefined`
  </acceptance_criteria>
  <done>template.route.ts 包含分享链接创建端点（authGuard + nanoid 12位 + PUBLISHED 校验）和 requireIdentity 更新支持</done>
</task>

<task type="auto">
  <name>Task 2: 新建 public.route.ts + 注册到 index.ts</name>
  <files>backend/src/modules/public/public.route.ts, backend/src/index.ts</files>
  <read_first>
    - backend/src/index.ts
    - backend/src/modules/template/template.route.ts
    - backend/prisma/schema.prisma
    - backend/src/utils/errors.ts
  </read_first>
  <action>
1. 创建 `backend/src/modules/public/public.route.ts`：

```typescript
import { Elysia, t } from 'elysia';
import { prisma } from '../../plugins/prisma';
import { notFound, BizError } from '../../utils/errors';

export const publicFillModule = new Elysia({ prefix: '/public/f' })
  // GET /:code — 获取模板 schema（公开，无鉴权）(per D-03: 仅模板下线时失效)
  .get('/:code', async ({ params }: any) => {
    const link = await prisma.shareLink.findUnique({
      where: { code: params.code },
      include: {
        template: {
          select: {
            id: true,
            name: true,
            description: true,
            schema: true,
            schemaVersion: true,
            status: true,
            requireIdentity: true,
          },
        },
      },
    });
    if (!link) throw notFound('链接无效');
    if (link.template.status !== 'PUBLISHED') {
      throw new BizError('该表单已停止收集', 410, 'TEMPLATE_OFFLINE');
    }
    // 仅返回填写所需字段，不暴露内部数据
    return {
      templateName: link.template.name,
      description: link.template.description,
      schema: link.template.schema,
      requireIdentity: link.template.requireIdentity,
    };
  })
  // POST /:code/submit — 提交表单数据（公开，无鉴权）
  // per D-13: JSONB 存储, D-14: schemaVersion 快照, D-15: 关联 ShareLink, D-16: 独立身份字段
  .post(
    '/:code/submit',
    async ({ params, body }: any) => {
      const link = await prisma.shareLink.findUnique({
        where: { code: params.code },
        include: { template: true },
      });
      if (!link) throw notFound('链接无效');
      if (link.template.status !== 'PUBLISHED') {
        throw new BizError('该表单已停止收集', 410, 'TEMPLATE_OFFLINE');
      }
      // per D-09/D-12: 如果模板要求身份信息，校验必填
      if (link.template.requireIdentity) {
        if (!body.submitterName?.trim()) throw new BizError('请输入姓名');
        if (!/^1\d{10}$/.test(body.submitterPhone ?? '')) throw new BizError('请输入有效手机号');
      }
      // per D-14: schemaVersion 从服务端读取，不信任客户端
      const submission = await prisma.submission.create({
        data: {
          data: body.data,
          schemaVersion: link.template.schemaVersion,
          submitterName: body.submitterName || null,
          submitterPhone: body.submitterPhone || null,
          templateId: link.template.id,
          shareLinkId: link.id,
        },
      });
      return { id: submission.id };
    },
    {
      body: t.Object({
        data: t.Any(),
        submitterName: t.Optional(t.String()),
        submitterPhone: t.Optional(t.String()),
      }),
    },
  );
```

2. 修改 `backend/src/index.ts`：

   a) 在导入区域新增：
   ```typescript
   import { publicFillModule } from './modules/public/public.route';
   ```

   b) 将现有的 `.group('/api/v1', ...)` 和 `.listen(...)` 之间插入公开模块注册。
   修改路由注册结构为：
   ```typescript
   .group('/api', (app) =>
     app
       .group('/v1', (app) =>
         app
           .use(authModule)
           .use(userModule)
           .use(departmentModule)
           .use(roleModule)
           .use(permissionModule)
           .use(dashboardModule)
           .use(formTemplateModule),
       )
       .use(publicFillModule),
   )
   ```
   这样 publicFillModule 注册在 `/api/public/f/` 路径下，与 `/api/v1/` 平级，不继承 authGuard。
  </action>
  <verify>
    <automated>cd E:/webspace/oa/backend && node -e "try{require('./src/modules/public/public.route.ts')}catch(e){}" && ls src/modules/public/public.route.ts && grep -c "publicFillModule" src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/modules/public/public.route.ts 文件存在
    - public.route.ts 包含 `export const publicFillModule`
    - public.route.ts 包含 `prefix: '/public/f'`
    - public.route.ts 包含 `GET /:code` 端点
    - public.route.ts 包含 `POST /:code/submit` 端点
    - public.route.ts GET 端点仅返回 templateName, description, schema, requireIdentity（不含 creatorId, updatedAt）
    - public.route.ts POST 端点包含 `link.template.requireIdentity` 身份校验
    - public.route.ts POST 端点使用 `link.template.schemaVersion`（服务端读取，非客户端传入）
    - public.route.ts 不包含 `authGuard`
    - backend/src/index.ts 包含 `import { publicFillModule }`
    - backend/src/index.ts 包含 `.use(publicFillModule)` 且在 `/api/v1` group 之外
  </acceptance_criteria>
  <done>public.route.ts 提供公开 GET/POST API（无鉴权），template.route.ts 有分享链接端点（有鉴权），index.ts 正确注册两个模块在不同作用域</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/v1/templates/:id/share-links | 需鉴权，仅已登录员工可创建分享链接 |
| client → /api/public/f/:code | 无鉴权，任何人可访问，需防止信息泄露 |
| client → /api/public/f/:code/submit | 无鉴权，任何人可提交，需输入验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Spoofing | POST /share-links | mitigate | authGuard('form:template:share') 确保仅授权用户可创建链接 |
| T-08-04 | Information Disclosure | GET /public/f/:code | mitigate | 仅返回 templateName/description/schema/requireIdentity，不暴露 creatorId/updatedAt 等内部字段 |
| T-08-05 | Tampering | POST /public/f/:code/submit | mitigate | schemaVersion 从服务端 FormTemplate 读取，不信任客户端传入值；body 使用 t.Object schema 验证 |
| T-08-06 | Denial of Service | POST /public/f/:code/submit | accept | 当前不实现速率限制，Elysia 默认请求体大小限制提供基本保护；后续可加 IP 限流 |
| T-08-07 | Tampering | body.data (JSONB) | mitigate | Prisma 参数化查询，JSONB 值不参与 SQL 拼接；body.data 使用 t.Any() 但存储为 JSONB 无注入风险 |
| T-08-08 | Information Disclosure | nanoid code | mitigate | nanoid(12) 使用 crypto.getRandomValues()，62^12 组合使暴力枚举不可行 |
</threat_model>

<verification>
- GET /api/public/f/{valid_code} 返回 200 + 模板数据
- GET /api/public/f/{invalid_code} 返回 404
- POST /api/public/f/{code}/submit 返回 201 + submission id
- POST /api/v1/templates/{id}/share-links 无 token 返回 401
</verification>

<success_criteria>
分享链接创建 API 需鉴权且仅对 PUBLISHED 模板生效，公开 API 无需鉴权可获取模板和提交数据，提交数据正确关联 schemaVersion 和 ShareLink。
</success_criteria>

<output>
After completion, create `.planning/phases/08-share-public-fill/08-02-SUMMARY.md`
</output>
