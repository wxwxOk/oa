---
phase: 10-schema
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - frontend/src/pages/PublicFillPage.vue
  - frontend/src/components/submission/SubmissionDetail.vue
  - frontend/src/stores/submission.ts
autonomous: true
requirements:
  - SCHEMA-02
  - SCHEMA-03

must_haves:
  truths:
    - "PublicFillPage 使用 GridFormRenderer fill 模式渲染 v2 schema"
    - "PublicFillPage formData 通过 flattenFields 初始化所有字段"
    - "SubmissionDetail 检测 schema.version === 2 时用 GridFormRenderer print 模式"
    - "SubmissionDetail 对旧 v1 schema 保留现有 table 渲染 fallback"
    - "FormFieldRenderer.vue 可安全删除（无引用）"
  artifacts:
    - path: "frontend/src/pages/PublicFillPage.vue"
      provides: "使用 GridFormRenderer fill 模式的公开填写页"
    - path: "frontend/src/components/submission/SubmissionDetail.vue"
      provides: "v2 print 模式 + v1 table fallback 的提交详情"
  key_links:
    - from: "frontend/src/pages/PublicFillPage.vue"
      to: "frontend/src/components/renderer/GridFormRenderer.vue"
      via: "component usage"
      pattern: "<GridFormRenderer"
    - from: "frontend/src/pages/PublicFillPage.vue"
      to: "frontend/src/types/schema.ts"
      via: "import flattenFields, SchemaV2"
      pattern: "import.*from.*types/schema"
    - from: "frontend/src/components/submission/SubmissionDetail.vue"
      to: "frontend/src/components/renderer/GridFormRenderer.vue"
      via: "component usage for v2 schema"
      pattern: "<GridFormRenderer"
---

<objective>
重构 PublicFillPage 和 SubmissionDetail 使用新渲染器，删除旧 FormFieldRenderer，完成 Phase 10 全部消费方迁移。

Purpose: 填写页和提交详情是面向终端用户的页面，必须正确渲染 v2 schema；旧提交数据的 fallback 保证 SCHEMA-03 兼容性
Output: 2 个页面组件重构完成 + FormFieldRenderer 删除
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-schema/10-CONTEXT.md
@.planning/phases/10-schema/10-RESEARCH.md
@.planning/phases/10-schema/10-UI-SPEC.md
@.planning/phases/10-schema/10-01-SUMMARY.md
@.planning/phases/10-schema/10-02-SUMMARY.md

<interfaces>
From frontend/src/types/schema.ts:
```typescript
export type FieldType = 'text' | 'textarea' | 'radio' | 'checkbox' | 'date' | 'phone' | 'signature';
export interface SchemaField { id: string; type: FieldType; label: string; required: boolean; colSpan: number; placeholder?: string; options?: string[]; }
export interface SchemaV2 { version: 2; items: SchemaItem[]; }
export function flattenFields(schema: SchemaV2): SchemaField[];
```

From frontend/src/components/renderer/GridFormRenderer.vue:
```typescript
// Props: schema: SchemaV2, mode: 'designer' | 'fill' | 'print', modelValue?: Record<string, any>
// Emits: update:modelValue
```

From frontend/src/components/renderer/FieldRenderer.vue:
```typescript
// Props: field: SchemaField, mode: 'designer' | 'fill' | 'print', modelValue?: any
// Expose: validate(value, field): boolean, saveSignature(): string, sigRef
```

<!-- 现有页面（将被重构） -->
From frontend/src/pages/PublicFillPage.vue:
```typescript
import FormFieldRenderer from 'src/components/public-fill/FormFieldRenderer.vue';
import type { FormField } from 'src/stores/template';
const schema = ref<FormField[]>([]);
// schema.value = (data.schema as FormField[]).sort((a, b) => a.sort - b.sort);
// formData 初始化: for (const f of schema.value) { formData[f.id] = ... }
// 模板中: <FormFieldRenderer v-for="field in schema" :field="field" ... />
```

From frontend/src/components/submission/SubmissionDetail.vue:
```typescript
import type { SubmissionDetail } from 'src/stores/submission';
// parseSchema(raw): any[] | null — 解析 flat array
// displayFields computed — 用 schema 字段定义渲染
// versionMismatch — schema version 不一致时 fallback
// signatureField — 查找签名字段
```

From frontend/src/stores/submission.ts:
```typescript
export interface SubmissionDetail extends SubmissionRow {
  template: { name: string; schema: any[]; schemaVersion: number; };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 PublicFillPage 使用 GridFormRenderer fill 模式</name>
  <files>frontend/src/pages/PublicFillPage.vue</files>
  <read_first>
    - frontend/src/pages/PublicFillPage.vue (完整内容)
    - frontend/src/components/renderer/GridFormRenderer.vue (Plan 02 产出，确认 props/emits)
    - frontend/src/components/renderer/FieldRenderer.vue (确认 expose 的 validate/saveSignature)
    - frontend/src/types/schema.ts (flattenFields 函数)
  </read_first>
  <action>
重构 `frontend/src/pages/PublicFillPage.vue`：

1. 替换导入：
   - 删除 `import FormFieldRenderer from 'src/components/public-fill/FormFieldRenderer.vue'`
   - 删除 `import type { FormField } from 'src/stores/template'`
   - 添加 `import GridFormRenderer from 'src/components/renderer/GridFormRenderer.vue'`
   - 添加 `import type { SchemaV2 } from 'src/types/schema'`
   - 添加 `import { flattenFields } from 'src/types/schema'`

2. 修改 schema ref 类型：
   ```typescript
   const schema = ref<SchemaV2 | null>(null);
   ```

3. 修改 onMounted 中的 schema 赋值（约第 140 行）：
   ```typescript
   schema.value = data.schema as SchemaV2;
   // 初始化 formData（使用 flattenFields 遍历嵌套结构）
   for (const f of flattenFields(schema.value)) {
     formData[f.id] = f.type === 'checkbox' ? [] : (f.type === 'signature' ? '' : '');
   }
   ```
   - 删除旧的 `.sort((a, b) => a.sort - b.sort)`（v2 无 sort 字段）

4. 替换模板中的 FormFieldRenderer 循环为 GridFormRenderer：
   ```html
   <!-- 动态表单字段 -->
   <GridFormRenderer
     v-if="schema"
     ref="gridRef"
     :schema="schema"
     mode="fill"
     :model-value="formData"
     @update:model-value="Object.assign(formData, $event)"
   />
   ```
   - 删除旧的 `<FormFieldRenderer v-for="field in schema" ...>` 循环

5. 修改 handleSubmit 中的校验逻辑：
   - QForm validate 保持不变
   - 删除旧的 fieldRefs 循环校验（FieldRenderer 内部已处理 radio/checkbox/signature 校验）
   - 签名保存：遍历 flattenFields 找 signature 类型字段，通过 GridFormRenderer 的 ref 获取对应 FieldRenderer 的 saveSignature

6. 删除 `const fieldRefs = ref<InstanceType<typeof FormFieldRenderer>[]>([])` 声明
   添加 `const gridRef = ref<InstanceType<typeof GridFormRenderer> | null>(null)`

注意：GridFormRenderer 的 `update:modelValue` emit 返回完整的 formData 对象（含更新的字段），用 `Object.assign(formData, $event)` 合并到 reactive formData。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - PublicFillPage.vue 包含 `import GridFormRenderer from 'src/components/renderer/GridFormRenderer.vue'`
    - PublicFillPage.vue 包含 `import { flattenFields } from 'src/types/schema'`
    - PublicFillPage.vue 包含 `<GridFormRenderer` 和 `mode="fill"`
    - PublicFillPage.vue 不包含 `import FormFieldRenderer`（旧导入已删除）
    - PublicFillPage.vue 不包含 `import.*FormField.*from.*stores/template`（旧类型已删除）
    - PublicFillPage.vue 不包含 `.sort((a, b) => a.sort - b.sort)`（旧排序已删除）
    - PublicFillPage.vue 包含 `flattenFields`（formData 初始化）
  </acceptance_criteria>
  <done>PublicFillPage 使用 GridFormRenderer fill 模式渲染 v2 schema，formData 通过 flattenFields 初始化</done>
</task>

<task type="auto">
  <name>Task 2: 重构 SubmissionDetail v2 print + v1 fallback + 清理</name>
  <files>frontend/src/components/submission/SubmissionDetail.vue, frontend/src/stores/submission.ts</files>
  <read_first>
    - frontend/src/components/submission/SubmissionDetail.vue (完整内容)
    - frontend/src/stores/submission.ts (SubmissionDetail 接口，template.schema 类型)
    - frontend/src/components/renderer/GridFormRenderer.vue (确认 print 模式 props)
    - frontend/src/types/schema.ts (SchemaV2 类型)
  </read_first>
  <action>
重构 `frontend/src/components/submission/SubmissionDetail.vue`（per SCHEMA-03, D-05）：

核心策略：检测 schema 是否为 v2 格式。v2 用 GridFormRenderer print 模式，v1 保留现有 table 渲染。

1. 添加导入：
   ```typescript
   import GridFormRenderer from 'src/components/renderer/GridFormRenderer.vue';
   import type { SchemaV2 } from 'src/types/schema';
   ```

2. 添加 v2 检测 computed：
   ```typescript
   const isV2Schema = computed(() => {
     const schema = props.submission.template.schema;
     return schema && typeof schema === 'object' && !Array.isArray(schema) && (schema as any).version === 2;
   });

   const v2Schema = computed(() =>
     isV2Schema.value ? (props.submission.template.schema as unknown as SchemaV2) : null
   );
   ```

3. 修改模板 — 在 `#print-area` 内部，用 v-if/v-else 分支：
   ```html
   <!-- v2 schema: GridFormRenderer print 模式 -->
   <template v-if="isV2Schema && v2Schema">
     <GridFormRenderer
       :schema="v2Schema"
       mode="print"
       :model-value="submission.data as Record<string, any>"
     />
   </template>

   <!-- v1 schema fallback: 保留现有 table 渲染 -->
   <template v-else>
     <table class="detail-table" style="width: 100%; border-collapse: collapse">
       <tr v-for="field in displayFields" :key="field.id">
         <th>{{ field.label }}</th>
         <td>{{ formatFieldValue(field) }}</td>
       </tr>
     </table>
     <div v-if="signatureField" class="signature-section" style="margin-top: 16px">
       <div style="font-size: 14px; font-weight: 600; margin-bottom: 8px">签名</div>
       <img :src="signatureField.value" style="max-width: 300px; max-height: 150px; border: 1px solid var(--oa-border)" alt="手写签名" />
     </div>
   </template>
   ```

4. 保留 versionMismatch banner（在两个分支之后，对 v1 和 v2 都显示）

5. 保留所有现有的 v1 渲染逻辑（parseSchema, displayFields, signatureField, formatFieldValue）— 这些只在 v1 fallback 分支中使用

6. 修改 `frontend/src/stores/submission.ts` 的 SubmissionDetail 接口：
   ```typescript
   export interface SubmissionDetail extends SubmissionRow {
     template: {
       name: string;
       schema: any;  // 从 any[] 改为 any（v2 是对象，v1 是数组）
       schemaVersion: number;
     };
   }
   ```

7. 最后，删除 `frontend/src/components/public-fill/FormFieldRenderer.vue` 文件（所有引用已在本 plan 和 Plan 03 中移除）。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - SubmissionDetail.vue 包含 `import GridFormRenderer`
    - SubmissionDetail.vue 包含 `isV2Schema` computed（版本检测）
    - SubmissionDetail.vue 包含 `mode="print"`（v2 print 模式）
    - SubmissionDetail.vue 保留 `detail-table` class（v1 fallback table）
    - SubmissionDetail.vue 保留 `parseSchema` 函数（v1 fallback 使用）
    - SubmissionDetail.vue 保留 `versionMismatch` computed（版本不一致提示）
    - submission.ts 的 SubmissionDetail.template.schema 类型为 `any`（不是 `any[]`）
    - frontend/src/components/public-fill/FormFieldRenderer.vue 不存在（已删除）
  </acceptance_criteria>
  <done>SubmissionDetail v2 schema 用 GridFormRenderer print 模式渲染，v1 旧数据保留 table fallback，FormFieldRenderer 已删除</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run --reporter=verbose` 全部通过
- `grep -rn "FormFieldRenderer" frontend/src/` 无输出（所有引用已清除）
- `grep -n "GridFormRenderer" frontend/src/pages/PublicFillPage.vue` 有输出
- `grep -n "isV2Schema" frontend/src/components/submission/SubmissionDetail.vue` 有输出
- `grep -n "mode=\"print\"" frontend/src/components/submission/SubmissionDetail.vue` 有输出
- `test ! -f frontend/src/components/public-fill/FormFieldRenderer.vue && echo "DELETED"` 输出 DELETED
</verification>

<success_criteria>
- PublicFillPage 使用 GridFormRenderer fill 模式，formData 正确初始化
- SubmissionDetail 对 v2 schema 用 print 模式渲染，对 v1 保留 table fallback
- FormFieldRenderer.vue 已删除，无残留引用
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/10-schema/10-04-SUMMARY.md`
</output>
