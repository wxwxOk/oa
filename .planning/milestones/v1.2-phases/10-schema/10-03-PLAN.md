---
phase: 10-schema
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - frontend/src/components/designer/fieldRegistry.ts
  - frontend/src/components/designer/FieldPalette.vue
  - frontend/src/components/designer/PropertyEditor.vue
  - frontend/src/components/designer/DesignerCanvas.vue
autonomous: true
requirements:
  - SCHEMA-01

must_haves:
  truths:
    - "fieldRegistry 使用新 FieldType/SchemaField 类型，defaultProps 含 colSpan: 12"
    - "FieldPalette cloneField 输出 SchemaField 格式（含 colSpan: 12，无 sort）"
    - "PropertyEditor 支持编辑 colSpan（1-12 QSlider）"
    - "DesignerCanvas 使用 GridFormRenderer designer 模式渲染"
  artifacts:
    - path: "frontend/src/components/designer/fieldRegistry.ts"
      provides: "使用新类型的字段注册表"
    - path: "frontend/src/components/designer/FieldPalette.vue"
      provides: "输出 SchemaField 格式的 cloneField"
    - path: "frontend/src/components/designer/PropertyEditor.vue"
      provides: "含 colSpan 编辑的属性面板"
    - path: "frontend/src/components/designer/DesignerCanvas.vue"
      provides: "使用 GridFormRenderer 的设计器画布"
  key_links:
    - from: "frontend/src/components/designer/fieldRegistry.ts"
      to: "frontend/src/types/schema.ts"
      via: "import FieldType, SchemaField"
      pattern: "import.*from.*types/schema"
    - from: "frontend/src/components/designer/DesignerCanvas.vue"
      to: "frontend/src/components/renderer/GridFormRenderer.vue"
      via: "component usage"
      pattern: "<GridFormRenderer"
    - from: "frontend/src/components/designer/FieldPalette.vue"
      to: "frontend/src/components/designer/fieldRegistry.ts"
      via: "import FIELD_GROUPS"
      pattern: "import.*FIELD_GROUPS.*from.*fieldRegistry"
---

<objective>
重构设计器组件链（fieldRegistry → FieldPalette → PropertyEditor → DesignerCanvas）使用 v2 schema 类型和新渲染器。

Purpose: 设计器是模板编辑的核心入口，必须产出 v2 schema 格式数据
Output: 4 个设计器组件全部使用新类型，DesignerCanvas 使用 GridFormRenderer designer 模式
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-schema/10-CONTEXT.md
@.planning/phases/10-schema/10-RESEARCH.md
@.planning/phases/10-schema/10-UI-SPEC.md
@.planning/phases/10-schema/10-01-SUMMARY.md
@.planning/phases/10-schema/10-02-SUMMARY.md

<interfaces>
From frontend/src/types/schema.ts:
```typescript
export type FieldType = 'text' | 'textarea' | 'radio' | 'checkbox' | 'date' | 'phone' | 'signature';
export interface SchemaField {
  id: string; type: FieldType; label: string; required: boolean;
  colSpan: number; placeholder?: string; options?: string[];
}
export interface SchemaRow { type: 'row'; fields: SchemaField[]; }
export interface SchemaV2 { version: 2; items: SchemaItem[]; }
export function flattenFields(schema: SchemaV2): SchemaField[];
export function createEmptySchema(): SchemaV2;
```

From frontend/src/stores/template.ts (Plan 02 重构后):
```typescript
import type { SchemaV2, SchemaField } from 'src/types/schema';
export interface Template { ...; schema: SchemaV2; ... }
// selectedField getter 使用 flattenFields
// update action payload: schema?: SchemaV2
```

From frontend/src/components/renderer/GridFormRenderer.vue (Plan 02 产出):
```typescript
// Props: schema: SchemaV2, mode: 'designer' | 'fill' | 'print', modelValue?: Record<string, any>
// Emits: update:modelValue
```

<!-- 现有设计器组件（将被重构） -->
From frontend/src/components/designer/fieldRegistry.ts:
```typescript
import type { FormField } from 'src/stores/template';
export interface FieldTypeDef {
  type: FormField['type']; label: string; icon: string;
  group: 'basic' | 'special'; defaultProps: Partial<FormField>;
}
export const FIELD_TYPES: FieldTypeDef[] = [...];
export const FIELD_GROUPS = { basic: {...}, special: {...} };
```

From frontend/src/components/designer/FieldPalette.vue:
```typescript
function cloneField(item: FieldTypeDef) {
  return { id: crypto.randomUUID(), type: item.type, label: item.label,
    required: false, placeholder: item.defaultProps.placeholder ?? '',
    options: item.defaultProps.options ? [...item.defaultProps.options] : undefined,
    sort: 0 };
}
```

From frontend/src/components/designer/DesignerCanvas.vue:
```typescript
const fields = computed({
  get: () => store.current?.schema ?? [],
  set: (val: FormField[]) => { if (store.current) store.current.schema = val; },
});
// useDraggable(canvasRef, fields, { group: 'designer', ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 fieldRegistry + FieldPalette 使用新类型</name>
  <files>frontend/src/components/designer/fieldRegistry.ts, frontend/src/components/designer/FieldPalette.vue</files>
  <read_first>
    - frontend/src/components/designer/fieldRegistry.ts (完整内容)
    - frontend/src/components/designer/FieldPalette.vue (完整内容，特别是 cloneField 函数)
    - frontend/src/types/schema.ts (新类型定义)
  </read_first>
  <action>
重构 `frontend/src/components/designer/fieldRegistry.ts`：

1. 将 `import type { FormField } from 'src/stores/template'` 改为 `import type { FieldType, SchemaField } from 'src/types/schema'`
2. 修改 FieldTypeDef 接口：
   ```typescript
   export interface FieldTypeDef {
     type: FieldType;
     label: string;
     icon: string;
     group: 'basic' | 'special';
     defaultProps: Partial<SchemaField>;
   }
   ```
3. 每个 FIELD_TYPES 条目的 defaultProps 增加 `colSpan: 12`（per D-03）：
   - text: `{ placeholder: '请输入', colSpan: 12 }`
   - textarea: `{ placeholder: '请输入', colSpan: 12 }`
   - radio: `{ options: ['选项1', '选项2'], colSpan: 12 }`
   - checkbox: `{ options: ['选项1', '选项2'], colSpan: 12 }`
   - date: `{ colSpan: 12 }`
   - phone: `{ placeholder: '请输入手机号', colSpan: 12 }`
   - signature: `{ colSpan: 12 }`

重构 `frontend/src/components/designer/FieldPalette.vue`：

修改 cloneField 函数输出 SchemaField 格式（per D-03）：
```typescript
function cloneField(item: FieldTypeDef): SchemaField {
  return {
    id: crypto.randomUUID(),
    type: item.type,
    label: item.label,
    required: false,
    colSpan: 12,
    placeholder: item.defaultProps.placeholder,
    options: item.defaultProps.options ? [...item.defaultProps.options] : undefined,
  };
}
```
- 删除 `sort: 0`（v2 schema 无 sort 字段）
- 新增 `colSpan: 12`
- 添加 `import type { SchemaField } from 'src/types/schema'` 用于返回类型标注

注意：FieldPalette 的 useDraggable clone 回调现在返回 SchemaField 而非 FormField。DesignerCanvas 的 onAdd 回调需要将 clone 结果包装为 SchemaRow（在 Task 2 处理）。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - fieldRegistry.ts 包含 `import type { FieldType, SchemaField } from 'src/types/schema'`
    - fieldRegistry.ts 不包含 `import.*FormField.*from.*stores/template`
    - fieldRegistry.ts 的每个 FIELD_TYPES 条目 defaultProps 包含 `colSpan: 12`
    - FieldPalette.vue 的 cloneField 返回值包含 `colSpan: 12`
    - FieldPalette.vue 的 cloneField 返回值不包含 `sort`
  </acceptance_criteria>
  <done>fieldRegistry 和 FieldPalette 使用新 FieldType/SchemaField 类型，cloneField 输出含 colSpan: 12 的 SchemaField</done>
</task>

<task type="auto">
  <name>Task 2: 重构 PropertyEditor + DesignerCanvas</name>
  <files>frontend/src/components/designer/PropertyEditor.vue, frontend/src/components/designer/DesignerCanvas.vue</files>
  <read_first>
    - frontend/src/components/designer/PropertyEditor.vue (完整内容)
    - frontend/src/components/designer/DesignerCanvas.vue (完整内容)
    - frontend/src/stores/template.ts (重构后的 store，确认 selectedField 类型)
    - frontend/src/components/renderer/GridFormRenderer.vue (Plan 02 产出)
    - frontend/src/types/schema.ts (SchemaV2, SchemaRow 类型)
  </read_first>
  <action>
重构 `frontend/src/components/designer/PropertyEditor.vue`：

1. 添加 colSpan 编辑控件（在 "必填" toggle 之后，"提示文字" 之前）：
   ```html
   <div style="font-size: 14px; color: var(--oa-text-secondary)" class="q-mt-sm">列宽</div>
   <q-slider
     v-model="field.colSpan"
     :min="1"
     :max="12"
     :step="1"
     label
     :label-value="field.colSpan + ' / 12'"
     color="primary"
   />
   ```
2. 确认 `field` computed 引用的 `store.selectedField` 现在返回 `SchemaField | null`（Plan 02 已重构 store）
3. 无需修改 import（PropertyEditor 通过 store getter 获取 field，不直接 import FormField）

重构 `frontend/src/components/designer/DesignerCanvas.vue`：

核心变更：从直接操作 flat FormField[] 改为使用 GridFormRenderer designer 模式 + 操作 SchemaV2.items。

1. 删除旧的 field-card 模板（整个 v-for field in fields 的 QCard 渲染），替换为：
   ```html
   <GridFormRenderer
     v-if="store.current?.schema?.items?.length"
     :schema="store.current.schema"
     mode="designer"
   />
   ```

2. 修改 fields computed 为操作 SchemaV2.items：
   ```typescript
   import type { SchemaV2, SchemaRow, SchemaField } from 'src/types/schema';
   import { createEmptySchema } from 'src/types/schema';
   import GridFormRenderer from 'src/components/renderer/GridFormRenderer.vue';

   // 确保 schema 初始化为 v2 格式
   function ensureSchema(): SchemaV2 {
     if (!store.current) return createEmptySchema();
     if (!store.current.schema || !('version' in store.current.schema)) {
       store.current.schema = createEmptySchema();
     }
     return store.current.schema;
   }
   ```

3. 修改 useDraggable 的 onAdd 回调：当 FieldPalette clone 的 SchemaField 被拖入时，将其包装为新 SchemaRow 并 push 到 schema.items：
   ```typescript
   function addFieldAsRow(field: SchemaField) {
     const schema = ensureSchema();
     const row: SchemaRow = { type: 'row', fields: [field] };
     schema.items.push(row);
   }
   ```

4. 修改 removeField：遍历 schema.items 找到包含该 field 的 row，从 row.fields 中移除。如果 row.fields 为空则移除整个 row。

5. 保留 useDraggable 用于画布级别的拖拽排序（Phase 11 会完善行级拖拽）。当前 Phase 10 的 designer 模式：渲染 + 选中 + 属性编辑 + 添加/删除字段。拖拽重排放 Phase 11。

6. 保留空状态显示："从左侧拖入字段开始设计表单"（per UI-SPEC copywriting）

7. 删除旧的 `mapOptions` 函数（已移入 FieldRenderer）
8. 删除旧的 `reindex` 函数（v2 schema 无 sort 字段）
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - PropertyEditor.vue 包含 `q-slider` 和 `colSpan`（colSpan 编辑控件）
    - PropertyEditor.vue 包含 `:min="1"` 和 `:max="12"`（colSpan 范围）
    - DesignerCanvas.vue 包含 `<GridFormRenderer` 或 `GridFormRenderer`（使用新渲染器）
    - DesignerCanvas.vue 包含 `mode="designer"`（designer 模式）
    - DesignerCanvas.vue 包含 `import.*from.*types/schema`（新类型导入）
    - DesignerCanvas.vue 包含 `createEmptySchema`（schema 初始化）
    - DesignerCanvas.vue 不包含 `f.sort` 或 `reindex`（旧排序逻辑已移除）
    - DesignerCanvas.vue 不包含 `mapOptions`（已移入 FieldRenderer）
  </acceptance_criteria>
  <done>PropertyEditor 支持 colSpan 1-12 编辑，DesignerCanvas 使用 GridFormRenderer designer 模式渲染，字段添加/删除操作 SchemaV2.items</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run --reporter=verbose` 全部通过
- `grep -rn "FormField" frontend/src/components/designer/fieldRegistry.ts` 无输出（旧类型已移除）
- `grep -n "colSpan: 12" frontend/src/components/designer/fieldRegistry.ts` 有 7 行输出
- `grep -n "GridFormRenderer" frontend/src/components/designer/DesignerCanvas.vue` 有输出
- `grep -n "colSpan" frontend/src/components/designer/PropertyEditor.vue` 有输出
</verification>

<success_criteria>
- fieldRegistry 使用新类型，所有字段默认 colSpan: 12
- FieldPalette clone 输出 SchemaField 格式
- PropertyEditor 支持 colSpan 滑块编辑
- DesignerCanvas 使用 GridFormRenderer designer 模式
- 设计器可添加/删除字段，字段存储为 v2 schema 格式
</success_criteria>

<output>
After completion, create `.planning/phases/10-schema/10-03-SUMMARY.md`
</output>
