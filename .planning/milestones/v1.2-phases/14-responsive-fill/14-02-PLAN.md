---
phase: 14-responsive-fill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/renderer/DynamicTableFill.vue
autonomous: false
requirements: [RENDER-02]
must_haves:
  truths:
    - "Mobile dynamic table renders as vertical cards instead of HTML table"
    - "Each card shows row number title (第 1 行, 第 2 行)"
    - "Cards are expanded by default, user can collapse/expand"
    - "Each card has delete button in header, add row button at bottom"
    - "PC dynamic table remains unchanged (HTML table)"
    - "Mobile card inputs have 44px min-height touch targets"
  artifacts:
    - path: "frontend/src/components/renderer/DynamicTableFill.vue"
      provides: "Mobile card layout with QExpansionItem, PC table unchanged"
      contains: "QExpansionItem"
  key_links:
    - from: "DynamicTableFill.vue v-if isMobile"
      to: "useResponsive()"
      via: "composable import"
      pattern: "isMobile"
    - from: "DynamicTableFill.vue expandedStates"
      to: "rows.value.length"
      via: "watch sync"
      pattern: "expandedStates"
---

<objective>
Mobile card layout for DynamicTableFill — switch from HTML table to QExpansionItem cards on mobile.

Purpose: RENDER-02 requires dynamic table to display as collapsible cards on mobile for touch-friendly data entry. Each row becomes a card with vertical field layout.
Output: Modified DynamicTableFill.vue with v-if template branching for mobile/desktop.
</objective>

<execution_context>
@C:/Users/11828/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/11828/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-responsive-fill/14-CONTEXT.md
@.planning/phases/14-responsive-fill/14-RESEARCH.md
@.planning/phases/14-responsive-fill/14-UI-SPEC.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From frontend/src/composables/useResponsive.ts:
```typescript
export function useResponsive(): {
  isDesktop: ComputedRef<boolean>;
  isMobile: ComputedRef<boolean>;
}
```

From frontend/src/components/renderer/dynamicTableUtils.ts:
```typescript
export interface TableColumn {
  key: string;
  label: string;
  type: DynamicTableColumnType;  // 'text' | 'phone' | 'date' | 'radio' | 'checkbox'
  width?: number;
  options?: string[];
}
export function createEmptyRow(columns: TableColumn[]): Record<string, any>;
```

From frontend/src/components/renderer/DynamicTableFill.vue (current props):
```typescript
const props = defineProps<{
  label: string;
  columns: TableColumn[];
  modelValue?: Record<string, any>[];
}>();
```

From frontend/src/components/renderer/DynamicTableFill.vue (existing functions to reuse):
```typescript
function addRow(): void;
function removeRow(idx: number): void;
function updateCell(rowIdx: number, colKey: string, value: any): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile card layout with QExpansionItem to DynamicTableFill</name>
  <files>frontend/src/components/renderer/DynamicTableFill.vue</files>
  <read_first>
    - frontend/src/components/renderer/DynamicTableFill.vue
    - frontend/src/components/renderer/dynamicTableUtils.ts
    - frontend/src/composables/useResponsive.ts
  </read_first>
  <action>
Modify DynamicTableFill.vue to add mobile card layout (per D-05, D-06, D-07, D-08, D-09).

**Script changes:**

1. Add imports at top of script setup:
```typescript
import { ref, watch, onMounted } from 'vue';  // add watch to existing import
import { useResponsive } from 'src/composables/useResponsive';
```

2. Add after existing `const rows = ref(...)` line:
```typescript
const { isMobile } = useResponsive();

const expandedStates = ref<boolean[]>([]);
watch(() => rows.value.length, (newLen) => {
  while (expandedStates.value.length < newLen) {
    expandedStates.value.push(true);
  }
  expandedStates.value.length = newLen;
}, { immediate: true });
```

**Template changes:**

Wrap the entire existing template content in `<template v-if="!isMobile">...</template>` (the desktop path). Add a new `<template v-if="isMobile">` block for mobile BEFORE the desktop block.

The mobile template structure:
```html
<template v-if="isMobile">
  <q-card flat bordered class="dynamic-table-fill q-mb-sm">
    <div class="table-label">{{ label }}</div>
    <div v-for="(row, rowIdx) in rows" :key="rowIdx" class="card-row">
      <q-expansion-item
        v-model="expandedStates[rowIdx]"
        expand-separator
        dense
      >
        <template #header>
          <q-item-section>第 {{ rowIdx + 1 }} 行</q-item-section>
          <q-item-section side>
            <q-btn
              flat dense round
              icon="delete_outline"
              size="xs"
              color="negative"
              :aria-label="`删除第 ${rowIdx + 1} 行`"
              @click.stop="removeRow(rowIdx)"
            />
          </q-item-section>
        </template>
        <q-card-section class="card-fields">
          <div v-for="col in columns" :key="col.key" class="card-field">
            <div class="card-field-label">{{ col.label }}</div>
            <!-- text -->
            <q-input v-if="col.type === 'text'"
              :model-value="row[col.key]"
              @update:model-value="updateCell(rowIdx, col.key, $event)"
              outlined dense hide-bottom-space />
            <!-- phone -->
            <q-input v-else-if="col.type === 'phone'"
              :model-value="row[col.key]"
              @update:model-value="updateCell(rowIdx, col.key, $event)"
              outlined dense type="tel" mask="###########"
              hide-bottom-space />
            <!-- date -->
            <q-input v-else-if="col.type === 'date'"
              :model-value="row[col.key]"
              @update:model-value="updateCell(rowIdx, col.key, $event)"
              outlined dense readonly placeholder="选择日期"
              hide-bottom-space>
              <template #append>
                <q-icon name="calendar_today" class="cursor-pointer" size="xs">
                  <q-popup-proxy cover transition-show="scale"
                    transition-hide="scale">
                    <q-date :model-value="row[col.key]"
                      @update:model-value="updateCell(rowIdx, col.key, $event)"
                      mask="YYYY-MM-DD" />
                  </q-popup-proxy>
                </q-icon>
              </template>
            </q-input>
            <!-- radio -->
            <q-select v-else-if="col.type === 'radio'"
              :model-value="row[col.key]"
              @update:model-value="updateCell(rowIdx, col.key, $event)"
              :options="col.options ?? []"
              outlined dense emit-value map-options hide-bottom-space />
            <!-- checkbox -->
            <q-select v-else-if="col.type === 'checkbox'"
              :model-value="row[col.key] ?? []"
              @update:model-value="updateCell(rowIdx, col.key, $event)"
              :options="col.options ?? []"
              outlined dense multiple emit-value map-options
              hide-bottom-space />
          </div>
        </q-card-section>
      </q-expansion-item>
    </div>
    <q-btn flat dense icon="add" label="添加行" color="primary"
      class="add-row-btn" @click="addRow" />
  </q-card>
</template>
```

Then wrap the existing desktop template in `<template v-else>`:
```html
<template v-else>
  <!-- existing q-card with table layout, unchanged -->
</template>
```

**Style changes** — append to existing style scoped:
```css
.card-row {
  border-bottom: 1px solid var(--oa-border);
}
.card-row:last-of-type {
  border-bottom: none;
}
.card-fields {
  display: flex;
  flex-direction: column;
  gap: 12px;
}
.card-field-label {
  font-size: 14px;
  font-weight: 600;
  color: var(--oa-text-secondary);
  margin-bottom: 4px;
}
@media (max-width: 1023px) {
  .card-fields :deep(.q-field__control) {
    min-height: 44px;
  }
}
```

Do NOT modify the existing desktop table template, existing script functions (addRow, removeRow, updateCell, emitUpdate), or existing CSS classes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DynamicTableFill.vue contains `import { useResponsive }` from composables
    - DynamicTableFill.vue contains `const { isMobile } = useResponsive()`
    - DynamicTableFill.vue contains `expandedStates`
    - DynamicTableFill.vue contains `v-if="isMobile"`
    - DynamicTableFill.vue contains `q-expansion-item`
    - DynamicTableFill.vue contains `第 {{ rowIdx + 1 }} 行`
    - DynamicTableFill.vue contains `aria-label` on delete button
    - DynamicTableFill.vue contains `@click.stop="removeRow(rowIdx)"`
    - DynamicTableFill.vue contains `card-field-label`
    - DynamicTableFill.vue contains `min-height: 44px`
    - DynamicTableFill.vue still contains the existing `.fill-table` class (desktop preserved)
  </acceptance_criteria>
  <done>Mobile renders QExpansionItem cards per row with title/delete/fields. Desktop renders unchanged HTML table. Cards default expanded, collapsible. Touch targets 44px.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of responsive fill page</name>
  <files>frontend/src/pages/PublicFillPage.vue</files>
  <action>
Human visual verification of the complete responsive fill page across PC and mobile viewports.

What was built (across Plan 01 and Plan 02):
- PC 960px 12-col grid layout (Plan 01 Task 1)
- Mobile single-column layout with sticky submit (Plan 01 Tasks 1-2)
- Mobile dynamic table card layout with QExpansionItem (Plan 02 Task 1)

Steps to verify:
1. Open a fill page URL in browser at desktop width (>= 1024px):
   - Form container is 960px wide (not 640px)
   - Multi-column fields display side-by-side per colSpan
   - Dynamic table shows as HTML table
   - Groups show title bar + multi-column grid
2. Resize browser to less than 1024px (or use DevTools mobile emulation):
   - All fields become single-column full-width
   - Group fields become single-column (title bar preserved)
   - Dynamic table switches to card layout
   - Cards show "第 1 行", "第 2 行" titles
   - Cards are expanded by default
   - Click a card title to collapse, click again to expand
   - Delete button on each card header
   - "+ 添加行" button at bottom
   - Submit button is sticky at bottom when scrolling
   - Input fields have adequate touch target size
  </action>
  <verify>Human visual inspection — user types "approved" or describes issues</verify>
  <done>User confirms responsive layout works correctly on both PC and mobile viewports</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run` passes
- Desktop: 960px container, 12-col grid, HTML table for dynamic tables
- Mobile: single-column, card layout for dynamic tables, sticky submit
- Cards: expanded by default, collapsible, row titles, delete buttons
- Touch targets: 44px min-height on mobile inputs
</verification>

<success_criteria>
1. Mobile dynamic table renders as QExpansionItem cards (not HTML table)
2. Each card shows "第 N 行" title and delete button
3. Cards default expanded, user can toggle collapse
4. Add row button visible at bottom of card list
5. Desktop table layout completely unchanged
6. Visual verification approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/14-responsive-fill/14-02-SUMMARY.md`
</output>
