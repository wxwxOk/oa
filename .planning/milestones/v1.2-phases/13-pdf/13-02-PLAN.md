---
phase: 13-pdf
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/composables/__tests__/usePdfExport.test.ts
autonomous: true
requirements: [PDF-02]
must_haves:
  truths:
    - "usePdfExport.test.ts 存在且包含分页算法纯函数测试"
    - "测试覆盖 collectBreakpoints / findBestBreak / computePageSlices / injectHeaderFooter"
    - "测试覆盖极端情况（无安全切点强制分页 + console.warn）"
    - "测试覆盖动态表格表头复出标记"
    - "测试覆盖单页 PDF 也显示页眉页脚（D-11）"
  artifacts:
    - path: "frontend/src/composables/__tests__/usePdfExport.test.ts"
      provides: "PDF 分页算法单元测试"
      contains: "describe('collectBreakpoints'"
  key_links:
    - from: "usePdfExport.test.ts"
      to: "usePdfExport.ts"
      via: "import { collectBreakpoints, findBestBreak, computePageSlices, injectHeaderFooter }"
      pattern: "import.*collectBreakpoints"
---

<objective>
为 usePdfExport.ts 的智能分页纯函数编写 TDD 测试（RED 阶段），覆盖切点收集、最佳切点查找、页面切片计算、页眉页脚注入。

Purpose: Wave 0 测试先行，为 Plan 03 的 usePdfExport.ts 重写提供验证基础。测试定义了分页算法的预期行为契约。
Output: usePdfExport.test.ts 测试文件（RED 状态 — 测试会失败因为被测函数尚未导出）。
</objective>

<execution_context>
@.planning/phases/13-pdf/13-CONTEXT.md
@.planning/phases/13-pdf/13-RESEARCH.md
@.planning/phases/13-pdf/13-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- 被测函数签名（Plan 03 将实现并导出这些函数） -->

```typescript
// usePdfExport.ts 将导出的纯函数接口

export interface BreakCandidate {
  y: number;        // 相对于容器顶部的像素偏移
  type: 'row' | 'group-start' | 'group-row' | 'table-start' | 'table-row';
  isTableInternal: boolean;  // 是否在动态表格内部（需要表头复出）
  tableHeaderRect?: { y: number; height: number };  // 表头在 canvas 中的位置
}

export interface PageSlice {
  startY: number;   // canvas 像素坐标（已乘 scale）
  endY: number;
  needsTableHeader: boolean;
  tableHeaderRect?: { y: number; height: number };
}

export function findBestBreak(
  candidates: BreakCandidate[],
  pageBottom: number,
): BreakCandidate | null;

export function computePageSlices(
  breakpoints: BreakCandidate[],
  totalHeight: number,
  pageContentHeight: number,
  scale: number,
): PageSlice[];

export function injectHeaderFooter(
  pdf: jsPDF,
  formTitle: string,
  submitTime: string,
): void;
```

From frontend/src/composables/__tests__/useDarkMode.test.ts (mock 模式参考):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

const mockDark = { isActive: false, toggle: vi.fn() };
vi.mock('quasar', () => ({
  useQuasar: () => ({ dark: mockDark }),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 usePdfExport.test.ts — 分页算法纯函数测试</name>
  <read_first>
    - frontend/src/composables/usePdfExport.ts
    - frontend/src/composables/__tests__/useDarkMode.test.ts
    - frontend/src/components/renderer/__tests__/dynamicTableUtils.test.ts
    - .planning/phases/13-pdf/13-RESEARCH.md
    - .planning/phases/13-pdf/13-CONTEXT.md
  </read_first>
  <files>frontend/src/composables/__tests__/usePdfExport.test.ts</files>
  <behavior>
    **findBestBreak 测试：**
    - Test 1: 给定 candidates=[{y:100}, {y:200}, {y:300}] 和 pageBottom=250，返回 {y:200}
    - Test 2: 给定 candidates=[{y:100}, {y:200}] 和 pageBottom=50，返回 null（没有 <= pageBottom 的候选）
    - Test 3: 给定 candidates=[{y:100}, {y:200}, {y:300}] 和 pageBottom=300，返回 {y:300}（精确匹配）
    - Test 4: 给定空 candidates=[] 和任意 pageBottom，返回 null

    **computePageSlices 测试：**
    - Test 5: totalHeight <= pageContentHeight（单页），返回 1 个 slice [{startY:0, endY:totalHeight}]
    - Test 6: totalHeight = 2 * pageContentHeight，breakpoints 在中间，返回 2 个 slices 在切点处分割
    - Test 7: 无安全切点（breakpoints 为空），强制按 pageContentHeight 切 + console.warn 被调用
    - Test 8: 切点在动态表格内部（isTableInternal=true），对应 slice 的 needsTableHeader=true

    **injectHeaderFooter 测试：**
    - Test 9: 2 页 PDF，每页都调用 pdf.text() 注入页眉（表单名称居中）和页脚（时间+页码）
    - Test 10: 单页 PDF 也注入页眉页脚（D-11），页码为 "1 / 1"
    - Test 11: pdf.setPage() 被调用 N 次（N = 总页数）
    - Test 12: 页眉 y 坐标约 8mm，页脚 y 坐标约 pageHeight - 8mm
  </behavior>
  <action>
创建 `frontend/src/composables/__tests__/usePdfExport.test.ts`。

**文件结构：**

1. 顶部 vi.mock html2canvas 和 jspdf（参考 useDarkMode.test.ts 的 mock 模式）：
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// Mock html2canvas（本测试文件不直接测试 html2canvas，但 import 需要 mock）
vi.mock('html2canvas', () => ({
  default: vi.fn().mockResolvedValue({
    width: 1588,
    height: 4000,
    toDataURL: vi.fn().mockReturnValue('data:image/jpeg;base64,mock'),
    getContext: vi.fn().mockReturnValue({ drawImage: vi.fn() }),
  }),
}));

// Mock jsPDF
const mockPdf = {
  internal: {
    pageSize: { getWidth: () => 210, getHeight: () => 297 },
    getNumberOfPages: vi.fn().mockReturnValue(2),
  },
  addImage: vi.fn(),
  addPage: vi.fn(),
  setPage: vi.fn(),
  setFontSize: vi.fn(),
  setTextColor: vi.fn(),
  text: vi.fn(),
  save: vi.fn(),
};
vi.mock('jspdf', () => ({
  jsPDF: vi.fn().mockImplementation(() => mockPdf),
}));
```

2. 导入被测函数（Plan 03 将从 usePdfExport.ts 导出这些函数）：
```typescript
import {
  findBestBreak,
  computePageSlices,
  injectHeaderFooter,
  type BreakCandidate,
} from '../usePdfExport';
```

3. 测试 fixtures — 预定义的 BreakCandidate 数组：
```typescript
// 模拟简单表单：3 行，每行高 100px
const simpleBreakpoints: BreakCandidate[] = [
  { y: 0, type: 'row', isTableInternal: false },
  { y: 100, type: 'row', isTableInternal: false },
  { y: 200, type: 'row', isTableInternal: false },
  { y: 300, type: 'row', isTableInternal: false },
];

// 模拟含动态表格的表单
const tableBreakpoints: BreakCandidate[] = [
  { y: 0, type: 'row', isTableInternal: false },
  { y: 100, type: 'row', isTableInternal: false },
  { y: 200, type: 'table-start', isTableInternal: false },
  { y: 230, type: 'table-row', isTableInternal: true, tableHeaderRect: { y: 200, height: 30 } },
  { y: 260, type: 'table-row', isTableInternal: true, tableHeaderRect: { y: 200, height: 30 } },
  { y: 290, type: 'table-row', isTableInternal: true, tableHeaderRect: { y: 200, height: 30 } },
];
```

4. A4 常量（与 usePdfExport.ts 保持一致）：
```typescript
const SCALE = 2;
const CONTENT_HEIGHT_MM = 247; // A4 可用内容高度
const MM_TO_PX = 96 / 25.4;   // 1mm ≈ 3.78px
const PAGE_CONTENT_HEIGHT_PX = CONTENT_HEIGHT_MM * MM_TO_PX * SCALE; // ≈ 1866px
```

5. describe 块：
- `describe('findBestBreak', () => { ... })` — 4 个 test
- `describe('computePageSlices', () => { ... })` — 4 个 test
- `describe('injectHeaderFooter', () => { ... })` — 4 个 test

每个 test 的具体断言见 `<behavior>` 块。

**beforeEach 重置 mock：**
```typescript
beforeEach(() => {
  vi.clearAllMocks();
  mockPdf.internal.getNumberOfPages.mockReturnValue(2);
});
```

**关键断言示例：**

findBestBreak Test 1:
```typescript
it('返回 pageBottom 之前最近的候选切点', () => {
  const candidates: BreakCandidate[] = [
    { y: 100, type: 'row', isTableInternal: false },
    { y: 200, type: 'row', isTableInternal: false },
    { y: 300, type: 'row', isTableInternal: false },
  ];
  const result = findBestBreak(candidates, 250);
  expect(result).not.toBeNull();
  expect(result!.y).toBe(200);
});
```

computePageSlices Test 7 (D-07 极端情况):
```typescript
it('无安全切点时强制分页并输出警告', () => {
  const warnSpy = vi.spyOn(console, 'warn').mockImplementation(() => {});
  const slices = computePageSlices([], 4000, 1866, 2);
  expect(slices.length).toBeGreaterThan(1);
  expect(warnSpy).toHaveBeenCalledWith(
    expect.stringContaining('无法找到安全切点')
  );
  warnSpy.mockRestore();
});
```

injectHeaderFooter Test 10 (D-11):
```typescript
it('单页 PDF 也显示页眉页脚', () => {
  mockPdf.internal.getNumberOfPages.mockReturnValue(1);
  injectHeaderFooter(mockPdf as any, '员工入职登记表', '2026-04-21 10:30');
  expect(mockPdf.setPage).toHaveBeenCalledWith(1);
  expect(mockPdf.text).toHaveBeenCalledWith(
    '员工入职登记表',
    expect.any(Number),
    expect.any(Number),
    expect.objectContaining({ align: 'center' })
  );
  // 页码为 "1 / 1"
  expect(mockPdf.text).toHaveBeenCalledWith(
    '1 / 1',
    expect.any(Number),
    expect.any(Number),
    expect.objectContaining({ align: 'right' })
  );
});
```

**注意：** 这是 TDD RED 阶段。测试文件创建后，运行 `npx vitest run` 会失败（因为 usePdfExport.ts 尚未导出 findBestBreak/computePageSlices/injectHeaderFooter）。这是预期行为。Plan 03 将实现这些函数使测试通过（GREEN 阶段）。
  </action>
  <verify>
    <automated>cd E:/webspace/oa/frontend && npx vitest run src/composables/__tests__/usePdfExport.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    usePdfExport.test.ts 存在，包含 12+ 个测试用例覆盖 findBestBreak / computePageSlices / injectHeaderFooter。测试处于 RED 状态（预期失败，因为被测函数尚未从 usePdfExport.ts 导出）。测试文件语法正确，vitest 能解析并运行（即使 fail）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 测试文件，不涉及安全边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | — | 本 plan 仅创建测试文件，无安全威胁 |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/composables/__tests__/usePdfExport.test.ts` 能运行（即使测试失败）
- 测试文件包含 describe('findBestBreak'), describe('computePageSlices'), describe('injectHeaderFooter')
</verification>

<success_criteria>
- usePdfExport.test.ts 存在且语法正确
- 包含 12+ 个测试用例
- 覆盖所有 behavior 中定义的场景
- vitest 能解析并运行测试文件（RED 状态 — 失败是预期的）
</success_criteria>

<output>
After completion, create `.planning/phases/13-pdf/13-02-SUMMARY.md`
</output>
