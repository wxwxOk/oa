{
  "name": "oa-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../.env run --watch src/index.ts",
    "start": "bun --env-file=../.env run src/index.ts",
    "build": "bun build src/index.ts --target bun --outdir dist",
    "prisma:generate": "bun --env-file=../.env prisma generate",
    "prisma:migrate": "bun --env-file=../.env prisma migrate dev",
    "prisma:deploy": "bun --env-file=../.env prisma migrate deploy",
    "seed": "bun --env-file=../.env run prisma/seed.ts"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.1.1",
    "@elysiajs/jwt": "^1.1.1",
    "@elysiajs/swagger": "^1.1.5",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "elysia": "^1.1.24",
    "nanoid": "5"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^5.22.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.6.0"
  }
}
